# study

`razer`. Reading, annotating, bibliography, spaced repetition, office formats
and grammar checking.

Empty. Its packages will be added later; installing them now would be adding
packages ahead of the step that decides how they are configured. It is declared
in `hosts/razer.txt` so the place exists.
