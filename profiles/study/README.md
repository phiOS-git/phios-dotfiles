# study

`razer`. Reading, annotating, bibliography, spaced repetition, office formats
and grammar checking.

Empty. Its packages are listed in master plan §15.4 and every one of them is
M6; installing them now would be adding packages ahead of the step that decides
how they are configured. It is declared in `hosts/razer.txt` from S-03 so the
place exists, and it stays empty until M6 fills it.
