# server

`mini`. Services only, nothing graphical.

Empty. The services of master plan §10 are M6 and the `[phi]` repository it
will serve over the overlay network is M1, so there is nothing to declare yet.
It is listed in `hosts/mini.txt` and populated at the first real service.
