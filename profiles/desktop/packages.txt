hyprland
xorg-xwayland
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
pipewire
wireplumber
pipewire-pulse
pipewire-alsa
ttf-dejavu
librewolf
kitty
solaar
mpv
mpd
cava
rmpc
zathura
zathura-pdf-mupdf
imv
wl-clipboard
bluez
bluez-utils
sof-firmware
rtkit
quickshell
libnotify
grim
slurp
wf-recorder
zbar

# Freedesktop sound set for notifications (played via pw-play from
# Services/Notifications.qml). Sound is off by default; absolute paths also
# accepted. Provides named defaults: message.oga, bell.oga, complete.oga.
sound-theme-freedesktop

# GTK/Qt native-app theming. qt5ct/qt6ct palettes confirmed against real
# upstream source (github.com/trialuser02/qt6ct).
qt5ct
qt6ct
gsettings-desktop-schemas

# Night shift daemon.
hyprsunset

# Idle timeout policy daemon. Configuration is deferred.
hypridle

# Boot animation. zotac and razer only (both carry this `desktop` profile;
# mini does not).
plymouth

# Cursor theme. "whiteglass" chosen; does not perfectly match the design but
# accepted over producing a bespoke alternative.
xcursor-themes

# AI agent subsystem. zotac and razer only. opencode is the engine;
# bubblewrap is the containment mechanism.
bubblewrap
opencode

# A2's egress whitelist. socat bridges the --unshare-net'd A2 container to
# a host tinyproxy that forwards only to whitelisted hosts.
socat
tinyproxy

# WireGuard VPN, driven by `phi vpn` and the Connectivity settings section.
# Tunnel .conf files (with private keys and endpoint addresses) are not
# stored in this repository. up/down go through sudo -n via a sudoers drop-in.
wireguard-tools

# Firewall (nftables). The user chose nftables directly. `phi firewall` owns
# `table inet phi` in /etc/nftables.conf; control calls go through sudo -n
# via a sudoers drop-in.
nftables

# GUI file manager. thunar-volman + gvfs provide removable-media auto-mount
# and trash/network-location backends. tumbler is the thumbnailer.
# No new template needed: thunar is GTK3 and inherits gtk-3.0 theme.
thunar
thunar-volman
gvfs
tumbler

# Network overlay's Wi-Fi speedtest trigger. Services/SpeedTest.qml
# (phi-shell) runs `speedtest-cli --simple` on demand, never polled.
speedtest-cli
