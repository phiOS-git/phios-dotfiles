hyprland
xorg-xwayland
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
pipewire
wireplumber
pipewire-pulse
pipewire-alsa
ttf-dejavu
librewolf
kitty
solaar
mpv
zathura
zathura-pdf-mupdf
imv
wl-clipboard
bluez
bluez-utils
sof-firmware
rtkit
quickshell
libnotify
grim
slurp
wf-recorder
zbar

# S-41: GTK/Qt native-app theming (master plan §6.7 target group 2). Added
# by explicit user approval this step — not in master plan §15's registry.
# qt5ct/qt6ct substitute for §6.7's named "Kvantum": their real
# [ColorScheme] palette format was confirmed against real upstream source
# (github.com/trialuser02/qt6ct), Kvantum's non-SVG palette-override format
# could not be from here.
qt5ct
qt6ct
gsettings-desktop-schemas

# S-42: night shift daemon, pre-approved master plan §15.3 for M4.
hyprsunset

# Pre-approved master plan §15.3 for M4 ("idle, illuminazione statica
# Chroma"). Added now since it is already registered; NOT configured yet —
# no S-4x AGENT bullet names an idle-timeout policy (auto-lock, DPMS) to
# write into hypridle.conf. S-46's Chroma "static colour on idle
# (hypridle)" behaviour is the first place this step's own backlog actually
# calls for hypridle to run something; its config is deferred there.
hypridle

# S-53: boot animation, pre-approved master plan §15.3 for M5. zotac and
# razer only per master plan §9.1 ("non mini: headless, root non cifrata,
# timeout 0") — both are in this `desktop` profile, `mini` is not.
plymouth

# S-54: cursor theme. NOT in master plan §15 before now — approved by
# explicit user choice this session, over producing a bespoke one, per
# this step's own AGENT card ("either find a conforming one or produce
# it"). ADR 125: "whiteglass" does NOT actually match architettura §8.2.3's
# "thin, black, white-outlined" (it is white, not black) — a real mistake
# found and corrected before handoff; the user chose to accept this
# deviation anyway rather than have a bespoke theme produced.
xcursor-themes
