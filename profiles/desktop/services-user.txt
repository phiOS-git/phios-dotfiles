# Arch does not auto-enable user units when a package is installed, so these
# three stay off until they are enabled once, by hand.
pipewire.service
pipewire-pulse.service
wireplumber.service

# S-71: the AI agent credential broker (docs/phios-agente.md §6.2). One
# instance per opencode instance. a1 is enabled at the end of milestone 0;
# a2 at S-76, when the current uncontained engine is retired. The units are
# only ever enabled by hand, after V-01..V-04 and V-08/V-09 pass:
#   systemctl --user enable --now phi-agent-broker@a1.service
phi-agent-broker@a1.service
phi-agent-broker@a2.service

# S-73: the A1 assistant engine (contained `opencode serve`). Pulls in
# phi-agent-broker@a1 via Requires=. `phi agent project use` restarts it.
#   systemctl --user enable --now phi-agent-a1.service
phi-agent-a1.service

# S-74: the A2 worker engine (local, contained, loopback), enabled at the
# S-76 transition. The remote surface is the two units below it: start them
# ONLY for the duration of a declared-remote session, never `enable`.
#   systemctl --user enable --now phi-agent-a2.service
#   systemctl --user start phi-agent-a2-remote.service   # remote session only
phi-agent-a2.service
phi-agent-a2-remote-engine.service
phi-agent-a2-remote.service

# S-72: A2's egress whitelist (docs/phios-agente.md §5.1). Both are only
# needed when A2 runs (from S-76). phi-agent-net-bridge pulls in
# phi-agent-proxy via Requires=.
#   systemctl --user enable --now phi-agent-proxy.service phi-agent-net-bridge.service
phi-agent-proxy.service
phi-agent-net-bridge.service
