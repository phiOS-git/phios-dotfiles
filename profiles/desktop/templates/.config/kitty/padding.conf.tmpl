# phiOS — kitty window padding (docs/TODO.md: "terminal panels should have
# larger padding", references/panel-reference-1.JPG and -2.JPG). Separate
# from theme.conf.tmpl/fonts.conf.tmpl on the same reasoning those two are
# already split from each other: this only changes if design/tokens.common.sh's
# PHI_TERM_PADDING changes, never on a plain `phi theme set` variant switch.
window_padding_width ${PHI_TERM_PADDING}
