zsh
zsh-history-substring-search
git
zoxide
fzf
fd
bat
ttf-iosevkatermslab-nerd     # Patched; remove after glyph check passes.
adobe-source-code-pro-fonts  # font-mono, unpatched.
adobe-source-serif-fonts     # font-reading.
adobe-source-sans-fonts      # font-ui.
ttf-nerd-fonts-symbols-mono  # font-symbol glyphs; Mono metrics for terminal grid.
noto-fonts                   # Targeted fallback: Greek (Φ) + extended Latin only.
yazi
neovim
nnn
gettext
rsync
7zip
unrar
poppler
imagemagick
pipewire-jack
ffmpeg
ripgrep
jq
resvg
btop

tesseract
tesseract-data-eng

# Out-of-plan: clamav. Real-time (on-access) antivirus, wanted on all
# three hosts regardless of role — including `mini`, headless or not.
# extra/T0. See profiles/base/manual.txt for the one-time setup (clamd.conf
# hardening, the sudoers/virus-event system/ material, enabling the units)
# and profiles/base/services-system.txt for which units this profile
# declares.
clamav
fastfetch
# openresolv provides /usr/bin/resolvconf, which wg-quick uses to manage
# tunnel DNS servers. Without it, `phi vpn` requires manual resolvconf calls.
# Conflicts with systemd-resolvconf; pacman resolves at install time.
openresolv

# Requested: `phi fan` (real hwmon PWM control, internal/fan) needs the
# in-kernel hwmon driver modules this package's udev/modprobe hooks help
# autoload, and its own `sensors`/`sensors-detect` tools are the standard
# way to inspect what a host exposes. extra/T0. All three hosts, same
# reasoning clamav above already gives for a base-profile hardware tool
# (on-access scanning / sensor reads both work headless too) — and this
# was already installed (unmanaged) on zotac when its hwmon layout was
# checked for `phi fan`; declaring it here just makes that real.
lm_sensors
