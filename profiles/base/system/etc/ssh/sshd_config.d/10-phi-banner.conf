# phiOS SSH banner. A drop-in, not an edit to sshd_config: additive,
# cannot conflict with existing sshd_config. OpenSSH reads
# /etc/ssh/sshd_config.d/*.conf automatically (default Include line; confirm
# this on the real machine). Uses Unicode Φ (SSH client is a real terminal
# emulator with the project's own font stack, unlike raw TTY console).
Banner /etc/phi-banner
