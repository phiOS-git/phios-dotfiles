# openrazer-daemon claims org.razer on the SESSION bus and needs to be
# enabled once by hand (Arch does not auto-enable package user units on
# install). Services/Chroma.qml's busctl calls cannot work if this unit was
# never started.
openrazer-daemon.service
