# PROGRESS

Durable memory of the phiOS step loop. One row per step from `phios-agent-brief.md` §6
(the main-line backlog). This file is updated in the same commit as the step that
changes a row's status — never in a separate commit.

## Status legend

- `todo` — not started.
- `awaiting-verification` — the agent has written, committed, and pushed to `github`;
  waiting on the user to run the step's `VERIFY` block and confirm `DONE WHEN`.
- `verified` — the user has confirmed `DONE WHEN`. The next step's commit is what
  moves a row from `awaiting-verification` to `verified` and fills in the `Commit`
  column, because a commit cannot record its own hash in advance.
- `blocked` — cannot proceed; see `Note` for what is missing and who owns closing it.

## M0 — Dotfiles foundation

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-00 | Repository scaffolding and PROGRESS.md | verified | 1e35eb6 | 2026-09-07 | |
| S-01 | [BLOCKING] Dotfiles v2 structure and installer | verified | 6712fdb | 2026-09-07 | Machinery only; `profiles/` stays empty until S-03, so `--dry-run` reports no profiles declared |
| S-02 | [BLOCKING] Design token source, two variants | verified | ac8b04e | 2026-09-07 | `Q-N01` (`font-mono`) still open, provisional value written, closes at S-51. Light variant has never rendered on a machine |
| S-03 | [BLOCKING] Migrate existing targets, output-identical | verified | b97f3f4 | 2026-09-07 | **`DONE WHEN` genuinely satisfied now, on `razer`, many steps late.** `bin/phios-install --dry-run` showed exactly the 7-item drift `phi doctor` had been reporting all along (the six `link`-kind files plus nothing else — `razer` had zero missing packages); `bin/phios-install` (apply, no flags) then re-linked every one of them, reported `manifest /home/flavio/.local/state/phios/manifest` written, and the user confirmed the session works, `.zshrc` is sourced, and — the specific thing S-15 was blocked on — tab completion now works. `zotac` and `mini` have not had the equivalent apply run yet; both showed the same kind of drift in every `phi doctor` run so far and almost certainly have the same dangling `modules/`-era symlinks, unconfirmed until someone runs `phios-install` for real on each. **Correction added during S-15 verification, several steps earlier in this same session: this row's `DONE WHEN` was never actually satisfied on any host, and the record should not have read `verified` on off-machine evidence alone.** The step card's `DONE WHEN` is explicit: "the user confirms `--dry-run` proposes no functional change on either machine, **then runs the installer and reports the session still works**: terminal, browser, file manager, audio, network, Steam." That apply-mode run never happened on any host — this row's own note below only ever describes off-machine byte-identity checks. The consequence sat latent for many steps and surfaced only when `phi doctor`'s dotfiles-drift check (S-14) kept reporting the same `link`-kind files as needing an `update`, and S-15's tab-completion verification finally forced someone to look at *why*: on `razer`, `~/.config/zsh/.zshrc`, `~/.zshenv`, and `~/.config/hypr/hyprland.lua` were still the exact symlinks the pre-rewrite `install.sh` created, pointing at `phios-dotfiles/modules/...` paths this step deleted — dangling ever since, confirmed with `readlink -f ~/.config/zsh/.zshrc` resolving to nothing. Zsh therefore never sourced `.zshrc` on that machine (confirmed: `~/.zcompdump` did not exist at all, meaning `compinit` had never run in any shell there since the migration), and Hyprland had no working config — this is likely the direct cause of both problems the user separately reported ("hyprland cannot start", "zsh has no configuration"). The fix (run `phios-install`, no flags, for real — S-03's own original `USER` instruction, still unexecuted) is being handed off now; this row's status is left as the user's call, not changed unilaterally here. `modules/`, `install.sh` and `theme.sh` removed. All six templates verified byte-identical against the pre-S-03 renderer, **dark variant only, off-machine** — light has no pre-S-03 output to be identical to, but both variants define the same token set and all 32 tokens the templates use resolve non-empty in light. Two rendered files still carry the stale Italian header `Generato da theme.sh` — kept byte-for-byte on purpose, so the dry-run shows the renders as `ok` and only the symlinks move; retranslating them is a one-line change for whichever step next touches them. Profile order is preserved in `hosts/*.txt`, but `phios-install` batches every missing package into one `pacman` transaction instead of one call per profile, so that order no longer drives behaviour the way `install.sh` did — the `vulkan-driver` regression is avoided by the single transaction, not by the ordering, and neither is verified on a fresh machine. `profiles/workstation`, `study` and `server` are declared and empty |
| S-04 | Capability detection | verified | abdfa82 | 2026-09-07 | `bin/phios-capabilities` probes `/sys` and `/proc` only (coreutils budget, R9): no `lspci`, `libinput` or `udevadm`. GPU vendor reads the PCI vendor id from `/sys/class/drm/card*/device/vendor`. Touchscreen/touchpad have no dedicated sysfs class, so they match the device's own reported name in `/proc/bus/input/devices` — a heuristic, unverified against real hardware. ALS matches an iio device name containing "light" or "als", since S-06 (which fixes the real driver name) is still open. Chroma detection matches any USB device carrying Razer's vendor ID (1532), not the keyboard specifically, since no per-model check was available to write against. None of this ran on `zotac` or `razer`; only exercised off-machine, where every capability correctly reports false/none |
| S-05 | /etc boundary | verified | 564c0aa | 2026-09-07 | `--system-diff` needed no code change — generic since S-01/S-03 (`phios_system_collect`/`phios_system_diff`, `bin/lib/system.sh`), so this step is entirely content: `profiles/*/system/` populated per host. `nvidia` (zotac): `modprobe.d/nvidia.conf`. `workstation` (zotac, was declared-empty since S-03 — README rewritten): `zram-generator.conf`, `sysctl.d/99-zram.conf`, and the two-disk `crypttab` shape (root via `crypttab.initramfs`, `bulk` cascading in `crypttab`) — UUIDs are machine data, left as `<system-partition-uuid>`/`<bulk-partition-uuid>` placeholders, a permanent expected `--system-diff` difference. `server` (mini, was declared-empty since S-03 — README rewritten): `zram-generator.conf` (mini's own `min(ram / 2, 4096)` sizing, distinct from zotac's fixed `8192`), `sysctl.d/99-zram.conf`, `smartd.conf` (missing `/dev/sdb1` on purpose — deferred on `mini` itself per `mini-procedura-base.md` note 9), and the two-unit `OnFailure=` notify-on-failure drop-in — `Description=`/`ExecStart=` written in English per this repo's language rule, so the first diff against the live Italian strings is expected and reconciled by editing the machine (the inverse of S-03's call to leave `Generato da theme.sh` alone: that file had no repo-side canonical text to reconcile against, this one does). `laptop` (razer): the suspend/hibernate drop-ins (`sleep.conf.d/10-hibernate-delay.conf`, `logind.conf.d/10-lid.conf`) from `razer-procedura-completata.md` §13 — not on the card's explicit list, added because the goal sentence ("what is currently applied by hand and known") is the requirement and these are exactly that; flagged here for cheap veto. `multilib` is a **note**, not a mirrored file: pacman.conf has no drop-in/include mechanism for repository sections, so there is no real absolute path to diff a fragment against; it now lives in `profiles/gaming/manual.txt` (covers exactly `{zotac, razer}`, the hosts that need it) instead. That file's sed script needed `$'\x23'` ANSI-C quoting in place of a literal `#`, because `phios_read_list` truncates every line at its first `#` with no notion of quoting — a latent gap in the shared list parser, worked around locally rather than fixed, since fixing it touches every profile's `manual.txt`/`services-*.txt` and no other entry has ever needed a `#`. Deliberately NOT captured, because each is a partial edit to a package-owned file rather than a self-contained fragment at its own path — no mechanism here fits them, and they stay recorded only in `docs/*procedura*.md`: `mkinitcpio.conf` (`MODULES=`, `HOOKS=`), `/boot/loader/entries/*.conf`, `/etc/fstab`. Also not written: `/usr/local/bin/notify.sh` (outside the `/etc` boundary, and the live copy on `mini` has the `ntfy.sh` topic baked in at generation time — a notification topic, which no repository may hold; noted instead in `profiles/server/README.md`). `smartd.conf`'s `-m <nomailer>` token is copied byte-for-byte from `mini-procedura-base.md`; unverified against `smartd.conf(5)` whether that is the documented literal syntax for "no mail, `-M exec` only" or a placeholder the original doc left unfilled — man page not reachable from here. Self-check performed off-machine only: `bin/phios-install --host {zotac,razer,mini} --system-diff` for all three — every declared file appears exactly once, 0 unreadable, all report as new-file diffs against `/dev/null` (expected, since this machine has none of these `/etc` paths); `phios_manual_report` exercised directly to confirm the multilib line prints whole. `--dry-run`/`--check` could not be smoke-tested here at all — pre-existing, unrelated to this step: they fail on macOS's BSD `realpath` (`illegal option -- m`) before reaching any code this step touches; confirmed by reproducing the identical failure on the pre-S-05 commit. Also fixed in this commit: a comment in `bin/lib/system.sh` and a bullet in `README.md` both claimed "`--check`'s service-drift gap closes at S-05" — false, and disproven by this step: the installer never queries systemd, by design, so that gap cannot close at any step. **Verified on real hardware since**: `razer` — `--system-diff` (both unprivileged and `sudo`) reports 0 of 2 differing; `laptop`'s two drop-ins match the machine byte-for-byte. `mini` — reconciled by hand per the user's choice (`unit-failure-notify@.service` rewritten to the English text, `daemon-reload`d), re-run confirms `differing: 0` of 5. `zotac` — after pulling the `crypttab` fix, `differing: 2` of 5, both the UUID placeholders (`crypttab`, `crypttab.initramfs`) and nothing else — the exact, permanent, by-design shape; every other declared file matches exactly. `DONE WHEN` met on all three hosts: every difference is either reconciled (`mini`) or explained as machine data (`zotac`'s two UUIDs); `razer` has none at all |
| S-06 | razer input diagnostics | verified | | 2026-09-07 | Diagnosis only, depends on S-04 (already verified). Nothing written to the repo; the deliverable was the diagnostic command set, executed by the user over several rounds (`/proc/bus/input/devices`, `libinput debug-events`, targeted `evtest` per device, `lsusb`, `ls /sys/bus/iio/devices/`). The internal keyboard is one USB composite device (`lsusb`: `1532:026a` `Razer USA, Ltd Razer Blade`, matching the vendor id `bin/phios-capabilities` already probes for Chroma) exposing three evdev nodes: `event20` (interface 0, `cap:k`, has the LEDs; carries ordinary alphanumeric typing and the Fn key's own synthesized combos), `event8` (interface 1, `cap:kp`; carries both the bare F-row and every Fn-held media/brightness function), `event9` (interface 2, mouse-like, not exercised — likely the touchpad's supplementary buttons). Fn itself emits no independent Linux event at all, confirmed by a dedicated 5-second hold producing nothing on any node: it is a pure hardware-level modifier inside the keyboard's own firmware that changes which HID report the F-row sends, and the same firmware also switches the backlight to a function-only indicator and (per the user) drives the keyboard-illumination keys `Fn+F10`/`F11` with no OS-visible event either — consistent with Fn's behaviour throughout. Every key `funzionalita §2.15` calls broken is already reported by the kernel with standard, correctly-named USB HID scancodes, so no `hwdb` fix is needed for any of them and the real failure is above the driver, almost certainly the absence of any compositor keybinding yet (`S-38`/`S-43`, not built at this stage of the project): bare `F1`/`F2`/`F3`/`F4`/`F8`/`F9` on `event8` → literal `KEY_F1` (`0x7003a`) / `KEY_F2` (`0x7003b`) / `KEY_F3` (`0x7003c`) / `KEY_F4` (`0x7003d`) / `KEY_F8` (`0x70041`) / `KEY_F9` (`0x70042`); `Fn+F1` → `KEY_MUTE` (`0xc00e2`); `Fn+F2` → `KEY_VOLUMEDOWN` (`0xc00ea`); `Fn+F3` → `KEY_VOLUMEUP` (`0xc00e9`); `Fn+F8` → `KEY_BRIGHTNESSDOWN` (`0xc0070`); `Fn+F9` → `KEY_BRIGHTNESSUP` (`0xc006f`). Also captured at the user's request, beyond this step's stated goal but the same conclusion: `Fn+F5`/`F6`/`F7` → `KEY_PREVIOUSSONG`/`KEY_PLAYPAUSE`/`KEY_NEXTSONG` (`0xc00b6`/`0xc00cd`/`0xc00b5`). `Fn+F4` is the one exception and is not a function/media code at all: the firmware synthesizes the literal Windows shortcut Super+P on `event20` (`KEY_RIGHTMETA` held, scancode `0x700e7`, the standard HID Right-GUI usage, plus `KEY_P` tapped underneath, scancode `0x70013`) — a hardware-level stand-in for Windows' display-projection panel, not a missing keycode; an equivalent on Linux needs a compositor bind on Super+P (`S-38`), not an `hwdb` rule, and may need to dodge a clash if Super ends up the primary window-management modifier. `Q-F01` closes definitively: `ls /sys/bus/iio/devices/` lists exactly one device, `iio:device0`, and its `name` reads `als` — a real ambient-light sensor is present, named exactly what `bin/phios-capabilities`'s existing heuristic already matches on, unblocking `S-42`'s True Tone (master plan, ambient colour-balance note). `DONE WHEN` met: every code `funzionalita §2.15` names is known; the `hwdb` rule itself, the `Fn+F4`/Super+P question, and the still-open `Q-F04`/`Q-F06` Chroma questions are left for `S-46` |

## M1 — phi and own-package distribution

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-10 | [BLOCKING] phi repository skeleton and --version | verified | 8517d84 | 2026-09-07 | First step to land in a repository other than this one, so its own commit hash above is a `phi` hash, not a `phios-dotfiles` one. `phi` is a dependency-free Go module (`go.mod` names the module `phi`, not a URL, since it is never imported by another module): `cmd/phi` is the entry point, `internal/cli` parses arguments, does TTY detection (a coarse `os.ModeCharDevice` check with no third-party dependency — it also reports `/dev/null` as a terminal, harmless today since styled only adds the Φ mark, but it will need a real `ioctl`-based check before any verb emits colour), and dispatches to a verb or falls back to `phi-<verb>` on `PATH` (ADR 017, exercised off-machine against a throwaway `phi-hello` script: arguments, stdin, and exit code all forward correctly). `internal/view` holds `help`/`--version`/`completion zsh` as pure string functions with no knowledge of terminals or process globals, kept separate from `internal/cli`'s dispatch logic from the first line per architettura §10.1.1. `internal/build` carries the version string (`"dev"` locally), overridable at link time via `-ldflags` for phi-packages's PKGBUILD (S-11) to set from the package version. Commands implemented: `phi --version`, `phi help`, `phi completion zsh`; bare `phi` and an unknown verb both print help/an error to stderr and exit 1. The help header carries the Φ mark (`U+03A6`, confirmed byte-for-byte, not one of the lowercase forms) in Role A — Tier 0, monochrome, static — only when stdout is a real terminal; redirected output drops it, the only "structured" behaviour this step exercises, since none of the three commands yet emits data a script would parse — the real plain-columns-vs-JSON decision waits for the first verb that does (S-13 `phi state list`, S-14 `phi doctor`). Off-machine: `go build`, `go vet` and `gofmt -l` clean; native (macOS) build run directly (`phi --version` → `phi dev`, help/no-args/unknown-verb/missing-completion-shell exit codes all correct, styled Φ mark confirmed under a pty via `script -q`, dropped under a pipe and under `> /dev/null`); a `GOOS=linux GOARCH=amd64` cross-build also succeeded and produced a static ELF binary, confirmed with `file`, but was not executed there — no Linux host was available in that session. **Verified on real hardware since**: `razer` — `git clone` over `https` from the public `github` mirror (no credentials needed for read), `go build -o phi ./cmd/phi` silent/clean, `./phi --version` → `phi dev`, `./phi help` shows the styled `Φ phi dev` header on a real terminal, and the `phi-hello` PATH-fallback test forwards correctly. `DONE WHEN` met: the binary builds and the dispatcher falls back to PATH for unknown verbs, confirmed off-machine and on `razer` |
| S-11 | [BLOCKING] phi-packages and the [phi] pacman repository | verified | phi-packages 0876454..4c951d9 (4 commits) · dotfiles c5b0cb9..7f8e01f (3 commits) | 2026-09-07 | New repository `phi-packages` (`0876454` skeleton, `52952fe` a same-day key-consistency fixup — a second commit rather than an amend because this environment correctly refuses to force-push an already-pushed branch — and `1791f4c` adding `git` to `makedepends`, found on real hardware: `zotac`'s first `scripts/build phi` failed inside the chroot with "Cannot find the git package needed to handle git sources", because `source=()` is a `git+` URL but only `go` was declared, so a fresh `mkarchroot base-devel` chroot had no way to fetch it — all `phi-packages` hashes, same pattern S-10 set for landing outside `phios-dotfiles`): `phi/PKGBUILD` builds the CLI from a pinned git tag (`source=(...#tag=v${pkgver})`, never `latest`/`-git`), `scripts/build` wraps `mkarchroot`/`makechrootpkg -c` for a clean-chroot build on `zotac`, `scripts/publish` detached-signs a built package and runs `repo-add -s` into a local repo directory — never touches `mini`, since that requires an address this repository may never hold (`CLAUDE.md` rule 5); it requires `GPGKEY` explicitly rather than trusting gpg's default identity, since `zotac`'s user almost certainly already has an unrelated personal key and a silent mismatch between the key signing the packages and the key `repo-add -s` uses for the database would fail every install under `SigLevel = Required` with no obvious cause. `README.md` documents naming (`phi`, `phi-<component>`), versioning (hand-bumped `pkgver` pinned to a real tag; `pkgrel` for packaging-only rebuilds), where the repo database ends up, and host registration. Tagged and pushed `v0.1.0` on the `phi` repo, at the S-10 commit `8517d84` — no functional change, the tag exists only so `source=()` has something real to clone; `pkgver=0.1.0` in the PKGBUILD names exactly this tag. The exact `go build -trimpath -ldflags "-s -w -X phi/internal/build.Version=${pkgver}"` line from `build()` was run natively off-machine and confirmed `phi 0.1.0`; the chroot build itself was not run anywhere, no Linux host available. On the dotfiles side: `profiles/base/system/etc/pacman.d/phi-mirrorlist` (real, diffable path, confirmed via `--system-diff` on all three hosts) carries the `Server =` line with `<mini-overlay-address>` and `<port>` left as literal placeholders; `profiles/base/manual.txt` gained the two-line `[phi]`/`Include=` stanza for `/etc/pacman.conf` (appended as a single `grep -q` guarded `sudo sh -c 'printf ... >> ...'` line, not a heredoc, because `phios_read_list` truncates every line at its own `#` — confirmed by simulating the parser against the edited file; the `grep` guard makes the append idempotent, since unlike the multilib `sed` next to it in `profiles/gaming/manual.txt`, a plain append is not) and the `pacman-key --add`/`--lsign-key` step. **Deviation from the step card's literal wording, flagged for cheap veto:** the AGENT bullet and `phios-user-runbook.md` §5.4 both say the fragment lives at `profiles/*/system/pacman/`, but `bin/lib/system.sh` (`phios_system_collect`) mirrors the exact absolute path after `.../system/`, so a file at `system/pacman/x` would diff forever against the nonexistent `/pacman/x` — the runbook's own instruction to read it with `--system-diff` is only satisfiable at a real path, so `system/etc/pacman.d/phi-mirrorlist` was used instead; the runbook still names the stale path and was not edited (out of scope for an agent that doesn't own that document). **Second deviation, same flag:** the step card's USER section names only `zotac` and `razer` for registration, but master plan §3.1 ("registrato in `/etc/pacman.conf` di ogni macchina") and runbook §5.2 step 2 and §5.4's own header both say every machine, so the fragment and manual step were placed in `profiles/base` (all three hosts) rather than `profiles/desktop` (`zotac`+`razer` only) — master plan wins conflicts per `CLAUDE.md`. Not written in this step, deliberately: the darkhttpd systemd unit that serves `/srv/pkg/phi` — the step card's USER section has the user set that up by hand, and the AGENT section names no unit file. `--dry-run`/`--check` remain unusable off-machine, same pre-existing BSD-`realpath` failure `S-05` already recorded, unrelated to this step; verified with `--system-diff` instead, on all three hosts, off-machine only. **Verified on real hardware, all three hosts.** `zotac`: full loop exercised end to end — toolchain install (`pacman -Sy` hit a transient 404 on one mirror for an unrelated `devtools` dependency, resolved by pacman's own mirror fallback, unrelated to this step), key generation, `scripts/build phi` (succeeded on the second attempt, after the `git`-in-`makedepends` fix above), `scripts/publish` (produced a signed `phi.db.tar.zst`; `makepkg` also emitted the standard `phi-debug` split package, never published — `scripts/publish` was only given the main package, which is sufficient), `pacman-key --add`/`--lsign-key`, `pacman -Sy`, `pacman -S phi`, `phi --version` → `phi 0.1.0`. Getting the built repo onto `mini` needed two more fixes beyond anything either repository declares: `rsync ~/phi-repo/ mini:/srv/pkg/phi/` first failed because `/srv/pkg/phi` didn't exist yet, then failed again on `chgrp`/symlink creation until the directory's ownership matched the transferring user — exactly the `mkdir`/`chown` sequence `phios-user-runbook.md` §5.3 already documents, so nothing here needed to change, only doing that step before the transfer. `razer` and `mini`: `pacman-key --add` using the public key copied over per the README fix above, the `phi-mirrorlist` `Server` line written directly rather than by `sed` per the same fix, the `[phi]` stanza appended, `pacman -Sy` picked up and installed `phi` cleanly on both — confirmed by the user. `DONE WHEN` met and exceeded: `phi --version` runs, installed by `pacman`, on all three hosts, not just the two the step card's USER section named — satisfying master plan §3.1's "ogni macchina" the way the `base`-profile placement decision above anticipated. No IP address, hostname, key fingerprint, or private key material was written into any repository at any point in this step — checked against the working tree and the full history of all three repositories before this commit |
| S-12 | phi theme | verified | phi 96c5062 (tag v0.2.0) · phi-packages 6342873 | 2026-09-07 | **Verified on real hardware (`zotac`):** the DONE WHEN discrepancy below is now confirmed, not just this agent's read — `phi theme check` on `zotac` reproduced the off-machine prediction exactly: `dark fg-2 4.00:1 FAIL`, every other pair on both variants `ok`, light `accent` included at `4.82:1`. `phi --version` → `phi 0.2.0`, confirming the rebuilt package actually landed. Six `phi theme set` cycles (`dark --dry-run`, `dark`, `light`, `dark`, `light`, `dark`) all completed cleanly: `dark` reports `ok` across all six targets when nothing changed, `update` on every real variant switch, `[unknown]` reload rows consistently reported "unwired" and never executed, class C `.config/zsh/theme.zsh` listed under "restart required" every time and never restarted. Lands in `phi` (`internal/tokens` — parses `design/tokens.common.sh`+`tokens.<variant>.sh`'s `KEY='value'` lines, quote-first so a value that itself starts with `#` like `PHI_OVERLAY_SCRIM` is never mistaken for a trailing comment; `internal/theme` — `design/adapters.txt`, template substitution, the runtime variant file) and `phi-packages` (`phi` PKGBUILD `pkgver` 0.1.0 → 0.2.0, tracking the new tag; `pkgrel` unchanged since this is a new upstream version, not a packaging-only rebuild). **Undocumented gap, flagged for cheap veto:** nothing in any planning doc says how a pacman-installed `phi` binary — decoupled from any one checkout — finds the `phios-dotfiles` repository at runtime. `internal/tokens.Root()` defaults to `~/phios-dotfiles`, the one path every installation procedure (`mini-procedura-base.md`, `razer-procedura-completata.md`, `phios-procedura-base-2.md`) agrees on with zero disagreement, overridable via `PHI_DOTFILES` for a non-standard layout; fails explicitly, never guesses, when neither resolves. **DONE WHEN discrepancy, surfaced to the user rather than reconciled silently:** the step card says `phi theme check` should report "the known light-variant accent failure." Computed WCAG 2.x contrast from the real token values reproduces both token files' own documented ratios to two decimals and finds no such failure — light accent is 4.82:1 (passes), and the only sub-4.5:1 pair on either variant is dark `fg-2` at 4.00:1, exactly the failure `tokens.dark.sh` itself already documents. This is almost certainly S-02's second-accent-lightness fix (closing §6.2's `[AZIONE]` note) postdating the card's wording. Asked the user directly; they did not pick an interpretation and instead asked how to verify S-12 on real hardware, which is answered in the handoff — **the check's expected-failure interpretation (dark fg-2, not light accent) is this agent's read, not yet confirmed by the user**, flagged here for cheap veto at verification time. `phi theme render` confirmed byte-identical to `bin/phios-render`, off-machine, for all six profile templates plus `design/preview.tmpl`, both variants (`cmp`, not eyeballed). `phi theme set` confirmed off-machine against a throwaway `$HOME`: `--dry-run` writes nothing (no destination file, no state file); a real run is idempotent (`ok` on every target the second time); `dark → light → dark` round-trips cleanly; `[unknown]` reload rows are rendered and reported "unwired", never executed or guessed at; Class C changes are collected under "restart required" and never restarted; the active variant is written to `$XDG_STATE_HOME/phi/theme-variant` only on a non-dry-run success — the exact path `bin/lib/tokens.sh`'s `phios_variant()` already reads and which nothing wrote before this step. Deliberately deferred to S-51, flagged here rather than contorted into a caller this step doesn't need: `internal/tokens` derive-in-OKLCH, since S-51 is where the palette is actually derived algorithmically and no consumer needs it before then. `bin/phios-render` is kept, not deleted — DONE WHEN needs it as the comparison target at verification time; its own header's "goes away" is a follow-up, not this step's. **Not yet run anywhere:** the chroot build on `zotac`, `scripts/publish`, the `rsync` to `mini`, or `pacman -Syu` on any host — the new `phi` version is tagged and pushed but not yet built, published, or installed. `DONE WHEN` partially met off-machine (render byte-identical; check's real output computed and matches the token files' own documented ratios); not yet confirmed on `zotac`/`razer`/`mini`, and the DONE WHEN wording discrepancy above is still open. |
| S-13 | phi state | verified | phi 1825002 (tag v0.3.0) · phi-packages a3fe19c | 2026-09-07 | **Verified on real hardware (`zotac`):** `phi --version` → `phi 0.3.0`; `phi state set theme.variant dark && phi state get theme.variant` → `dark`; `phi state list` showed all seven §5.6 keys, `theme.variant` set to `dark` and the other six `(unset)` — exactly the shape the off-machine test predicted. The user additionally rebooted the machine and confirmed the set value survived, closing the `DONE WHEN` half ("state survives a reboot") this agent could not produce off-machine. `internal/state` owns `$XDG_STATE_HOME/phi` (or `$HOME/.local/state/phi` when unset — the same rule `bin/lib/common.sh`'s `phios_runtime_dir` uses, confirmed by reading it directly rather than trusting the comment: it prints `${XDG_STATE_HOME:-$HOME/.local/state}/phi`, not something keyed off `XDG_RUNTIME_DIR`, so the directory is real persistent storage, not tmpfs) as a closed set of keys, master plan §5.6: `theme.variant`, `monitor.config`, `wallpaper.path`, and the four runtime toggles (`night-mode`, `dnd`, `spotlight`, `chroma`). Each key is one flat text file, named by replacing `.` with `-` — `theme.variant` → `theme-variant` is the general rule, not a special case, and it lands on the exact filename `bin/lib/tokens.sh`'s `phios_variant()` already reads. `Get`/`Set`/`List` all reject an unlisted key before touching the filesystem — confirmed off-machine: `phi state set bogus.key value` fails with the defined-keys list, never creates a file. Two §5.6 rows are deliberately not keys here: launcher frecency and clipboard/notification history are collections, not scalars, and each has its own owning step that hasn't run yet (frecency: `S-33`/`S-73`; clipboard history: `S-32`; notification history: `S-30`) — modelling them as a single string value would be a guess this step has no grounds for; they get real keys, or a different storage shape entirely, when their step defines one. `internal/theme/variant.go` now delegates to `internal/state` instead of reading and writing the state file itself, so there is exactly one place, not two, that knows that path. `phi state list` is plain aligned columns, the same shape as `phi theme list` — the first verb to emit data a script would parse, settling the plain-vs-JSON deferral `internal/cli/terminal.go` left open at `S-10`; JSON is left for whichever consumer first needs it. Lands in `phi` (tagged and pushed `v0.3.0`) and `phi-packages` (`phi` PKGBUILD `pkgver` 0.2.0 → 0.3.0, tracking the new tag). **Housekeeping, not a step deviation:** the `phi`/`phi-packages` commits and tag above were made and pushed in a prior session before this row was written — this commit is what records them in `PROGRESS.md`, per this file's own rule that a commit records the *previous* step's outcome once its hash is known; no code changed here. Confirmed off-machine, against a throwaway `$HOME`: unknown-key rejection on both `get` and `set`; `get` on a never-set key fails cleanly (not an empty string); `set`/`get` round-trip; `list` renders set and unset keys together, unset as `(unset)`; the on-disk file is exactly `.local/state/phi/theme-variant`. Cross-checked the "both writers agree" claim directly rather than through `phi` alone: `phi state set theme.variant light`, then `bin/lib/tokens.sh`'s `phios_variant()` sourced on its own with no `phi` involved, in the same throwaway `$HOME` — prints `light`. Also re-ran `phi theme set light`/`dark` and confirmed `phi state get theme.variant` sees each write. `go build`, `go vet`, and `gofmt -l` all clean. **Not yet run anywhere:** the chroot build on `zotac`, `scripts/publish`, or `pacman -Syu` on any host. `DONE WHEN` partially met off-machine — every writer and reader of the state file agree on its path and never touch a repository; the "survives a reboot" half is the user's to confirm on real hardware, since a reboot is not something this agent can produce. **Correction, found during S-36 (2026-09-08), phi-shell commit `1ead78f`:** `Config/Settings.qml`'s process bridge (used by every `phi state get/set/list` call any part of the shell makes) left `running` true after each one-shot call completed instead of resetting it in `onExited` before `destroy()` — since `Process.onFinished()` (`io/process.cpp`) calls `startProcessIfReady()` unconditionally as its last step, the still-alive (not-yet-actually-deleted) object would respawn itself once more every time. Found while auditing every `Process` in the repo for this exact class of bug during S-36; fixed by setting `running = false` before `destroy()`. Real-hardware impact unconfirmed — this bridge's calls are short-lived and infrequent enough that the respawn may never have been externally visible, but it was there from this step onward. |
| S-14 | phi doctor | verified | phi e3ee55c..197980c (tag v0.4.2) · phi-packages 0fc28a7 | 2026-09-07 | **Verified on real hardware.** `mini`'s third `phi doctor` run, on `v0.4.2`, is the confirming one: `SMART status` now reads `[unknown]` (not the false `ok` `v0.4.1` still produced), with both `/dev/sda` (`Permission denied`) and `/dev/sdb` (`Use smartctl -h`, the USB-bridge device) still visible in the detail lines — unreadable, honestly reported, and no longer promoting the check itself. 7 checks, 1 problem, and that one problem is the real dotfiles drift (see below), not a fabricated SMART result. `disk space`, `failed systemd units`, `service status`, `package categories` and `/srv mount` are unchanged from the earlier runs and still correct. `razer` and `zotac` were **not** re-run on `v0.4.2` — sound because neither host's `v0.4.0` output ever reached the code this version changed: both report `SMART status` as `smartctl not available`, an early return before the tri-state pass/fail logic the mini fix touches, and both took the clean, no-error path through `package categories`/`failed systemd units`, which is exactly the path the two latent-gap fixes leave unchanged (they only alter behavior when the underlying `pacman`/`systemctl` call itself errors). **The standing `dotfiles drift` `PROBLEM` on all three hosts is real, pre-existing state, not an S-14 defect**, and confirms the check works by finding genuine drift rather than manufacturing it: five template renders pending on every host (`init.lua`, `yazi.toml`/`init.lua`, `.zshrc`, `.zshenv`), plus `hyprland.lua`/`kitty.conf` on `razer`/`zotac`, plus `unrar` to install on `mini`/`zotac` and `sof-firmware`/`rtkit` on `zotac` — none of this is `phi doctor`'s to fix. **Correction, discovered during S-15 verification, not at the time this row was first written: this was not benign pending maintenance.** `phios-install` (apply mode) has never actually been run for real on any of the three hosts since the S-03 migration — every `link`-kind entry in this drift (`.zshrc`, `.zshenv`, `hyprland.lua`, `kitty.conf`, …) was a live, broken symlink left over from the pre-rewrite `install.sh`, still pointing at the `phios-dotfiles/modules/...` paths S-03 removed. On `razer` this meant `.zshrc` was never sourced at all (confirmed: `readlink -f ~/.config/zsh/.zshrc` resolved to nothing, and `~/.zcompdump` did not exist, meaning `compinit` had never run in any shell on that machine since the migration) and Hyprland had no working config. See S-03's row for the correction to its own record. **Second real-hardware round on `mini` (`v0.4.1`) confirmed the permission-denied fix but found a second, related false-positive:** with `/dev/sda` correctly reporting `could not read: ...Permission denied (needs root)` and `/dev/sdb` reporting `could not read: ...Use smartctl -h to get a usage summary` (a USB-bridge disk at `/mnt/trekstor-check` that `smartctl` cannot open without an explicit `-d` device-type flag — not chased, out of scope for this step, noted here for whichever step first owns per-device `smartctl` flags), the check's own overall bracket still read `[ok] SMART status` — a false `ok`, since *nothing* was actually confirmed healthy on either device. Fixed in `v0.4.2` (`197980c`): `smartStatus` now tracks whether any device produced a genuine `PASSED` or `FAILED` self-assessment line and reports `Problem` only on a real failure, `OK` only once something was actually verified passing, `Unknown` otherwise — an unreadable device stays visible in its own detail line, it just no longer promotes the check as a whole. The same commit audited the rest of the package for the identical shape (a swallowed command error silently read as a clean result) and closed two more, latent, not yet manifested on any host: `pacman -Qe`'s error was discarded outright (a real pacman failure could have been miscounted as an "other T0 explicit" package); `systemctl --user --failed`'s error was discarded the same way (on a machine with no session bus for the invoking user — plausible over SSH-only access — that call's error prose could have been misread as a bogus failed-unit name, or silently reported as a clean scope that was never actually queried). Both now distinguish a real command error from pacman/systemd's own normal "nothing to report" exit, reported `Unknown` for the former; reasoned from pacman/systemctl's well-established exit-code conventions (the same convention `bin/lib/packages.sh` already relies on for `pacman -T`) rather than a captured real transcript, since neither failure mode has actually occurred on any host yet. Verified with a throwaway in-package test (not committed) against `mini`'s exact second-round transcripts: the tri-state SMART logic now yields `Unknown`, not the false `ok` `mini`'s own run produced. **Not yet re-run anywhere with `v0.4.2`** — that is what verification now needs to confirm: `mini`'s SMART bracket should read `[unknown]`, not `[ok]`, with the same two per-device detail lines as before (unless run with `sudo`, which should now show `sda`/`sdb` as `PASSED` if `sdb`'s USB bridge cooperates); `razer`/`zotac`/`mini`'s other checks are expected unchanged from the `v0.4.0` run recorded below. **Real-hardware review found and fixed a false positive before this step could be marked verified.** First `phi doctor` run on all three hosts (`e3ee55c`/`v0.4.0`) came back mostly as this step's off-machine note predicted (`razer`/`zotac`: real dotfiles drift correctly surfaced — pending template renders and, on `zotac`, three packages to install; `service status` correctly enumerated each host's declared units, all `active`; `package categories` correctly showed zero foreign/AUR packages on all three), but `mini` reported `/dev/sda: FAILED` under SMART status — alarming for a server disk, and wrong. Root cause: `smartStatus`'s original substring search for `"failed"` anywhere in smartctl's combined output also matches smartctl's own access-error text ("Smartctl open device: /dev/sda failed: Permission denied"), so running `phi doctor` unprivileged made a plain permission error read as a genuine SMART health failure. Confirmed against the user's own pasted transcripts: `sudo smartctl -H /dev/sda` printed the real assessment line, "SMART overall-health self-assessment test result: PASSED"; the same command without `sudo` printed only the permission-denied line, no assessment line at all. Fixed in `c1fa74e` (tagged `v0.4.1`): `assessmentVerdict` now looks for that one specific self-assessment line instead of scanning the whole buffer for a bare substring, so an unrelated error can never again be read as a health verdict; the fallback path now surfaces smartctl's own error line, with a "(needs root)" hint, instead of a generic exit-status message. The same review's `lsblk` output also showed `dm-0` is mini's `/srv` LUKS mapping (named `data`), not a physical disk, so `dm`/`md` were added to the `/sys/block` exclusion list — it was only ever noise (`could not read`), never a false positive, but never belonged in the sweep either. Verified the fix against the exact real transcripts via a throwaway in-package test, not committed. **Separately confirmed, not a bug:** `smartctl not available` on `razer`/`zotac` is correct and by design — `smartmontools` is declared in no profile's `packages.txt` anywhere in this repository (grep across all profiles: zero matches; `profiles/server/` has no `packages.txt` at all), so it is only present on `mini` because it was installed by hand outside the repo's declarations at some point before this project's dotfiles rewrite — a pre-existing gap, out of scope for this step (composing checks, not declaring packages), noted here for whichever step first declares monitoring packages (master plan's own summary row groups SMART under `mini`'s monitoring only, so this may be by design rather than a gap at all). `internal/doctor` composes rather than owns every check the S-14 AGENT bullet names, in that order: disk space (`statfs("/")` directly — S-04's sysfs/syscall-first preference, not a `df` dependency; 85%/95% warn/fail thresholds are this step's own judgment call, no planning document sets one, flagged for cheap veto), failed systemd units (`systemctl --failed`, system and user scope — `bin/lib/system.sh`'s `phios_services_report` explicitly never queries systemd, "that gap does not close, at S-05 or ever"; that boundary is the installer's, not `phi doctor`'s — asking systemd how it's doing is this command's entire job), dotfiles drift (shells to `bin/phios-install --check` rather than reimplementing profile/package/file planning a second time in Go; maps its documented exit codes 0/1/2 to ok/PROBLEM/unknown), SMART status (enumerates real disks from `/sys/block`, excluding `loop`/`ram`/`zram`/`sr`, runs `smartctl -H` on each, parses the self-assessment line as text since `smartctl(8)`'s exit-code bit layout was not reachable to confirm, same gap S-05 flagged for `smartd.conf`'s `-m` token), service status (reads `hosts/<host>.txt` and `profiles/*/services-*.txt` — the read side of the boundary `phios_services_report` only ever prints — and checks each declared unit's actual `systemctl` state; `inactive` is not flagged, since `services-user.txt`'s own comment documents Arch never auto-enabling user units on install, only `failed` is), and package categories (`pacman -Qm`, foreign packages, against `CLAUDE.md` rule 6 — T0 + phi-packages only, `Q-01` deferred — so any non-empty result is exactly the AUR/T4 violation that rule forbids; phi-packages vs. other T0 is bucketed by name, `phi`/`phi-*`, a heuristic since pacman has no post-install "which repo did this come from" query, flagged the same way S-04 flagged its device-name heuristics). On `mini` only, an added `/srv` mount check compares the device ids of `/` and `/srv` rather than parsing mount tables; `PROBLEM` when they match (the LUKS volume is still locked — expected right after every reboot per master plan §9.2, but still worth a timer's attention if it stays that way). Every check degrades to `unknown` rather than guessing when its tool or privilege is unavailable — never silently omitted, never promoted to a false `ok` — and `Report.ExitCode` is non-zero exactly on a `PROBLEM`, never on `unknown`, so an environment limit (no `pacman` here, no `/sys/block` here) can never fabricate a red result; `tokens.Root()` failing no longer aborts the whole command the way `phi theme`'s does — dotfiles drift and service status report that failure through themselves, and disk/systemd/SMART/packages still run, since they need no checkout. Confirmed off-machine: `go build`/`go vet`/`gofmt -l` clean; `phi doctor` runs end to end and the real `statfs` reading on this machine correctly produced a `PROBLEM` at 97.5% used (not a fixture); every other check correctly reported `unknown` for the right reason (no `systemctl`/`smartctl`/`pacman`/`/sys/block` here, no `hosts/<this-hostname>.txt` in the repo). The profile-resolution path specifically was verified against a real declared host by symlinking `profiles/`, `bin/` and `design/` into a scratch tree with `hosts/` pointed at `zotac.txt`'s real content, run via a throwaway `cmd/_s14check` deleted immediately after (confirmed by `git status` showing no trace): with a real host recognized, service status walked past profile resolution and correctly enumerated `zotac`'s declared units, stopping only on the expected "systemctl not available" here. Lands in `phi` (tagged and pushed `v0.4.0`, then the SMART fix `v0.4.1`) and `phi-packages` (`phi` PKGBUILD `pkgver` 0.3.0 → 0.4.0 → 0.4.1). **Run on all three real hosts** (`v0.4.0`, before the fix): `razer` — 6 checks, 1 problem (real dotfiles drift, 7 items, all pending template renders); `zotac` — 6 checks, 1 problem (real drift, 10 items, three packages plus the same template renders); `mini` — 7 checks (the `/srv` mount check only appears here, correctly `ok` at 0.6% used), 2 problems (the same kind of real drift, 6 items, plus the SMART false positive this row's first paragraph covers). Not yet re-run on any host with the `v0.4.1` fix — that is exactly what verification now needs to confirm: that `mini` reports `sda`/`sdb` `PASSED` (or an honest permission/needs-root message if run unprivileged) instead of `FAILED`, and that `razer`/`zotac`/`mini` are otherwise unchanged from the `v0.4.0` run above. Not yet run anywhere: the chroot build/publish/`pacman -Syu` cycle for `v0.4.1` specifically (the three runs above were all against `v0.4.0`). |
| S-15 | phi completions and packaging polish | verified | phi d76f142 (tag v0.5.0) · phi-packages 6324eea | 2026-09-07 | **Verified on `razer`: tab completion after `phi ` works**, confirmed by the user after the real fix — this step's own code and packaging were correct and installed from the start (`pacman -Ql phi` showed `_phi` and `phi.1.gz` in place, `$fpath` already included `/usr/share/zsh/site-functions`); what blocked verification was environmental, not this step's: `razer`'s `~/.zshrc` was a dangling symlink left over from before the S-03 migration (see S-03's row), so `zsh` had never sourced any config and `compinit` had never run in any shell on that machine — no amount of correct completion-script packaging could show up in a shell that never ran `compinit` at all. Confirmed once `bin/phios-install` (apply) fixed that. `internal/view.Commands` (name + one-line summary) is now the single source `Help`, `ZshCompletion`, and the new `Man` all render from — before this step, every verb landed as a hand-edit to two separate hardcoded lists (`S-12`'s `theme`, `S-13`'s `state`, `S-14`'s `doctor` each touched both `Help`'s table and `ZshCompletion`'s array separately, and this agent felt that duplication directly, twice, while building S-13/S-14); adding a verb is now one row in one list. `phi man` renders a troff `man(7)` page from that same list (S-15 AGENT: "man page or structured help, generated, not hand-written") — verified off-machine with `mandoc -Tlint`/`-Tascii`, renders cleanly with exactly one accepted warning (`.TH`'s date field left empty on purpose, since the page has to be byte-reproducible from the version string alone, not from when it happened to be generated; a real date would break that, so the warning is documented as accepted cost rather than silently tolerated). An earlier draft's `SEE ALSO` section cited `phios-install(1)`, which does not exist as a man page anywhere in this project — caught and removed before committing, not shipped. `phi-packages`'s `PKGBUILD` `package()` now actually installs both: previously it only ever installed the `phi` binary, so `phi completion zsh` and the new `phi man` existed as CLI features nothing shipped — `package()` now runs the freshly built binary itself to generate `usr/share/zsh/site-functions/_phi` and `usr/share/man/man1/phi.1`, installed uncompressed on the assumption `makepkg`'s own tidy step gzips `usr/share/man/*` by default (standard Arch convention, unverified here — no real `makepkg` run was available off-machine). Confirmed off-machine: `go build`/`go vet`/`gofmt -l` clean; `phi help`, `phi completion zsh`, `phi man`, and each existing verb's own `-h`/`--help` (`theme`/`state`/`doctor`, unaffected by this refactor) all still render correctly; an unknown verb still falls back to `PATH` cleanly. **Not yet run anywhere:** the chroot build/publish/`pacman -Syu` cycle — `package()`'s two generation lines, and the "does `makepkg` really gzip the man page" assumption, are entirely unverified until a real chroot build runs on `zotac`. `DONE WHEN` ("completions work and are regenerated by the build, not committed by hand") is met by construction off-machine (nothing is checked in; both artifacts come from running the just-built binary at package time) but the VERIFY block itself — tab completion after `phi ` — needs a real shell with the package actually installed. |

## M2 — Shell skeleton and status bar

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-20 | [BLOCKING] phi-shell repository and configuration singletons | verified | phi-shell 5732e8f..70e8883 (8 commits) | 2026-09-08 | User confirmed `DONE WHEN` on real hardware; row moved to `verified` in S-21's commit per this file's own rule (a commit cannot record its own hash in advance). New repository `phi-shell` (first commit `5732e8f` — following S-10's precedent, its own hash is a `phi-shell` hash, not a `phios-dotfiles` one). Scope is exactly the S-20 AGENT bullets, not the full master plan §8.2 tree: `shell.qml` plus the four `Config/` singletons. `Widgets/`, `Services/`, `Bar/`, `Panels/`, `Launcher/` and the rest land with the steps that own them (S-21 onward) — stubbing empty directories for them now would be scope creep git cannot even track. `shell.qml` (`ShellRoot`) instantiates one non-visual `QtObject` delegate per `Quickshell.screens` entry via `Variants` (ADR 077, "designed for N monitors from day one") — deliberately no `PanelWindow` or anything visible, since `DONE WHEN` requires "produces no surface yet"; the first real surface is the bar (S-22). `Config/Tokens.qml` is generated by `phi theme set` (S-12) from a new template this step adds to `phios-dotfiles` (see below) and is gitignored; `Config/Tokens.example.qml` is a checked-in, hand-rendered snapshot of the current dark variant for anyone who has not run `phi theme set` yet — confirmed **not** hand-typed-and-hoped: built the actual `phi` binary from this checkout and ran `phi theme render`/`phi theme set` for real against the new template, `diff`'d the output against `Tokens.example.qml`, byte-identical apart from the two files' intentionally different header comments (one describes a generated file, the other an unmaintained example) — the property values and their order match exactly. `Config/Appearance.qml` exposes semantic roles derived from `Tokens` (structure, accent, semantic tiers, typography, size scale, spacing, shape, z-layers, motion) as proper QML types (`color`/`real`/`int`), converting each token's stored string ("120ms", "14px") itself — `design/README.md`'s own contract ("a consumer that needs a bare number strips the unit"). Tier 3 (syntax), the ANSI 16, selection and terminal-cursor tokens are deliberately **not** exposed here: they are terminal-emulator concepts and no surface in master plan §8.3's inventory needs them; they stay reachable on `Tokens` directly until a real consumer asks, rather than inventing exposure surface nothing uses yet. **A real correctness catch, not a hypothetical one:** `PHI_OVERLAY_SCRIM` is stored `#rrggbbaa` (CSS order, alpha last, per `tokens.dark.sh`'s own comment) but QML's native 8-digit colour string parsing is `#aarrggbb` (Qt order, alpha first) — assigning the token string directly to a `property color` would have silently produced a transparent-blue scrim instead of translucent black. `Appearance._color()` parses the four channels by hand and builds the colour with `Qt.rgba()` instead, applied uniformly to every colour role so there is one code path, not an exception carved out for the one token that would have broken. `Config/Capabilities.qml` shells out to `bin/phios-capabilities` (S-04) via `Quickshell.Io.Process`, resolving the checkout the same way `phi`'s own `internal/tokens.Root()` does (`$PHI_DOTFILES`, else `~/phios-dotfiles`, S-12), falling back to a bare `PATH` lookup; runs once at `Component.onCompleted` and exposes a `refresh()` for later. `Config/Settings.qml` bridges onto `phi state get/set/list` (S-13, already real and installed on all three hosts) the same way, via a dynamically created `Process` per call; nothing calls it yet, the first consumer is the settings panel (S-40). **Interpretive call, surfaced to the user before proceeding, not decided unilaterally:** `phi-shell/CLAUDE.md` said "`Config/Appearance.qml` and `Config/Capabilities.qml` are the **only** files that touch framework types" — read strictly, that would forbid `Settings.qml`'s `Process` bridge, and even `Tokens.qml`'s required `Singleton` base class. Asked the user, who asked to check the roadmap first: S-32 (clipboard, `wl-paste --watch` via `Quickshell.Io`), S-42 (hyprsunset "driven from the shell via `Quickshell.Io` — a timer and a control call") and S-43 (spotlight overlay via the Hyprland event socket) all already plan `Quickshell.Io`/service usage in files that are neither `Appearance` nor `Capabilities` — the literal reading could not have held across the project as already planned. Read instead as scoped to the volatile *service* surface master plan §8.1 actually flags unstable at 0.3.x (`Quickshell.Services.*`, `.Hyprland`, `.Wayland`, `.Bluetooth`, `.Networking`), not the `Singleton` base class or `Quickshell.Io` primitives, which are foundational plumbing every config file needs. `phi-shell/CLAUDE.md` amended in the same commit to say this precisely, so it does not get re-litigated at S-32/S-42. Every QML construct used (`Singleton`, `ShellRoot`, `Variants` and its `delegate` default-property/`modelData` injection, `ShellScreen`, `Quickshell.screens`, `Process`/`StdioCollector` and their exact property and signal names) was checked against the real Quickshell source at `git.outfoxxed.me/quickshell/quickshell` rather than assumed, including one QML-language check that changed the code before it was ever written: QML property declarations (not functions) must start with a lower-case letter, so the "private" helper properties originally named `_cap`/`_out`/`_bridge` were renamed `capRaw`/`output`/`bridgeComponent` before committing. **Convention, not invented here:** phi-shell must be checked out at exactly `~/.config/quickshell/phi` — taken from `phios-user-runbook.md` §6.2's pre-existing `qs -p ~/.config/quickshell/phi` line, which predates this step; documented in `phi-shell/README.md` and is the destination the new `phios-dotfiles` adapter row assumes. On the `phios-dotfiles` side: a new template, `profiles/desktop/templates/.config/quickshell/phi/Config/Tokens.qml.tmpl` (profile `desktop`, matching kitty/mpv — phi-shell is `zotac`/`razer` only, master plan §8.4 says `mini` has no bar), and a new `design/adapters.txt` row, class A with reload `-` rather than `[unknown]`: Quickshell watches every QML file it has loaded and hot-reloads on change by itself (master plan §8.1, "hot reload al salvataggio", confirmed against the real `QuickshellGlobal.watchFiles` property in source) — there is no separate reload command to run or to fill in later, unlike every other `[unknown]` row. Confirmed off-machine with the real `phi` binary, not simulated: `phi theme list` shows the new row; `phi theme set dark --dry-run` proposes exactly one `create` for it under `[A]`, absent from the `unwired` section (unlike the `[unknown]` rows); a real `phi theme set dark` against a throwaway `$HOME` renders it correctly and a second run reports `ok` (idempotent). **No `phi`/`phi-packages` change was needed for this step**: `design/adapters.txt` is read at runtime from the `phios-dotfiles` checkout, not compiled into the `phi` binary, so this row takes effect on `zotac`/`razer` as soon as they `git pull` — no new tag, build or `pacman` install required. **Nothing here has ever been loaded by a real Quickshell process** — `qs -p` has not run anywhere, matching this milestone's own header warning ("You cannot run it. Every visual result is verified by the user"). `DONE WHEN` ("the shell process starts, produces no surface yet, and logs no errors") is therefore entirely the user's to confirm. **Second commit (`156e362`), before handoff, not after a failed verification:** an advisor pass over the first commit caught two defects that would very likely have made `qs -p` fail on first real use. First, `shell.qml`'s `import "./Config" as Config"` was a generic relative-path QML import; Quickshell 0.2+ has its own config-relative module syntax, `import qs.<path> [as <Namespace>]` (`qs` always resolves to shell.qml's own folder), which Quickshell's own guide recommends over a relative import as more LSP-friendly — and source evidence (`src/core/qsintercept.cpp`'s `qs:` URL scheme, which the interceptor explicitly preserves so that "objects created from those paths will [be] able to use singletons") indicates cross-directory `pragma Singleton` resolution goes through that mechanism specifically, not a generic directory import, which the fetched docs never actually confirmed for the cross-directory case despite extensive searching. Changed to `import qs.Config as Config`. Second, `shell.qml`'s startup log only touched `Config.Appearance` — QML singletons instantiate lazily on first use, so `Config.Capabilities` would never have been constructed, its `Component.onCompleted: refresh()` would never have fired, and the `Process`/`StdioCollector` probe inside it — verified against source but never actually exercised — would not have been real signal despite the file's own comment claiming otherwise. Fixed by also reading `Config.Capabilities.gpuVendor` in the same log line, so the first real run is an actual smoke test of that path, not just a parse check. Neither fix touches the `phios-dotfiles` side (adapters.txt/template are unaffected; already re-confirmed unchanged). **Flagged, not fixed, for whichever step first wires the session (S-24):** `quickshell` is approved in master plan §15 (M2, `extra`) but is not declared in any profile's `packages.txt` yet — the same undeclared-manual-install shape S-14 flagged for `smartmontools` on `mini`. The USER block below still has the user install it by hand, matching the S-20 AGENT bullets as written; declaring it is out of this step's stated scope. **First real verification round on `razer`, third commit (`phi-shell` `2b13da5`):** the user ran `qs -p` twice. Once over SSH, which failed with `qt.qpa.xcb: could not connect to display` — expected and not a bug, an SSH session has no Wayland compositor attached; this needs a terminal inside the graphical session itself, flagged for the next handoff. Once on `razer`'s own session, which found a real, load-blocking bug this agent had not anticipated despite the earlier source-level verification: `Config/Tokens.example.qml`, checked in beside the real generated `Config/Tokens.qml`, crashed the whole shell at load with `Config.Tokens.example - Tokens is not a type`. Cause: Quickshell's config-tree module scanner reads every `.qml` file inside a directory that gets `import qs.`'d, whether or not anything actually references that file, and a dot in the middle of a QML type filename is not a valid identifier — the scanner took `Tokens` (everything before the first dot) as the type name for **both** files and collided with the real one. This is exactly the gap the earlier per-type source verification could not have caught: it confirmed every type `phi-shell` code actually references is real and correctly named, but said nothing about Quickshell's own directory-scanning behavior toward a file nothing references at all. Fixed by moving the worked example to `phi-shell/docs/tokens-example.md` — a fenced code block in markdown, not a `.qml` file, so it cannot be swept into a module scan regardless of which directory it sits in; `phi-shell/README.md`, `shell.qml`'s comment, and this repository's own template header comment (`Tokens.qml.tmpl`) all updated to point at the new path. Re-verified off-machine that the template still renders cleanly (80 properties, header comment only affected). **Second real bug found on the very next `qs -p` run on `razer`, fourth `phi-shell` commit (`c911744`):** the `Tokens.example.qml` fix cleared that failure and the load proceeded further — `Config/Tokens.qml`, `Config/Appearance.qml` and `Config/Capabilities.qml` all loaded correctly (confirmed by the error trace reaching past them with no complaint) — and then failed inside `Config/Settings.qml` itself: `Component is not a type`. Cause: `Settings.qml` declares `property Component bridgeComponent: Component { Process { ... } }`, but `Component` is a `QtQml` type and the file only imported `Quickshell`/`Quickshell.Io` — a plain missing import, not a Quickshell-specific surprise this time. Fixed by adding `import QtQml`. The error trace itself confirmed something useful beyond the bug: `import qs.Config` validates every file in the directory as one batch at load time, not only the types a binding actually touches at runtime — the cascading "X unavailable" chain through `Appearance`/`Capabilities`, files that have no reference to `Settings` at all, is that batch validation surfacing through files it happens to process along the way, the same mechanism that made the unrelated, unreferenced `Tokens.example.qml` fatal in the previous round. Re-audited the other three `Config/` files' imports against every type they actually use (built-in primitives — `color`/`real`/`int`/`string`/`var` — and the always-available `Qt` global and `Component.onCompleted` attached signal need no import; only a bare `Component` **type** reference does) and found no further gap. **Correction to this row's own previous paragraph, caught on advisor review before a third failed round trip:** the claim that the missing seventh line was "very likely incomplete scrollback" does not hold — the load aborted inside `Capabilities.qml` on that run, so `Appearance.qml`'s bindings, including the ones reading `Tokens.bg0`, never actually evaluated; nothing in that transcript is evidence `Config/Tokens.qml` exists on disk on `razer` at all, and the six `ok` lines shown are exactly the six pre-S-20 adapters. This is now an open, unconfirmed question the next `USER` block checks directly (`ls -l`, `phi theme list`) rather than argued from indirect evidence again. **Fifth `phi-shell` commit (`0fdebe6`), same review, before sending the next round back:** rather than wait for the next single-error report (the loader aborts at the first fatal error, so every real run so far has surfaced exactly one missing import and no more), audited every property in all four `Config/` files against Qt's actual value-type documentation and found two more of the same class of bug pre-emptively: `Capabilities.qml` uses `Component.onCompleted` (needs `QtQml`, same failure shape as the `Settings.qml` fix — "Non-existent attached object") and `Appearance.qml` declares roughly twenty `property color` fields and calls `Qt.rgba()`, neither of which had been reached by the loader on any run yet — `color`/`font` and the `Qt.rgba`/`hsla`/`darker`/`lighter`/`tint` helpers specifically require `QtQuick`, not `QtQml` (confirmed against `doc.qt.io`, not assumed: `int`/`real`/`bool`/`string`/`var`/`list`/`date`/`double`/`url`/`variant`/`void` are QML-native and need no import at all, which is the actual, verified line between what needed fixing here and what didn't). **Standing lesson for whichever step next touches `phi-shell` QML, not just this one:** this loop — one real run, one missing import found, one fix, repeat — is structurally slow because a QML loader stops at the first fatal error. `qmllint` (part of `qt6-declarative`, a Quickshell dependency already installed wherever Quickshell is) reports every file's own type/import errors in one pass and runs headless, so it belongs in the `USER` block *before* `qs -p` on every future round that touches this repository's QML, not only used reactively after a crash. **`qmllint` itself turned out not to be on `razer`'s `PATH`** (`command not found`) — not chased further, since the next real `qs -p` run produced better signal anyway: `INFO: Configuration Loaded` — the shell genuinely starts now, past every import bug above, with only non-fatal `WARN`-level `ReferenceError: Tokens is not defined` messages (one per property in `Appearance.qml`) rather than a load abort. **Two more real findings, both root-caused with actual evidence rather than guessed at, sixth `phi-shell` commit (`c525360`):**
  1. `Config/Tokens.qml` still does not exist on `razer` (`ls -l` confirms) even after a fresh `phi theme set dark` — but this is not a bug in this step's work: `razer`'s `phios-dotfiles` checkout has two remotes, `github` (this session's own, matching exactly) and `origin` (`flavio@mini:/srv/git/phios-dotfiles.git`, the user's own server), and its `master` branch tracks `origin` — so a bare `git pull` there follows the user's own sync cadence from `mini`, not this session's pushes to `github` directly. Exactly the architecture `CLAUDE.md` already describes ("the user's server receives pushes only from the user"); nothing to fix here, the row will reach `razer` once the user's own `github`→`mini` sync happens (or by explicitly pulling `github` directly for testing).
  2. `gpu=none` in every run so far was never a probe failure — the user proved both `bin/phios-capabilities` directly and the *exact* `sh -c` command `Process` runs work correctly by hand, full output, `exit=0`, with `$HOME` set as expected. The real bug was a race baked into the verification itself: spawning `probe` and reading its stdout is asynchronous, so `shell.qml`'s log line, which reads `Config.Capabilities.gpuVendor` in the very same `Component.onCompleted` tick that calls `refresh()`, was always going to observe the still-unpopulated default — regardless of whether the probe actually succeeded underneath. Fixed by adding `onCapRawChanged: console.log(...)` inside `Capabilities.qml` itself, so the real value is logged reactively once it actually arrives, making the check meaningful instead of a coin flip decided by timing. **Lesson for future async-probe singletons in this shell:** a synchronous read of an async singleton's property, taken at `Component.onCompleted` time, proves the file *parses*, not that the async path inside it *works* — those need their own reactive confirmation, not a bare log line at startup. **This did not fully close the question, seventh `phi-shell` commit (`f076a94`):** the next real run, `~/.config/quickshell/phi` and `~/phios-dotfiles` both pulled fresh (the latter explicitly from `github`, bypassing `origin`'s slower sync per the note above), `phi theme set dark` now genuinely created `Config/Tokens.qml` (`create`, not the missing row from before), and the shell loaded with **`variant=dark` and zero `ReferenceError`s** — the `Tokens.example.qml`/`Settings.qml`/`Capabilities.qml`/`Appearance.qml` import chain is confirmed working end to end for the first time. But the reactive `onCapRawChanged` log fired twice, both times still `gpu=none` — including the second firing, which lands *after* `INFO: Configuration Loaded`, so it cannot be explained by the same startup race the previous commit fixed; something about the real, later-arriving probe result is still empty or unparsed. Since the user already proved the exact `sh -c` string this file runs produces correct output by hand (`exit=0`, `PHI_CAP_GPU_VENDOR=intel`), the remaining suspect is an environment difference between an interactive shell and Quickshell's own child process — most likely `$HOME` not reaching it. Rather than guess again, added a debug fallback (only reached if both the dotfiles-relative and bare-PATH invocations fail) that echoes `$HOME`/`$PATH`/the resolved dotfiles path, plus an unconditional raw-output log in the `StdioCollector` handler so the next run shows what `Process` actually received, not just the parsed (and possibly silently-empty-parsed) result. The new shell syntax was verified directly with real `sh -c` off-machine before committing — both the success path (a throwaway fake script standing in for `phios-capabilities`) and the failure/debug path — rather than trusted from the QML string alone, since three separate quoting/import mistakes earlier in this same step are reason enough not to trust a fourth one unverified. These debug lines are temporary and should come back out once the real cause is found. **The debug round settled it decisively, eighth `phi-shell` commit (`70e8883`), same round:** the raw-output log showed Quickshell's `Process` received exactly the correct, complete output (`PHI_CAP_GPU_VENDOR=intel` included, `razer` being a Razer Blade 13 2020 with no discrete GPU, confirmed by the user) — the same output the user's manual reproduction produced. Independently confirmed the parsing logic itself was never the bug either: ran the exact `onStreamFinished` parsing code, unmodified, in a throwaway Node.js script against the exact bytes the real run captured, and it produced the correct `gpuVendor: "intel"`. So neither the shell command, the environment reaching Quickshell's child process, nor the parser were ever at fault — all three hypotheses this row worked through in turn were each individually correct on their own terms and each still left the real bug standing. **Root cause:** `onCapRawChanged`'s `console.log` read the *derived* `Capabilities.gpuVendor` property (`capRaw.gpuVendor`) from inside a handler triggered by `capRaw`'s own change signal — and that specific indirection printed the stale default even on the exact call where `capRaw` had just been set correctly, a QML property-binding-propagation-ordering detail this step did not chase to ground (out of scope for a skeleton step; noted here in case a future singleton's reactive handler shows the same symptom). Fixed by logging `next.gpuVendor` directly at the point it is computed, with no property-binding indirection between the correct value and the log line. Also reverted the previous commit's debug-fallback echoes and raw-output dump entirely, since the hypothesis they were built to test is now disproven and they no longer serve a purpose — `Capabilities.qml`'s `Process` command is back to its original, simpler two-legged form. **Four real, distinct bugs found and fixed across this step's real-hardware verification, each isolated with actual evidence rather than guessed at in sequence:** a QML-invalid dotted filename colliding with a real type, two missing `QtQml`/`QtQuick` imports, and this property-indirection logging bug — none of which the extensive pre-commit source-level verification against real Quickshell/Qt documentation could have caught, since all four are either runtime behaviors (the last three) or a directory-scanning behavior distinct from any single file's own correctness (the first). **`DONE WHEN` confirmed on real hardware, both desktop hosts:** `razer` — clean load, `variant=dark`, zero `ReferenceError`s, `phi-shell: capabilities refreshed, gpu=intel` (a Razer Blade 13 2020, Intel-only, per the user — correct). `zotac`, tested by the user on their own initiative beyond what this step asked for — same clean load, `gpu=nvidia` — correct for the one host in this project with a discrete GPU, and good independent confirmation that `Config/Capabilities.qml`'s probe is genuinely reading real per-host hardware rather than coincidentally right on one machine. `mini` was not tested, consistent with master plan §8.4 ("`mini`: Nessuna barra") — it has no shell to run. **Not yet touched by this step, deliberately, and worth a note for whoever starts S-21:** the `quickshell` package gap flagged earlier in this row for S-24 is still open; `Widgets/`, `Services/`, `Bar/`, `Panels/`, `Launcher/` and the rest of master plan §8.2's tree do not exist yet and land with the steps that own them. **Correction, found during S-36 (2026-09-08), phi-shell commit `ee197b6`, the most severe instance of a bug found across the whole repo that session:** `Config/Capabilities.qml`'s hardware probe left `running` true after each run instead of resetting it in `onExited`. Because `Process.onFinished()` (`io/process.cpp`) calls `startProcessIfReady()` unconditionally as its last step, this meant `bin/phios-capabilities` has been respawning in a tight, uninterrupted loop from the moment `phi-shell` starts, on every real session run to date since this step — not a periodic re-probe, a continuous one. Fixed by setting `running = false` in `onExited`. Real-hardware CPU/battery impact from this specific defect was never measured before the fix (no `phi doctor`/`top` capture from that period isolates it), so its practical severity on `razer`/`zotac` is unconfirmed, only its mechanism. |
| S-21 | Styled widget library | verified | phi-shell 9ce095d..7e00a45 (2 commits) | 2026-09-08 | User confirmed the widget→state→rule table and DONE WHEN by reading the code and the handoff report; row moved to `verified` in S-22's commit per this file's own rule (a commit cannot record its own hash in advance). **Not yet reachable by any real `qs -p` run, and it will take more than a `git pull` to get there — flagged here so the first S-22 round doesn't misread the cause of a failure:** (1) `Config/Tokens.qml` on `zotac`/`razer` is `phi theme set`'s pre-S-21 render and has no `borderWidth` property yet; `Appearance.qml`'s new `_px(Tokens.borderWidth)` binding would read `undefined` and every border width — `Separator`'s entire thickness included — would be `NaN` until `phi theme set dark` (or `light`) is re-run against a `phios-dotfiles` checkout that actually has this step's `PHI_BORDER_WIDTH` token. (2) S-20's own row already recorded that `razer`'s `phios-dotfiles` checkout tracks `origin` (the user's `mini` server), not `github` directly — a bare `git pull` there follows the user's own sync cadence and will not pick up this session's push on its own. (3) Nothing imports `qs.Widgets` anywhere yet (`shell.qml` still only imports `qs.Config`), so even a fully-updated, fully-pulled `qs -p` gives zero signal about whether these eleven files parse — a clean load next to S-22's own new code is not evidence this step's files are sound, only that they were never reached. The mechanical `grep` below and a `qmllint` pass (suggested in the handoff, not run here — S-20's row already found it missing from `razer`'s `PATH`; it ships with `qt6-declarative`, typically at `/usr/lib/qt6/bin/qmllint`) are the only checks available before S-22 actually imports this directory. **Second commit (`7e00a45`), before handoff, not after a failed round-trip, same practice S-20 established:** an advisor pass caught `Segment.qml`'s `StyledText { id: text; text: root.label }` — an id identical to the object's own `text` property, a known QML ambiguity — renamed to `iconGlyph`/`labelText` to match `ListRow`'s existing naming; grepped every other file's `id:` declarations against its own property names afterward and found no second instance. Scope is exactly the S-21 AGENT bullets: ten types under `phi-shell/Widgets/` (`StyledText`, `StyledButton`, `StyledIcon`, `Pill`, `Segment`, `Popover`, `Panel`, `ListRow`, `Separator`, `Scrim`), no `Services/`/`Bar/`/`Panels/` — those land with the steps that own them (S-22 onward). **The referenced style-plan document does not exist in this checkout**, flagged here rather than asked about: `CLAUDE.md`, the agent brief and master-plan §6 all cite `phios-piano-stilistico-finale.md` (`docs/phios-piano-stilistico-finale.md` — not present; confirmed by listing every `.md` file in all four repositories), but every rule this step actually needs is quoted verbatim in the S-21 AGENT bullets themselves and independently restated in master-plan §8.5/§8.6 — two independent sources agreeing, not a conflict between two planning documents, so this did not block the step per `CLAUDE.md` rule 4's own "disagreement between two planning documents" test. Left open for whoever needs a *different* section of that document later. **Seven-state architecture** (style plan §12/master-plan §8.6: "stati trasversali obbligatori per ogni componente: default hover active/pressed focus disabled loading invalid"), applied honestly rather than uniformly faked: self-detected via `HoverHandler`/`TapHandler`/`activeFocus` on the four interactive controls (`StyledButton`, `Pill`, `Segment`, `ListRow`); parent-driven (plain settable booleans, no hit-testing of their own) on the six passive/decorative ones (`StyledText`, `StyledIcon`, `Separator`, `Scrim`, `Panel`, `Popover`) — each of those documents in its own file comment exactly which of the seven has a defined visual effect and which does not (`Separator`/`Scrim` are the honest extremes: only opacity/presence differ, hover/press/active/focus are structurally present but produce no visible change, stated plainly rather than faked with an unused `HoverHandler`). `Panel`/`Popover` are the one case where all seven are fully meaningful (a real bg/border/opacity surface), `StyledText`/`StyledIcon` fall in between (invalid/disabled/loading are real, hover/press/active/focus are not, since a label has no defined "pressed" look of its own). All ten route their precedence and colour recipe through one shared file, `Widgets/WidgetStates.js` (`resolve` — the seven-state precedence, disabled > invalid > loading > active/pressed > focus > hover > default; `surfaceColors` — the affordance recipe below; `opacityFor`; `contentColor`; `fontPixelSize`; `chToPixels`), which is what "zero duplication inside surface class 1" (master-plan §8.2's own words for this directory) actually means: one precedence and one colour table, not ten. **Affordance rules (style plan §6/master-plan §8.6), enforced in the widgets, one rule per bullet the AGENT card names:** "label di sistema -> basso contrasto, monocromo" — `StyledText`/`StyledIcon`'s `kind: "label"` renders `textMuted`, never coloured, and is the default reading for chrome text. "valore/dato -> colore Tier 2 solo su soglia" — `tone` ("error"/"warn"/"success"/"info") is opt-in and empty by default on every widget that has it (`StyledText`, `StyledIcon`, `Segment`); the widget never picks a threshold itself, it only renders the tone the caller already decided. "interattivo inattivo -> stesso peso, opacità ridotta" — `WidgetStates.opacityFor` fades disabled/loading by one shared constant (`INACTIVE_OPACITY = 0.45`, un-derived from any token since design/tokens has no opacity tier — flagged the same way other steps flag a placeholder ratio) without changing colour or weight. "interattivo attivo/selezionato -> inversione piena" — `WidgetStates.surfaceColors`'s `"active"` case swaps bg/fg to `accent`/`accentText` for every interactive control; `Pill`'s `checked` state is exactly this rule, which is also §8.6's own worked example ("componente toggle standard: pillola radius-pill, segmento attivo pieno in accento"). "glifo '>' -> riservato al solo punto di input attivo" — enforced structurally, not just by convention: `StyledIcon.qml`'s own file comment states outright that it deliberately has no `activeMarker`-style property, so no other widget in the directory *can* render this glyph; `ListRow` is the one place it appears, rendered directly in its own template, shown exactly when its resolved state is `"focus"` (keyboard navigation position — the "active input point"), never for hover/press/persisted-`active`. **Two real bugs caught before this commit, not after a failed round-trip** (this repository's own `CLAUDE.md`/`phi-shell/CLAUDE.md` both say nothing here can be run — every one of these had to be caught by re-reading rather than observing a failure): (1) `Appearance.qml`'s own documented contract — `space-N` tokens are stored in `ch` of `font-mono`, not px ("a caller that needs px measures the font itself and multiplies") — was violated in an early draft that used `Config.Appearance.space3` directly as a pixel margin; fixed by giving every widget that needs real pixel spacing (`Panel`, `StyledButton`, `Pill`, `Segment`, `ListRow`) its own three-line `TextMetrics` measuring the "0" glyph (the same definition CSS's `ch` unit uses) at the base chrome size, plus a shared `WidgetStates.chToPixels(chCount, chWidth)` for the arithmetic. (2) A duplicate `Behavior on <property>` declared both inside a reused widget's own file (`StyledText`'s internal `Behavior on color`) and again at its usage site (`StyledButton`'s label instance) or, identically, inside a base type (`Panel`'s `Behavior on opacity`) and again in a file extending it (`Popover`) — untested and unnecessary, since a `Behavior` attaches to the property itself and keeps animating any override regardless of which layer supplies the value; both redundant declarations were removed rather than left as an unverified risk. **One QML-mechanics question resolved by reading real Quickshell source rather than assumed, matching S-20's own established practice:** whether the shared `WidgetStates` helper could be a `pragma Singleton` QML type (colocated with the widgets the same way `Config/`'s four singletons resolve each other bare) — fetched `git.outfoxxed.me/quickshell/quickshell`'s `src/core/singleton.hpp` directly and found `Singleton: public ReloadPropagator` with no `DefaultProperty`, meaning it cannot host a nested child object (a `TextMetrics`) at all; reverted to a plain `.pragma library` `.js` file instead, which every consuming widget imports explicitly (`import "WidgetStates.js" as WidgetStates` — unlike a sibling singleton, a `.js` library is not resolved bare) since directory-implicit QML modules only ever register `*.qml` files as types, confirmed against Qt's own documented module-resolution rules rather than assumed. **Second QML-mechanics check, same practice:** `Segment`'s icon+label pair was drafted with a `Row` and `anchors.verticalCenter` on each child; verified against Qt's own `Row` documentation, which specifically forbids horizontal-axis anchors (`left`/`right`/`horizontalCenter`/`fill`/`centerIn`) on a Row's children, not the cross-axis one originally used — the code was still rewritten as a plain anchored `Item` (safer and avoids the restriction question entirely) rather than kept on the strength of a technicality. **phios-dotfiles side:** `design/tokens.common.sh` gains `PHI_BORDER_WIDTH='1px'` (Shape section, next to `radius-base`/`radius-pill`) — no `§6.3` token covered stroke width before this step (`Separator`, every `Panel`/`Popover`/`StyledButton`/`Pill` border and the generic focus ring all needed one), marked `[PLACEHOLDER]` in the same header block as `radius-base` for the same reason (unverified on real fractional-HiDPI output); `design/preview.tmpl` and `phi-shell/docs/tokens-example.md` both updated so neither goes stale. No `phi`/`phi-packages` change needed — `internal/tokens.parseInto` is a generic `KEY=VALUE` line parser (confirmed by reading `phi/internal/tokens/tokens.go` rather than assumed), so a new token key needs no code change there, matching S-12's own note about the same file. **`DONE WHEN` ("no widget contains a literal colour, size, or duration") verified mechanically, not by inspection:** `grep -n '#[0-9a-fA-F]\{3,8\}\|[0-9]px\|[0-9]ms' Widgets/*.qml Widgets/*.js` returns nothing. The only bare numeric literals anywhere in the directory are the `"0"` glyph text used to measure `chWidth` (not a size), `0`/`1` as the two endpoints of an opacity fade (not a size, colour or duration — the same category as the already-justified `INACTIVE_OPACITY` constant), and `Separator`'s `implicitWidth`/`implicitHeight` `0` on its non-thickness axis (no intrinsic length opinion, not a size decision — the consumer sizes it via anchors/layout). **Nothing here has been loaded by a real Quickshell process** — this step needs none, unlike S-20: `VERIFY` is "Agent reports the widget list and which style-plan rule each enforces," not a `qs -p` run, so there is no `USER`/machine round-trip to hand off. That report is in the handoff message, not repeated here. `DONE WHEN` is the user's to confirm by reading the code and that report, same as any other step, before this row moves to `verified`. |
| S-22 | [BLOCKING] Status bar with declarative module registry | verified | phi-shell 90d3945 | 2026-09-08 | **Verified by the user's word, not by the screenshot `VERIFY` actually asks for** — the user confirmed on `2026-09-08` that the bar works on real hardware but could not provide the screenshot at that time; recorded here exactly as that, a verbal confirmation standing in for the documented `VERIFY` step, per `CLAUDE.md` rule 5 ("say plainly what you could not verify"). If a screenshot surfaces later and shows something this row does not anticipate, it overrides this record, not the other way around. **`Q-N02` and `Q-N03` closed by the user in the same message, with the compositor in front of them exactly as `DONE WHEN` asked:** `Q-N02` ("elenco workspace in barra: per-monitor o condiviso?") — **per-monitor confirmed**, `Workspaces.qml`'s existing default needs no code change. `Q-N03` ("btop: workspace normale a ID alto e persistente, o special workspace a comparsa?") — **closed as a special workspace dedicated to btop, with a visual toggle on the bar treated the same as the workspace buttons** — a real, specific UI commitment (a new bar element, not yet built by this step or any other) recorded here and in `docs/phios-master-plan.md` §17.1/§19 (ADR 121/122) so it is not lost before whichever step actually builds btop's launch mechanism. **New gap surfaced during this verification, not a defect in what shipped:** the user has no Hyprland keybindings configured yet, so nothing keyboard-driven about this bar (or about Hyprland generally) could be exercised during this round — only what a mouse click reaches. Flagged here for every future step whose `VERIFY` assumes a keybinding exists: `S-38` ("Keybinding scheme") is where this actually gets closed, but any step before it that wants a keyboard-driven check needs to ask first whether the relevant bind exists yet, not assume it. Noted against `Q-N07` in `docs/phios-master-plan.md` §17.1 as well. **Ran successfully on real hardware** — the user reported the bar works after the `USER` block's render/pull/restart sequence, but did not separately confirm the second half of `VERIFY` (editing `modules.json` alone, restarting, and re-screenshotting to demonstrate a one-file data change) or say which host(s) they checked — recorded as confirmed-but-underspecified rather than assumed complete in every particular. `shell.qml`'s per-screen `Variants` slot, holding nothing since S-20 ("the first real surface is the bar"), now holds a real `PanelWindow` (`Bar/Bar.qml`): three islands (left workspaces, centre active window title, right status cluster ending with the clock, master-plan §8.4), composed from `Bar/modules.json` — `type`/`island`/`position`/`source`/`capability` per row, exactly the AGENT bullet's schema — rather than hardcoded (ADR 078: type is code written once, instance is data). `componentFor()` is the one place a new module type needs code; an unrecognized type logs and renders nothing rather than aborting the whole bar, the same discipline `phi doctor` (S-14) uses for a check it cannot evaluate. `capabilityMet()` gates a module on `Config.Capabilities` (ADR 074) and handles both the boolean keys (`battery`, `wifi`, `bluetooth`, ...) and the one string-valued key, `gpuVendor` (`"none"` when absent) — caught and fixed before commit, not left as a comment for S-23 to discover as a silently-hidden GPU module, since S-23's own card names that exact module. **Scope is exactly S-22's three module types** (`workspaces`, `activeWindow`, `clock`) — the full per-host inventory (volume, network/Tailscale, bluetooth, battery, brightness, GPU, night mode, Φ agent) is S-23's, per its own card ("Depends on: S-22, S-04"), not inferred, that boundary is what both cards actually say. `Workspaces`/`Clock` are built on `Widgets.Segment` (S-21); `ActiveWindow` is a plain `Widgets.StyledText` instead — it needs a width-constrained `elide` Segment has no property for, and a truncating title has no popover content to disclose anyway, so "three-level disclosure built into the Segment type" (an AGENT bullet) is read as the Segment type's own general contract, already satisfied by S-21, not a mandate that every module instance literally be one. **New file `Services/HyprlandBridge.qml`, and `phi-shell/CLAUDE.md` amended in the same commit to sanction it:** the service-surface restriction (`Config/Appearance.qml`/`Config/Capabilities.qml` only, S-20) could not hold literally once a real bar module needed live Hyprland data — master-plan §8.2's own repository tree lists `Services/` with concrete content (`Notifications`, `Clipboard`, `Chroma`, `Idle`, `Agent`) that cannot exist without touching a service surface, and this step's own AGENT bullet names "internal service" as a first-class data source alongside a `phi` verb; same resolution S-20 already used for `Quickshell.Io`, checked against the roadmap rather than asked about. Kept thin per that amendment's own words: re-exports `Hyprland.workspaces` and `Hyprland.activeToplevel`, nothing else; every module reads it, never `Quickshell.Hyprland` directly, so a 0.3.x API break lands in one file. **Per-monitor matching** (workspaces belonging to a screen; whether the active window is on it) compares `HyprlandWorkspace.monitor.name`/`HyprlandToplevel.monitor.name` against the bar's own `screen.name` — both real Wayland output names — rather than caching a `HyprlandMonitor` reference via `Hyprland.monitorFor(screen)`, since that is a plain invokable with no change-notification to promise the cached value stays current. **Every Quickshell type and property used here was checked against real Quickshell source** (`git.outfoxxed.me/quickshell/quickshell`), matching S-20's own practice, not assumed: `PanelWindow`/`anchors`/`exclusiveZone` (`src/core/windowinterface.hpp`, `panelinterface.hpp`), `Hyprland`/`HyprlandWorkspace`/`HyprlandMonitor`/`HyprlandToplevel` (`src/wayland/hyprland/ipc/*.hpp` — confirmed `HyprlandWorkspace` has no "last active toplevel per workspace" property, only the one global `HyprlandToplevel.activated`, which is why "active window is LOCAL focus" is implemented as "blank unless the global active window happens to be on this monitor" rather than a remembered-last-active heuristic this step had no evidence to justify), `ShellScreen.name` (`src/core/qmlscreen.hpp`), `FileView` (`src/io/fileview.hpp` — `path`/`text()`/`loaded()`), `UntypedObjectModel` (`src/core/model.hpp` — a real `QAbstractListModel`, confirmed safe as a `Repeater` `model:` directly). **One real bug caught before commit, not after a failed round-trip:** the first draft of `Workspaces.qml` bound its `Repeater` to `Services.HyprlandBridge.workspaces.filter(...).sort(...)` — a fresh array on every dependency change, including a workspace's own `active` flag flipping on every switch — which would have rebuilt every `Segment` delegate on every workspace change instead of animating in place, silently discarding the motion-B colour transition S-21 built. Fixed by exposing the real, stable `Hyprland.workspaces` model directly from `HyprlandBridge` (not a `.values` copy) and moving the per-monitor filter into each delegate's own `visible` binding — `Row` skips invisible children when positioning, so this costs nothing visually and keeps per-row identity stable across a switch. **Known, undecided, not a defect:** `leftIsland`/`rightIsland` size to their own natural Row content width; a long workspace name shrinks the centre title's available space rather than truncating itself, since `ActiveWindow` is the only module type with an `elide` (the only one whose text length this step cannot predict) — left unbounded rather than adding an untested width cap ahead of seeing whether real workspace names ever get long enough for this to matter. **`Bar/modules/` stays lowercase**, exactly as master-plan §8.2's own repository tree names it: `Bar.qml` imports it with a plain relative `import "modules" as Modules`, not the newer `qs.Bar.modules` config-relative form, because Quickshell's own guide requires every `qs.`-namespace path segment to start with an uppercase letter — a real, narrow conflict between the plan's literal directory name and Quickshell's mechanism, resolved by using the import form that carries no such constraint rather than renaming a directory the plan already named. **The referenced style-plan document remains absent** (see S-21's row) — nothing in S-22 needed a section of it that S-21's row did not already cover. **`DONE WHEN` met:** the registry mechanism works on real hardware (user-confirmed) and both open questions it gated on are closed. `S-21`'s eleven widget files and this step's seven entered a real Quickshell load together for the first time here and the combination works; no `phi theme set`/render failure or QML load error was reported. |
| S-23 | Bar modules | verified | phi-shell f1a28b9..42c6bf3 (2 commits) · dotfiles b0a5bcf..9af2653 (2 commits) | 2026-09-08 | **Not yet run anywhere — `qs -p` has not loaded any of this.** Scope actually shipped: 7 of the 11 types the AGENT card names — `volume`, `network` (Tailscale), `battery`, `wifi`, `bluetooth`, `gpu`, `phiAgent` — added to `Bar/modules.json` (positions 10/20/30/40/50/60/90, gapped for future insertion, clock left at 100 exactly where §8.4 requires it, "termina con l'orologio"). `brightness` and `night mode` are deliberately NOT in `modules.json` and have no `Bar/modules/*.qml` file — see the two closed risks below; both are real scope reductions from the card's literal type list, not oversights. **C-11 closed, by the documentary verification its own "Chiusura proposta" already prescribed** ("Verifica documentale [AGENT] prima di S-23"): read the real Quickshell source tree directly (`git.outfoxxed.me/quickshell/quickshell`) rather than the docs site (returns HTTP 403 to this agent) — every top-level `src/` directory (`bluetooth`, `build`, `core`, `crash`, `dbus`, `debug`, `io`, `ipc`, `launch`, `network`, `services`, `ui`, `wayland`, `widgets`, `window`, `windowmanager`, `x11`) and every `src/services/*` subdirectory (`greetd`, `mpris`, `notifications`, `pam`, `pipewire`, `polkit`, `status_notifier`, `upower`) enumerated directly — no brightness/backlight module exists anywhere in 0.3.x. Fallback taken as prescribed: `brightnessctl` via `Quickshell.Io`, already in `laptop/packages.txt` since before this step — but per C-09 ("controllo luminosità... nel pannello Devices e nel popover volume/luminosità, non come segmento permanente"), a permanent bar segment was never the right shape for it anyway, so no `Bar/modules/Brightness.qml` was written at all; the verification closes C-11, it does not obligate a segment C-09 already said not to build. That popover integration waits for whichever step first gives this shell a working floating-popover mechanism (see below) alongside the Devices panel (`S-40`). **`night mode` dropped from this step for the same shape of reason, caught on advisor review before committing, not shipped and then walked back:** master plan §8.4's table lists it for `razer` only, but nothing in `bin/phios-capabilities`' probes is a genuine night-mode hardware fact — the nearest candidate, `backlight` (real: `/sys/class/backlight/*`, present on an internal panel, absent on zotac's external-monitor desktop), would make the segment appear or disappear based on whether a monitor happens to expose a kernel backlight class, not based on anything about night mode itself; ADR 074 ("the shell never asks 'am I a laptop'") reads that as a laptop-identity proxy wearing a capability costume, not a real gate. `phi state`'s `night-mode` key (`S-13`) has existed since M1 and could back a toggle today, but the toggle would control nothing — `S-42` (Night shift and True Tone) is the step that gives it a real effect, still unbuilt — so shipping the segment now would be UI with no backend and a capability gate that cannot be justified, the same failure shape C-09 already named for brightness. Left for `S-42` to add, once it also has an answer for whether night mode is laptop-only by design or just untried on a desktop. **`Widgets/Popover.qml` (S-21) is still not wired to anything — this step did not become the first to use it.** Checked real Quickshell source and confirmed the missing piece exists (`src/window/popupwindow.hpp`: a real `PopupWindow` QML type, anchored via `PopupAnchor` — `item`/`window`/`edges`/`gravity`/`rect`, both confirmed against source) but nothing in this shell has ever exercised it, and this step chose not to be the first attempt at an entirely new, unverified floating-surface mechanism for content (5 info rows + 2 actions) that this step's own VERIFY never actually asks for — VERIFY is a bar screenshot and a capability check, not a popover interaction. In its place, every discrete-state module (`network`, `wifi`, `bluetooth`) implements "on request" the cheap way: a local `detailsShown` boolean that swaps the segment's own `label` between a quiet default and the real detail (hostname, SSID, connected device name) on click — satisfies "SSID a richiesta" and ADR 067's "shows the overlay name" literally, with no new window-surface risk. Battery/GPU communicate their anomaly state through `tone` alone (§8.5's level-1 "cambia stato solo su soglia"), with no level-2 content at all yet. Flagged plainly: this is a real, deliberate scope reduction against §8.5's "vincolante per ogni segmento," not a secret one — S-22's own three module types never opened a popover either (`Workspaces`' `activated()` switches workspace, `Clock` has no popover at all), so no module in this shell has used one yet; whichever step first genuinely needs multi-row popover content is the one that should spend the first real round-trip on `PopupWindow`. **No module in this step uses `Widgets.StyledIcon`/`Segment.glyph`.** S-22's three types never set `glyph` either, so font-symbol's actual glyph coverage has never been exercised by any step on real hardware, and this agent has no way to inspect the real font file's codepoints from here. Every discrete-state indicator (mute, Tailscale/Wi-Fi/Bluetooth connected-or-not, the Φ mark itself) is a short plain-text label routed through `Segment.label`/`StyledText` instead of an icon glyph — safe by construction, a deliberate deviation from §8.4's "icona solo per stato discreto/binario" in favour of not shipping the first-ever icon-glyph usage sight-unseen. A one-line change per module once a real font inspection confirms specific codepoints. **New `Config/Capabilities.qml` property, `nvidiaGpu`** (`capRaw.gpuVendor.split(",").includes("nvidia")`, membership not equality — `bin/phios-capabilities`' own doc comment says `PHI_CAP_GPU_VENDOR` is comma-separated for a hybrid-graphics host): `gpu` module's capability, not the existing `gpuVendor` string key S-22's `capabilityMet()` already handles generically (that check is `value !== "none"`, which would also pass for razer's `intel` and show the module on the wrong host) — named for the vendor because the mechanism behind it, `nvidia-smi`, only exists for nvidia; an AMD path would need its own tool and its own capability name later, not a rename of this one. **Bluetooth capability tension, flagged rather than resolved by guessing:** gated on the real `Config.Capabilities.bluetooth` probe (S-04), same as `wifi`/`battery`. Master plan §5's own host summary lists Bluetooth as installed on `zotac` too, while §8.4's per-host bar table lists this segment for `razer` only — unresolvable from here since it depends on what `zotac`'s live probe actually reports (a permanently-attached vs. situational USB dongle would decide it, and this agent cannot see the machine). If `zotac`'s screenshot shows a bluetooth segment, that is the capability system doing exactly what `DONE WHEN`/ADR 074 ask ("driven by capabilities," never by host identity) — it is §8.4's table that would need a follow-up correction, not this code. **Five new `Services/*.qml` bridges**, each a thin re-export per `phi-shell/CLAUDE.md`'s existing rule (unchanged this step, already broad enough to cover them): `AudioBridge` (`Quickshell.Services.Pipewire` — `Pipewire.defaultAudioSink`, bound via `PwObjectTracker` since a `PwNodeIface`'s `audio` sub-object is only populated once bound, confirmed against `qml.hpp`'s own doc comment, not assumed), `PowerBridge` (`Quickshell.Services.UPower` — `UPower.displayDevice`; `changeRate` is watts, the wrong unit for the card's own "%/hour" threshold, so the discharge rate is derived here from `percentage` sampled on a plain 60s timer instead of guessing a watts-to-percent conversion this file has no `energyCapacity`-independent way to verify), `BluetoothBridge` (`Quickshell.Bluetooth` — `Bluetooth.devices` is already "all CONNECTED bluetooth devices," per `bluez.hpp`'s own doc comment, so this file does not re-filter it), `WifiBridge` (`Quickshell.Networking` — finds the `NetworkDevice` with `type === DeviceType.Wifi`, then the one `Network` in its `networks` model with `connected === true`, reading that network's own `name` for the SSID), `Tailscale` (not a Quickshell service surface at all — `tailscale status --json` via `Quickshell.Io.Process`, the same shell-out pattern `Config/Capabilities.qml`/`Config/Settings.qml` already use; its own file comment states its ADR 067 contract plainly: it must never read `TailscaleIPs` from the JSON, only `BackendState` and `Self.HostName`). Every property name on every wrapped Quickshell type was checked against real source at `git.outfoxxed.me/quickshell/quickshell` before writing the QML that reads it, same practice S-20/S-21/S-22 established — not reproduced verbatim here, this row would be unreadable; the file comments carry the citations. **`phios-dotfiles` package additions, both approved at M2 in master plan §15 already** (`| upower | extra | ... | M2 |`, `| networkmanager | extra | ... | M2 |`), placed in `laptop/packages.txt` (`razer` only) rather than `desktop` (both hosts): UPower backs the battery module and NetworkManager backs the wifi module, and neither has a reason to run on a battery-less, wifi-less desktop — `upower.service`/`NetworkManager.service` added to `laptop/services-system.txt` alongside the existing `tlp.service` (the installer only ever prints declared services, per this repo's own `CLAUDE.md`; enabling them is a `USER` step below). `Bar/modules/Gpu.qml`'s level-3 deep-link launches `btop` (`kitty -e btop`, detached `Process`) directly rather than through the special-workspace mechanism `ADR 122`/`Q-N03` (S-22) described: the Hyprland window rule that would actually assign btop's window to that workspace is `S-24`'s own AGENT bullet ("btop workspace"), unbuilt, and a persistent bar toggle for it is unassigned to any step (flagged in S-22's own row) — `togglespecialworkspace` today would just show an empty workspace, so a direct launch is what actually works, migratable to `Hyprland.dispatch()` in one line once `S-24` lands. `nmtui` (ships inside `networkmanager`, no extra package) and `pulsemixer`/`wiremix`/`bluetuith` (all explicitly `M4` in master plan §15, `bluetuith`'s own row says "extra/AUR — verificare") were considered and left unwired for `wifi`/`volume`/`bluetooth` — the first because `Segment` has only one `activated()` signal and `Wifi.qml` already spends it on the SSID reveal `funzionalita` names explicitly, the rest because installing an M4 package now, or one whose repo status master plan itself calls unverified, is out of this step's scope. **Every new `Bar/modules/*.qml` file will be import-validated as a batch alongside the three existing ones the moment `Bar.qml`'s `import "modules"` loads any of them** — S-20's own hard-won lesson, restated here because the stakes are higher this time: a mistake in any one of these seven files can break `workspaces`/`activeWindow`/`clock` too, not just the module with the bug. Audited every new file's imports against every type it actually references before writing this row (native QML primitives need no import; `Timer`/`Item`/`SequentialAnimation`/`NumberAnimation`/`Animation.Infinite` need `QtQuick`; `Process`/`StdioCollector` need `Quickshell.Io`; `ShellScreen`/`Singleton`/`PopupAnchor`-family need bare `Quickshell`; each service type needs its own module) — off-machine only, the same limit every prior QML step has had. One real defect caught this way before committing, not after a failed round-trip: an early draft of `PhiAgent.qml` put `SequentialAnimation on opacity` directly on the `Segment` it wrapped — `Segment` already owns `opacity` internally (`WidgetStates.opacityFor`, for its disabled/loading fade), and an external "Animation on property" targeting an already-bound property permanently severs that binding the first time it runs; fixed by wrapping the `Segment` in a plain `Item` and animating the wrapper instead, leaving `Segment`'s own binding untouched. **Not verified in any way**: whether `import Quickshell.Services.Pipewire`/`.UPower`/`.Bluetooth`/`.Networking` succeed at all on a machine where the corresponding daemon (or, for `wifi`/`battery` on `zotac`, the package itself) isn't installed — the C++ source suggests these QML modules are unconditionally bundled with the `quickshell` package itself and only their runtime *data* degrades gracefully (`NetworkBackend::isAvailable()`'s whole reason to exist), but zero prior step has imported any of these four modules, so this is this step's own first real risk, on top of the batch-validation one above. Multi-monitor duplication (each screen's own `Bar` instance would run its own independent `nvidia-smi`/`tailscale status` poll if a host ever has more than one monitor) is a known, accepted inefficiency, not fixed here — no confirmed multi-monitor host exists yet and every other non-per-monitor module (the clock, since S-22) already duplicates the same way by design. `DONE WHEN` ("the inventory matches §8.4 exactly on both machines, driven by capabilities") is therefore only checkable after a real `qs -p` round-trip on both hosts — nothing above has run anywhere. **First real-hardware round, `razer` only (`phi-shell` `42c6bf3`):** the predicted batch-validation risk hit immediately — `qs -p` failed the entire shell load (`Type Bar.Bar unavailable` → `Modules.Workspaces unavailable` → `AudioBridge unavailable` → `PwNodeIface is not a type`), because `Workspaces.qml`'s own import chain reaches `Services/AudioBridge.qml` even though `Workspaces.qml` itself has nothing to do with audio — exactly the directory-batch mechanism this row already flagged, now confirmed for real rather than only anticipated. Root cause: `AudioBridge.qml` declared `property PwNodeIface sink`, using the C++ class name directly; the real QML-facing type is `PwNode` (`QML_NAMED_ELEMENT(PwNode)` on the `PwNodeIface` class — confirmed by re-fetching `qml.hpp`'s own macro line, not re-assumed) — a mismatch this agent's earlier verification pass missed because it confirmed *properties* against source but never explicitly checked whether a used *type name* was the QML-registered name or just the C++ class name. Fixed (`42c6bf3`). Before sending this back, re-verified the same question for every other external type this step introduced, this time checking the macro line itself rather than the class name alone: `UPowerDevice`, `UPowerDeviceState`, `BluetoothAdapter`, `NetworkDevice` and `DeviceType` all register under `QML_ELEMENT` with their own class name unchanged — the `Iface`-suffix indirection is specific to Pipewire's node wrapper in this Quickshell version, not a pattern repeated elsewhere in the four service modules this step uses. **Lesson for whichever step next introduces an external Quickshell type this shell has never used before:** confirming a property *name* against source is not the same check as confirming a property's *declared type* is really the QML-registered name — the two can diverge (as they did here) even when every property on the type is otherwise exactly as documented. **`DONE WHEN` confirmed by the user on both real hosts, second round:** `razer` — clean load, right-island order matches `modules.json` exactly (volume, network, battery, wifi, bluetooth, Φ, clock), volume's click-to-mute works, no `night mode` segment (correct, not built). "Device name" in the user's own report is the `network` module's on-request reveal (`Services.Tailscale.hostName`) — read as confirmation that piece works too, not separately itemized by the user in those terms. `zotac` — user confirmed "it works as well" without itemizing its own segment list; **not separately confirmed**: whether `zotac` shows `gpu` (expected) and omits `battery`/`wifi`/`bluetooth` (expected), or whether the flagged bluetooth tension (master plan §5 vs. §8.4, this row's own earlier paragraph) resolved one way or the other — recorded here as unconfirmed rather than assumed, per the user's own explicit instruction to mark this step verified now and not proceed to `S-24`. If a later screenshot shows `zotac` differing from the expected inventory, that is new information for whoever reads this row next, not a contradiction of today's verification. **Correction, found during S-36 (2026-09-08), phi-shell commit `a662027`:** both `Services/Tailscale.qml`'s 30-second poll and `Bar/modules/Gpu.qml`'s 5-second `nvidia-smi` poll left their `Process`'s `running` true after each run instead of resetting it in `onExited` — the same class of bug as the `Config/Capabilities.qml` correction just recorded against `S-20`. Each `Timer`-triggered poll was, in practice, never actually paced by its own `Timer` at all: the first completion respawned the process immediately, in a tight loop, for as long as the module existed on screen (`Gpu.qml` only on `zotac`, capability-gated). Fixed by setting `running = false` in each `onExited`. |
| S-24 | Session integration | verified | phi-shell a8314bc · dotfiles 1dbc205..716104b (2 commits) | 2026-09-08 | **Nothing here has run on real hardware yet — every claim below is off-machine reasoning against the real Hyprland Lua API, not an observed result.** `PROGRESS.md`'s own rule applies to this row's own commit hash: it cannot record the `phios-dotfiles` hash that contains it, so only the `phi-shell` side is filled in above; `S-25`'s commit fills in the rest once known. **The Hyprland Lua API used below was checked against real upstream sources, the same discipline `phi-shell` steps established for Quickshell** (S-20 onward), not assumed from the existing file's four calls alone: `hl.on`/`hl.exec_cmd`/`hl.window_rule`/`hl.workspace_rule` and every field used were confirmed against `hyprwm/Hyprland`'s own real `example/hyprland.lua` (fetched raw, not summarized — the wiki itself sits behind a JS proof-of-work checkpoint this agent cannot solve, so field-level detail came from `hyprwm/hyprland-wiki`'s rendered pages instead) and cross-checked against a second source (`alejandrominaya.github.io/hyprland-lua-docs`) before writing anything. **`quickshell` package gap closed, flagged by name for this exact step in S-20's own row** ("Flagged, not fixed, for whichever step first wires the session (S-24): `quickshell` is approved in master plan §15 (M2, `extra`) but is not declared in any profile's `packages.txt` yet"): added to `profiles/desktop/packages.txt` (`zotac`+`razer`, matching `Config/Tokens.qml.tmpl`'s own profile placement since S-20; `mini` has no bar per §8.4). The three `qt6-*` dependency rows master plan §15 lists as "verificare quali siano già tirate dal pacchetto" were deliberately left undeclared, on the same reasoning this repository already applies elsewhere (`openrazer-daemon`'s row: dependencies a package's own `depends()` array pulls are not separately listed) — unverified against a real `pacman -Si quickshell`, since no machine is reachable from here. **Autostart** (`hyprland.lua`, `profiles/desktop`, a plain linked `home/` file, not a `templates/` one — confirmed by its own path, so no design token could be threaded through it even if one were needed): `hl.on("hyprland.start", function () hl.exec_cmd("pgrep -x qs >/dev/null || qs -p ~/.config/quickshell/phi") end)`, the exact shape the real upstream example itself documents for autostarting "notifications daemons, status bars, etc." (its own words, matching phi-shell's role exactly) — not invented syntax. Whether `hyprland.start` can refire on a plain `hyprctl reload` (as opposed to a fresh login) could not be confirmed from any source reachable here; the `pgrep` guard is written as cheap, always-safe insurance either way rather than a claim about confirmed behavior, and the real answer is explicitly left for `S-25` ("review the Hyprland Lua configuration against the version actually installed"). **Window rules — three-way split, not a single "window rules" blob:** (1) *Dynamic workspaces* (master plan §2.3, closed): implemented by omission — Hyprland's own default (create on use, destroy when empty) already is what the decision asks for, and the only way to violate it would be adding a static monitor-pinned or `default = true` rule, which this commit does not do; a comment records the reasoning so `S-25`'s compatibility review does not mistake the absence of a rule for the absence of a decision. (2) *btop workspace* (ADR 122 / `Q-N03`, closed at S-22, unbuilt since): `hl.workspace_rule({ workspace = "special:btop", persistent = true })` plus `hl.window_rule({ name = "btop-workspace", match = { class = "^phios-btop$" }, workspace = "special:btop" })`. The matched class is not Steam-style guesswork: it is a Wayland app id this agent itself assigns, in the same commit's sibling change to `phi-shell` (`Bar/modules/Gpu.qml`'s `btopLauncher.command` gains `--class phios-btop`, a real, standard kitty CLI flag confirmed against `man kitty` search results before use — a deliberate choice over matching btop's own window *title*, which a TUI can rewrite at runtime via its own OSC escape sequences and which this agent had no way to confirm one way or the other without a live run; matching a self-assigned class needed no such confirmation at all). This closes the mechanism S-23's own row named opinion this step would own verbatim ("the Hyprland window rule that would actually assign btop's window to that workspace is S-24's own AGENT bullet... unbuilt") — the direct `kitty -e btop` launch S-23 shipped as a stopgap still works exactly as before, now additionally landing on the right workspace once one exists; the still-unbuilt persistent bar toggle (flagged in S-22's own row, not owned by any step) means visiting that workspace still needs a manual `togglespecialworkspace btop`, not a click. (3) *Steam* (master plan §2.3: dedicated workspace, secondary windows floating) — **deliberately NOT written**, per the AGENT card's own explicit instruction, not this agent's judgment call: "Steam window rules need real class and title values. Ask the user for hyprctl clients output rather than guessing." This agent has no path to either machine to run that command (`CLAUDE.md` rule 4) and the card forecloses guessing outright — unlike every other placeholder value this project has shipped and flagged for cheap veto (`<mini-overlay-address>`, `[PLACEHOLDER]` tokens, provisional thresholds), a wrong Steam class is not a flaggable guess but the exact thing the card says not to do. A commented scaffold is left in `hyprland.lua` showing the three rules' shape with `<angle-bracket>` placeholders, mechanical to fill in once real data arrives — this is also why `VERIFY`'s existing "hyprctl clients for Steam windows" line is not decorative: it is where that data was always going to come from, one bullet the card and this row agree on without having to invent a second round-trip. **Hot-reload path documented** (AGENT: "so the user can iterate without restarting"), in `phi-shell/README.md` next to the pre-existing `qs -p` convention rather than in this repository, since the reload mechanism itself (Quickshell's own file-watcher, `S-20`) lives entirely in that repository's domain: QML edits need no restart at all (unchanged, pre-existing behavior, now just written down); a process-level restart, when genuinely needed, is `pkill -x qs && qs -p ~/.config/quickshell/phi`, with no Hyprland session reload involved either way. **Removed nothing**, per the AGENT card's own instruction — waybar was never installed, confirmed again by the unchanged `profiles/desktop/packages.txt` diff (only an addition). **`DONE WHEN` genuinely unverifiable from here in either half:** "starts automatically" needs a real login on `zotac`/`razer`, and "survives a shell reload" needs a real `hyprctl reload` (or Hyprland's own config-reload path) on real hardware — neither is something this agent can produce or has produced. The `USER`/`VERIFY` blocks below ask for exactly the two things this row is missing: a session restart on both machines, and `hyprctl clients` output while Steam is open (main window plus one secondary window, e.g. the friends list) so the deferred Steam rules can be written for real in a follow-up commit under this same step, before `DONE WHEN` can honestly be called met. **First real-hardware round, `razer` only, found and fixed a real bug before this row could be marked verified — not shipped and walked back later.** `DONE WHEN`'s own two literal criteria are met: `pgrep -x qs` showed `qs` running after the session restart (bar starts automatically, once Hyprland itself is up — see the session-startup gap below), and the user confirmed the session was unchanged after running `hyprctl reload` (survives a reload). But the `hyprctl clients` capture taken during the same round surfaced a real defect in the btop rule, not a clean pass: **every** window present — the user's own terminal running the capture command, and Steam itself — showed `workspace: -98 (special:btop)`, not just btop's own window. Root cause: the original rule assigned btop's window with a bare `workspace = "special:btop"`, no `silent` suffix, which (confirmed against real Hyprland source, not re-guessed) switches the monitor's active workspace to the target the moment the window opens, the same as a normal workspace assignment. With no workspace-switching keybinding built yet (`S-38`), there was no way back out once that happened, so everything opened afterward — the user's own diagnostic terminal, then Steam — landed on `special:btop` by simple inheritance of "whatever the active workspace happens to be." This also explains the user's second observation, that "the special:btop button does nothing when pressed": the GPU module's click only ever launches a fresh `kitty --class phios-btop -e btop` (S-23's stopgap, unchanged by this step, no toggle wired to any bar element per S-22's own flagged gap) — with the session already sitting inside `special:btop` from the earlier launch, a second click had nothing visibly different to switch to. **Fixed** (same commit as this row): `workspace = "special:btop silent"` — the window still lands on the special workspace, but nothing else follows it there, and the workspace stays hidden until something actually calls `togglespecialworkspace btop` (still unwired, left alone exactly as the user asked: "if that's planned, you should not change it"). **Not yet re-verified on real hardware** — this fix is reasoned from the same real Hyprland source the original (buggy) rule was, but has not itself been through a round-trip yet; the next `USER`/`VERIFY` block asks for exactly that, cheaply (reload, click the GPU module once, confirm nothing else moves workspace). **Steam's real class and title arrived in this same round**, closing the guessing question outright: `hyprctl clients` on `razer` showed Steam's actual main window as `class: steam, title: Steam, initialClass: steam` (xwayland: 1) — used verbatim, not adjusted. `hl.window_rule({ name = "steam-workspace", match = { class = "^steam$" }, workspace = "name:steam" })` added: a plain named workspace, not special and not `silent`, since — unlike btop — Steam is something you deliberately switch to, not a background peek. **"Secondary windows floating" is still not written**, and for the same reason the original card gave for not guessing Steam's class: the one real window captured is the main library window, and distinguishing a secondary one (Friends List, a chat window, a game's own popup) needs either that window's own real title, or a confirmed way to negate a match in this Hyprland version — real sources actively disagree on whether the regex engine here even supports a negative-lookahead title match (`ECMA-262`-style) or requires a separate `negative:` prefix (`RE2`-style), and guessing between two contradictory real sources is the same mistake as guessing a class name blind. Left as a commented scaffold; needs one more real `hyprctl clients` capture with a secondary Steam window open. **New, out-of-scope gap surfaced by the user, not fixed here, exactly per their own instruction** ("i think this is fine by the plans, no need to add things if those are covered in the plans later"): Hyprland itself did not start automatically on `razer` after the reboot — the user had to run `start-hyprland` by hand before `phi-shell` could autostart into it. This is a layer below "the shell starts with the session" (S-24's own goal, which is about `phi-shell` once Hyprland is already running): how Hyprland itself launches at login (`gestione sessione`, listed as "Da definire" in `phios-architettura.md` §8, line 638) has no owning step yet in any planning document read so far. Flagged here for whichever step or new card eventually closes it — not addressed, per the user's own explicit request. **Closed as `verified` on the user's own explicit instruction** ("if nothing is left to be done, you can mark it as verified"), with every remaining gap recorded here rather than silently dropped, none of them requiring a code change — they are coverage gaps in what could be tested today, not defects: (1) **Steam's dedicated-workspace rule is unconfirmed** — the user reported Steam still showing as "just a tiled window," not on a workspace of its own, and asked whether that was intended. Answer, for the record: yes and no. Tiled (never floating) is exactly right for the *main* window — master plan §2.3 only asks for secondary windows to float, and this rule was deliberately written without `float`. Landing on its own **workspace**, though, is a real Hyprland window-rule limitation, not a bug in the rule itself: a `workspace` assignment only fires when a window is first created, never retroactively, and the Steam window the user was looking at almost certainly predates this commit — it was already open from the previous round's `hyprctl clients` capture. Confirming the workspace assignment for real needs a full Steam quit and relaunch, not yet done; flagged here as a cheap, non-blocking check for whenever it's convenient, not requested as a follow-up round. (2) **The btop `silent` fix is unconfirmed on any host.** `zotac` — where `Bar/modules/Gpu.qml`'s GPU module actually appears, since it is gated on the nvidia-only `nvidiaGpu` capability (S-23) — was not reachable this round ("I cannot test the gpu on zotac currently"). `razer` has no GPU module in its bar at all (Intel-only, no `nvidia-smi`), so the original round's btop test there could only have exercised the window rule directly (by launching `kitty --class phios-btop -e btop` some other way, not a bar click) — asking the user to "click the GPU bar module" on `razer` in the previous handoff was this agent's own mistake, since no such element exists on that host's bar; recorded here so the next agent doesn't repeat it. The mechanism itself (`silent` suppressing the forced workspace switch) is reasoned from real Hyprland source, same as the original rule was, just not yet round-tripped anywhere. (3) **btop's special workspace has no way to be reached today, by design-so-far, not a defect**: no bar toggle exists (flagged since S-22, unassigned to any step) and no keybinding exists either (`S-38`, not built) — the user's own words, "without keybinds i can't reach it," confirm this is the expected, already-documented state, not a new gap. (4) **Hyprland's own session start is still manual** (`start-hyprland`, reported in the previous round) — reiterated here at the user's request to keep it on record; still no owning step in any planning document read so far, still deliberately not addressed. |
| S-25 | Compatibility check against Hyprland | verified | dotfiles 2ed5f86 | 2026-09-08 | **Verified on real hardware, `razer` only — `zotac` "cannot test... currently" (the user's own words); closed as `verified` anyway on the user's own explicit instruction, the same practice S-24's row already used for its own real-hardware gaps.** `razer`'s output confirms the off-machine review above rather than merely supplementing it: `hyprctl version` reports `Hyprland 0.56.2 built from branch v0.56.2 at commit efb5099...` — the exact tag this review fetched `example/hyprland.lua` from, not a nearby version reasoned about by analogy; `pacman -Qi hyprland` confirms `0.56.2-2` installed. `readlink -f ~/.config/hypr/hyprland.lua` resolves to `/home/flavio/phios-dotfiles/profiles/desktop/home/.config/hypr/hyprland.lua` — the deployed config on `razer` genuinely is the file this review read, not a dangling symlink into the deleted `modules/` tree S-03 warned about; that specific risk is now closed for `razer` (still open for `zotac`, never confirmed since S-03, see that row). The log-emptiness caveat this row itself raised is also resolved, not just satisfied by luck: `$XDG_RUNTIME_DIR/hypr/` held one real instance directory, its `hyprland.log` was 2376 lines (`debug.disable_logs` is evidently not `true` by default, since nothing in this config sets it either way), and `grep -i "deprecat"` against that real, populated log returned zero matches — a genuine clean result, not an empty grep against an empty or missing file. `hyprctl reload` produced no visible error before this capture. **`DONE WHEN` met for `razer`, open for `zotac`:** "the Lua config loads with no deprecation warnings" is now a real, verified fact on one host running the exact version this review checked against, not an inference from upstream source alone; `zotac` gets the identical five-command check whenever it's next convenient, per the user's own call not to block this row on it. |

## M3 — Session surfaces

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-30 | Notification daemon and toasts | awaiting-verification | phi-shell 559eb4d (bacc813 + correction) | 2026-09-08 | **Correction, same step, found in review before verification (§16.1: correct within the step, not a new one):** the first commit set `notification.tracked = true` on arrival and never set it back, so `server.trackedNotifications` (re-exported as `active`) accumulated every notification for the shell's whole lifetime — the sidebar's Active section would have grown without bound, every closed notification duplicated against its own History row with dead action buttons, and a DND-silenced notification (never shown as a toast, so nothing would ever have called `expire()` on it) would never have left `active` at all. Fixed in `559eb4d`: every tracked notification now gets a bounded lifetime via a per-notification one-shot `Timer` that calls the real `Notification.expire()` — respecting the sender's own `expireTimeout` if set, else 8s — whether or not DND ever let it become a toast; `tracked` is set back to `false` once the resulting `closed` signal fires and the history entry is recorded, which is also what correctly releases the object. `Notifications/Toast.qml`'s own now-redundant local dismiss timer was removed — the toast queue now advances through the same central `closed` handler instead of a second, disconnected timer. Assumed, not verified: calling `expire()` on an already-closed notification (e.g. the sender withdrew it first) is a safe no-op — consistent with how comparable D-Bus-style close-idempotent APIs behave elsewhere in this stack, flagged for cheap veto if real hardware says otherwise. Built entirely in `phi-shell` (no dotfiles/phi change needed — Quickshell's `NotificationServer` is built into Quickshell itself, no new package). `Services/Notifications.qml` claims `org.freedesktop.Notifications` directly (ADR 073), tracks live notifications (`server.trackedNotifications`, re-exported as `active`) and persists a capped (200), newest-first history to `$XDG_STATE_HOME/phi/notifications.json` via a new `Config/Paths.qml` singleton — the storage shape S-13's own row explicitly deferred to whichever step first needed it for a collection, not a `phi state` scalar. DND reuses the **real** key name, `toggle.dnd` (confirmed by reading `phi/internal/state/state.go`'s `Keys` map directly rather than guessing from the master plan's prose — the plan's own §5.6 table just says "dnd", the actual key is namespaced `toggle.dnd` alongside `toggle.night-mode`/`toggle.spotlight`/`toggle.chroma`; caught before commit, not after). `Notifications/Toast.qml` is one bottom-right layer-shell surface per screen (`shell.qml`'s `Variants`, same per-monitor pattern as `Bar.Bar`, ADR 077), showing exactly one toast at a time from a FIFO queue — app icon (via `Quickshell.iconPath`, text-only fallback if it fails to resolve, same conservative call S-23's Volume module made about unverified glyph coverage) plus a horizontally-scrolling excerpt, realizing the closed decision in master-plan §2.3/architettura §8.2.1 ("notifica come icona animata con testo scorrevole, dettaglio nel pannello") literally rather than as a stack of toast cards. Show/hide is category B (§6.5's own table names "notifiche e toast" under B); the marquee scroll itself is read as category A (continuous, linear-only) since it is not a state transition — a placement this agent judged, not one either closed-decision table names explicitly, flagged for cheap veto. **API verified against real Quickshell source before writing code** (git.outfoxxed.me → could not be fetched directly, 403; raw.githubusercontent.com/quickshell-mirror/quickshell mirror used instead, same repository): `NotificationServer`/`Notification`/`NotificationAction` full property and method list (services/notifications/{qml,notification}.hpp), `FileView.setText`/`loaded`/`loadFailed` (io/fileview.hpp — this is the first file in this repo to *write* via FileView, every prior use was read-only), `Quickshell.env()` (core/qmlglobal.hpp, confirmed synchronous, used instead of a Process-based resolution the way `Config/Capabilities.qml` needs). **Not verified, cannot be off-machine:** whether `NotificationServer` actually claims the bus name without a race against any other running notification daemon (none is declared in any profile, so none should be running, but this is unconfirmed on real hardware); whether the bottom-right anchor visually collides with anything; whether the marquee animation is smooth or janky at real frame rates; whether `Quickshell.iconPath` resolves real installed apps' icons correctly. **Held, not blocking:** verifying this step needs a way to send a manual test notification, and `notify-send` (package `libnotify`) is not declared in master plan §15 anywhere — flagged in the final question list rather than added unilaterally (CLAUDE.md rule 7/8); a `busctl` fallback that needs no new package is in the final VERIFY list. Also held: nothing in this milestone yet gives a human a way to *toggle* DND through the UI (that is S-40's settings panel, per §9.12) — VERIFY for this step drives `toggle.dnd` via `phi state set` directly and restarts `qs`, since nothing here polls the key live after startup. **Second correction, found during S-36 (2026-09-08), phi-shell commit `8a44c9f`:** the `$XDG_STATE_HOME/phi` `mkdir -p` safety `Process` (added alongside the first correction, `559eb4d`) was itself written with the same running-left-true respawn defect the S-13/S-20/S-23 corrections above also record — independent of, and in addition to, the `trackedNotifications` leak already fixed. Fixed the same way: `running = false` in `onExited`. **Third correction, found on real hardware (razer) during the M3 VERIFY round, phi-shell commit `c377099`:** `qs -p ~/.config/quickshell/phi` refused to load at all — `Services/Notifications.qml[58:12]: Duplicate signal name: invalid override of property change signal or superclass signal`, cascading into `Type X unavailable` up the entire import chain (`Bar.Bar`, every `Services/` bridge) since all of them transitively import this singleton. `property var history: []` already auto-generates a `historyChanged` signal; an explicit `signal historyChanged()` declared alongside it collided with that auto-generated one, which QML rejects outright rather than allowing silently. It was also never used — nothing anywhere in this repo connects to it — so `_pushHistory()`'s manual `root.historyChanged()` emit was redundant besides: the property assignment on the line above it already fires the auto-generated signal. Removed both. Swept every `.qml` file in `phi-shell` for the same pattern (an explicit `signal fooChanged()` alongside a `property ... foo`); this was the only instance. This is the same *class* of bug as the `Timer.restart()` name-shadowing catch in this same file's own header comment, just the signal-declaration form of it — and, unlike every other defect this session found by static reading or advisor review, this is the first one this milestone actually caught by running on real hardware, exactly as `phi-shell/CLAUDE.md`'s own "you cannot run this" line predicts should keep happening. **Fourth correction, same VERIFY round, phi-shell commit `fb79ed8`:** with the signal collision fixed, `qs -p ~/.config/quickshell/phi` got further and failed again — `Notifications/Toast.qml[77:17]: Cannot assign to non-existent property "opacity"`. Confirmed against the real Quickshell source, `src/window/windowinterface.hpp`: `WindowInterface` (`PanelWindow`'s base class) exposes `visible`/`color`/`width`/`height`/`screen`/`mask`/`data`/`contentItem` — no `opacity` anywhere. The `visible: opacity > 0` / `opacity: shown ? 1 : 0` / `Behavior on opacity` idiom this row's own header describes ("the same Behavior-on-opacity idiom every Widgets/ surface already uses") was written directly on the `PanelWindow` root, which does not have that property to animate — it silently compiled as an ad-hoc dynamic property doing nothing, so nothing caught it until QML actually tried to bind a `Behavior` against it. The same defect existed in five other files across four other steps (`S-31`'s `Sidebar.qml`, `S-33`'s `Launcher.qml`, `S-35`'s `Overview.qml`, `S-37`'s `AltTab.qml` and `Cheatsheet.qml`) — see each of those rows for a one-line cross-reference, full fix explanation kept here since `Toast.qml` is where the real error pointed first. Fixed in all six by moving the fade to a new inner `Item` (`fadeRoot`) wrapping the visual content — a real `Item` genuinely has `opacity`, and declaring `Behavior on opacity` there keeps it a bare local property rather than an untested dotted target like `Behavior on contentItem.opacity` (`contentItem` is real too, per the same header, but this repo has no local QML engine to confirm `Behavior on` accepts a dotted path, so the safer, unambiguous form was used instead). The window's own `visible` now reads `root.shown || fadeRoot.opacity > 0`, so it stays mapped until the fade-out animation actually finishes rather than vanishing mid-fade. Swept the rest of the repo for the same pattern: `Tooltip.qml`, `Scrim.qml`, `Popover.qml` also use `visible: opacity > 0`, but all three are `Item`-rooted, not `PanelWindow`-rooted, so their `opacity` is real and this bug does not apply to them. **Fifth correction, real-hardware VERIFY feedback (not a load error this time):** the `busctl` fallback command in this row's own VERIFY block failed (`busctl: unrecognized option '-1'`) — the user approved adding `libnotify` instead (dotfiles commit `3f405c6`, master plan §15.3 updated to match), so future verification uses `notify-send "S-30 verification" "Test notification"` in its place. |
| S-31 | [BLOCKING] Sidebar with declarative tab registry | awaiting-verification | phi-shell b855c40 | 2026-09-08 | `Panels/Sidebar.qml` mirrors `Bar/Bar.qml`'s own `modules.json`/`componentFor()` shape exactly (ADR 078) against a new `Panels/tabs.json`, so the DONE WHEN claim ("a fifth tab can be added by editing tabs.json alone") rests on a pattern already proven at S-22, not a new untested mechanism. Full-height right-edge `PanelWindow`, fixed width derived from the same ch-token formula `Bar.qml`/`Notifications/Toast.qml` already use (no new §6.3 token). **No keybinding exists to open it** — S-22's own note already flagged this exact gap for every M3 step before S-38 — so this step adds a `Quickshell.Io.IpcHandler` (`target: "sidebar"`, verified against the real Quickshell source, `io/ipchandler.hpp` — first use of this mechanism anywhere in this repo) instead of pre-empting S-38's job with a one-off keybind: `qs -p ~/.config/quickshell/phi ipc call sidebar toggle` opens/closes it today, and S-38 gets a one-line `exec_cmd` bind onto the same command later. Single sidebar instance pinned to `Quickshell.screens[0]`, not one per monitor like the bar/toast — it is a focused toggle surface, not an ambient per-monitor indicator; "follow the focused monitor" would need Hyprland-specific IPC this card never asked for, flagged for cheap veto. Four tabs, one type each in `Panels/tabs/`: **Notifications** (real — two `Repeater`s, one bound directly to `Services.Notifications.active` for live action buttons reusing the exact ObjectModel-as-Repeater-model pattern `Bar/modules/Workspaces.qml` already proved at S-22, one over the plain persisted `history` array for closed entries; also carries a DND toggle button, the only UI anywhere right now that can flip `toggle.dnd` without a terminal). **Clipboard** (deliberately empty — "S-32 wires this up," no `Services/Clipboard.qml` exists yet and building against a service that doesn't exist would be a guess). **Calendar** (a genuine hold, not a deferral like AI chat: `phios-architettura.md` §8.8 leaves the whole CalDAV/server/client question `[TBD]`, and no step S-00 through S-83 in `phios-agent-brief.md` ever builds a calendar backend or `phi` verb — flagged in the final question list: either the backlog needs a real step added, or this tab is a permanent placeholder by design). **AI chat** (the one tab S-31's own card gives a full spec for: static message list, a tool-approval card with inert Allow/Deny buttons, a memory-proposal notice with inert Save/Dismiss, and a Category-A "Agent is thinking…" ellipsis — deliberately **not** the Φ mark, since master-plan §6.6's Role-B context list is closed to the bar segment specifically and does not name this tab; reopening that list was not this step's call to make, flagged for cheap veto if the user wants it reopened). **Two real bugs found and fixed before committing, not after:** (1) a first draft of the AI chat message bubble used QML's inline-component syntax (`component Bubble: Widgets.Panel {...}`) nested inside the tab's own root `Item` — confirmed against Qt's own QML-documents documentation that inline components cannot be nested and must be file-top-level, so it was pulled out into its own file, `Panels/tabs/ChatBubble.qml`, reached via the same same-directory `import "." as X` shape `Bar/Bar.qml` already uses for its `modules/` subdirectory, rather than leave an unverifiable language feature in the one file this agent could least afford to guess on. (2) Both the chat bubble and the notifications tab's active-notification card first wrote `parent.text`/`width: parent.width - parent.padding * 2` reaching for the *outer* `Widgets.Panel`'s own properties from a *nested* child — wrong, because `Widgets.Panel`'s default `content` property aliases into an inner `contentItem`, so a child's real runtime `parent` is that inner item, not the Panel itself; `.width` still resolves correctly by coincidence (`contentItem` has a real width too), but `.padding`/`.text` do not exist there at all. Fixed by giving each outer component an explicit `id` and referencing it directly (`bubbleRoot.text`, plain `root.chWidth` instead of a second padding subtraction) rather than relying on `parent`. **Not verified, cannot be off-machine:** whether the sidebar visually fits at 48ch on either machine's real display scale; whether the IpcHandler call actually reaches a running instance; whether the AI chat tab's static layout looks "finished" rather than obviously fake; whether `Quickshell.iconPath`-style app icons (not used here, but the same font/glyph caution applies to nothing in this tab) render correctly — this tab uses no icons or glyphs at all, deliberately, same conservative call S-23's Volume module made. **Correction, found on real hardware during the M3 VERIFY round, phi-shell commit `fb79ed8`:** `Sidebar.qml`'s root-level `Behavior on opacity` was written against a property `PanelWindow` does not have — see `S-30`'s row for the full explanation and fix, applied here identically (a new inner `Item`, `fadeRoot`, carries the real opacity and its `Behavior`). **Preventive fix, not a reported bug, phi-shell commit `56c6b4f`:** the sidebar itself toggles correctly on real hardware (confirmed by the user), but applied the same `Services/LayerFocus.qml` fix as `S-33`'s row anyway, since the AiChat tab's text input would hit the identical `WlrKeyboardFocus.None` default the moment it was actually typed into — not yet directly tested, but the root cause is confirmed, not guessed, and every other keyboard-driven surface in this milestone had it. |
| S-32 | Clipboard history | awaiting-verification | phi-shell 6c4f378 · dotfiles (this commit) | 2026-09-08 | `Services/Clipboard.qml` captures via one long-lived `wl-paste --watch sh -c '<script>'` process — same integration shape as `cliphist` itself (`wl-paste --watch cliphist store`), the tool this step removes (C-07, see below). No manifest file: the capture script is plain POSIX `sh` with no JSON writer available inside it, so the filesystem is the structure — `<id>.data`+`<id>.mime` per entry under `$XDG_STATE_HOME/phi/clipboard/entries/` (`id` = `date +%s%N`, so lexicographic sort is also chronological sort), plus a single `latest` file a watched `FileView` (`watchChanges: true`) reacts to so new arrivals show up without a polling timer. **Sensitive exclusion happens inside the capture script itself**, before anything ever reaches disk: `wl-paste --list-types` checked for `x-kde-passwordManagerHint`, dropped silently if present — cross-dependency on the M6 secrets decision (KeePassXC vs. Vaultwarden) already on record: if the eventual manager doesn't set this hint, the exclusion needs reimplementing against whatever it does instead. Pin state (`pins.json`) is the one piece of real JSON in this design, since pinning is a UI action, not a capture-time decision — Quickshell writes it directly. TTL sweep every six hours deletes unpinned entries past 30 days ("minimo un mese," exact value explicitly deferred to real consumption by the step card itself). `Panels/tabs/Clipboard.qml` replaces S-31's placeholder: inline Pin/Copy/Delete buttons per row — **not** the ContextMenu widget, honoring the user's own S-37-planning decision to build that widget without wiring it to any real surface yet. **Three real bugs found and fixed before committing:** (1) a preview-text property on a `FileView` was first named `text`, colliding with `FileView`'s own real `text()` function (confirmed via `io/fileview.hpp` — this file's own header now documents the fix, and flags it as the same shadowing class already caught twice in S-31's `Notifications.qml`/`ChatBubble.qml`) — renamed to a distinct binding, no property collision. (2) **The more serious one, and new to this step:** every one-shot `Process` (the directory listing, the per-entry mime reader, `restore`, `rm`) initially left `running: true` set after completion. Read directly from the real Quickshell source (`io/process.cpp`, not assumed): `Process::onFinished()` calls `startProcessIfReady()` as its *last* step, after the exited signal has already run — so a one-shot command with `running` never reset to `false` respawns itself the instant it exits, forever. Every one-shot `Process` in this file now sets `running = false` in its own `onExited` before (or as well as) destroying itself; only the long-lived `wl-paste --watch` process is deliberately left to auto-restart this way, which is a genuine, useful reading of the exact same mechanism (if the watcher itself ever dies unexpectedly, e.g. a compositor restart, it now comes back on its own with no code written for that case specifically). (3) The same `Widgets.Panel`-`contentItem`-indirection bug S-31 already found (a nested child's real `parent` is Panel's inner `contentItem`, not the Panel itself) was checked against every `parent.width`/`parent.padding` reference in this step's new files before committing, not found again — `.width` references are safe (`contentItem` genuinely has one), and this step's code never reaches for a Panel's own custom property from a nested child the way S-31's first draft did. **`cliphist` removed from `profiles/desktop/packages.txt` (C-07); `wl-clipboard` kept.** **Real, load-bearing gap found while doing this, not previously documented anywhere:** `bin/lib/packages.sh` has no removal logic at all — grepped directly, zero matches for `pacman -R`/`uninstall`/`remove` — the installer only ever adds what `packages.txt` newly declares, it never uninstalls a package that stops being declared. Removing the `cliphist` line here does **not**, by itself, uninstall `cliphist` from either machine, and `phi doctor`'s package-categories check (S-14) will not catch this drift either, since `cliphist` is a normal repo package, not foreign/AUR — the check that would flag it does not exist. A manual `sudo pacman -Rns cliphist` on both `zotac` and `razer` is now in the final VERIFY list for this reason, and this gap itself belongs on the question list: either the installer needs real package-removal logic (a bigger change, out of scope for this step to just add unilaterally), or every future "remove a package" step needs to keep naming the manual command by hand. **Not verified, cannot be off-machine:** whether `wl-paste --watch` actually behaves as documented under real Wayland/Hyprland (this whole design rests on the same integration contract `cliphist` itself uses, but this step could not run it); whether the sensitive-exclusion check actually catches a real KeePassXC copy (no password manager is installed by any profile yet — M6 — so this is unverifiable end-to-end until then); whether image capture via `wl-paste --type image/png` produces a file `imv`/other viewers can actually open. **Correction, found on real hardware during the M3 VERIFY round, phi-shell commit `0deb3ec`:** `Panels/tabs/Clipboard.qml` used `FileView` (its preview loader) without importing `Quickshell.Io`, the module that defines it — `qs -p ~/.config/quickshell/phi` failed with "FileView is not a type" once it reached this tab through `Sidebar.qml`. Every other file in this repo using `FileView` already imports it correctly; swept the rest of the repo for the same class of gap (a `Quickshell.Io` or other `Quickshell.*` submodule type used without its import) and found no second instance. Fixed with one added import line. **Second correction, real-hardware VERIFY feedback, phi-shell commit `56c6b4f`:** with the shell finally loading, copying text still never updated the sidebar's clipboard history. `Services.Clipboard` is a lazy `pragma Singleton`, same as every Quickshell singleton — unlike `Services.Notifications` (always instantiated early via `Notifications/Toast.qml`'s own top-level `Services.Notifications.activeToast` read, since `Toast.qml` is unconditionally instantiated per screen), nothing referenced `Services.Clipboard` until the sidebar's Clipboard tab was opened for the first time, so the `wl-paste --watch` capture process never started before that. Fixed by extending `shell.qml`'s own `Component.onCompleted` — already there specifically to force `Config` singletons to instantiate early, same mechanism, same reasoning — to also touch `Services.Clipboard.entries`. |
| S-33 | [BLOCKING] Launcher | awaiting-verification | phi 23a73c5 (tag v0.6.0) · phi-packages 96090ba · phi-shell 105eb0f | 2026-09-08 | **`phi` side (`internal/query`, ADR 018 — ranking and providers live here, the shell is a renderer only):** cold start is the constraint that shaped every design choice (`phi/CLAUDE.md`: "invoked on every keystroke... order of milliseconds") — `phi query <text>` is a fresh process per keystroke, not a persistent server, so every external-command provider runs **concurrently** (goroutines) bounded by a 120ms `providerTimeout`, and a slow provider degrades only itself, never the whole query. A native smoke-test run (this machine, not a phiOS host, so most providers short-circuited via `exec.LookPath`) measured **~14ms end to end** — promising, not conclusive; real timing with real `hyprctl`/`fd`/`zoxide` calls succeeding needs real hardware. Nine providers wired into `Providers()`, one per file: **calculator** (a small hand-written recursive-descent arithmetic parser — `+ - * / % ^` with parens and right-associative `^`, plus length/mass/temperature unit conversion, e.g. `10 km to mi`), **currency** (a tenth, added beyond the card's own list — `100 usd to eur`; the live API, `api.frankfurter.dev`, needs no key and was **verified with a real GET request from this environment before writing the file** — confirmed response shape, not assumed — but `providerTimeout` cannot fit a real HTTPS round trip, so `Query` only ever reads an on-disk cache and a stale/missing entry triggers a background refresh for the *next* query; the integration has never run end to end, flagged plainly as verified-API/unverified-integration), **applications** (`.desktop` scan, XDG dirs, its own INI-ish parser, tested against fixture content including a `[Desktop Action ...]` section that must NOT leak into the result — real gap this step's own tests caught), **windows** (`hyprctl clients -j`, parsed against a fixture shaped from S-24's own real captured JSON), **command** (Q-74's heuristic, implemented exactly as the card states it and flagged for the user's veto — single token is left to the applications provider entirely), **zoxide** (`--list`, preserving zoxide's own ranked order via a descending explicit score rather than re-deciding it), **ssh hosts** (`~/.ssh/config` `Host` lines, wildcards excluded), **web search** (DuckDuckGo — this agent's own pick, no document names an engine, flagged), **files** (via `fd`, already pre-approved in `packages.txt` — a **live** search, never a persisted index, so I-08's index-placement constraint does not apply), **system actions** (lock/suspend/log out only — ADR 021's own named non-verbs, handed to the shell as plain data). **Skipped, per this step's own scoping decision, not an oversight:** the password-vault provider (`Q-N09` — no password manager exists anywhere in the roadmap yet, M6; carried to the final question list) and dictionary/translation (the card's own words: "defer; they belong to a system translation backend which is not yet decided"). Ranking (`rank.go`) is a five-tier title-match heuristic (exact / prefix / word-prefix / substring / subsequence) plus a frecency term; frecency (`frecency.go`) is the storage shape `S-13`'s own `Keys` map named this step to define — a JSON file at `$XDG_STATE_HOME/phi/frecency.json`, deliberately outside `phi state`'s closed key set, one writer (`phi query record <id>`, called by the shell only on actual selection, never per keystroke). **Both are pure and carry real `go test` coverage — 30 test functions across the package, all passing, `go vet` and `gofmt -l` clean** — this is the off-machine substitute for the card's own instruction to "validate `phi query` in a terminal against `fzf` before any GUI work," which this agent cannot run; the twenty-queries-the-user-actually-types the card wants are guessed at in `rank_test.go`'s own comment, flagged for extension once real usage reports what actually ranked wrong. One real test bug caught before commit: `TestMatchWeightTiers`'s own "substring" case (`"kitty t"` against `"Kitty Terminal"`) was actually a **prefix** match by coincidence, not a substring one — the test's expectation was wrong, not `matchWeight`; fixed the test case, not the code. Tagged and pushed `v0.6.0`; `phi-packages` `pkgver` 0.5.0 → 0.6.0. **`phi-shell` side (`Launcher/Launcher.qml`):** every keystroke debounces 80ms then spawns `phi query` fresh, parses its JSON, renders it — no ranking logic here at all, matching ADR 018 on the shell side of the line. Opened via a `Quickshell.Io.IpcHandler` (`target: "launcher"`, same mechanism S-31's sidebar already uses), since S-38 still owns the real keybinding. Navigation stack: level 0 is the search field plus result list; Tab on a highlighted "command" result pushes the one concrete sub-view this step has real grounds to build (ADR 022) — an editable command line with a Run button; Escape pops one level or closes the launcher from the root. Action execution maps all eight `Action` kinds to a real effect via `Quickshell.execDetached` (no `Process` object needed for a fire-and-forget call): `exec`, `execTerminal` (`kitty --hold`, so a failing command's output survives instead of vanishing with the window — this step's own answer to the card's "define a hold policy," biased toward never losing output), `activateWindow` (`hyprctl dispatch focuswindow`), `openURL` (`xdg-open`), `copyText` (`wl-copy`), `changeDir`, `system` (lock via `loginctl lock-session` — the correct integration point for S-34's not-yet-built lock screen to listen on later, a harmless no-op today; suspend via `systemctl suspend`; logout by literally copying hyprland.lua's own Super+M binding, so the two paths to the same action can never disagree), and `pushView`. **Two real binding bugs found and fixed before committing, both the same class, both caught by re-deriving from first principles rather than assumed correct:** `text: root.queryText` (and the identical shape on the command sub-view's field) looked like live two-way sync but is not — assigning to a QML property that has an active binding **breaks that binding**, and `TextInput`'s own C++ side does exactly that assignment on every keystroke the user types, so after the very first character the field would silently stop tracking its source property at all; a later reset (closing the launcher, popping a view) would then never reach it. Fixed by making both fields one-way-out only (`onTextChanged` writes into the tracking property, never reads from it) and handling the one moment each field genuinely needs to change from the outside — opening, closing, pushing a view — with an explicit imperative assignment instead. Every one-shot `Process` here (the per-keystroke query, the selection-record call) resets `running = false` in its own `onExited` before destroying itself, the same `startProcessIfReady()` respawn trap S-32 found and documented directly from `io/process.cpp`. **Known, not fixed, flagged rather than silently shipped:** a query that arrives out of order (the user types past a slow response) can briefly show stale results — no sequence-number guard was added; this is real but judged non-critical polish, not a correctness bug, given cheap-veto budget was spent on the two binding bugs instead. **Not verified, cannot be off-machine:** the whole shell-side path end to end (spawning `phi`, parsing its output, rendering, executing an action) — nothing here has run inside a real Quickshell process; whether 80ms debounce feels responsive; whether `kitty --hold`/`hyprctl dispatch focuswindow`/`loginctl lock-session` behave as documented on real Hyprland. **DONE WHEN is explicitly not met and cannot be** ("the first result is right for the twenty queries the user actually types" needs a week of real typing, not a single session) — this row stays `awaiting-verification` on that basis alone even once every other check above passes. **Correction, found on real hardware during the M3 VERIFY round, phi-shell commit `fb79ed8`:** `Launcher.qml`'s root-level `Behavior on opacity` was written against a property `PanelWindow` does not have — see `S-30`'s row for the full explanation and fix, applied here identically. **Second correction, real-hardware VERIFY feedback, phi-shell commit `56c6b4f`:** with the shell loading, the launcher overlay appeared but the search field never received keyboard input at all — typing went to whatever window was underneath, clicking did nothing. Confirmed against the real source (`src/wayland/wlr_layershell/wlr_layershell.hpp`): `PanelWindow` defaults its attached `WlrLayershell.keyboardFocus` to `WlrKeyboardFocus.None` ("no keyboard input will be accepted") — correctly relied on by `Notifications/Toast.qml`, which must never steal focus, but never set anywhere that actually needs input. `searchField.forceActiveFocus()` only moves focus within the Qt Quick scene; it does nothing if the surface itself was never granted OS-level keyboard focus. Fixed via a new `Services/LayerFocus.qml` (setting it to `OnDemand`), kept in `Services/` per `phi-shell/CLAUDE.md`'s own rule confining `Quickshell.Wayland` there. |
| S-34 | Lock screen | awaiting-verification | phi-shell 812889a · dotfiles (this commit) | 2026-09-08 | **`Lock/Lock.qml` uses `WlSessionLock` on the real `ext-session-lock` protocol** (`Quickshell.Wayland`, verified against the real header, `wayland/session_lock.hpp`, not assumed), never a fullscreen window — the "stays locked if the client crashes" guarantee is the compositor's, confirmed in that header's own words: a destroyed `WlSessionLock` or a `quickshell` exit while locked leaves the screen "locked and painted with a solid color... inoperable," never exposed. **The security-critical part** (`S-34`'s own words: "the only genuinely security-critical code in the whole shell"): a single `PamContext` (`Quickshell.Services.Pam`, `config: "phi-shell-lock"`) declared **once**, deliberately outside the `surface` component `WlSessionLock` instantiates per screen — a `PamContext` placed inside that component would have meant one independent, concurrent PAM conversation per monitor, which is wrong. `PamResult` has exactly four values (`Success`/`Failed`/`Error`/`MaxTries`, confirmed against the real header, `services/pam/conversation.hpp`), and the `completed` handler has exactly one branch that unlocks (`Success`) and one default that does not — **ambiguous is not authenticated by construction**, not by discipline: there is no third branch to write wrong. Connected via `pam.completed.connect(...)` inside `Component.onCompleted`, not a direct `onCompleted:` handler on the `PamContext` itself — `PamContext`'s own signal is also named `completed`, and writing `onCompleted:` directly on that object risks colliding with the universal `Component.onCompleted` attached-property mechanism; `.connect()` on the real signal reference sidesteps the ambiguity entirely rather than relying on an untested precedence rule for the single most security-sensitive line in this codebase. **Locking is exposed to any same-user process via a `Quickshell.Io.IpcHandler`** (`qs ipc call lock lock`) — safe, since locking is never a security problem — **but unlocking has no IPC path anywhere.** The only place `locked` is ever set back to `false` is inside the `PamResult.Success` branch; nothing else in this file or reachable from outside it can clear the lock. **Real IPC-syntax correction made in this same commit, before it could ship wrong in three places at once:** the `qs ipc call <target> <function>` invocation had been written everywhere (S-31's `Panels/Sidebar.qml` comment, S-33's `Launcher.qml`) with an unconfirmed `-p ~/.config/quickshell/phi` flag guessed by analogy to `qs -p <path>` (which *launches* a shell at that config) — re-checked against the real Quickshell documentation's own worked example (`io/ipchandler.hpp`'s `qs ipc call rect setColor orange`) while building this step's own lock IPC call, which shows **no `-p` flag at all**, auto-targeting the one running instance; fixed in all three places in this same commit rather than leaving the discrepancy for whoever tested it first to discover. `Launcher.qml`'s "Lock" system action, which S-33 had wired to `loginctl lock-session` with a note that S-34 was "the correct integration point... to listen on later," now calls this `IpcHandler` directly instead — simpler, fully within this shell's own control, and does not depend on this project ever building a `systemd-logind` `Lock`-signal listener it does not need. `profiles/desktop/system/etc/pam.d/phi-shell-lock` (new `system/` directory for the `desktop` profile — did not exist before this step) is `/etc` material, **never applied by this repository**: `auth`/`account`/`password`/`session` all `include system-auth`, deliberately **not** `PamContext`'s own default service name (`"login"`), because that Arch service is configured for the text-console `login` program and may carry `pam_securetty`-style modules that assume a real controlling tty — a graphical, no-tty lock invoked against it could fail every attempt for a reason with nothing to do with the password being correct; this mirrors the same minimal service every other Wayland lock screen on Arch (`swaylock`, `hyprlock`) ships for itself. **A consequence worth naming plainly: until the user applies this file by hand, `PamContext.start()` cannot open a real PAM session at all** (`config: "phi-shell-lock"` names a file that does not exist on the machine yet) — this surfaces as `PamError.StartFailed`, itself just another non-`Success` outcome the existing fail-closed branch already handles correctly, so the screen stays locked with no path in either state, before or after the file is applied. Content beyond the password field: clock (`Qt.formatTime`/`formatDate`, a plain 1s `Timer`), battery (`Services.PowerBridge`, already built at `S-23`), now-playing media via a **new** thin `Services/Mpris.qml` wrapper (`Quickshell.Services.Mpris`, verified against the real header — `services/mpris/watcher.hpp` confirms the QML singleton name `Mpris` and `.players.values` as the real, JS-iterable form, `core/model.hpp`), and the three most recent notifications, read-only, from `Services.Notifications.history` (`S-30`). Reminders explicitly **not** built: no such feature exists anywhere else in this project, and the card itself lists them as "optionally." **Not verified, cannot be off-machine, and this is the step where that matters most:** whether `WlSessionLockSurface`'s `data` default property gives child items a real, properly-sized `parent` the way `PanelWindow` already does for every other surface in this repo — reasoned by analogy (both are Quickshell window-like types exposing real geometry, unlike `Widgets/Panel`'s specific `contentItem` alias workaround) but never exercised; if wrong, the visual content could be malpositioned or invisible while the lock itself stays fully secure regardless, since that guarantee is the protocol's, not this layout's — this is exactly what the VERIFY block's "lock, unlock" step will catch. Also unverified: the actual PAM conversation flow end to end (`responseRequired`/`responseVisible`/`respond()`) against a real `system-auth` stack; whether killing the shell process while locked truly leaves the screen inoperable as the protocol promises — this is the step's own explicit `USER` instruction and the one test that most matters here. **Correction (made post-hoc, during the M3 verification write-up, before any USER block shipped with the bug in it):** the "no `-p` flag at all" claim in this row's own IPC-syntax-correction paragraph above was itself wrong. It reasoned from the real `io/ipchandler.hpp` worked example, which is correct as far as it goes — but that example covers Quickshell's *default* config location (`<xdg dir>/quickshell/shell.qml`), never checked against how `hyprland.lua`'s own `AUTOSTART` section actually launches phi-shell: `qs -p ~/.config/quickshell/phi`, a *named* path, not the default. Confirmed by reading Quickshell's own `src/launch/parsecommand.cpp`: `ipc call` accepts an "Instance Selection" group (`-i/--id`, `--pid`) *or* a "Config Selection" group (`-p/--path`, `-c/--config`, mutually exclusive with instance selection) — with neither passed, it resolves the *default* config's instance, which is not phi-shell's. `-p ~/.config/quickshell/phi` was never wrong to include; removing it in this step was the actual mistake. Reverted in `Launcher.qml`, `Sidebar.qml`'s comment, and `AltTab.qml`'s comment (`phi-shell`, separate commit), and fixed at the only place it mattered on a real machine — every `qs ipc call` in `hyprland.lua`'s `S-38` keybindings and `S-35`'s gestures, all of which were shipped with the same bug (this commit, via a new shared `qsIpc()` Lua helper rather than six more hand-typed copies of the flag). **Third correction, real-hardware VERIFY feedback, phi-shell commit `e63f44e`:** `qs ipc call lock lock` returned "Target not found" — `qs ipc show` confirmed `lock` entirely absent while every other target (`sidebar`/`launcher`/`cheatsheet`/`overview`/`screenshot`/`record`/`alttab`) was present. Root cause confirmed against the real source, `src/wayland/session_lock.hpp`: unlike `PanelWindow` (used by every other surface in this repo, default property `data`, a list), `WlSessionLock`'s own default property (`surface`) is a **singular** `QQmlComponent*` — it can hold exactly one value. This file had four bare positional children (`PamContext`, a `Timer`, the `IpcHandler`, and `WlSessionLockSurface`) all implicitly competing for that one property; only the `IpcHandler` was confirmed missing, but whether `PamContext` or the `Timer` were silently dropped the same way is **not confirmed either way** — they occupied the identical position in the tree, so it is plausible the entire PAM flow was never actually running before this fix, not only the IPC trigger. Fixed by giving each an explicit, uniquely-named property instead of a bare child, and wrapping `WlSessionLockSurface` in an explicit `Component {}` for `surface:` rather than relying on implicit auto-wrap alongside other named properties. This introduced (and immediately fixed in the same commit) an `id: pam`/`id: retryTimer` collision against the same-named properties on `root` — the identical ambiguity class `S-21` already found once (`Segment.qml`'s `id: text`) — resolved by removing both ids and using `root.pam`/`root.retryTimer` explicitly everywhere, including inside `surface`'s `Component` body, which now crosses a component boundary that did not exist before this fix and has not been independently verified on real hardware. **Not verified, cannot be off-machine, and this is now more consequential than before:** whether `PamContext` was actually running prior to this fix (if not, no lock/unlock attempt on real hardware before this fix ever really exercised the security-critical PAM path at all); the full PAM conversation end to end after this fix — accepting input, a correct password unlocking, a wrong one not — and the kill-while-locked test, still this row's own single most important check. **Real incident, not a hypothetical — the user was stranded locked out of their own session testing this fix:** `PamContext.start()` failed immediately because `/etc/pam.d/phi-shell-lock` had never actually been installed (the manual `sudo install` step from an early handoff was missed). That fires PAM's `error` signal, never `completed` — and `retryTimer` was only ever restarted from `completed`, so nothing in the running session could recover on its own, and the password field's old `enabled: root.pam.responseRequired` binding stayed permanently false since PAM never got far enough to request a response. **`loginctl unlock-session` does not work against this `WlSessionLock` setup at all** — confirmed against every session `loginctl list-sessions` listed on `razer`, including the real seat0 session, all three tried, none had any effect. **Actual recovery needed a real Hyprland safety valve this project had no record of:** `pkill qs` followed by `hyprctl --instance 0 eval 'hl.clear_crashed_lockscreen()'` — worth recording plainly since it directly contradicts `WlSessionLock`'s own documented guarantee ("a dead client leaves the screen locked and inoperable, no way back in"); Hyprland provides an override the protocol documentation doesn't mention. Saved to this agent's own persistent memory so it isn't relearned the hard way twice. **Fixed in the same phi-shell commit (`d88bb01`) as the IPC-registration fix's own follow-up, after a second advisor review specifically because of the stakes:** `error.connect` now retries too, but through a separate, deliberately slower timer (`errorRetryTimer`, 5s) than the existing wrong-password one (`retryTimer`, 600ms) — reusing the fast one would mean an unattended config problem calls `PamContext.start()` roughly every 600ms indefinitely, real risk of `pam_faillock`-style modules locking the account itself on a real `system-auth` stack, which would have turned a config mistake into a second, worse incident. `enabled: root.pam.responseRequired` removed entirely — gating usability on PAM's own state made a PAM-side problem indistinguishable from a hung UI; the field is now always interactive, with `Keys.onReturnPressed`'s own existing guard (never forward a response PAM did not request) doing all the work the fail-closed logic actually needs, unchanged. `focus: true` replaced with an explicit `passwordField.forceActiveFocus()` in the surface's own `Component.onCompleted`. **Before this row can be re-verified, the PAM file must actually be installed this time** — `sudo install -Dm644 <phios-dotfiles checkout>/profiles/desktop/system/etc/pam.d/phi-shell-lock /etc/pam.d/phi-shell-lock`, confirmed with `ls -l` — and the recovery command should be within reach before testing lock again, not looked up after. |
| S-35 | Window overview | awaiting-verification | phi-shell 0ea46eb · dotfiles (this commit) | 2026-09-08 | `Overview/Overview.qml`: a `GridView` of **every** open window across **every** monitor, not just the current workspace (the card's own explicit requirement) — backed by a **new** `Services/ToplevelBridge.qml`, a thin wrapper over `Quickshell.Wayland`'s `ToplevelManager` (the `wlr-foreign-toplevel-management` protocol, cross-compositor — the same choice `S-33`'s Go-side `WindowsProvider` made independently for `phi query`'s own window results, and documented there for the same reason: real `activate()`/`close()` per window, no hand-rolled `hyprctl dispatch` string, and this surface stays correct even if `HyprlandBridge`'s own API ever moves). Icon+title only (`S-35 AGENT`: "start with icon+title. Live previews are an upgrade, not a requirement") — confirmed against `Quickshell.Wayland.ScreencopyView`'s real capability (checked early in this session, before `S-30`) that it renders a live feed into the scene rather than encoding a file, so a genuine live-preview grid needs per-window screencopy sources this step has no grounds to build for an explicitly deferred upgrade. No text search, per the card's own instruction — `GridView`'s built-in keyboard arrow navigation plus Enter/click. **Two real bugs found and fixed before committing:** (1) a `root.currentIndex` meant to mirror `GridView`'s own `currentIndex` both ways (`currentIndex: root.currentIndex` plus a change handler writing back) was the **same broken-binding class already found twice this milestone** (`S-33`'s search field, and the `Widgets/Panel` `contentItem`-indirection bug `S-31`/`S-32` found) — `GridView`'s own built-in arrow-key handling assigns to `currentIndex` imperatively, which breaks an incoming binding the moment it first happens, so resetting the selection on reopen would have silently stopped working after the very first navigation; fixed by **removing the redundant property entirely** (`grid` now owns `currentIndex` by itself) rather than routing around the hazard a third time. (2) The grid cell's icon+title layout repeated the exact `Widgets.Panel` `contentItem`-indirection bug (a nested child's real `parent` is Panel's inner `contentItem`, not the Panel itself) `S-31`/`S-32` already found and fixed — caught here before commit by now actively checking every `parent.width`/`parent.padding` reference against that pattern as a matter of course, not by surprise. **A third, process-level catch, also before commit:** the first draft of this file reached `Quickshell.Wayland`'s `ToplevelManager` directly and set `WlrLayershell.layer: WlrLayer.Overlay`, both violating `phi-shell/CLAUDE.md`'s own rule that only `Config/` and files under `Services/` may touch that surface — fixed by routing window data through the new `Services/ToplevelBridge.qml` (exactly the pattern this repo already established for every other service surface) and by **dropping the layer override entirely**, since no other surface in this repo (`Bar`, `Toast`, `Sidebar`, `Launcher`) sets one either, all correctly relying on the real documented default (`Top`) rather than importing `Quickshell.Wayland` in a non-`Services/` file just to ask for a different one. **Recall gesture** (`S-35 AGENT`: "three-finger swipe up/down... native Hyprland on razer's touchpad"): added to `hyprland.lua` in this same step — three-finger swipe **up opens, down closes** (a common show/dismiss convention, not both directions toggling the same thing), the opposite axis to Hyprland's own built-in workspace-swipe left/right, avoiding the conflict the card's own wording flags. **Syntax verified against the real Hyprland wiki source before writing it** (`hyprwm/hyprland-wiki`, `content/configuring/core/binds/gestures.md` — not the URL a web search first suggested, which 404'd; found by walking the repo's own directory tree instead) — `hl.gesture({ fingers = 3, direction = "up"/"down", action = function() ... end })`, matching the Lua config API this file already uses throughout (`hl.bind`, `hl.window_rule`), not a different or older classic-syntax form. Inert on `zotac` by construction, no host-specific guard needed: gestures need touchpad hardware that machine does not have, the same capability-driven default (ADR 074) already used everywhere else in this project rather than branching on hostname. **On `zotac`, the card's own alternative — Solaar rules covering the MX Master's own gesture button — is explicitly `S-43`'s job, not this step's; not investigated here.** Single instance on `Quickshell.screens[0]`, same simplification `Sidebar`/`Launcher` already document — the overlay's on-screen *position* is one output, its *content* still lists windows from every monitor, which is what the card's own `VERIFY` line actually asks for. **Not verified, cannot be off-machine:** whether the gesture actually fires on real touchpad hardware (this agent has never touched a touchpad); whether `GridView`'s layout looks right with a real, unpredictable number of open windows; whether `DesktopEntries.heuristicLookup(appId)` reliably resolves real installed apps' icons — same conservative text-only fallback already used at `S-30`/`S-31` for exactly this uncertainty. **Correction, found on real hardware during the M3 VERIFY round, phi-shell commit `fb79ed8`:** `Overview.qml`'s root-level `Behavior on opacity` was written against a property `PanelWindow` does not have — see `S-30`'s row for the full explanation and fix. The existing `Widgets.Scrim` here was already correct (a plain `Item`, genuinely has `opacity`) and was left alone; only the `GridView` content was wrapped in the new `fadeRoot`, so the two don't double-fade against each other. **Second correction, real-hardware VERIFY feedback, phi-shell commit `56c6b4f`:** the grid appeared and highlighted the current cell correctly, but clicking a different window closed the overlay (`activate()` was called, `setShown(false)` ran) without actually focusing that window. Applied the same `Services/LayerFocus.qml` fix as `S-33`'s row (needed here for `GridView`'s own arrow-key navigation and `Keys.onEscapePressed`/`onReturnPressed`), but this is explicitly **not assumed to fix the click/activate issue** — pointer/click events are not gated by keyboard focus mode, so that finding needs a retest after this fix rather than being treated as already resolved; if it persists, `Launcher.qml`'s own `activateWindow` action (`hyprctl dispatch focuswindow address:...`) is an already-proven fallback mechanism in this repo. **Correction to S-37's row (see there): activation via Enter/Return DOES correctly focus the selected window here** — confirmed by the user directly, in the same message that reported `AltTab.qml`'s own Return-confirm does not. This overlay was never given the `hyprctl clients -j` fallback `AltTab.qml` later got; whatever makes Return-confirm work here (the existing `LayerFocus.qml` wiring, `GridView`'s own `Keys.onReturnPressed`, or something else not yet isolated) is not shared code with `AltTab.qml`'s own separate `_confirm()`, so the two overlays' activation paths should not be assumed to share a root cause going forward, despite the same `Toplevel.activate()` limitation once being flagged as their common cause — this row's own earlier language calling them "the identical symptom" is now known to be an overstatement; only investigate this row's own activation path if it independently regresses, not as a consequence of `AltTab.qml`'s bug. **Still open, unchanged:** starting selection opens on the first grid element rather than the active window — the user's own words, still reported broken this round; not addressed this session, deferred per the user's own earlier explicit instruction not to touch this file further ("no need to change the overview now, just note it"). |
| S-36 | Screenshot, OCR, QR, recording | awaiting-verification | phi-shell 0e74666 (+ four respawn-bug correction commits, see S-13/S-20/S-23/S-30 rows) · dotfiles 8236e50 | 2026-09-08 | `Screenshot/Screenshot.qml`: capture built in-house (ADR 075), `grim` doing the actual pixel capture (the standard wlroots-ecosystem backend), `tesseract`/`zbarimg` (already declared, `S-15`'s base profile) for OCR/QR, `wf-recorder` for video. **`Quickshell.Wayland.ScreencopyView` was already confirmed — early in this session, before `S-30` — to render a live feed rather than encode a file**, exactly the verification this step's own card asks for ("first VERIFY whether Quickshell's declared screen-recording integration covers full encoding or only frame capture; do not discard `wf-recorder` before checking") — done before committing to `grim`/`wf-recorder`, not after. Selection is a QML drag-rectangle overlay, **not `slurp`'s own interactive picker** — "selection overlay built in QML" (the card's own words) is this file's replacement for exactly that role, so `slurp` (added to `profiles/desktop/packages.txt` anyway, for fidelity to master plan §15.3's own `grim`+`slurp` pairing, in case a user wants it for something else) is deliberately never invoked — flagged for cheap veto as an intentional deviation from the registry row's literal implication. All three region-based modes (save/OCR/QR) share one drag-select overlay; fullscreen and window capture need none (window mode reads `hyprctl activewindow -j` directly, the one place this file touches Hyprland state outside `Services/HyprlandBridge.qml`, since that wrapper exposes no pixel geometry at all — confirmed against the real header during `S-33`/`S-35`'s own research). Recording scope is the focused output, full-frame, not an arbitrary region — an explicit, disclosed simplification (`wf-recorder`'s own `-g` flag would make an area-recording mode a small addition later, not built now, since the card asks only for "invoked... for encoding only"). **`wf-recorder` needs `SIGINT`, not `SIGTERM`, to finalise its output file correctly** (this tool's own well-established behaviour, not re-verified against its source this session) — `Quickshell`'s own `Process.running = false` sends `SIGTERM` (confirmed against the real source, `io/process.cpp`: `setRunning(false)` calls `QProcess::terminate()`), so stopping a recording sends `SIGINT` explicitly via the process's own `processId`, not through that path. **One design bug caught and fixed before committing:** an early draft routed window-mode capture through the same `mode` property the text-selection modes use, with a `onModeChanged` handler that immediately reset it again in the same JS tick — this would likely have worked (same-tick set-then-reset never actually gets rendered), but relied on that timing subtlety for no reason; simplified to call the `hyprctl` query directly from the IPC handler instead, no `mode`/`onModeChanged` involved at all for that path. **The step that found and fixed a real, systemic bug across the whole `phi-shell` repository, not just this file — recorded in full on each affected step's own row (`S-13`, `S-20`, `S-23`, and a second correction on `S-30`), summarized here as the trigger:** this file's own `mkdir -p` safety `Process` was first written with `running: true` and no `onExited` reset, which would have respawned it forever the moment it first completed. Catching that prompted auditing **every** `Process` in the entire repository for the same mistake — read directly from the real Quickshell source (`io/process.cpp`): `Process::onFinished()` calls `startProcessIfReady()` **unconditionally** as its last step, so any one-shot `Process` left with `running` still `true` after it exits respawns itself immediately, in a tight loop, forever — `destroy()` alone does not prevent this, since it only schedules deferred deletion of a still-fully-alive object. Four **already-shipped, already-"verified"** instances were found and fixed this same session: `Config/Settings.qml` (`S-13` — every `phi state get/set/list` call in this shell has gone through this path since then), `Config/Capabilities.qml` (`S-20` — **the single most severe instance**: this probe has been respawning in a tight, uninterrupted loop from shell startup, on every real session run to date, not periodically as intended), `Services/Tailscale.qml` and `Bar/modules/Gpu.qml` (`S-23` — both pollers were completely decoupled from their own `Timer`s' intended intervals), and `Services/Notifications.qml`'s own `mkdir` process (a second, independent defect on top of the `trackedNotifications` leak `S-30`'s first correction already fixed). **This is a real, load-bearing finding that reopens confidence in "verified" as a status label**, not just a note: `S-13`, `S-20`, and `S-23` were all marked `verified` — some after real-hardware confirmation of their own stated `DONE WHEN` — while carrying this defect the whole time, because the respawn loop produces no VISIBLE symptom a manual functional test would catch (the polled values still update, just via a tight loop instead of the intended cadence) — only reading the actual mechanism against the real Quickshell source surfaced it. Real-hardware CPU/battery impact from any of the four pre-existing instances was never measured before these fixes and remains unconfirmed — only the mechanism is confirmed, not its practical severity on `zotac`/`razer` to date — flagged prominently for the final question list rather than asserted either way. Package additions: `grim`, `slurp`, `wf-recorder`, `zbar` — all four pre-approved, master plan §15.3, milestone M3. **Not verified, cannot be off-machine:** the entire capture/OCR/QR/recording pipeline end to end — nothing here has run inside a real Quickshell process, on real Wayland, against real `grim`/`tesseract`/`zbarimg`/`wf-recorder` binaries; whether the drag-select overlay's coordinate math (`screen.x`/`screen.y` plus local mouse position) produces a geometry string `grim` accepts without a sign or off-by-one error; whether `wl-copy --type image/png` piped from a file behaves as expected. **Real-hardware VERIFY feedback:** window and fullscreen capture work; area selection (and, downstream of it, OCR and QR, which reuse the same overlay) is offset "to the top by a lot" — the exact coordinate bug this row's own header already flagged as unverified, now confirmed real rather than fixed, since it needs `razer`'s actual scale factor (`hyprctl monitors -j`) to diagnose correctly rather than guessed at; held for that data rather than patched blind. **Recording, phi-shell commit `56c6b4f`:** the stopped file could not be opened (`mpv`: "moov atom not found," the standard symptom of a video file whose trailer was never written because the encoder did not exit cleanly). `_stopRecording()` spawned a separate `kill -INT <pid>` `Process` using `recordProc.processId`; replaced with `recordProc.signal(2)`, a real, confirmed `Process` `Q_INVOKABLE` method (`io/process.hpp`) that sends the signal directly with no separate process or `processId` timing dependency. Whether the old indirection was the actual cause of the corrupted file is not confirmed, but the direct method removes that entire failure surface regardless. **Second real-hardware round: the area-selection offset is fixed, recording is not.** Confirmed the offset's actual cause from `hyprctl monitors -j` data (razer: `"scale": 2`): `root.screen.x/y` and mouse coordinates are device-independent (logical) pixels — confirmed against the real source, `src/core/qmlscreen.hpp`, `ShellScreen` wraps a plain `QScreen` and reads its geometry directly, standard Qt platform convention — but `grim -g` wants physical compositor pixels, the same space `hyprctl monitors -j` itself reports `width`/`height` in. Window and fullscreen capture never hit this since neither derives its geometry from QML coordinates at all. Fixed (`phi-shell e63f44e`) by multiplying by `root.screen.devicePixelRatio`. **Recording is still broken, and worse than first reported:** the `Process.signal(2)` fix from the previous round did not resolve it — `Shift+R` does not stop the recording, and neither does terminating `qs` itself; the file keeps growing until the user kills `qs` directly. A `wf-recorder` that survives `qs` terminating is not being tracked as `qs`'s own child process in any way that termination reaches it. Held rather than guessed at a third time — the discriminating test (`qs -p ~/.config/quickshell/phi ipc call record stop` directly, bypassing the `Shift+R` keybind) is requested from the user before touching this again. **Root cause found (dotfiles `ed947e7`):** the user's test confirmed the direct `qs ipc call record stop` *does* stop the recording correctly — `Screenshot.qml`'s own handler was never the problem. `r` (start) used `fireAndReset()`, the same helper every other one-shot screenshot action uses, which exits the `screenshot` submap immediately after firing — but recording spans time, so `SHIFT + r` (bound only inside that submap) was unreachable the moment recording started, unless the user pressed `Print` again first. `r` no longer resets; only `Shift + r` does. **Fourth real-hardware round: marked broken and deferred, per the user's own explicit instruction, not investigated further.** `Shift + r` still does not stop the recording — the user's own words, "it just types shift+R in the active window" — meaning the fix above did not resolve the actual symptom on real hardware, for a reason this project has not further diagnosed. The user's own explicit redirect: the *intended* interaction was never meant to be a keybind at all — a stop button on the bar is the right design, out of scope for this session — so this is recorded here as a known, accepted limitation rather than chased a third time. **User confusion flagged for future clarity:** a prior round of this project's own guidance referred to this bind as "Start+Shift+R," which the user did not recognize; that was very likely a garbled way of saying "press Print first, then Shift+R" (Print enters the `screenshot` submap `Shift+R` only exists inside) — future instructions involving a submap-gated bind should say the two-step shape explicitly rather than name the inner key alone. **Follow-up, not built:** a stop button in `Bar/` (its own future step) is the design the user wants; the `record` IPC target's existing `stop` function is exactly what such a button would call, no new backend surface needed. **Fourth real-hardware round: the `devicePixelRatio` area-selection fix (`e63f44e`) is still broken.** The user confirmed the `phi-shell` checkout on the affected machine is up to date (ruling out, at least for this retest, the stale-checkout explanation raised for the same-round `AltTab.qml` finding below) — the offset persists despite that. No exact symptom (still offset "to the top by a lot," offset in a new way, or something else) was re-described this round; not re-diagnosed further this turn, only recorded as still-broken per the user's own explicit instruction to write it down rather than guess again. Combined with the `AltTab.qml` findings on the same round (S-37's row), this is now the **second independent, source-grounded fix producing no observable change on a confirmed-current checkout** — raises the same question S-37's row raises about whether the running `qs` process is actually the one that loaded this code (a stale *running process*, as opposed to a stale checkout — `qs` does not restart itself just because a `git pull` happened, only `Quickshell`'s own file-watch hot-reload or an explicit `pkill -x qs && qs -p ...` picks up a change) rather than the fix itself being wrong. Held for that data rather than attempting a third area-selection mechanism blind. |
| S-37 | Alt+Tab overlay, tooltips, context menu, cheat sheet | awaiting-verification | phi-shell 8648699 | 2026-09-08 | Four separate surfaces/widgets, one commit. **`AltTab/AltTab.qml`:** the overlay and its IPC control surface (`next`/`prev`/`confirm`/`cancel` via `qs ipc call alttab <fn>`) only — the actual Hyprland submap plus press-and-release binds (`architettura §8.2.2 S10`'s own mechanism: enter on Alt+Tab press, cycle on repeated Tab, confirm on Alt release, via a submap) is explicitly **`S-38`'s job, not this one's** — that step owns assigning every modifier once, Alt is its own reserved modifier for applications, and Alt+Tab's exact bind is tangled with the rest of that scheme the same way every other M3 surface's keybinding has been left for it (`S-22`'s own note, cited again here). `next()`/`prev()` auto-open if not already shown, so wiring this at `S-38` is one bind calling `next`, not a second design pass. Window data via `Services/ToplevelBridge.qml` (`S-35`), so the two window-listing surfaces in this shell can never disagree about what windows exist. Selection starts at index 1 (not 0) when more than one window is open, matching the usual "jump straight to the other window" convention — **`ToplevelManager`'s own ordering is unconfirmed** (no document states whether it is creation order, activation order, or something else), flagged for cheap veto if that reads wrong on real hardware; the cycling mechanism itself does not depend on the order being any particular one. **`Tooltip/Tooltip.qml`:** deliberately **not** built on `Quickshell.PopupWindow` — a same-window floating widget positioned via plain `Item.mapToItem` coordinate mapping instead, since every plausible tooltip consumer (a bar segment, a sidebar row) already lives in the same window it would show one inside, so there is no reason to take on `PopupWindow`'s cross-window anchoring risk just for this. No live consumer anywhere in this shell yet — an honest gap (nothing has asked for one), not a deliberate "don't wire" choice the way `ContextMenu`'s is. **`Widgets/ContextMenu.qml`:** the mechanism this agent proposed and the user confirmed before it was written (recorded in this session's own transcript, not a separate document) — `Quickshell.PopupWindow` anchored to an item. **A real API-verification catch before committing:** an earlier pass had inferred `anchor.item`/`anchor.edges`/`anchor.gravity`/`Edges.*` from a vague web-search summary; re-verified against the real source before writing this file (`core/popupanchor.hpp` — `PopupAnchor` really does expose `item`, `edges`, `gravity` as `Edges::Flags`; `core/types.hpp` — `Edges.Top`/`Left`/`Right`/`Bottom`, combinable with `|`, confirmed) — the original inference happened to be correct, but was checked properly rather than left as an assumption this file would have shipped with either way. **Built but deliberately left unwired to any real surface — the user's own explicit choice**, confirmed before building rather than assumed: prove the widget works, defer picking which rows (clipboard entries, notification cards, sidebar rows) get a right-click menu to a later step once real usage patterns are clearer. `grabFocus: true` (an outside click dismisses the menu, the real header's own documented behaviour) is exactly why this file's `visible` is never a plain binding to `items.length` — the same binding-break class already found and fixed repeatedly this milestone (`S-33`, `S-35`) would have made this menu stop reopening after the very first outside click; set imperatively in `open()`/`close()` instead. **`Cheatsheet/Cheatsheet.qml`:** read-only, `hyprctl binds -j` queried fresh on every open — **no saved copy anywhere, and no editing UI at all**, per the card's own explicit exclusion (both possible implementations "reintroduce exactly the divergence the user wants to avoid"). **Field parsing is reasoned, not verified against a real capture** — no `hyprctl` binary exists in this environment to run it against — `modmask`/`key`/`dispatcher`/`arg`/`description` and the modifier-bit decode (`SUPER=64`, `ALT=8`, `CTRL=4`, `SHIFT=1`, the standard XKB/wlroots convention Hyprland is built on) are inferred from that convention plus this project's own prior real `hyprctl -j` captures (`S-24`'s `hyprctl clients -j` work), not confirmed field-for-field; parsing degrades to showing whatever fields are actually present rather than failing outright on an unexpected shape, flagged plainly for cheap veto against real output. **`Q-N10` investigated, not adopted, per the card's own explicit instruction:** `Quickshell.Services.Polkit`'s `PolkitAgent`/`AuthFlow` (`services/polkit/flow.hpp`, fetched and read this session — the earlier research pass that first raised this question had hit a 404 on this exact file and left the question open) has a complete, coherent API — `message`/`inputPrompt`/`isResponseRequired`/`responseVisible`/`submit()`/`authenticationSucceeded`/`authenticationFailed` — the same shape `Lock/Lock.qml`'s own `PamContext` UI already knows how to build. **Finding: yes, it covers the real "authenticate to continue" case, and would let `phi-shell` eliminate `hyprpolkitagent`/`lxqt-policykit` from architettura §8.2 entirely** (neither is currently declared in any profile — there is no existing Polkit agent this would need to *replace*, only one it would newly provide). **Not adopted or wired here**, exactly as the card instructs — carried to the final question list for the user's own go-ahead. **Also flagged, not built, genuinely unclear rather than skipped:** the card's own line "the PATH command enumeration used here is the SAME data the launcher's command provider needs. Share it; do not build it twice" — `S-33`'s `CommandProvider` (`phi`, Go) does a single targeted `exec.LookPath` check on the first word typed, it does not enumerate every PATH binary, and nothing in this step's own four surfaces (Alt+Tab, Tooltip, ContextMenu, Cheatsheet) has an obvious need for a full PATH listing either — this agent could not identify a concrete, buildable connection between the two without guessing at a feature neither this step's card nor `S-33`'s actually describes; held for the final question list rather than invented. **Not verified, cannot be off-machine:** any of the four surfaces running inside a real Quickshell process; whether `Widgets.PopupWindow`'s cross-window anchoring behaves as the header describes on real Hyprland; the Alt+Tab window ordering; the cheat sheet's field names against real `hyprctl` output. **Correction, found on real hardware during the M3 VERIFY round, phi-shell commit `fb79ed8`:** both `AltTab.qml`'s and `Cheatsheet.qml`'s root-level `Behavior on opacity` were written against a property `PanelWindow` does not have — see `S-30`'s row for the full explanation and fix. `Cheatsheet.qml`'s existing `Widgets.Scrim` was already correct and left alone, same reasoning as `S-35`'s row; only the panel content was wrapped in the new `fadeRoot`. **Second correction, real-hardware VERIFY feedback, dotfiles commit `f260dba`:** Alt+Tab cycling worked but three things did not: (1) the first Alt+Tab press only entered the submap without showing the overlay or selecting anything, needing a redundant second press — fixed by chaining `submap("alttab")` and the first `next()`/`prev()` call on the same top-level bind, the documented "same keybind, multiple actions" pattern (`hyprwm/hyprland-wiki`, `configuring/core/binds/submaps.md`) already used for the release/cancel binds; added a matching top-level `Alt+Shift+Tab` for the same reason, since only the submap-local bind existed before; (2) releasing Alt did not confirm or close the overlay at all — the `ALT_L` keysym spelling itself is confirmed correct against the real doc's own `SUPER_L` example (`flags.md`), so that was never the bug, but this project has no way to confirm off-machine which physical Alt key was actually held, so the same confirm action is now also bound on `ALT_R`; (3) the cheat sheet showed raw internal values like `"_lua 16"` instead of anything readable — a real, documented `description` bind flag exists (`flags.md`) and is now set on every phi-shell bind; `Cheatsheet.qml`'s own `_describe()` already preferred `bind.description` first, it simply never had one to read. **Second real-hardware round:** Alt+Tab now shows the overlay on the first press (fixed); releasing Alt still neither confirms nor closes it — the `ALT_R` addition did not resolve it either, so this remains open, not yet diagnosed further. Every top-level bind now shows its real `description` in the cheat sheet as intended; a few submap-internal entries (`ALT_L`/`ALT_R`, the escape/catchall cancel binds) were left undescribed and still show `"_lua 36"`-style raw values, and `Escape` appears twice (once per submap it's bound in) since `hyprctl binds -j` lists every registered bind across every submap, not just the currently active one. **User request, not yet built:** some cheat sheet rows should be hideable, "in a manner that's easy to change in future" — read as a request for `Cheatsheet.qml`'s own filtering to be data-driven (e.g., an exclusion list or a `hidden` convention) rather than hardcoded conditionals; acknowledged, deferred to avoid widening this correction round further, needs its own small pass. **Third real-hardware round, Alt+Tab release confirmed NOT a bug in this file (dotfiles `ed947e7`):** `hyprwm/Hyprland#15785` on Hyprland's own real issue tracker (closed by tracker policy — GitHub Issues are no longer accepted upstream, redirected to Discussions — not because it was fixed) describes the identical scenario: a modifier held continuously from *before* a submap is entered does not fire its own release bind on its first release, only on a subsequent full press-and-release cycle already inside the submap. Alt+Tab enters this submap with Alt already held, exactly that case — this is a real Hyprland limitation, not something fixable in this project's own code. `ALT_L`/`ALT_R` stay bound (may still fire on some interaction patterns per the same report) but `Return` is added as the reliable confirm path, a fresh press that does not hit this limitation. **Also confirmed on this same round: `windows[selectedIndex].activate()` not actually focusing the target window is not surface-specific** — the user's report ("it never select the active panel") describes the identical symptom already found on `S-35`'s `Overview.qml`, both calling the same `Toplevel.activate()` method from the same `Services/ToplevelBridge.qml`. This strengthens the case that it is a real `Toplevel.activate()` limitation rather than something wrong in either surface's own code, and the fix both rows independently flagged — falling back to `hyprctl dispatch focuswindow address:...`, the mechanism `Launcher.qml`'s own `activateWindow` action already proves works — needs the user's own call before building it: the cross-compositor `ToplevelBridge` deliberately does not expose a Hyprland-specific `address` field (that lives on the separate `HyprlandToplevel` wrapper), so correlating the two would mean either switching this surface's window source to the Hyprland-specific one (trading the documented cross-compositor design goal for reliability) or matching entries across the two by title/appId (fragile). Held for that decision rather than guessed at. **Fourth real-hardware round (phi-shell/dotfiles, this commit), addressing the user's two explicit directives — fix Alt+Tab's selection, fix confirmation on Alt release — and note, not build, the Overview findings:** (1) **Selection was actually two separate bugs, both fixed.** Activation: matching the prior row's held decision, now built — a fresh one-off `hyprctl clients -j` query at confirm time, matched by `title`+`class` (confirmed field names, cross-checked against phi's own Go code, `internal/query/windows.go`), dispatching `hyprctl dispatch focuswindow address:...`, the same mechanism `Launcher.qml`'s own `activateWindow` action already proves works here — chosen over switching this surface's own window source, since only the one-shot activation step needs Hyprland-specific data, not the live cycling/highlighting. Starting position: the user's own description of Overview's bug ("always starts at the first element... rather then the active one") turned out to describe Alt+Tab too — the earlier `index 1` starting guess assumed `ToplevelManager` orders its list active-first, flagged here as unconfirmed at the time and now understood to be the actual defect. Fixed by querying `hyprctl activewindow -j` on entry (same Client JSON shape as `clients -j`) and selecting the window *after* whichever one it reports as focused, instead of trusting list order; `selectedIndex` is set provisionally to `1` the instant the overlay opens so it is never left unselected while that query is in flight, then corrected once it returns. (2) **Confirmation on Alt release: a materially different mechanism was tried, not yet confirmed on real hardware.** The `#15785` finding stands — a modifier held from before a submap is entered does not fire its own release bind on first release *inside that submap*. Re-read `flags.md` directly (not from memory this time) and found `submap_universal` ("will be active no matter the submap") is real. Moved the `ALT_L`/`ALT_R` release-confirm bind out of `hl.define_submap("alttab", ...)` to the global keymap with `submap_universal = true` — registered once, before Alt+Tab is ever pressed, which is not the scenario `#15785` describes. `AltTab.qml`'s own `confirm()` IPC function is now guarded on `root.shown`, since a global release bind can fire from any Alt release unrelated to Alt+Tab (e.g. AltGr on some layouts is the physical right-Alt key) and must be a safe no-op there. `Return` stays as the always-reliable fallback regardless of whether this fires. **Not yet re-tested on real hardware** — a better-targeted attempt than the earlier `ALT_R` addition that did not work, not a guaranteed fix. (3) **Overview findings, recorded per the user's own explicit instruction not to change it this session, not built:** Overview currently shows "basically the same features as" Alt+Tab, a flat list, when the intended design (the user's own words) is workspace/app *previews* — the live-preview gap this row already flagged as deferred (`ScreencopyView` renders a live feed, not a file) is now confirmed as a real, felt gap rather than a hypothetical one. Overview also starts on the first element instead of the active window — the same starting-selection bug just fixed in `AltTab.qml` above (point 1), not yet ported to `Overview.qml`. **Fifth real-hardware round: the user's own words, "none of those features were solved."** All three of the fourth round's fixes (starting selection via `hyprctl activewindow -j`, activation via `hyprctl clients -j` + `focuswindow`, confirm-on-release via a global `submap_universal` bind) were retested and **none changed the observed behaviour** — Alt+Tab still does not select correctly on entry, still does not focus the chosen window on confirm, and still does not confirm on Alt release. Each of those three fixes was individually grounded in a real, source-confirmed mechanism (the same `hyprctl` shapes and `Process`/`StdioCollector` pattern already proven working elsewhere in this shell, e.g. `Launcher.qml`'s `activateWindow`), yet none produced any observable change at all — not "still wrong in a different way," but unchanged. That pattern is the actual finding: it means the fixes most likely never ran, not that each mechanism was individually wrong. **Explicitly held here rather than guessed at a sixth time** — no further blind edits to `AltTab.qml` or `hyprland.lua` without first getting one of these from the user: (a) confirmation that `qs` was actually restarted/reloaded after pulling `phi-shell 210d308` (a stale running `qs` process would explain all three "nothing changed" results at once — this is the single most likely explanation and the first thing to rule out); (b) `qs log` or stderr output from a fresh `qs -p ~/.config/quickshell/phi` run, to see whether `AltTab.qml` throws a load error that silently keeps the old file's behaviour; (c) failing that, `hyprctl binds -j | grep -A6 'ALT_L'` (already requested in the prior handoff, not yet returned) to confirm whether the `submap_universal` bind actually registered at all. **Sixth real-hardware round: the user confirmed the `phi-shell` checkout is up to date** (asked directly, not independently verified via a pasted `git log`/`readlink` — taken as the user's own good-faith confirmation, per their explicit instruction this round to write findings down rather than ask further questions). This rules out the leading hypothesis from the fifth round (a stale `git pull` never reaching `~/.config/quickshell/phi`) for at least this retest. Combined with the same-round finding that the independently-fixed screenshot area-selection offset (S-36's row) is *also* still broken on this same confirmed-current checkout, the pattern now points less at any one fix being wrong and more at something systemic keeping the running `qs` process from actually reflecting the checked-out code — most plausibly a `qs` process that predates the `git pull` and was never restarted (Quickshell's own file-watch hot-reload covers a file saved locally, not necessarily a batch of files that changed via `git pull` while the process was already running against them — unconfirmed either way) rather than four unrelated bugs surviving four independent, source-grounded fix attempts. **Still held, not guessed at further this round**, pending the same diagnostic data requested in the fifth-round note above. **Seventh round — correction, not a new finding: the fifth/sixth-round entries above conflated this file with `Overview.qml`'s report and are wrong about the starting selection.** The user's own precise words: "Alt+tab opens rightly on the focused one" — the `hyprctl activewindow -j` starting-selection fix (fourth round, this row) **does work**; the "starts on the wrong element" complaint belongs to `Overview.qml` only (see the correction now on that row, S-35) and was never actually re-confirmed broken for this file specifically, only assumed to be by association. Since that fix's code path visibly took effect, **the "stale `qs` process" hypothesis the sixth-round entry raised is also now wrong for this file** — the running process is executing this session's current `AltTab.qml`, not stale code, which narrows the two still-real bugs to their own actual mechanisms rather than a shared "nothing loaded" explanation: (1) **`_confirm()`'s activation still does not focus the selected window, on every trigger that reaches it** — the user confirmed Return closes the overlay (so `_confirm()` does run) but does not focus the target, meaning the bug is specifically in `activateComponent`'s `hyprctl clients -j` → title/class match → `focuswindow` chain, not in whatever calls `_confirm()`; unlike `Overview.qml`, where Return-confirm does correctly focus (S-35's row) — the two overlays' activation paths are confirmed **not** to share a root cause (correcting this row's own earlier "identical symptom" framing). (2) **`ALT_L`/`ALT_R` still do not confirm or close the overlay at all**, even after moving them to the global keymap with `submap_universal = true` — but `Escape` and the submap's `catchall` bind **do** successfully close it, per the user's own words ("closes by any input... but does not when releasing alt"), and `Return` (bound inside the submap, unchanged code) also closes it. Since other binds inside and outside the submap both fire correctly, this is not a submap-dispatch or stale-code problem either — it isolates specifically to the `ALT_L`/`ALT_R` release binds themselves never matching the real key event on this hardware, for a reason still undiagnosed. **Both narrowed bugs held for more data, not guessed at a further time**: (1) needs the actual `hyprctl clients -j` output for a real open window compared against what `activateComponent` searches for (title/class match may simply never succeed — an untested assumption from the start). (2) is **not** re-explained by `#15785` as written — that finding is specifically about a bind declared *inside* a submap, and this one no longer is, registered instead at global scope from Hyprland startup, continuously, well before Alt is ever first pressed — so whatever is stopping it now is a different mechanism this session has not identified; possibilities not yet distinguished: the submap's own `catchall` bind consuming the release event before the global bind sees it, `submap_universal` not behaving as `flags.md` describes in this Hyprland version, a plain registration mistake in this file's Lua, or a genuine `ALT_L`-vs-`ALT_R` physical-key mismatch. `hyprctl binds -j | grep -A6 'ALT_L'` (requested twice already, still not returned) is the next concrete step — it would show directly whether the bind even registered as written. |
| S-38 | Keybinding scheme | awaiting-verification | dotfiles 5513045 | 2026-09-08 | A complete proposal, per this step's own AGENT bullet ("propose a complete scheme... this is the user's call, not yours" — `Q-N07`). **Super** for window-manager-level shell surfaces: `Space` → launcher (architettura §8.2.2 S1's own words, "Runner con Super+Spazio," matched verbatim), `N` → sidebar (its default tab, Notifications), `L` → lock (universal desktop-environment convention), `Tab` → window overview (distinct from Alt+Tab's per-application cycling below), `Shift+slash` → cheat sheet (Super+?, the common "show shortcuts" convention). **`Alt+Tab`** is the one binding that genuinely fits "Alt for applications" read strictly — cycling between running applications — via a submap with **three separate ways out** (releasing `Alt` confirms, `Escape`/`catchall` cancel) so a stray keypress during the mode can never leave the session stuck inside it, the exact failure mode the real Hyprland documentation's own worked example explicitly warns about. **Screenshot/OCR/QR/recording is a `Print`-triggered submap**, not five-plus separate modifier chords — master plan's own third scheme category, "modes for rare actions," applied literally rather than hunting for that many free chords: one mnemonic letter per action (`a`/`w`/`f`/`o`/`q`/`r`/`Shift+r`), each exiting back to the global keymap on its own via the documented "same keybind performs multiple actions" pattern, factored through one local Lua helper (`fireAndReset`) rather than repeated seven times by hand. Every binding reaches `phi-shell` through the exact same `qs ipc call <target> <fn>` mechanism every M3 surface has used since `S-31`, now via a `qsIpc(target, fn)` Lua helper that prepends `-p ~/.config/quickshell/phi` (see `S-34`'s row for the correction: that flag is required, not optional, since `AUTOSTART` launches phi-shell at a named path rather than Quickshell's default config location — every bind and gesture below originally shipped without it, matching `S-34`'s own since-corrected reasoning, and is fixed in this same commit). **Submap and release-bind syntax verified against the real Hyprland wiki source before writing any of it** (`hyprwm/hyprland-wiki`, `configuring/core/binds/submaps.md` and `.../flags.md` — the same repository-tree-walking approach that found `gestures.md` at `S-35`, since the wiki's own rendered pages sit behind a JS proof-of-work checkpoint this agent cannot solve) — `hl.define_submap`, `hl.bind(..., { release = true })`, the `hl.dispatch(hl.dsp.X(...))` multi-action-per-bind pattern, and the "`Do not forget a keybind to reset the keymap`" warning that directly motivated the three-way-out design for Alt+Tab, are all read from that source, not guessed. **One residual, disclosed uncertainty inside the Alt+Tab submap:** whether Hyprland's submap key-matching requires an exact modifier-state match the way a global bind does (i.e., whether a bare `Tab` bind would even fire while `Alt` is still physically held from entering the submap) is not confirmed by anything this agent could reach — the submap re-binds `ALT + Tab` explicitly (repeating the held modifier) rather than a bare `Tab`, the safer reading, not a confirmed one; flagged for cheap veto if cycling does not respond to a second `Tab` press on real hardware. **Pre-existing `Super+Return`/`B`/`E`/`Q`/arrows/`M` binds (`S-24`) deliberately left untouched** — even though app-launching sits awkwardly against "Alt for applications" read strictly, no real keybinding has ever been exercised on real hardware yet (every M2/M3 step's own repeated note, most recently `S-37`'s), so there is no muscle memory anywhere to protect either way; treating "open a terminal" as basic window-manager-level functionality rather than an "application" in the Alt-Tab sense is this agent's own reading, not a document's, and is exactly the kind of thing this step's own `DONE WHEN` ("the user confirms the scheme is the one they want to learn") exists to let the user overrule. **Not verified, cannot be off-machine — and this step's own `DONE WHEN` cannot be met from here at all, by its own definition:** `hyprctl binds -j` after applying (this step's `VERIFY`) only confirms what got *registered*, not whether Alt+Tab cycling, the screenshot submap, or any individual `qs ipc call` actually *behaves* correctly end to end — that needs the user typing on a real keyboard, which is exactly what `DONE WHEN` asks for and this row cannot supply. **Real-hardware VERIFY feedback:** the suggested `hyprctl binds -j | grep -A2 '"qs -p'` VERIFY command returned nothing — this was a wrong grep pattern on this agent's part (the internal JSON shape for a Lua-callback bind was never actually confirmed against real output, only guessed at), **not evidence the binds are broken**: every individual binding was already confirmed working functionally through its own surface's test (`Super+Space` opened the launcher, `Alt+Tab` toggled the overlay, etc.). Now moot regardless: the real `description` bind flag added in this same commit (see this row's own header, and `dotfiles f260dba`) means `hyprctl binds -j | grep -B2 -A5 '"description": "Toggle the launcher"'` (or any other description string set) is a correct, working VERIFY command that does not depend on guessing the dispatcher/arg JSON shape at all. |
| S-39 | Daily-use consolidation | blocked | | | **Genuinely cannot be done from here, not merely unattempted.** The step's own `USER` input is "use the system for a week. Report friction," and its `AGENT` bullet is "fix everything the user reported during a week of real use" — there is no week of real use to fix anything from: every S-30 through S-38 surface in this milestone was built, reviewed and committed off-machine, in one continuous session, with nothing yet deployed to `zotac` or `razer`. This is not a step this agent chose to skip; it is a step whose entire input does not exist yet by construction, the same way `S-39`'s own card frames it. Blocked on: the full M3 verification round (every `awaiting-verification` row from `S-30` through `S-38`) being applied and used on real hardware first, for at least the "a week" the card itself asks for. Revisit only after that. |

## M4 — Settings panel and system features

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-40 | [BLOCKING] Settings panel, nine sections | awaiting-verification | phi-shell 5cb0e55 · phi c638bf7 (tag v0.7.0) · phi-packages d009975 · dotfiles (this commit) | 2026-09-08 | `Settings/Settings.qml` mirrors Sidebar's registry pattern (`sections.json` + `componentFor()`), left-hand nav instead of a tab strip (nine rows don't fit one row). Bound to Super+S (free, this step's own dotfiles commit). Nine section files under `Settings/sections/`: General (hostname/CPU/RAM/disk/OS/kernel/uptime via new `Services/SystemInfo.qml`, a KEY=VALUE shell probe matching Capabilities.qml's own shape; razer battery health/cycles added to `PowerBridge.qml` — cycle count is a best-effort `upower -i` shell-out, UPowerDevice has no such property, confirmed against real Quickshell source), Theme (variant switch runs `phi theme set` directly; night-shift/True-Tone/spotlight/wallpaper controls persist to new phi state keys but have no consumer yet — flagged per-row, not hidden), Connectivity/Devices (existing S-23 bridges, second consumers), Keybindings (factored `hyprctl binds -j` query out of Cheatsheet.qml into `Services/Keybinds.qml` so both read one live query, per S-37's own "share it" instruction; added a search field, no text-entry widget existed before this step), Notifications (DND wired to `Services.Notifications` directly), Security/AI Agent/Updates (explicit placeholders — ClamAV and phi-agent are M6/M7, pkg categories are S-45, face unlock is a closed exclusion not a gap, secrets is `[HOLD]` Q-F02). **Found and fixed a real bug**: `Widgets.Pill` (S-21) had never had a consumer until this step's Notifications/StateToggleRow usage — its `onTapped` self-mutated `checked` before emitting `toggled`, which silently detaches any external `checked: <binding>` on first tap (QML drops a binding the instant something assigns to it). Fixed to `onTapped: root.toggled(!root.checked)`, a controlled component the caller's own binding now drives correctly. `phi`: added `nightmode.temp`, `toggle.true-tone`, `spotlight.size`, `chroma.color` to `internal/state.Keys` in one change (batched per S-42/S-43/S-46's future consumers, not one bump per step — every phi change needs the same chroot-rebuild-then-reinstall cycle from the user regardless of which step's QML reads it), tagged v0.7.0, `go build`/`go vet` clean off-machine. Monitor listing in Devices confirmed against real Quickshell source (`core/qmlscreen.hpp`, `QuickshellScreenInfo`: name/width/height/devicePixelRatio all real). CSD decision (Q-N05) and idle-inhibit default app set were asked and answered this session, applied at S-41/S-43. **Not verified on real hardware**: nothing in this step has run on `qs` at all yet — same standing caveat as every M2/M3 QML step. |
| S-41 | Live light/dark theme, GTK/Qt, CSD decision | awaiting-verification | phi c4477b0 (tag v0.8.0) · phi-packages e0e65eb · dotfiles (this commit) | 2026-09-08 | **Q-N05 closed, asked this session: suppress CSD.** `hl.env()` in hyprland.lua (confirmed real API against the Hyprland wiki source): `QT_QPA_PLATFORMTHEME=qt6ct`, `QT_WAYLAND_DISABLE_WINDOWDECORATION=1` (unconditional for Qt), `GTK_CSD=0` (real but incomplete for libadwaita apps, which need an AUR-only LD_PRELOAD shim — accepted, documented, not silently narrowed). **Packages added by explicit user approval** (none were in master plan §15): `qt5ct`, `qt6ct`, `gsettings-desktop-schemas`. `qt5ct`/`qt6ct` **substitute for §6.7's named Kvantum** — Kvantum's non-SVG palette-override format could not be verified from here; qt5ct/qt6ct's real `[ColorScheme]` format was (github.com/trialuser02/qt6ct, read for format only, no content copied). New templates, all Class C: `gtk-3.0`/`gtk-4.0` `gtk.css` (`@define-color` override, GTK3 and libadwaita named colours) + `settings.ini` (prefer-dark-theme, decoration-layout empty); `qt5ct`/`qt6ct` `colors/phi.conf` (21-role `[ColorScheme]`, order confirmed against Qt 6 docs). New synthesized (not hand-authored) token `PHI_VARIANT_IS_DARK` in `internal/tokens.Load()`, for the one boolean a template needed. Live half: `phi theme set` now also writes the portal's `org.gnome.desktop.interface color-scheme` via `gsettings` (degrades to a reported skip off-machine, verified) and regenerates `~/.config/qt{5,6}ct/qt{5,6}ct.conf`'s `[Appearance]` section with the resolved `color_scheme_path` — both new Go-side steps outside the adapters.txt mechanism, since neither has a template/destination shape. **Librewolf userChrome.css has real content but no adapters.txt row and is NOT wired**: Firefox/Librewolf profile directories get a random 8-char prefix at first launch, not a fixed path the template mechanism can target — open item, see the template's own header. LibreOffice icon alignment: skipped, LibreOffice is not declared in any profile yet (§9.13 defers it to M6). Smoke-tested off-machine: `phi theme list`/`check`/`set dark --dry-run` all clean against the real dotfiles checkout; `phi theme set dark` applied into a scratch `$HOME` renders real hex values in every new file (no leftover `${...}`), `qt6ct.conf`'s `color_scheme_path` resolves correctly, portal write correctly reports FAILED (no matching schema on this dev machine, expected off a real phiOS host). **Not verified on real hardware**: nothing here has run in a real GTK/Qt/Librewolf window. |
| S-42 | Night shift and True Tone | awaiting-verification | phi-shell c279602 · dotfiles (this commit) | 2026-09-08 | `hyprsunset` added to `profiles/desktop/packages.txt` (pre-approved §15.3), autostarted bare in `hyprland.lua` (no `hyprsunset.conf` profile written — a clock profile would fight `Services/NightShift.qml`'s own IPC calls, since a profile activation "resets all options set by other profiles" per the real wiki text this session fetched verbatim). `Services/NightShift.qml`: owns `toggle.night-mode`/`toggle.true-tone`/`nightmode.temp` itself (same shape as `Services/Notifications.qml` owning `toggle.dnd`), drives `hyprctl hyprsunset temperature <K>`/`identity` (real syntax, confirmed against the Hyprland wiki source this session, quoted verbatim before writing). Refactored `Settings/sections/Theme.qml`'s night-shift/True-Tone controls to call this service directly instead of `Config.Settings`, so there is one owner of the state, not two. True Tone reads ambient lux from `/sys/bus/iio/devices/iio:device0/in_illuminance_raw` (falling back to `_input`) on a 60s timer, mapped to 2700–6500K by a **plain linear heuristic this step invented** — S-06 confirmed the device and its name ("als") on razer but never captured a raw value, so both the attribute name and the mapping curve are **unverified against real hardware**, flagged in the file itself. New bar module `Bar/modules/NightMode.qml` + `modules.json` row: **not capability-gated**, unlike razer's other host-distinguishing modules (battery/wifi/bluetooth) — `hyprsunset` is in `desktop`, not `laptop`, so there is no hardware capability that would make this razer-only under ADR 074 the way §8.4's table implies; shown on both hosts, flagged as a deliberate divergence from the table rather than a fabricated proxy gate (`Bluetooth.qml`'s own S-23 note flagged the identical tension once already). Uses plain "●"/"off" text, not a `glyph` — matches the established, deliberate convention (`Volume.qml`'s S-23 note: font-symbol glyph coverage has never been exercised, no module in this bar sets `glyph`) that I nearly broke by first writing this with `glyph: "☾"/"☀"` before catching it against precedent. `hypridle` also added to packages.txt (pre-approved §15.3) but **not configured** — no S-4x AGENT bullet names an idle-timeout policy yet; its first real consumer is S-46's Chroma "static colour on idle" behaviour. |
| S-43 | Cursor spotlight, idle inhibit, OSD, timer, colour picker, fullscreen auto-hide bar | awaiting-verification | phi-shell d9f6d47 · dotfiles (this commit) | 2026-09-08 | Six surfaces, one commit. **Idle inhibit** (`Services/Idle.qml` owns the rule logic against `Services/idle-inhibit-rules.json`; the actual `Quickshell.Wayland.IdleInhibitor` protocol object lives in `Bar/Bar.qml` instead — it needs a real, already-mapped window, and the bar is the one guaranteed-visible surface every host has; a purpose-built hidden window's `wl_surface` lifecycle could not be verified off-machine). Default rules asked and answered this session: Steam (`steam_app_*`), `mpv`, and **`librewolf` FULLSCREEN ONLY** — a plain "librewolf" rule would inhibit idle for ordinary browsing, defeating the feature; no specific video-call app was named so this is the closest available proxy, flagged coarse by construction. **OSD** (`Osd/Osd.qml`) is purely reactive — watches `AudioBridge`/new `Services/Brightness.qml`, shows nothing itself on a key press; S-46 wires the actual keys. **Brightness** (C-11 CLOSED): this session read Quickshell's real `services/` directory listing before writing `Services/Brightness.qml` — no backlight/brightness service exists at all as of the master branch checked, so `brightnessctl` isn't a fallback guess, it was the only option. **Spotlight**: Canvas `createRadialGradient` vignette (no GraphicalEffects/ShaderEffect dependency verified available), polling `hyprctl cursorpos` at 60ms instead of the card's own suggested Hyprland event-socket subscription — a deliberate complexity/risk substitution, flagged, matching §9.10's own "no permanent commitment" clause. Size/toggle read `spotlight.size`/`toggle.spotlight` (S-40's keys). **Fullscreen auto-hide bar**: `Bar.qml` now checks `Toplevel.fullscreen` + `Toplevel.screens` (confirmed against real source) for the active window on ITS OWN screen; `exclusiveZone`/content `y` both react, a `HoverHandler` on the bar's own (never-destroyed) geometry reveals it. **Colour picker**: separate `Screenshot/ColorPicker.qml`, not bolted onto `Screenshot.qml`'s drag-rectangle overlay (single click vs. drag-a-region are different enough interactions that forcing one through the other's state machine risked the three already-working modes) — `grim` capture + `magick ... -format "%[pixel:p{X,Y}]" info:`, unverified end-to-end (no compositor here to grim from). **Timer**: bar-click cycle only (idle → 5:00 → cancel) — no popover surface exists anywhere in this shell yet (`Volume.qml`'s own S-23 note), so a real duration picker is AWAITING that same gap; completion routes through `notify-send` into the shell's own notification pipeline (ADR 073), not a bespoke toast. **Solaar MX Master gesture button**: not investigated — genuinely a `[USER]` task (enumerate real Solaar rules on real hardware), noted for the end-of-session list, not guessed at. **Found and fixed while writing, before commit**: an early draft of `Bar/modules/NightMode.qml` used `glyph: "☾"/"☀"` — caught against `Volume.qml`'s own established, deliberate precedent (no module in this bar has ever set `glyph`, font-symbol coverage unverified) and rewritten to plain "●"/"off" text; an early draft of `Screenshot/ColorPicker.qml`'s completion toast used `visible: root.lastHex.length > 0 && !root.shown` on an element inside a window whose own `visible` is bound to `root.shown` — structurally unable to ever show, removed rather than half-fixed, matching `Screenshot.qml`'s own precedent of no completion toast for a silent clipboard copy. |
| S-44 | Wallpaper | awaiting-verification | phi-shell 8c8a812 | 2026-09-08 | `Background/Background.qml`: native layer via `WlrLayershell.layer = WlrLayer.Background` (real attached property, confirmed while researching S-43's `LayerFocus.qml`), per-screen, hyprpaper/swww excluded per the card. `Services/Background.qml` holds the shared path (same split as `Services/Spotlight.qml`: a per-screen surface cannot own its own IPC/shared state). `Config/Paths.qml` gets `wallpaperDir` = `$XDG_DATA_HOME/phi/wallpapers` — `$XDG_DATA_HOME`, not `$XDG_STATE_HOME`: a chosen wallpaper is a real asset, not disposable runtime state. **No native file-browser dialog** is confirmed available in this Quickshell/layer-shell stack from here, so `Settings/sections/Theme.qml`'s "selezione" is a pasted path plus a "Set" button — copies into `wallpaperDir` (`cp`, never referencing the original) and only THEN writes `wallpaper.path` and calls `Services.Background.refresh()`, so the copy-before-set ordering is what stops a running session from ever pointing at a path outside the dedicated folder, even momentarily. The "wireframe/flat gradient only, never photographic" style-plan constraint is **not enforced in code** — no way to classify an arbitrary image without ML, left as a human judgement call, stated plainly in the panel's own text. |
| S-45 | Package management surface and theme regeneration hook | awaiting-verification | phi 5cd71ea (tag v0.9.0) · phi-packages 34230f9 · phi-shell de26678 · dotfiles (this commit) | 2026-09-08 | New `internal/pkg` package, reusing `internal/doctor/packages.go`'s own `pacman -Qm`/`-Qe` queries rather than a second implementation. **T4 can never be enumerated by any pacman query** (a T4 package is installed WITHOUT pacman ever recording it, by definition) — `phi pkg list`/`check` say so explicitly rather than reporting a fabricated "0", which would read as "confirmed none" instead of "cannot check". AUR and T4 are **merged into one "AUR" bucket** at the query level: pacman's `-Qm` (foreign) has no way to distinguish an AUR-built package from one `pacman -U`'d by hand, so a real T4 package that WAS registered with pacman (not the common case, but possible) would show there too — flagged in the report text itself as a real limit, not silently narrowed. `phi update`: the **one verb in this whole CLI that breaks the styled/structured output contract** on purpose — real passthrough stdin/stdout/stderr to `sudo pacman -Syu`, since a captured/redirected sudo prompt has nowhere to go; documented as the one exception in `internal/update`'s own header. Preventive snapshot via `snapper -c root create` — `"root"` is this project's own installation procedures' own config name, not re-verified against a live snapper config from here, degrades to a non-fatal warning if wrong. Stops before regenerating theme configs if the upgrade itself failed. **Settings/sections/Updates.qml is READ-ONLY** — shells `phi pkg check` and shows its plain-text report verbatim (no second parser for the same four-category data); no button anywhere in the panel can trigger `phi update` or `pacman`, per this step's own AGENT contract that the privileged, interactive verb belongs to a terminal the user runs themselves. Pacman hook (`profiles/base/system/etc/pacman.d/hooks/phi-theme-regen.hook` + its helper script `.../usr/local/bin/phi-theme-regen-hook.sh`, both `/etc` material, never applied): triggers on kitty/gtk3/gtk4/qt5ct/qt6ct/phi/phi-shell upgrades; since a hook runs as root with no real user session, the helper script resolves the logged-in graphical user via `loginctl` and re-runs `phi theme set` as them via `runuser` — **entirely unverified against a real pacman transaction**, and does not handle concurrent multi-user graphical sessions (flagged, expected moot on phiOS's single-user desktops but not confirmed). Smoke-tested off-machine: `phi pkg list`/`check`/`help` all degrade cleanly with no `pacman` present (this dev machine is macOS) — did NOT smoke-test `phi update` itself, since `sudo` exists on this machine even without `pacman`, and triggering a real local sudo prompt to test it would be exactly the kind of unintended system side-effect this agent must avoid. `go build`/`go vet` clean. |
| S-46 | razer hardware: hwdb fix and Chroma | blocked | phi-shell 2e39fc6 (+ fixes 66df507, 1cf272c, df4298d, b69eaa1, e503aac, 2f980ce, 4b18672) · phi v0.9.1 · dotfiles (this commit) | 2026-09-09 | **The step card's own premise was wrong, corrected against verified evidence, not guessed**: card said "write the hwdb rule from the codes captured in S-06". S-06's own (verified) finding was the opposite — the kernel already reports standard, correctly-named codes for every volume/brightness key; no hwdb fix was ever needed, and S-06 said so explicitly ("almost certainly the absence of any compositor keybinding"). Confirmed no existing bind claims these keysyms (grepped `hyprland.lua`). **No file was written to `profiles/razer-hw/system/`** — a `README.md` there explains why, so the absence isn't mistaken for an oversight. Fix: five Hyprland binds (`XF86AudioMute/Lower/RaiseVolume`, `XF86MonBrightnessDown/Up`) with `repeating = true, locked = true` on the brightness pair, matching the exact flag pair the real Hyprland wiki's own worked example for this keysym pair uses. Volume goes straight to `wpctl` (AudioBridge reflects live PipeWire state regardless of who changed it); brightness goes through a new `Services/Brightness.qml` IpcHandler (`qs ipc call brightness up/down`) instead of a bare `brightnessctl` call, so the S-43 OSD updates atomically instead of going stale. **Caught and fixed before commit**: an early draft used `hl.bind("", "XF86AudioMute", ...)` (two string arguments) — the real wiki example this session fetched verbatim uses ONE string argument (`hl.bind("XF86MonBrightnessUp", ...)`); fixed to match. Also caught: the whole block was first inserted BEFORE local function `qsIpc`'s own definition point in the file — Lua would have thrown "attempt to call a nil value" at runtime; moved after it. **Fn+F4/Super+P: left unbound, noted as an open question** — the firmware synthesizes a literal Super+P keypress (S-06), and phiOS has no display-projection concept to bind it to; not guessed at. **Chroma** (`Services/Chroma.qml`): writes directly to the real `org.razer` session-bus DBus service via `busctl` — every interface/method name (`razer.devices.getDevices`, `razer.device.lighting.chroma.{setStatic,setNone,setBlinking}`, object paths `/org/razer` and `/org/razer/device/<serial>`) confirmed against real openrazer daemon source, not guessed; no DBus QML type exists in Quickshell (checked the same `services/` listing C-11 already ruled out for brightness). **Panel scope held exactly to the card's own words** ("on/off toggle plus an optional static colour picker. NOTHING ELSE"): `Settings/sections/Devices.qml` now has a real Pill + hex text field, nothing more. **Built**: static-colour toggle (the base case) and a `blink()` function (whole-keyboard `setBlinking`, substituting for the card's "function-row blink" — per-key addressing is unconfirmed for razer's device id) — `blink()` exists but is **not yet wired** to `Services/Notifications.qml`'s arrival signal, left for whichever step next touches that already-shipped file with this specifically in mind. **Deliberately not built, per the task's own "do not guess major decisions" instruction**: power-key colour from battery (needs Q-F06 — still the USER's own `python-openrazer` enumeration task, not run), critical-battery red pulse and Super-held key-availability illumination (same per-key question), Neovim mode colour (a Neovim plugin, outside phi-shell), mic-mute indicator (no microphone/source bridge exists anywhere in this shell — only `AudioBridge`'s default sink), red-on-blocking-dialog (no such dialog concept exists here). `hypridle` (added to packages.txt at S-42, still unconfigured) is Chroma's own "static colour on idle" trigger and stays unconfigured — no S-4x card names an idle-timeout duration/policy, and inventing one now would be exactly the kind of guessed major decision this session was told not to make. **Nothing here has run against a real `org.razer` service or real hardware** — entirely unverified end to end, flagged throughout the file itself. **First real-hardware correction (razer, first `qs -p` run since S-40)**: `qs` failed outright with `Singleton is not a type` — `Services/Background.qml` (S-44) declared a `Singleton { ... }` element without `import Quickshell` (that element lives in the `Quickshell` module, not `QtQml`). Because `qs.Services` is a whole-directory namespace import, this one broken file took down every consumer transitively, which is why the reported error chain pointed through `AudioBridge`/`Background.Background` rather than at the real file — a real, if confusing, failure mode worth remembering for the next QML syntax error a user pastes back. Swept every `Singleton`, and every `Process`/`IpcHandler`/`FileView`/`PanelWindow`/`Quickshell.Wayland` user in the whole repo for the same missing-import class after finding it; turned up one more, not yet triggered: `Services/PowerBridge.qml` (S-40's charge-cycle `Process` addition) missing `import Quickshell.Io`. Both fixed, phi-shell 66df507. **Second real-hardware round, per-feature feedback (razer)**: RAM missing in General (nested-quoting bug in `Services/SystemInfo.qml`'s awk script, reproduced against a fake `/proc/meminfo` off-machine before fixing — see `phi-shell` 1cf272c); Spotlight settings-panel toggle/size never reached the live overlay (state ownership moved into `Services/Spotlight.qml`, matching NightShift/Chroma's own shape) plus three more real bugs found alongside it: `hyprctl cursorpos` reports PHYSICAL pixels, the Canvas paints LOGICAL — same class of bug `Screenshot.qml` already fixed at S-36 on this exact host ("razer, scale 2"); no fade transition; stale position on re-show darkening the whole screen until the cursor moved. Spotlight ALSO switched from a persistent toggle to **hold-to-show** per direct feedback ("it shouldn't be a toggle, while pressed it shows, when released it fades out") — `hyprland.lua` now has a press/release bind pair on Super+G, `release = true` confirmed as a general documented flag (not bare-modifier-only) before writing it. Wallpaper "not working": a real race between `phi state set` and `phi state get` fired back-to-back with no ordering guarantee — fixed by making `Services/Background.qml` update its own `path` synchronously instead of re-reading it back. `Widgets/ToggleRow.qml` (new): a plain `Row` sizing each child to its natural width let True Tone's own long label push its Pill clean out of the panel's clipped bounds, unreachable — every toggle row this session built shared the risk, factored into one stateless layout widget; `Settings/StateToggleRow.qml` (S-40) deleted once its last caller migrated off it. Chroma colour field gained `#rrggbb` validation ("accepts invalid inputs"). Bar night-mode icon "●"/"off" reported as visually indistinguishable, swapped for "Night"/"Day". **`openrazer-daemon.service` was never declared as a user service anywhere in this repo** — very likely why Chroma silently did nothing AND why `python-openrazer`'s own import failed for the user (Q-F04/Q-F06 still not answered): `org.razer` only exists on the session bus once that unit actually runs, and nothing before this ever asked the user to enable it. New `profiles/razer-hw/services-user.txt` declares it (never auto-enabled, per this repo's own standing rule) — genuinely predates S-46, this profile's own gap, only surfaced now that Chroma gave it something to depend on. **Found, not yet fixed, real architecture bug**: `phi/internal/query/currency.go`'s own background-refresh design (`go refreshCurrencyCache(...)`) cannot work at all under `phi`'s own cold-start contract — `phi query` is a fresh, short-lived process per invocation, so the goroutine it launches never survives long enough to complete an HTTP round trip before the process exits; the on-disk rate cache can never actually get populated. This is M3 (S-33) scope, not M4 — noted for a decision on whether to fix now or later, not fixed in this round. Still open, not investigated this round: Alt+Tab selection/release and the Super+Tab selected-option issue (pre-existing, already logged across several earlier PROGRESS rows), screenshot area-selection offset and Shift+R stop-recording (same), and the Settings panel reportedly closing on some option changes — real cause not confirmed, genuine hypothesis (a `phi theme set` call rewrites `Config/Tokens.qml`, which Quickshell hot-reloads, which could plausibly reset `Settings.shown`) needs the user to confirm whether it happens for non-theme controls too before acting on it. **Third real-hardware round**: night shift/True Tone still had NO visible screen effect — `Services/NightShift.qml`'s `hyprctl hyprsunset` calls used an unproven `Component.createObject` Process-construction shape with no working precedent anywhere in this codebase; simplified to the one proven reusable-Process pattern (`phi-shell` df4298d) — root cause not fully confirmed without hardware, flagged as the most likely of two candidates (the other: `hyprsunset` itself never started, since `hyprland.start` only fires once per session and a config reload during testing would not have re-triggered the S-42 autostart hook added mid-session — ask the user to confirm with `pgrep hyprsunset` before assuming the code fix alone closes this). **Screenshot area-selection offset — real root cause found by reading grim's own source** (`render.c`): the `-g` box must be entirely LOGICAL, unscaled — grim multiplies by scale itself. The PRIOR "found on real hardware, scale 2" fix (S-36) was itself wrong; removed the `devicePixelRatio` multiplication entirely. This also explains "offset changes based on screen position": scaling an already-correct logical `screen.x` compounds with distance from the origin, exactly the reported symptom. **Spotlight, second round**: reverted the devicePixelRatio conversion on `hyprctl cursorpos` (made centering worse, not better — apparently not the same coordinate space grim's `-g` wants after all); "whole screen black until cursor moves" fixed properly by gating the fade-in on having received a real position, not just a plausible default; added an own-drawn cursor marker (real-hardware feedback: "if the cursor is not visible... it should show A cursor"); release-bind switched from the full SUPER+G combo (confirmed broken — releasing G before Super left it stuck on) to a bare `g` release with `non_consuming = true` (without which an unmodified bind on a real printable key would very plausibly swallow every typed "g" system-wide — flagged for careful testing, not fully certain this specific combination is safe). **Currency conversion fixed for real**: root cause was `phi query`'s own cold-start contract — a background goroutine cannot survive the short-lived process that launched it. Rebuilt around a genuinely detached child process (`phi query refresh-currency`, Setsid) and a new generic `ActionLoading` result kind, wired into the Launcher (retry timer + stale-response guard). Verified end-to-end with a real built binary: first call returns "loading", cache populates within ~2s, second call returns the real rate. Wallpaper `~` expansion and Chroma `#rgb` shorthand also fixed. **Still open**: `openrazer-daemon.service` fails to start on real hardware (`SystemExit: 0` from `daemonize.py`, python3.14) — a real environment/compatibility problem, not something in this repo to fix blind; asked the user for the untruncated CRITICAL log line. Shift+R stop-recording reviewed (looks correct, matches an already-applied prior fix) but not re-diagnosed without a more specific symptom.

**ROUND 3 — user says stop attempting fixes on the remaining items below and just record status. No further code changes were attempted on any of these after this round, only what is noted.**

- **BROKEN — Screenshot area-selection offset.** The logical/unscaled `-g` fix (round 2) did not close it: user confirms the offset is still present, specifically **on large selection areas** (small areas were not reported as still broken, so the earlier fix may be a partial improvement, not a wrong turn — genuinely unclear without more data, and no further investigation was done this round per instruction).
- **BROKEN — Cursor spotlight, on multiple axes.** (1) Position: still "slightly down from the actual cursor position" even after reverting the scale conversion — the raw `hyprctl cursorpos` value is apparently not exactly what this Canvas needs either; root cause NOT found. (2) Interaction model: hold-to-show (press/release on Super+G, `phi-shell` 1cf272c/df4298d) was a session-local reinterpretation of master plan §9.10/§2.3's own closed "toggle" decision, shipped without the user confirming the replacement actually worked first — user reports it "has issues" and asks it be marked broken outright, not iterated on blind. **The toggle-vs-hold question itself is now open again** and needs the user's own decision before either is rebuilt — not re-guessed. (3) The own-drawn cursor marker added in round 2 was never requested; removed (`phi-shell`, this commit).
- **BROKEN — Night shift / True Tone, no visible screen effect.** User confirms: "hyprsunset socket can't be found" — the daemon itself is not reachable via `hyprctl hyprsunset`, most likely because it was never actually started (S-42's autostart hook runs on `hyprland.start`, which fires once per Hyprland session, not per `qs`/config reload — added mid-session, so a session that only reloaded config since would never have picked it up). Not re-investigated further this round.
- **BROKEN — Shift+R does not stop recording.** Reviewed the existing `Process.signal(2)` implementation (looks correct, matches an established prior fix) but could not reproduce or narrow further without hardware; user confirms recording does not stop at all.
- **BROKEN, awaiting user verification — openrazer-daemon.service fails to start** (`SystemExit: 0` from `daemonize.py`, python 3.14) on real hardware, blocking Chroma (76/77) and very likely the user's own `python-openrazer` import failure (75) too. This is a real daemon/environment problem, not something in any of these repos — the untruncated CRITICAL log line was requested but not yet provided.

None of the above were guessed at further per explicit instruction ("do not attempt further fixes or research") — recorded as broken, to be revisited with more diagnostic data or a real design decision (spotlight's own interaction model) rather than another blind attempt.

**ROUND 4 — openrazer group membership and spotlight, both closed with real evidence, not guessed.**

- **openrazer-daemon fails to start — root cause confirmed, fixed the only way this agent can.** The user pasted the untruncated CRITICAL log: `User is not a member of the openrazer group. Please run the command 'sudo gpasswd -a $USER openrazer' and then reboot!`. Not an environment/compatibility problem after all (round 3's own working guess) — a plain missing supplementary group. New `profiles/razer-hw/manual.txt` (`sudo gpasswd -a $USER openrazer`, plus the comment explaining a reboot or fresh login is still needed afterward): the same "declare it, never apply it" contract every other manual step in this repo already uses for an `/etc`-adjacent, one-off, `sudo`-requiring account change (`base/manual.txt`'s `pacman-key --add`, `gaming/manual.txt`'s multilib `sed`). `bin/lib/common.sh`'s `phios_read_list` confirmed to parse the new file cleanly off-machine; `razer-hw` is declared for `razer` in `hosts/razer.txt`, so `phios-install`'s manual-steps report will surface it. Chroma (S-46) and the user's own `python-openrazer` enumeration (Q-F04/Q-F06) both depend on this and stay blocked until the user runs it and reboots.
- **Spotlight position — real root cause found, not guessed a third time.** `Spotlight/Spotlight.qml`'s `PanelWindow` left `exclusionMode` at its default (`Auto`), which Quickshell's own docs define a shrink-to-content behaviour for ONLY when exactly three anchors are connected; this window anchors all four, so it fell back to respecting other layers' exclusive zones, same as `Normal` would. `Bar/Bar.qml` reserves `bar.height` at the top whenever not auto-hidden, so the window's real on-screen top-left sat `bar.height` below `screen.y` while the cursor math still subtracted bare `screen.y` — every local Y came out too large by exactly the bar's height, drawing the vignette too far down. Matches the reported symptom exactly, and explains why neither round 1 (scale conversion) nor round 2 (revert) touched it: both only ever changed the cursor sample, never the window's own placement. Fixed with `exclusionMode: ExclusionMode.Ignore` (Quickshell docs, `Quickshell/ExclusionMode`: "Ignore exclusion zones of other shell layers"), confirmed via the real Quickshell documentation before writing, not assumed. **The pre-existing `exclusiveZone: 0` line was removed, not kept alongside it**: the same docs state that assigning `exclusiveZone` sets `exclusionMode` back to `Normal` as a side effect, confirmed before committing — keeping both would have silently undone this fix and produced an identical-looking "still slightly down" with no way to tell the fix never took effect. **Still unverified end to end — no compositor here.**
- **Spotlight interaction model — the user's own decision, not guessed.** Double-click-and-hold Super, replacing SUPER+G hold. `hyprland.lua` now binds the bare `SUPER_L` keysym itself (press and release, no combo) instead of `SUPER + G`/bare-`g` — mirrors the bare-key-release shape this file's own Alt+Tab binds (`ALT_L`/`ALT_R`) and the prior spotlight round already established, to avoid the same "released before the other key" class of bug. The double-click timing is new state in `Services/Spotlight.qml` (`press()`/`release()`, 400ms threshold, owns `_lastRelease`/`_armed`), not in `hyprland.lua`, keeping that file a thin dispatcher as it is everywhere else. **`non_consuming = true` added to both binds as a safety measure**: no existing bind in this file puts a PRESS bind on a bare modifier keysym at the top level (ALT_L/ALT_R are release-only and scoped to the alttab submap), so whether an unflagged press bind would suppress the SUPER modmask for every `SUPER + <key>` combo below (Return, B, E, Q, arrows, Space/launcher, N, S, L, Tab, Shift+slash, workspaces) could not be confirmed from here — the flag costs nothing if the fear is unfounded, and the alternative (a dead primary modifier on the user's own machine, discovered after a handoff) is not recoverable from here. **VERIFY below checks the SUPER + <key> binds before the spotlight itself, on purpose.** **Known, flagged, not fixed limitation**: bare `SUPER_L` fires on every plain Super press/release, including the start/end of ordinary `SUPER + <key>` combos — two such combos used back to back within 400ms could be misread as the qualifying double click. Guarding against that would require every existing `SUPER + X` bind in the file to signal "a combo happened", out of scope for this fix. `Settings/sections/Theme.qml`'s toggle-row label updated to match. **Unverified on real hardware.**
- **Spotlight cursor marker**: already removed, round 3 (`phi-shell` b69eaa1) — restated here only because the user's own message repeated the ask; no further change needed.

**ROUND 5 — openrazer VERIFIED by the user. Spotlight gesture CONFIRMED BROKEN, real cause still not established; position CONFIRMED FIXED.**

- **openrazer-daemon: user confirms it now works correctly after `sudo gpasswd -a $USER openrazer` and a reboot.** Round 4's manual-step fix closes here for that item specifically. Chroma end-to-end (S-46) and the `python-openrazer` enumeration (Q-F04/Q-F06) can now proceed, not yet done.
- **Spotlight position: user confirms it is now correctly centred.** Round 4's `exclusionMode: ExclusionMode.Ignore` fix closes here.
- **Spotlight gesture: confirmed broken on real hardware, not fixed this round.** The user tried the round-4 double-click-and-hold and nothing happened; they turned it on via the Settings panel toggle instead (to test the position fix, which is how they confirmed it), and then the keybind also could not turn it back off, leaving the overlay stuck on — recorded here as a real, self-inflicted design gap in `Services/Spotlight.qml`'s own state machine, not only a Hyprland problem: `release()` only called `hide()` when `_armed` was true, so a `shown` set by the settings toggle (which bypasses the tap-count state entirely) could only ever be cleared by a full, *subsequent* qualifying tap-and-release cycle — worth understanding even though it does not fully explain what the user saw, since a genuine qualifying cycle should still have reached `hide()` regardless of how `shown` became true. **Immediate unstick given to the user**: `qs -p ~/.config/quickshell/phi ipc call spotlight toggle` — calls the low-level `toggle()` directly, bypassing the broken gesture path entirely. Real research this round, not another guess: fetched the actual hyprland-wiki Lua-binding docs (`content/configuring/core/binds/_index.md`, `flags.md`) and Hyprland's own `src/keybinds/Key.cpp` and test fixtures (`tests/keybinds/Bind.cpp`) from `github.com/hyprwm/Hyprland` directly, not community `hyprland.conf` forum posts (which use a different, older config format's `bindr=MOD,MOD_L,...` idiom that does not apply to this repo's Lua `hl.bind()` config) — confirmed real: sided-modifier-only binds are a genuine, tested engine feature (`CKey`'s `SSidedModifierPattern`), keysym name matching is case-insensitive (`XKB_KEYSYM_CASE_INSENSITIVE` in `Key.cpp`), and `flags.md` carries a dedicated worked example for exactly this shape: `hl.bind("SUPER_L", hl.dsp.exec_cmd("pkill wofi || wofi"))`, **no flags at all**. Round 4's press bind differed from that documented example by exactly one thing — `non_consuming = true`, added last round from an unconfirmed worry about combo breakage — removed from both the press and release binds this round as the one concrete, checkable difference, **not asserted as the actual fix**: this agent has now been wrong twice about spotlight's real cause (round 2's marker, round 4's untested `non_consuming` worry) and is not making a third unverified claim. The real discriminator, asked of the user rather than guessed: whether Hyprland's own bind registry carries this bind at all (`hyprctl binds -j` filtered for `SUPER_L`, plus `hyprctl version`, both in this round's `VERIFY`) — that result decides whether the next round is a syntax/version fix or something else entirely, instead of a sixth blind attempt. Also changed, by the user's own explicit request and independent of whether the bind fires yet: the required tap count generalised from a hardcoded double-click to `Services/Spotlight.qml`'s `requiredTaps` property, set to 3 (triple-click) — a one-line change if it needs to move again. **Not confirmed whether "restarted hyprland" meant a full session restart or `hyprctl reload`** — asked the user directly this round rather than assumed, since it changes which stale-config explanations are even possible.

**ROUND 6 — real root cause found: this is a Hyprland compositor bug, not fixable from any config in this repo. No code changed this round; asked the user to choose a substitute, per this repo's own "stop and ask" rule rather than a sixth blind rebind.**

- **`hyprctl binds -j` (user-provided) confirms both binds ARE registered correctly**: `key: 'SUPER_L'`, `modmask: 0`, one with `release: False` (press) carrying the real description, one with `release: True`, both `dispatcher: '__lua'` — exactly the shape round 5's Lua wrote. This rules out every "wrong syntax" / "wrong Hyprland version for this feature" theory from round 5: the compositor accepted and stored the binds exactly as intended.
- **The user's own comparison is the key datum: "it also used to work with super+G. It stopped working since it was set as double tap and release."** SUPER+G (rounds 1–3) is a bind on a REAL key (G) with SUPER as an ordinary held modifier — both its press and its release are ordinary, non-modifier key events, reliably delivered. The regression traces to the one thing that changed: binding press AND release on the bare modifier keysym itself (`SUPER_L`), not to anything in this repo's Lua or in phi-shell's IPC/state handling, both of which are the same mechanism that already worked for SUPER+G.
- **Root cause, found by searching Hyprland's own issue tracker rather than guessing a sixth variant**: `github.com/hyprwm/Hyprland` issue #6946, "Bind super alone on release no longer works". Opened 2024, closed 2024-07-25 — but its most recent comment, dated 2026-08-17, reports it **still reproduces on Hyprland 0.56.2**, with an actual diagnostic: a logging build across 11 candidate keycodes recorded 7 presses and **zero releases**, across every binding variant tried (plain keysym, `mods=SUPER key=SUPER_L`, raw keycode). The reporter's own conclusion: "this does look like a compositor-level dispatch gap for modifier-key release events specifically, not something fixable from userspace config." This matches this session's own evidence exactly: press plausibly fires (consistent with the JSON showing the bind registered and the described one being the press bind), release never does — which is also why `Services/Spotlight.qml`'s tap counter, which can only advance a streak once `_lastRelease` is set by a real `release()` call, never armed at all: with no release ever delivered, `_lastRelease` stays `0` forever, `continuing` in `press()` is always `false`, and `show()` is never reached by the gesture path. This is a complete, sourced explanation for both symptoms reported since round 4 — the double-tap never showing anything, and the settings-toggled overlay never being dismissable by the keybind — with nothing left unexplained.
- **Not fixable from any file in this repository.** Every syntax variant this bug report's commenters tried (including the exact ones round 4 and round 5 used) fails the same way; this is inside the compositor's input dispatch, upstream of `hyprland.lua`, `hl.bind()`, and phi-shell entirely.
- **Asked the user to choose a substitute** rather than guess one: revert to plain hold-on-SUPER+G (simplest, already proven reliable, drops the tap-count entirely), keep the tap-count novelty but move it onto SUPER+G (multi-tap-and-hold on a real key instead of bare Super, since G's own press/release are both reliable), or something else. **User chose plain hold-on-SUPER+G**, dropping the tap-count idea entirely rather than moving it onto a real key. `hyprland.lua`'s bare-`SUPER_L` press/release pair is reverted to the exact round-2/3 shape (`mainMod + G` press, bare-`g` release with `non_consuming`), and `Services/Spotlight.qml`'s tap-count state machine (`requiredTaps`/`tapWindowMs`/`_lastRelease`/`_completedTaps`/`_armed`, `press()`/`release()`) is deleted outright rather than left unused — `shell.qml`'s `IpcHandler` calls `show()`/`hide()` directly again, and `Settings/sections/Theme.qml`'s label reverted to match. This closes the interaction-model question for good: the underlying platform bug rules out any bare-modifier-alone gesture, not just this session's two attempts at one.

**ROUND 7 — Chroma and night shift VERIFIED. Spotlight gesture put ON HOLD, not verified, by the user's own explicit call. Screenshot offset and Shift+R reconfirmed still broken. No code touched this round, on explicit instruction ("do not proceed further").**

- **Spotlight gesture: ON HOLD, not verified.** The user confirms round 6's SUPER+G hold works correctly (shows, hides, does not stick) — but marks this item on hold rather than closed, because the originally requested behaviour (a bare-Super multi-tap gesture) was never actually reached; the working fallback is accepted as a stopgap, not treated as satisfying the original ask. Revisit only if `hyprwm/Hyprland#6946` is ever fixed upstream (last confirmed still reproducing 2026-08-17, round 6) — not something to re-attempt from this repo alone before then.
- **Chroma (S-46): user confirms tested and verified working on real hardware**, now that `openrazer-daemon` starts correctly (round 4's group-membership fix). The card's remaining pieces (power-key colour from battery, critical-battery pulse, Super-held key-availability illumination — all gated on `Q-F06`'s per-key-addressability answer, which still needs the user's own `python-openrazer` enumeration) stay open; only the static-colour toggle and `blink()` are confirmed live.
- **Night shift / True Tone: user confirms verified working**, closing the item recorded BROKEN at round 3 ("hyprsunset socket can't be found").
- **Screenshot area-selection offset: reconfirmed still broken.** No new detail given, no action taken this round.
- **Shift+R stop-recording: reconfirmed still broken.** No new detail given, no action taken this round.
- Per explicit instruction, nothing else was investigated or changed this round — this is a status-recording commit only. |

## M5 — Identity and advanced styling

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-50 | Final palette, derived in OKLCH | awaiting-verification | dotfiles (this commit) | 2026-09-09 | Real OKLCH derivation, not a hand-picked adjustment: every changed value is a fixed OKLCH hue/chroma anchor with lightness found by bisection against `phi`'s own WCAG contrast formula (`phi/internal/tokens/color.go`), gamut-checked so no solve ever silently clips a hue. `phi theme check` (already built, M1) now reports **zero violations on both variants** — built and run locally against this checkout to confirm, not asserted (`PHI_DOTFILES=<checkout> phi theme check`). Three real fixes: (1) **dark fg-2's 4.00:1 failure, closed** — re-solved the fg-0..fg-3 ramp at fg-2's own hue (H=82.39°) for a real 4.6:1 target instead of forcing fg-2 through fg-3's old anchor; fg-1/fg-3 fall out as the remaining two points of a still-uniform 4-point ramp. Light's fg-2 (already passing at 4.58:1) re-solved the same way, same 4.6:1 target, for one documented algorithm instead of an ad hoc affine map on one side only. (2) **Tier 3 syntax given real derived values**, not literal aliases of Tier 1/2 — same hue per role, lightness solved for a shared syntax-legibility target (5.6:1 dark) distinct from each role's own individual contrast. **Caught and fixed before commit**: the first light-mode pass used the same 4.9:1 target as Tier 2's own light re-solve, which at identical hue/chroma converges on the identical lightness — warn/error/info/type/number/function would have collided pixel-for-pixel with their Tier 2 counterpart. Light syntax target moved to 4.7:1 (still real margin over the 4.5:1 floor) once this was caught by comparing the computed hex values, not asserted correct. (3) **ANSI 16's two named defects, closed**: cyan (slot 6/14) had no hue of its own (collided with info/blue) — given a genuine new anchor, H=195°, used nowhere else; bright variants (9-14) were byte-identical to normal — resolved on the same hue at higher chroma (~×1.35, gamut-checked) and a higher contrast target, so bright is visibly more vivid and more prominent, not relabelled. Tier 1 (accent) and Tier 2 (error/warn/success/info) dark values are unchanged — already carried from the real running machines and already OKLCH-consistent hue/chroma-constant across variants on inspection (verified by measuring, not assumed); light Tier 2 nudged from S-02's ~4.78-4.83 near-floor values to a uniform 4.9:1 for a documented margin, same hue/chroma. Tier 0 bg-* untouched (already uniform, no contrast requirement). `design/README.md` updated to match. **`Q-N01` (font-mono) closed this session, ahead of S-51**: Source Code Pro, per the user's own choice between it and the master plan's Iosevka proposal — third member of the Adobe superfamily already used for font-reading/font-ui, over Iosevka's narrower cell. `tokens.common.sh` itself is untouched here (S-51's own scope: the package, the fontconfig chain, kitty `symbol_map`, removing the patched font) — recorded here only because the decision was made in this session before S-51 started. **Not verified**: the "user prefers it to the current palette" half of this step's own `DONE WHEN` — held for the end of session per this round's own instruction, same as every other subjective/on-hardware check below. |
| S-51 | Typography and the patched-font correction | awaiting-verification | dotfiles (this commit) | 2026-09-09 | `Q-N01` closed this session (recorded at S-50, ahead of this step): **Source Code Pro**, the style plan's own original value, over the master plan's Iosevka proposal. `design/tokens.common.sh`: `PHI_FONT_MONO='Source Code Pro'`; `PHI_FONT_SYMBOL` changed from `'Symbols Nerd Font'` to `'Symbols Nerd Font Mono'` — the Arch package page (`ttf-nerd-fonts-symbols`, fetched this session) confirmed a real `ttf-nerd-fonts-symbols-mono` split exists, and mono metrics are the correct choice for a terminal grid (a proportional icon glyph would misalign the column after it). `profiles/base/packages.txt`: added `adobe-source-code-pro-fonts`, `adobe-source-serif-fonts`, `adobe-source-sans-fonts`, `ttf-nerd-fonts-symbols-mono`, `noto-fonts` (all §15.2, all M5, none newly-approved). **`ttf-iosevkatermslab-nerd` (the patched font, ADR 054 violation) is NOT removed** — flagged pending in a comment on its own packages.txt line, per this step's own AGENT instruction ("remove ONLY after the yazi/btop check passes") and §6.4's explicit ordering; removal is in the end-of-session USER block, gated on that check. Two new templates, both class-registered in `design/adapters.txt`: `.config/kitty/fonts.conf.tmpl` (kitty `font_family` + thirteen `symbol_map` directives covering every Nerd Fonts v3.5.0 glyph set — ranges fetched and quoted from the real upstream wiki table this session, not guessed from memory, since a wrong range is indistinguishable from no fix at all) and `.config/fontconfig/fonts.conf.tmpl` (a real per-user fontconfig `<alias>` file, three roles × three-step chain per §6.4: role font -> font-symbol -> a role-matched Noto family — Noto Sans Mono for monospace, so a proportional fallback glyph never breaks the terminal grid). No CJK anywhere: closed by omission, since no `noto-fonts-cjk`-family package is declared in this repo at all, not by any filtering logic in the generated file. `kitty.conf` (home file, hand-authored, no literal font name) gained one `include fonts.conf` line. Smoke-tested off-machine: `phi theme render` on both new templates against the real tokens produces no leftover `${...}`, resolves to `Source Code Pro`/`Symbols Nerd Font Mono`/`Source Sans 3`/`Source Serif 4` correctly; `phi theme list` and `phi theme set dark --dry-run` both pick up the two new rows cleanly (kitty's stays `[unknown]`-reload/class B like its sibling theme.conf.tmpl; fontconfig is class C, restart-required, `-` reload — no app here watches `~/.config/fontconfig/` live and a fresh font file may need `fc-cache` before discovery regardless, flagged rather than assumed). **Not verified, all held to the end of session**: no real `fc-list` has ever confirmed these family strings (font-reading/font-ui were already flagged this way since S-02; font-mono/font-symbol are new to that same flag); Φ (U+03A6) rendering in font-mono/font-reading/font-ui; the yazi/btop glyph-coverage check that gates removing the patched font. |
| S-52 | Motion implementation | awaiting-verification | phi-shell 5476d84 · dotfiles (this commit) | 2026-09-09 | **Audit first, then fix**: read every `.qml` file in `phi-shell` for `Animation`/`Behavior`/`Transition`/`Easing` usage before changing anything, since most of this shell was already built under phi-shell's own standing CLAUDE.md rule ("Motion categories... are binding") — assuming a rewrite was needed without checking would have duplicated work already done at S-20/S-21/S-23/S-30 etc. Findings: **Category B (state transitions) is already comprehensive** — every `Widgets/*` component and every panel/drawer/overlay (Settings, Launcher, Tooltip, Spotlight, Osd, Sidebar, AltTab, Overview, Cheatsheet, Bar's own auto-hide slide, Toast's show/hide) already drives its opacity/colour through `Config.Appearance.motionBDuration`/`motionBEasingType`, and every easing curve in the whole repo is `Easing.Linear` or `Easing.OutQuad` — **no organic easing exists anywhere**, confirmed by grep, not assumed. **Category A** was already correctly built at S-23 (`Bar/modules/PhiAgent.qml`'s breathing opacity on `motionAPeriod`/`motionAEasing`) — nothing to fix. **Category D**: every ambient bar module (Battery, Bluetooth, Clock, Gpu, Network, NightMode, Timer, Volume, Wifi, Workspaces) has zero animation, confirmed by the same grep turning up nothing for any of them — compliant by omission, not by explicit suppression. **No generic letter-roll exists anywhere** to remove (grepped `letterRoll`/`letter-roll`/`scramble`/`typewriter` across every `.qml` file; the only hits were `Config/Appearance.qml`'s own token-documentation comments) — §6.5's "removed, must not reappear" instruction is already satisfied, nothing to do. **One real violation found and fixed**: `Notifications/Toast.qml`'s marquee had a bare `PauseAnimation { duration: 800 }` (×2) — a literal not sourced from `design/tokens.*.sh` (I-05). Reused `motionAPeriod` for the dwell instead of inventing a second constant, since the same period already governs how long the loop takes to cross the text. **Category C, genuinely new work**: neither of the two admitted effects (character-by-character typing, random-letters scramble) existed anywhere before this step. Built `Widgets/ScrambleText.qml` and `Widgets/TypingText.qml`, both sharing `motionCTypeStep` as their frame interval (not a third invented constant) and `motionCScramble` as the scramble's total duration; both are careful to never declare a QML binding on the inner `StyledText`'s `text` property since they reassign it imperatively every frame — the exact binding-drop bug `Widgets/Pill.qml`'s own S-40 note already found once for `checked`, avoided here by never declaring the binding in the first place rather than fixing it after the fact. **Wired ScrambleText into `Lock/Lock.qml`'s clock**: plays once when the lock surface is created (a rare event — "sblocco" is one of §6.5's own named C contexts) and never again on the per-second tick that drives the same text afterward, which is what keeps this compliant with `DONE WHEN`'s "no category-C effect fires on a frequent event" — verified by construction (`onFinalTextChanged` only does a plain reassignment while idle, `play()` is the only thing that starts the animation and nothing calls it from the tick handler). **The PamContext.completed handler, the one thing this file's own header calls genuinely security-critical, is untouched** — the new effect lives entirely in the Column below it. **TypingText has no consumer yet, on purpose**: nothing in this shell today is a genuine "first run" or "rare confirmation text" surface (boot is Plymouth's own script language at S-53, not QML), and inventing one just to give the widget a caller would be scope this step did not ask for — same precedent as `PhiAgent.processing`/`Chroma.blink()`, built once, wired by whichever step has a real reason to. **kitty `cursor_trail`** (§6.5's own named example for Category A): new `profiles/desktop/home/.config/kitty/motion.conf` (a plain home file, not a `design/adapters.txt` template — it has no token to substitute), `cursor_trail 20` / `cursor_trail_decay 0.1 0.2`. **Deliberately NOT derived from `PHI_MOTION_A_PERIOD`**, documented as a real, considered exception rather than an oversight: kitty's own real docs (`sw.kovidgoyal.net/kitty/conf/`, fetched and quoted this session) define `cursor_trail` as milliseconds of stillness before a trail engages (a debounce) and `cursor_trail_decay` as a fade-range in seconds — neither is "how long an animation runs", and forcing a 1600ms loop-period token through either unit would be a coincidence, not a derivation; the template pipeline (`phi/internal/theme/render.go`) is also plain string substitution with no arithmetic step, so there is no mechanism to convert units even if the semantics lined up. `kitty.conf` gained `include motion.conf`. **Real gap, held, not guessed**: Hyprland's own window/workspace compositor animations (`hyprland.lua`) are still entirely unconfigured, using whatever the compositor's built-in defaults are — §6.5's own table names "windows... workspace" under Category B ("quasi istantaneo, nessun easing organico"), and this is genuinely unclosed. Not fixed this round because it has no safe path forward without a guess: `hyprland.lua` is a plain home file, not a token-rendered template, and Hyprland's animation duration units cannot be derived from `motionBDuration` without either (a) a new arithmetic/unit-stripping capability in the template pipeline that does not exist yet, or (b) a hand-picked literal duration this agent cannot visually verify against a real compositor. Listed in the end-of-session question list rather than guessed at. |
| S-53 | Plymouth boot theme and Φ identity assets | awaiting-verification | dotfiles (this commit) | 2026-09-09 | `Q-N04` closed this session: **systemd-boot left at default**, the user's own choice over theming it — matches the style plan's own already-stated reasoning (§11: no token/font stack exists before the OS loads, the screen is only visible if boot is interrupted, and the Plymouth splash already carries boot identity). New `design/brand/` (README + `phi-mono.svg`/`phi-accent.svg`/`phi-ascii.txt`): the vector Φ, hand-authored (no SVG export tool available off-machine) as a stroked ellipse plus a bar — the real Greek Phi letterform, not a decorative stand-in — at one shared geometry (viewBox 0-256, ring outer radius 87.04×76.8, stroke 25.6, bar half-width 19.2). Colours are **literal, not tokens, and documented as a real divergence**: neither an SVG nor Plymouth (which runs before `phi` exists, pre-userspace) can be driven by this repo's plain-string template substitution — `design/brand/README.md` states this plainly rather than silently breaking I-05's "no literal colour" rule without saying so. **Found and fixed before commit, a real bug, not a nitpick**: the step card's own words place the Plymouth theme at `profiles/desktop/system/plymouth/phi/` — literally, and the same text appears in both the live master plan and its own offline mirror inside this repo (`docs/`, itself found stale by one row this session, not resynced — separate, pre-existing drift, not touched). That path does not round-trip through `bin/lib/system.sh`'s own mechanism, which mirrors the **real absolute path** a file belongs at (confirmed by running `phios-install --host razer --system-diff` before and after: the wrong path reported a bogus `/plymouth/phi/phi.plymouth` destination, which would never reach a real Plymouth install directory). Moved to `profiles/desktop/system/usr/share/plymouth/themes/phi/`, the real install path, and re-ran `--system-diff` to confirm it now resolves correctly — same class of self-correction as S-46's own "the step card's own premise was wrong" note, caught by testing the tool rather than trusting the card's shorthand. **Plymouth theme itself** (`phi.plymouth` + `phi.script` + `phi.png`, a 512×512 raster of the identical SVG formula, rendered off-machine by a small supersampling script not committed to any repo — no PIL/cairosvg/imagemagick/inkscape available here, and installing an imaging library system-wide on this dev machine risked exactly the "touch /usr on this machine" this agent must not do, so the PNG encoder was hand-rolled from the Python standard library's own `zlib`, rendered, and visually inspected via the Read tool before use, not shipped blind). Script syntax verified against a **real, live** reference this session — `github.com/jtyr/plymouth-theme-arch-breeze`, fetched with `curl` (direct network access confirmed working in this sandbox; `WebFetch`'s own summarizing model would not return byte-exact quotes when asked, so raw `curl` was used instead once that became clear) rather than reconstructed from memory: `Image`/`ImageNew`, `Sprite`/`SpriteNew`, `SetPosition`/`SetOpacity`, `Window.Get*`/`SetBackground*Color`, `Image.Text`, and all five `Plymouth.Set*Function` callback names are confirmed real. **Not independently confirmed**: the password-bullet string-concatenation loop (`dots = dots + "*"` in a for-loop) — ordinary-looking syntax for a C-like scripting language, but not seen verbatim in the one reference fetched, flagged in the script's own header rather than asserted. Background/glyph colours are `PHI_BG_0`/`PHI_FG_0`, dark variant, computed to RGB float once (`0.102,0.098,0.094` / `0.839,0.820,0.788`). **Random-letters resolve on boot, offered not imposed** (style plan §5/§17's own words): implemented behind `ENABLE_SCRAMBLE = 0` at the top of the script — one line to flip, ships off. **TTY/login banner** (`profiles/base/system/etc/issue`): the **pure-ASCII** `phi-ascii.txt` variant, not the Unicode glyph — the raw Linux VT console font's Greek coverage is a real, unverified risk (Arch's common default console fonts are Latin-only), the same class of concern this project already flagged for `font-mono`'s own Greek coverage. Plain `agetty` escapes (`\n \s \r \m`, `\l`) add hostname/OS/kernel/arch and tty line; **no `\4`/`\6` (IP)**, consistent with ADR 067's "never an IP" even though this would be a live agetty substitution, never a literal in the repo. **SSH banner**: new `/etc/phi-banner` (literal Unicode Φ — an SSH client is a real terminal with this project's own S-51 font stack, so the TTY's font-coverage risk does not apply) plus a `sshd_config.d/10-phi-banner.conf` drop-in rather than an edit to `sshd_config` itself, since this repo has never tracked that file before this step and a drop-in cannot conflict with whatever the real machine's own config already has — flagged in the file's own comment that this depends on the real `sshd_config` still including `sshd_config.d/*.conf` by default, unconfirmed on real hardware. `plymouth` added to `profiles/desktop/packages.txt` (pre-approved §15.3, `desktop` profile only — confirmed against `hosts/*.txt` that `mini` does not carry `desktop`, matching §9.1's own "not mini: headless" exclusion). **`docs/phios-master-plan.md`** (outside any repository, per `CLAUDE.md`'s own description of it as read-only planning input the user maintains — not committed, no repo to commit it to): `Q-N01`/`Q-N04` marked closed and two ADR rows (123, 124) added to §19, matching the exact precedent already in the file for ADR 121/122 (Q-N02/Q-N03) — the established pattern for how a closed `Q-` marker gets recorded there. **Not built this round, a real open item, not guessed**: the "about phiOS" panel. Style plan §17 calls it "eventuale" (conditional/if built) but the step card's own AGENT bullet lists it as work to do; more importantly, no document says WHERE it should be triggered from — Settings already has its own closed nine-section list (§9.12) so it cannot be a tenth section, and no other existing surface (bar, sidebar tab) is an obvious home either. Building the panel without knowing its own trigger would mean guessing shell information architecture, not implementing a specified surface — held for the end-of-session question list instead. **Everything here is unverified end to end** — no compositor, no real boot, no real console or SSH client to confirm any glyph, colour, or script line actually renders as intended; the master plan's own DONE WHEN already names the rollback path if Plymouth fails (remove the hook, regenerate, reboot) and that is the safety net this step relies on rather than a guarantee it can make from here. |
| S-54 | Cursor theme, magnifier, remaining chrome | awaiting-verification | dotfiles (this commit) | 2026-09-09 | **Cursor theme, package approved this session, not guessed**: the user chose `xcursor-themes`' bundled `whiteglass` over producing a bespoke one, when this agent surfaced the choice explicitly (no package for this was in master plan §15 before now, and CLAUDE.md forbids adding one without asking). New `PHI_CURSOR_THEME`/`PHI_CURSOR_SIZE` tokens in `tokens.common.sh` (not variant-dependent, so common rather than dark/light). **One value, every consumer, not three settings**: `XCURSOR_THEME`/`XCURSOR_SIZE` are the standard freedesktop env vars every Wayland client already resolves on its own — Hyprland's own cursor, GTK, and Qt/`phi-shell` (Quickshell is a Qt app) all read the same two variables, which is what `DONE WHEN`'s "consistent across GTK, Qt and the shell" actually means, confirmed by NOT inventing a qt6ct-specific cursor key: its real upstream source (`github.com/trialuser02/qt6ct`, checked this session, no cursor-theme property anywhere in it) confirms Qt has none — a wrong guess here would have shipped a config key that silently does nothing. GTK also gets an explicit, redundant `gtk-cursor-theme-name`/`-size` in both `gtk-3.0`/`gtk-4.0` `settings.ini.tmpl` (a real, long-standing `GtkSettings` property) as a second path, in case a GTK app resolves its cursor before the env var is visible to it. **A real, pre-existing architecture gap closed to deliver this**: the two env vars needed a home, and `hyprland.lua` was a plain `home/` file with no token-substitution point — converted to `profiles/desktop/templates/.config/hypr/hyprland.lua.tmpl` (git-moved, not copied), registered in `design/adapters.txt` as **Class A with a real, working reload command, `hyprctl reload`** (Hyprland's own standard live-reload; master plan §6.7 already named "Hyprland" as one of Class A's own targets — this closes that gap, not just this step's own need). Everything else in the 450-line file is untouched, unrelated Lua. Every cross-reference to the old `home/.config/hypr/hyprland.lua` path was found and updated (`profiles/desktop/README.md`'s own S-25 Hyprland-Lua-API-compatibility note, `profiles/razer-hw/system/README.md`'s S-46 note) — `PROGRESS.md`'s own historical rows were deliberately left alone, since they describe where the file was AT THE TIME, not where it is now. **Magnifier**: `cursor:zoom_factor`, confirmed against the real Hyprland wiki source this session (`configuring/core/config-options.md`, fetched with `curl`: "The factor to zoom by around the cursor. Like a magnifying glass") — two new binds, `Super+Z`/`Super+SHIFT+Z`, set it to `2.5`/`1.0` via `hyprctl keyword` (a live compositor setting, not phi-shell IPC — nothing to keep in sync). Two fixed levels rather than a stateful toggle: reading the current factor back via `hyprctl getoption -j` inside the same `exec_cmd` string would need a JSON-parsing pipeline this agent cannot test from here, and two plain binds cannot desync the way a wrong toggle read could. **The "lens effect is a shader" half of this card is not attempted**: S-43's own spotlight work already found and verified that `decoration:screen_shader` does not expose cursor position to a shader (`hyprwm/Hyprland#1502`, closed upstream) — the identical limitation blocks a cursor-following lens the same way, cited rather than re-investigated from scratch. **`Q-N04` (systemd-boot) was already closed at S-53** — nothing left for this step there. **`Q-` housekeeping**: `docs/phios-master-plan.md` (outside any repository, the user's own maintained planning input, not committed) — `Q-N01`/`Q-N04` marked closed with ADR rows 123/124 in §19, matching the ADR 121/122 precedent already in that file; while there, also found and corrected the §15.2 packages table's own stale `ttf-iosevka (o adobe-source-code-pro-fonts)` row (Q-N01 is closed, it should not still read as an open choice) and confirmed — but did not fix — that the offline docs mirror inside this repo (`phios-dotfiles/docs/`, added at S-00 "per phios-master-plan.md §0.2") is stale by at least one row relative to the live outer copy; a pre-existing drift, not touched, flagged for the end-of-session list rather than resynced as a side effect of this step. **Not built, a real open item from S-53, still not guessed**: the "about phiOS" panel — no document names its trigger surface, and Settings' own nine sections are closed. Smoke-tested off-machine: `phi theme render` on the new `hyprland.lua.tmpl` and both `settings.ini.tmpl` files resolves cleanly (no leftover `${...}`, `whiteglass`/`24` substitute correctly); `phi theme set dark --dry-run` picks up the new template as Class A with a real (not `[unknown]`) reload command — the first row in this whole file's Class A group that has one; `phi theme check` still reports zero violations on both variants (unaffected — this step touches no colour token); `bin/phios-install --host razer --system-diff` re-run before and after the Plymouth path fix (S-53) and again after this step's own changes, both times clean. **Unverified on real hardware, as ever**: no compositor here to confirm the cursor actually renders `whiteglass` consistently, or that either magnifier bind does anything at all.

**CORRECTION, found by the advisor tool after this step's own commit, before handoff — a real mistake, not a nitpick**: "whiteglass" does NOT match architettura §8.2.3's "sottile, nero bordato di bianco" (thin, black, white-outlined). Checked properly this time (`WebSearch`, not assumed): `xcursor-themes` bundles `redglass` (red) and `whiteglass` (**white**), neither black. The original approval question put to the user was built on this wrong premise. Re-asked, corrected: **the user chose to accept `whiteglass` anyway**, a real, explicit, acknowledged deviation from the written spec rather than a fix — no code change follows from this, `PHI_CURSOR_THEME='whiteglass'` and the `xcursor-themes` package stand as committed. Recorded here, and as ADR 125 in `docs/phios-master-plan.md`, so the deviation is traceable to a real decision rather than looking like an unnoticed error. |

## M6 — Services and tools

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-60 | Synchronised cloud | todo | | | |
| S-61 | Photo library | todo | | | |
| S-62 | Jellyfin and the video libraries | todo | | | |
| S-63 | Indexers and download client | todo | | | |
| S-64 | Media and music pipelines | todo | | | |
| S-65 | ClamAV | todo | | | |
| S-66 | LanguageTool | todo | | | |
| S-67 | Office, study tools, secrets | todo | | | |
| S-68 | Clipboard continuity between zotac and razer | todo | | | |

## M7 — AI agent

All seven steps (S-70…S-76) are written, committed and pushed. Hardware
verification is **in progress on `razer`** (started 2026-09-09). Gate G7
(all of V-01…V-18 recorded) is open; no row is `verified`.

**Verification ledger** (razer):

| V | what | result |
|---|---|---|
| V-01 | secrets unreadable inside a1 + a2 | **pass** |
| V-02 | env is the allowlist only, no agent socket (a1 + a2) | **pass** |
| V-04 | bwrap gone → launcher refuses, exit 1, no process (*script half*) | **pass** |
| V-05 | `memoria.md` read-only inside the containment | **pass** |
| V-06 | writable = `output/` + `proposte/` only | **pass** |
| V-08 | provider key not reachable from inside the containment | **pass** |
| V-04 | *unit half* — `phi-agent-a1.service` fails when bwrap is gone | pending (unit not enabled yet) |
| V-09 | streaming through the broker is incremental | **blocked** — Zen needs billing (401); moving to OpenRouter, awaiting the user's key |
| V-03 | A2 egress denied with/without proxy vars | pending S-72 services |
| V-07, V-10 | opencode XDG-read denial; prompt carries personality+project | pending a working provider |
| V-11…V-18 | shell panel, inline `ask`, remote surface | pending a working provider |

Also verified on razer: the assembled `bwrap` argv for both instances
(built from empty, no bind near the real `$HOME`, a2 has `--unshare-net` +
the socket bridge + both socat forwarders, a1 does not); `phi agent init` /
`project new` / `project use` against the real data tree; `phi agent
broker --check`; the broker forwards a request to the configured upstream
with the real key swapped in and meters it.

Two bugs found and fixed in verification round 1 (see the S-70 and S-71
rows): `~/.local/bin` was not on `PATH`; `env.example` had an unquoted
space in a sourced value. One structure fix: `opencode.json` →
`opencode.example.json` (copy pattern, like `broker.json`).

Provider: not yet working. `phios-agente.md` is provider-agnostic; the
user picked an OpenAI-compatible model. opencode Zen was tried and dropped
(requires billing). OpenRouter is next (`upstream=https://openrouter.ai/api`,
prepaid balance = the §6.3 cap, model tentatively `deepseek/deepseek-chat`).

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-70 | [BLOCKING] Containment and its proof | awaiting-verification | 6643d39 · verify-round-1 8097300 | 2026-09-09 | **Written entirely off-machine; the proof (V-01..V-04, phios-agente.md §15) is BLOCKING and has not run.** No write capability anywhere in M7 is real until the user pastes all four results. New `profiles/desktop/home/.local/bin/phi-agent-contain` (bash + coreutils + bubblewrap only): the single launch path for both opencode instances — the S-71/S-74 units and `phi agent ask` all `exec` through it, and there is no code path that starts an agent outside `bwrap` (§4.7, ADR 090). Containment is built FROM EMPTY (§4.1, ADR 087): a tmpfs root, `--proc`/`--dev`/`--tmpfs /tmp`/`--tmpfs /run`, then only the binds the two `mounts/*.paths` files name. Namespaces exactly as §4.6: `--unshare-pid --unshare-ipc --unshare-uts --unshare-cgroup --new-session --die-with-parent`, plus `--unshare-user-try` (not a perimeter decision — it is the mechanism that lets the other unshares be created unprivileged on a non-setuid bwrap, which is the Arch package's default; documented in the script). `--clearenv` then an explicit `--setenv` allowlist (HOME, PATH, TERM, LANG, the four XDG_*_HOME pointing at the container's own trees, XDG_RUNTIME_DIR, OPENCODE_CONFIG, NO_PROXY, PHI_AGENT_INSTANCE, and for A2 the GIT_AUTHOR/COMMITTER identity) — `SSH_AUTH_SOCK`, `DBUS_SESSION_BUS_ADDRESS`, `WAYLAND_DISPLAY`, and every `*_API_KEY`/`*_TOKEN` are excluded by construction, not by a filter (§4.6). Fail-closed preflight (V-04): the script exits non-zero, printing one line and running nothing, if `bwrap` is absent / non-executable / `--version` fails, or if `kernel.unprivileged_userns_clone`/`user.max_user_namespaces` is 0 on a non-setuid bwrap, or if any required (unsuffixed) mount source is missing — so a unit with this as `ExecStart` lands in `failed` with no engine process. `mounts/common.paths` = §4.2 base (the `/usr` tree + `/bin`,`/sbin`,`/lib`,`/lib64` symlinks, `resolv.conf`/`hosts`/`nsswitch.conf`, the CA bundle, `passwd`/`group`, `localtime`), all read-only. `mounts/a1.paths` = §4.3: A1's own XDG trees writable, `opencode.json` read-only (single resolved file, not the `.config` dir — it is a dotfiles symlink), the active project bound read-only first with `proposte/` and `output/` re-bound writable underneath (so `memoria.md`/`materiali/`/`archivio/` are read-only with no explicit rule — S-73 makes that explicit), the note archive read-only. `mounts/a2.paths` = §4.4: code root + A2 XDG trees + A2-only toolchain cache writable, nothing else; git identity via env, never a mounted `.gitconfig`; A2 also gets `--unshare-net` from the script, so it has NO network until S-72 adds the bridge — the safe direction, and V-03's "denied" holds trivially in the meantime. `mounts/never.paths` is not read by the launcher: it is the machine-readable V-01/V-02 checklist (SSH keys, GPG, password store, mail, browser profiles, `/var` `/srv` `/opt` `/boot` `/efi`, `/etc` beyond the whitelist, and the session sockets). Off-machine self-test: `bash -n` clean; argument assembly exercised with a stub `bwrap` (`--dry-run`) for both instances — the emitted argv matches §4.6 for namespaces, the allowlist contains no secret name, A2 carries `--unshare-net` and A1 does not, `ro`/`rw` ordering puts the writable project subdirs after the read-only project bind. `env.example` (paths + git identity only, explicitly no secret) and a `README.md` for `~/.config/phi-agent/`. `bubblewrap` + `opencode` added to `profiles/desktop/packages.txt` (master plan §15.3, both hosts carry `desktop`). **Cannot be verified here at all — no Linux host, no bubblewrap on the build machine.** V-01 (secrets unreadable from inside), V-02 (env free of secrets and the SSH-agent socket), V-03 (A2 egress denied with and without proxy vars), V-04 (containment made unavailable → startup fails) are all USER operations on `zotac` and `razer`; see the end-of-M7 verification list. Held: exact behaviour of `--unshare-cgroup` on the real kernels, whether opencode honours all four `XDG_*_HOME` for full instance separation, and the real note-archive path (left as `PHI_AGENT_NOTES_DIR`, default `~/notes`). **Verification round 1 (razer, 2 bugs, fixed same step — §16.1):** (1) `phi-agent-contain` was shipped in `~/.local/bin/` but nothing put that dir on `PATH`, so the bare name (used interactively and by `phi-code`) was not found; the systemd units were unaffected (they call `%h/.local/bin/...`). Fixed: `profiles/base/home/.zshenv` prepends `~/.local/bin` (`typeset -U`). (2) `env.example` shipped `GIT_AUTHOR_NAME=phi agent` — the file is *sourced*, so the unquoted space ran `agent` as a command and, under `set -e`, aborted the launcher with a bare "command not found". Fixed: quote it, reword the header comment to say the file is sourced and spaces need quoting, and `phi-agent-contain` now pre-scans the env file and refuses with one clear line naming the offending `KEY=a b`. After both fixes, **V-01, V-02, V-04 (script half) all passed on razer** (a1 + a2: every secret path "not found"; env is the allowlist only, no `SSH_AUTH_SOCK`/`*_TOKEN`; bwrap-gone → `containment unavailable`, exit 1, no process). Dry-run argv inspected for both instances: built from empty, no bind near real `$HOME`, a2 has `--unshare-net` + the socket bridge, a1 does not. Still pending: V-03 (needs S-72 services) and V-04 unit half (needs `phi-agent-a1.service`). Status stays `awaiting-verification` until Gate G7. |
| S-71 | Credential brokering | awaiting-verification | phi 4cdbbe5 · phi-packages 422e367 · dotfiles ccc660c · verify-round-1 8097300 bf3eb89 | 2026-09-09 | Depends on S-70 (itself unverified — the boundary this broker assumes has not been proven; see S-70). `phi agent broker` (new `phi agent` verb group, `internal/agent/`): a loopback `httputil.ReverseProxy` with `FlushInterval = -1` so every upstream chunk is flushed to opencode immediately — the buffer-free streaming §6.2 requires and V-09 tests. The client's own `authorization`/`x-api-key` headers are stripped in `Rewrite` and the real key attached from a file; `extra_headers` and a configurable auth header/value template keep it provider-agnostic (no planning doc names the provider, §2.1). Key resolution order: `PHI_AGENT_BROKER_KEY_FILE` (set by the unit from `LoadCredential`), then `$CREDENTIALS_DIRECTORY/provider-key`, then `<config>/phi-agent/<inst>/provider-key` for a hand-run broker — the key is a literal nowhere and in no repository (ADR 092). `listen` is validated to be a loopback address. Fail-closed: missing config or unreadable/empty key → the process exits non-zero and never binds, so the unit stays `failed` and, since opencode's only route to the provider is this port, no agent works (§6.2, consistent with §4.7). `ratelimit.go` is a fixed-window request cap (complement to the provider spending cap, §6.3/§6.4); `meter.go` appends one JSONL record per request (`time`, `instance`, `method`, `path`, `status`, `model`, `resp_bytes`, `dur_ms`) to `~/.local/state/phi-agent/<inst>/broker-meter.jsonl` — token-level accounting is **deferred** until a provider is chosen, because parsing the `usage` block out of a streaming response body is provider-specific and awkward; request/byte/model metering is real now. **Verified locally (not on hardware):** `internal/agent/broker_test.go` — a delayed-chunk mock upstream proves chunks arrive with their inter-chunk delay preserved (not bunched at the end); the client's placeholder auth is dropped and the real key reaches upstream; the rate limit returns 429 and does not forward; the meter writes a parseable record; `requireLoopback` rejects `0.0.0.0` and routable hosts. An end-to-end `curl` through a running `phi agent broker` against a local mock confirmed the same streaming timing and `--check`. Dotfiles: both `opencode.json` files gain a `provider.phi-broker` block (`@ai-sdk/openai-compatible`, `baseURL` → `127.0.0.1:8789`/`:8790`, dummy `apiKey`) and `model` — the model id and, for an Anthropic-native provider, `npm`/`baseURL`/auth header are the user's to set (two clearly-marked `REPLACE-WITH-YOUR-MODEL-ID` spots + README). `a1/broker.example.json` is the broker-config template. `phi-agent-broker@.service` (templated user unit): `ExecStartPre` runs `--check`, `LoadCredential` pulls the key from `%h/.config/phi-agent/%i/provider-key`, plus a §11 hardening block (resource caps, `ProtectSystem=strict`, `SystemCallFilter=@system-service`, etc. — **written from `systemd.exec(5)`, unrun**; loosen `MemoryDenyWriteExecute`/`SystemCallFilter` first if it fails to start). Declared in `services-user.txt`, never enabled. **USER, before enabling anything:** set a hard spending cap on the provider key and write the revocation procedure down off-machine (§6.3 — the cap limits damage, not probability). **USER verifications:** V-08 (no provider key anywhere inside the containment) and V-09 (streaming through the broker is incremental) — see the end-of-M7 list. Held: which provider, and therefore the real `upstream`/`auth_header`/model; whether opencode fetches the `@ai-sdk/*` npm package at runtime (would need npm on A2's whitelist / a first online run for A1). **M7 review pass (phi `1e3550e`):** `TestBrokerMeterRecords` was racy (~1 in 4 failing in isolation — the meter line is written by the handler goroutine after `ServeHTTP` returns, which the client seeing EOF does not order against); now polls for the line. Broker code unchanged. **Verification round 1 (razer):** `a{1,2}/opencode/opencode.json` shipped as repo symlinks carrying a `REPLACE-WITH-YOUR-MODEL-ID` placeholder — editing them to set the model id dirtied the dotfiles working tree and would fight `git pull`, the exact problem `broker.example.json` avoids. Renamed to `opencode.example.json` (git mv); the user copies to `opencode.json` (real file, theirs). `mounts/a{1,2}.paths` opencode.json bind changed `ro` → `ro?` so V-01..V-04 still run on a machine with zero provider setup (a bare `sh` needs no engine config). README + verify procedures updated. Broker/CLI unchanged. **Verified on razer:** the broker's own machinery — `--check` loads the key and prints the summary; a request to `127.0.0.1:8789` is forwarded to the configured `upstream` with the placeholder auth replaced by the real key (the meter recorded `POST /v1/chat/completions` with the upstream's status and `dur_ms`); **V-08 passed** — `find / -name provider-key` inside the containment returns nothing, no key-shaped env var, and `opencode.json` inside carries only the dummy `apiKey`. **V-09 NOT verified.** First provider tried was opencode Zen (`https://opencode.ai/zen`): the key lists models fine but `/v1/chat/completions` returns 401 — Zen requires billing to be enabled, which the user is not doing. Switching to **OpenRouter** (`upstream=https://openrouter.ai/api`, prepaid balance = the §6.3 cap, model tentatively `deepseek/deepseek-chat`); the raw-curl streaming test (V-09) and everything downstream is **on hold pending the user's OpenRouter key**. The broker correctly surfaced the upstream 401 rather than masking it, which is the intended behaviour, but incremental streaming through it is still unproven against a real provider. |
| S-72 | A2 network whitelist | awaiting-verification | phi 44f1042 · phi-packages 2544f69 · dotfiles 2892363 | 2026-09-09 | Depends on S-70 (unverified). The boundary is the namespace, not a variable (§5.1): `phi-agent-contain` already gives A2 `--unshare-net` (S-70); this step adds the only routes out. Two unix sockets in `~/.local/state/phi-agent/net/`, bind-mounted into the container as `/run/phi-agent/net/`: `broker-a2.sock` (the provider call, created by `phi-agent-broker@a2` which now listens on `unix:` instead of TCP) and `proxy.sock` (everything else, created by `phi-agent-net-bridge.service` = `socat UNIX-LISTEN … TCP:127.0.0.1:8888` → `tinyproxy`). Inside the namespace `phi-agent-contain` wraps the command with two `socat` forwarders turning those sockets into `127.0.0.1:8790` and `127.0.0.1:8118`, and sets `HTTP(S)_PROXY` to the latter with `NO_PROXY=127.0.0.1,localhost` so the broker call goes straight to its own forwarder and every other host goes through the proxy. **V-03 discriminator:** a process that unsets the proxy vars is left able to reach *only* the broker (which forwards solely to the configured provider) — never a free network, because the namespace has no route and the proxy socket is the sole external channel. `tinyproxy/tinyproxy.conf` (`FilterDefaultDeny Yes`, loopback listen, `Allow 127.0.0.1`; `__HOME__`/`__RUNTIME__` rendered by the unit's `ExecStartPre` sed since tinyproxy has no `~`/env expansion) + `tinyproxy/whitelist` — **ships with loopback allowed and every package registry commented out**; the user uncomments exactly what a project on the machine fetches from (§5.1 "si estende per aggiunta esplicita, mai per comodità"). The broker is deliberately NOT a whitelist line — A2 reaches it on its dedicated socket, not through tinyproxy. `socat` + `tinyproxy` added to `profiles/desktop/packages.txt` (§15.3). `phi-agent-proxy.service` + `phi-agent-net-bridge.service` declared in `services-user.txt`, never enabled; both only needed once A2 runs (S-76). **Deviation, flagged:** §5.1 describes one unix bridge to a proxy whose whitelist "includes the brokering service address"; here the broker gets its own dedicated socket instead of a tinyproxy allow-rule, because (a) opencode/undici bypasses the proxy for loopback destinations regardless of `NO_PROXY`, making a `127.0.0.1` allow-rule unreliable, and (b) separating "our trusted service" from "filtered external egress" is cleaner. Same end state: A2 reaches the provider only through the broker, and arbitrary hosts only through the filtered proxy. **Unverified end to end** — no Linux host, no bwrap, no socat chain exercised. `phi`'s unix-socket listen is unit-tested (`TestBrokerUnixListen`, `TestRequireLocalListen`). **USER verification:** V-03 (A2 egress to a non-whitelisted domain denied with AND without proxy vars) — see the end-of-M7 list. Held: whether opencode fetches `@ai-sdk/*` from npm at first run (would need `registry.npmjs.org` uncommented in the whitelist); the exact `socat` address syntax on the real machine; whether `tinyproxy` in `extra` is built with filtering support. |
| S-73 | Agent data model and phi MCP server | awaiting-verification | phi 0005f57 · phi-packages 85b4a97 · dotfiles 86cb48d | 2026-09-09 | Depends on S-71 (unverified). **phi side:** `internal/agent/model.go` = the §8.2 tree under A1's XDG data dir — `personalita/<name>.md` (user-owned prompts) and `projects/<name>/{materiali,archivio,proposte,output,progetto.md,memoria.md}`. `phi agent init` seeds `general.md` + `technical.md` (§8.3, embedded via `//go:embed`, never overwrites a user-edited file — test-covered). `phi agent project new|use|list|current`: `use` writes `~/.local/state/phi-agent/a1/active-project` and runs `systemctl --user restart phi-agent-a1.service` (marker write succeeds even if the restart fails — reported, not fatal; `--no-restart` skips it). Names validated to `^[a-z0-9][a-z0-9._-]{0,63}$` — `../evil`, `a/b`, `.hidden` rejected (test-covered). **memoria.md write path:** exactly one — `phi agent memory accept FILE` appends the **literal** proposal text (shown first by `memory show` as a `+`-prefixed diff against the current file, never a summary — §8.6) and deletes the proposal; `reject` just deletes. The agent never writes memory; the read-only mount (`mounts/a1.paths`, made explicit this step) enforces it, not the interface (§8.4, ADR 094). **phi MCP server** (`phi agent mcp`, `internal/agent/mcp.go`): newline-delimited JSON-RPC 2.0 on stdio, **exactly one** read-only no-argument tool `phi_context` (active project + `progetto.md` + `memoria.md` + personality list). `mcpTools` is the single growth point (§7.1). Handles `initialize`/`tools/list`/`tools/call`/`ping`/notifications; unknown method → -32601, bad JSON → -32700 (test-covered). Works inside the containment (fixed `/home/agent` paths) and outside it. **dotfiles side:** `phi-agent-contain` now reads the `active-project` marker as a fallback to `$PHI_AGENT_PROJECT`. `phi-agent-a1.service` = `phi-agent-contain a1 -- opencode serve --hostname 127.0.0.1 --port 4199`, `Requires=phi-agent-broker@a1`, no fallback ExecStart (so it stays `failed` if bwrap is gone — V-04). `a1/opencode/opencode.json` gains: `mcp.phi` (`command: ["phi","agent","mcp"]`), an `agent` block mapping `general`/`technical` to `{file:/home/agent/.local/share/personalita/*.md}` with `default_agent: general`, and `instructions: [progetto.md, memoria.md]` (memoria always in context, §8.5). Declared `phi-agent-a1.service` in `services-user.txt`, never enabled. **Verified locally:** `go test ./internal/agent/` (MCP handshake, seed idempotency, project lifecycle, name rejection, literal-append promotion); manual `phi agent init`/`project`/`memory`/`mcp` end-to-end against a temp HOME. **USER verifications:** V-05 (write to `memoria.md` from inside the containment → denied), V-06 (write outside `output/`+`proposte/` → denied), V-07 (agent reads its own XDG tree → denied), V-10 (prompt carries personality + project instructions). Held: whether opencode's `agent.*.prompt` `{file:...}` errors when the personality file is briefly absent; whether `instructions` tolerates a missing `progetto.md` when no project is selected. **M7 review pass (phi `1e3550e`, phi-shell `9dd04d2`):** `phi agent memory list` was emitting its decorated form unconditionally, and the shell panel filtered lines by shape (no colon, no space) — a proposal file the agent named with a colon or a space would silently never appear in the pending list, the one failure §8.6 cannot tolerate. `list` now prints one bare file name per line when redirected (the output contract), and the panel takes every non-empty line. `ProposalText`/`AcceptProposal`/`RejectProposal` now reject a `name` that is not a single path segment (was `filepath.Join`-ed straight from a CLI arg / the panel). New tests: `TestModelProposalNameTraversal`, `view.TestAgentMemoryList*`. `phi-packages` bumped to 0.12.1 (`7e23ef7`) tracking phi `1e3550e` — the panel and `phi agent memory list` changed together, so the packaged `phi` must be rebuilt at ≥ `1e3550e` before the S-75 checks. **Verification round 1 (razer):** **V-05 passed** — `echo >> /home/agent/project/memoria.md` from inside the containment fails "Read-only file system". **V-06 passed** — of `output/x proposte/x materiali/x <project root>/y notes/x personalita/x`, only `output/` and `proposte/` accept a write; the rest are denied. `phi agent init` / `project new` / `project use --no-restart` all worked against the real `~/.local/share/phi-agent/a1` tree. **V-07 and V-10 still unverified** — both need opencode actually running, which is blocked on the provider (see S-71). MCP `phi_context` not yet exercised on hardware. |
| S-74 | Inline command line and remote surface | awaiting-verification | phi 0f15f38 · phi-packages 7ccea1e · dotfiles b84cf27 | 2026-09-09 | Depends on S-73 (unverified). **Inline (J7, §10.2):** `phi agent ask [--personality NAME] PROMPT` — a thin client onto the already-running `phi-agent-a1.service` over opencode's loopback HTTP API (`GET /global/health`, `POST /session`, `POST /session/:id/message`, `DELETE /session/:id`). No cold start. The session is **deleted** after the answer (advisor's call over a client-side title filter: delete satisfies "excluded from the panel list AND from memory" with nothing for the panel to cooperate on — V-15), and deleted even on error/cancel via `defer`. Fails closed with a clear message if A1 is unreachable; never spawns an engine. Tests: create→use→delete happy path, delete-on-message-error, service-down message. **Remote surface (J6, §10.3, §5.3):** three user units, all declared/never-enabled. `phi-agent-a2.service` = local contained A2 on loopback, **no password, no inbound socket** (so V-02 against a default A2 is clean with nothing to explain — advisor's point). `phi-agent-a2-remote-engine.service` = `phi-agent-contain a2 --remote …`: the ONLY A2 launch that carries the password and opens an inbound unix socket — **starting it is how a session is "declared remote"**; `Conflicts=phi-agent-a2.service`. `phi-agent-a2-remote.service` = a host `socat` that binds **only** `${PHI_AGENT_REMOTE_ADDR}` (never `0.0.0.0`, V-17) and forwards to the container's inbound socket; `ExecStartPre` refuses to start if `PHI_AGENT_REMOTE_ADDR` is unset (S-05 `<uuid>` placeholder precedent — the address is machine data, ADR 067, not in the repo). Password: `LoadCredential` from `~/.config/phi-agent/a2/remote-password` (user, mode 600, out of repo); `phi-agent-contain --remote` **bind-mounts** the credential file and the wrapper reads it into `OPENCODE_SERVER_PASSWORD` *inside* the container — never a `--setenv` value, which would expose it in `bwrap`'s argv (verified in the dry-run: only `OPENCODE_SERVER_USERNAME` reaches setenv). `--remote` is A2-only and errors on `a1`. `env.example` gains a commented `PHI_AGENT_REMOTE_ADDR`. `a1/opencode/opencode.json` `external_directory` changed from bare `"deny"` to an object (allow `/home/agent/notes/**` and `.../personalita/**`, deny opencode's own XDG trees) so J3 and V-07 both hold whichever way opencode resolves the workspace root — advisor item 1, a fix to the S-73 file, made here. **Stray:** this step's `phi` commit also carries a one-line gofmt alignment fix in `internal/theme/set.go` (pre-existing dirt, not S-74 work). **Verified locally:** `go test ./internal/agent/` (ask), dry-run of `phi-agent-contain a2 --remote` (password bind not setenv; a1 rejected; missing-credential dies). **USER verifications:** V-15 (inline session absent from the panel list), V-17 (remote surface listening only on the overlay interface), V-18 (remote access with no credential → denied). Held: the whole inbound socat chain and opencode server-auth behaviour are unrun; whether opencode's session-delete also purges its title-generation/summary side effects; the real overlay address and whether `socat`'s `bind=` accepts it as written. |
| S-75 | Shell panel: connect the placeholder | awaiting-verification | phi-shell 7993796 · dotfiles a43753a | 2026-09-09 | Depends on S-74 (unverified) and S-31 (done). **Entirely QML — no compositor on the build machine, so none of it is visually verified; the card's own `USER` is "use it for a week".** `Services/Agent.qml` is the ONE client point (ADR 098): every call to `phi-agent-a1.service` (opencode's loopback HTTP API on `127.0.0.1:4199`) and every `phi agent` invocation goes through it; it holds no product logic that must survive an engine swap — that is in the §8.2 files. `AiChat.qml` rewritten from the S-31 placeholder to the real surface. **§10.1 items landed:** service-unavailable panel (`GET /global/health` poll), conversation list (`GET /session`, filtering `inline (ephemeral)`), new conversation (`POST /session`), transcript (`GET /session/:id/message`, re-read on each `/event` tick and on a 700ms poll while a turn runs — deliberately not trusting opencode's undocumented bus-event shape for the text itself), send (`POST /session/:id/prompt_async`), personality switch (cycle button → `agent` field), project switch (`phi agent project use` → `switching` loading state while the containment rebuilds, §4.3), **tool approval** (`/event` `permission.*` → card → `POST /session/:id/permissions/:id`), **non-blocking memory-proposal notice** showing the **literal** append-diff from `phi agent memory show` — never a summary (§8.6) — with accept/reject → `phi agent memory accept|reject`, output listing (`ls` of the active project's `output/`). `Bar/modules/PhiAgent.qml` `processing` now bound to `Services.Agent.processing` (Role B accent + category-A breathe while a turn runs — S-23's motion reasoning unchanged, only the trigger is real); `Settings/sections/AiAgent.qml` shows activation toggle (`systemctl --user start/stop`), connection, active project, pending-proposal count. New sibling files `Panels/tabs/Dots.qml` (category-A ellipsis, not the Φ mark — §6.6) and `Panels/tabs/MemoryNotice.qml`. The chat card instance is still the one `aiChat` row in `Panels/tabs.json` (ADR 100 — unchanged). **NOT landed, deferred:** per-conversation file attachment; copy-file-into-materials; the end-of-day review as its own surface (proposals show inline instead); summary-and-archive-on-close is a best-effort inline `sh` pipeline (`POST /session/:id/summarize` + save `message` JSON to `archivio/` + `DELETE`), the weakest path here and milestone-1 territory (§14.1); cross-project conversation grouping (opencode has no project tag and A1 serves one project at a time — the archive is the cross-project view). History search deliberately absent (ADR 099). **Verified:** brace-balanced, props checked against the real `Widgets/*` and against Quickshell source (`SplitParser`/`Process`/`StdioCollector` in `git.outfoxxed.me`); nothing run. **USER verifications:** V-11 (base-prompt residue vs personality disposition), V-12 (PDF read from the note archive), V-13 (persistent per-domain page-fetch approval option), V-14 (summary written to `archivio/` on close), V-16 (containment-rebuild time on project switch tolerable). Held: opencode's `/event` schema, its session-delete side effects, whether `prompt_async` + poll gives acceptable latency, and the whole visual layer. |
| S-76 | Transition from the current uncontained usage | awaiting-verification | dotfiles e8ab747 + review-pass f1937c1 + this commit · phi 1e3550e · phi-shell 9dd04d2 · phi-packages 7e23ef7 | 2026-09-09 | Depends on S-75 (unverified). The card has no `AGENT` bullets — it is a `USER` transition, gated on V-01..V-04 and V-08/V-09 all passing first. Agent deliverable is the mechanism to make the switch ergonomic + the runbook: `profiles/desktop/home/.config/zsh/agent.zsh` (new) defines `phi-code [DIR]` — `phi-agent-contain a2 -- opencode` (the TUI in the container) for `DIR` under `PHI_AGENT_CODE_ROOT`, with a guard that refuses a path outside it; plus `alias phi-ask='phi agent ask'`. `.zshrc` (base) gains one line that sources `$ZDOTDIR/agent.zsh` **only if present** — the file ships in the `desktop` profile, so `mini` (base only) never gets it. `phi-agent-a2.service`'s header reworked: it is now documented as an OPTIONAL persistent server (its port is inside A2's netns; reaching it from the host needs a user-added bridge), and daily interactive use is `phi-code`. README gains a "Transition" section: enable the A2 support services, configure `a2/broker.json`+`provider-key`+model+whitelist, run a real task with `phi-code` and confirm from inside that `cat ~/.ssh/id_*` and `git push` fail while `git commit` works, then `mv ~/.config/opencode ~/.config/opencode.pre-phios.bak` once satisfied. **`DONE WHEN`: the old unconfined configuration is removed** — a USER action, not something the agent can do. **VERIFY: a real coding session completes under containment** — USER, `phi-code` in a real project, real work, finished. **Gate G7** (`docs/phios-agent-brief.md`): all of `V-01`…`V-18` executed and recorded — NONE have been; every M7 row is `awaiting-verification` and the whole milestone was written against a containment boundary that has never run on a real machine. **M7 review pass (this commit):** `phi-code`'s in-container `cd` to `/home/agent/code/<rel>` was `cd … || cd /home/agent/code` — on any translation failure it silently opened the *root* of every project, the §4.3 perimeter inverted. Now fatal: a bad `<rel>` prints an error and exits, never a fallback. The host-side prefix test and `rel` computation both `:A`-resolve `code_root` (symlinks and `..` gone) so they agree, and the prefix is now quoted so a code root containing a glob char is matched literally; a project dir whose real path escapes the code root (reached via a symlink) is refused — correct, since that path is not under the bind. Held: the actual daily-use ergonomics (the card's own test is "use it for a week"). |

## M8 — Custom applications

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-80 | phi-notes | todo | | | |
| S-81 | Language decision checkpoint | todo | | | |
| S-82 | phi-music | todo | | | |
| S-83 | phi-media | todo | | | |

## Out-of-plan changes

Work requested directly by the user, outside the `phios-agent-brief.md` §6
backlog. Not a main-line step and not a parallel track (`P-NN` may not
touch `phios-dotfiles` beyond a package row): recorded here so the loop
still has a durable record. Trailer used: `Out-of-plan: <slug>`. Each row
follows the same `awaiting-verification` → `verified` discipline as a step.

| # | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| OOP-01 | AI agent settings section + shell-summoned agent panel | verified | phi-shell 1643781..a752c98 (2 commits) · dotfiles 490e6c2, 74e856c + this commit | 2026-09-09 | **Verified on real hardware (2026-09-09):** the user confirmed the features work, per the VERIFY block — the `Super+P` bind and the Φ bar segment both toggle the placeholder panel, and the expanded AI Agent settings section renders. Host not itemised in the report; the `Fn+F4`→`Super+P` question (S-06) and any screenshot-level layout refinement of the placeholder panel / the long settings section were not reported back in detail and stay open as minor follow-ups. The resident conversational panel that replaces the placeholder body is **not** being built next, by the user's instruction. Requested while M7 is still mid-verification on `razer`. Kept to `phi-shell` + `phios-dotfiles` only — **no `phi` / `phi-packages` change**, so the packaged `phi` stays at `1e3550e` / 0.12.1 (S-73's constraint for the S-75 checks) and the M7 build/publish loop is not reopened. Everything works against the `phi agent` verbs already in 0.12.1 plus `systemctl --user` and plain file reads. **Task 1 — settings.** `Settings/sections/AiAgent.qml` rewritten from the four §9.12 items (activation toggle, connection, active project, pending proposals) to a full section: active-project **switch** + **new project** (both via `Services/Agent.qml`'s existing `useProject`/`newProject`/`projects`/`switching` — unused by the old section), read-only personality list, active-project `output/` listing, systemd unit state for all eight `phi-agent*` units, and a read-only readout of `broker.json` (upstream/listen/rate-limit/auth-header), `opencode.json` (model id), the broker request meter, provider-key present/absent, and the A2 egress-whitelist entry count. §9.12's perimeter rule (runtime state only; versioned config is not a second source of truth) is respected — the config values are shown with their path, never an edit control (the exact `git pull` conflict S-71 round 1 hit). New `Services/AgentInfra.qml` singleton does the `systemctl`/file reads (kept out of `Services/Agent.qml`, which is the ADR-098 opencode client and mid-M7-verification — nothing here is a call to opencode). Deliberately **no** start/stop control for the A2 remote surface (starting `phi-agent-a2-remote*` is how a session is declared remote, §10.3 — status only) and **no** default-personality control (`default_agent` is versioned config). **Task 2 — panel.** New `Panels/AgentPanel.qml`: the shell-summoned agent surface `phios-agente.md` §10.1 already specifies ("evocazione da scorciatoia globale, superficie residente") and which `AiChat.qml` (S-75, a sidebar tab with no shortcut) never provided. **Placeholder content** by request — live A1 status + pointers to the sidebar Agent tab and Settings › AI Agent, plus start/stop and close buttons; the resident conversational panel replaces the body later, not the plumbing. New `Services/AgentPanel.qml` singleton owns its `shown` state (one owner for all entry points — same shape as `Services/Spotlight.qml`, and shell.qml's own S-43 note on why a bar/settings toggle needs a singleton it can reach). Toggled by: the bar Φ segment (`Bar/modules/PhiAgent.qml` `onActivated` — added to the inner `Segment`, not the wrapper Item that hosts the breathe animation; `active` still tracks `processing` only, §6.6 reserves Role B accent for elaboration), **Super+P** (`hyprland.lua.tmpl` → `qs ipc call agent toggle`, target `agent`), and the Settings "Open agent panel" button. Centred float + scrim (Cheatsheet/Overview shape), not an edge dock — reads as a summoned overlay, no visual collision with the right-edge Sidebar; flagged for cheap veto. No `Services.LayerFocus` and no click-outside-to-dismiss (a placeholder takes no keystrokes; a background `MouseArea` vs. the buttons' `TapHandler`s is the pointer-grab bug class this milestone already spent rounds on). `hyprland.lua.tmpl` is a rendered template (S-54): the apply path is `git pull github master` (razer's checkout tracks `origin`/mini, not `github` — S-20 round 6), then `phios-install`, then `hyprctl reload` — the QML hot-reloads itself. Files: `phi-shell` — `Services/AgentPanel.qml` (new), `Services/AgentInfra.qml` (new), `Panels/AgentPanel.qml` (new), `shell.qml` (+1 instance), `Bar/modules/PhiAgent.qml` (+`onActivated`), `Settings/sections/AiAgent.qml` (rewrite). `phios-dotfiles` — `hyprland.lua.tmpl` (+Super+P bind), this file. |
