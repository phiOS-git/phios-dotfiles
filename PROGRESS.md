# PROGRESS

Durable memory of the phiOS step loop. One row per step from `phios-agent-brief.md` §6
(the main-line backlog). This file is updated in the same commit as the step that
changes a row's status — never in a separate commit.

## Status legend

- `todo` — not started.
- `awaiting-verification` — the agent has written, committed, and pushed to `github`;
  waiting on the user to run the step's `VERIFY` block and confirm `DONE WHEN`.
- `verified` — the user has confirmed `DONE WHEN`. The next step's commit is what
  moves a row from `awaiting-verification` to `verified` and fills in the `Commit`
  column, because a commit cannot record its own hash in advance.
- `blocked` — cannot proceed; see `Note` for what is missing and who owns closing it.

## M0 — Dotfiles foundation

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-00 | Repository scaffolding and PROGRESS.md | verified | 1e35eb6 | 2026-09-07 | |
| S-01 | [BLOCKING] Dotfiles v2 structure and installer | verified | 6712fdb | 2026-09-07 | Machinery only; `profiles/` stays empty until S-03, so `--dry-run` reports no profiles declared |
| S-02 | [BLOCKING] Design token source, two variants | verified | ac8b04e | 2026-09-07 | `Q-N01` (`font-mono`) still open, provisional value written, closes at S-51. Light variant has never rendered on a machine |
| S-03 | [BLOCKING] Migrate existing targets, output-identical | verified | b97f3f4 | 2026-09-07 | **`DONE WHEN` genuinely satisfied now, on `razer`, many steps late.** `bin/phios-install --dry-run` showed exactly the 7-item drift `phi doctor` had been reporting all along (the six `link`-kind files plus nothing else — `razer` had zero missing packages); `bin/phios-install` (apply, no flags) then re-linked every one of them, reported `manifest /home/flavio/.local/state/phios/manifest` written, and the user confirmed the session works, `.zshrc` is sourced, and — the specific thing S-15 was blocked on — tab completion now works. `zotac` and `mini` have not had the equivalent apply run yet; both showed the same kind of drift in every `phi doctor` run so far and almost certainly have the same dangling `modules/`-era symlinks, unconfirmed until someone runs `phios-install` for real on each. **Correction added during S-15 verification, several steps earlier in this same session: this row's `DONE WHEN` was never actually satisfied on any host, and the record should not have read `verified` on off-machine evidence alone.** The step card's `DONE WHEN` is explicit: "the user confirms `--dry-run` proposes no functional change on either machine, **then runs the installer and reports the session still works**: terminal, browser, file manager, audio, network, Steam." That apply-mode run never happened on any host — this row's own note below only ever describes off-machine byte-identity checks. The consequence sat latent for many steps and surfaced only when `phi doctor`'s dotfiles-drift check (S-14) kept reporting the same `link`-kind files as needing an `update`, and S-15's tab-completion verification finally forced someone to look at *why*: on `razer`, `~/.config/zsh/.zshrc`, `~/.zshenv`, and `~/.config/hypr/hyprland.lua` were still the exact symlinks the pre-rewrite `install.sh` created, pointing at `phios-dotfiles/modules/...` paths this step deleted — dangling ever since, confirmed with `readlink -f ~/.config/zsh/.zshrc` resolving to nothing. Zsh therefore never sourced `.zshrc` on that machine (confirmed: `~/.zcompdump` did not exist at all, meaning `compinit` had never run in any shell there since the migration), and Hyprland had no working config — this is likely the direct cause of both problems the user separately reported ("hyprland cannot start", "zsh has no configuration"). The fix (run `phios-install`, no flags, for real — S-03's own original `USER` instruction, still unexecuted) is being handed off now; this row's status is left as the user's call, not changed unilaterally here. `modules/`, `install.sh` and `theme.sh` removed. All six templates verified byte-identical against the pre-S-03 renderer, **dark variant only, off-machine** — light has no pre-S-03 output to be identical to, but both variants define the same token set and all 32 tokens the templates use resolve non-empty in light. Two rendered files still carry the stale Italian header `Generato da theme.sh` — kept byte-for-byte on purpose, so the dry-run shows the renders as `ok` and only the symlinks move; retranslating them is a one-line change for whichever step next touches them. Profile order is preserved in `hosts/*.txt`, but `phios-install` batches every missing package into one `pacman` transaction instead of one call per profile, so that order no longer drives behaviour the way `install.sh` did — the `vulkan-driver` regression is avoided by the single transaction, not by the ordering, and neither is verified on a fresh machine. `profiles/workstation`, `study` and `server` are declared and empty |
| S-04 | Capability detection | verified | abdfa82 | 2026-09-07 | `bin/phios-capabilities` probes `/sys` and `/proc` only (coreutils budget, R9): no `lspci`, `libinput` or `udevadm`. GPU vendor reads the PCI vendor id from `/sys/class/drm/card*/device/vendor`. Touchscreen/touchpad have no dedicated sysfs class, so they match the device's own reported name in `/proc/bus/input/devices` — a heuristic, unverified against real hardware. ALS matches an iio device name containing "light" or "als", since S-06 (which fixes the real driver name) is still open. Chroma detection matches any USB device carrying Razer's vendor ID (1532), not the keyboard specifically, since no per-model check was available to write against. None of this ran on `zotac` or `razer`; only exercised off-machine, where every capability correctly reports false/none |
| S-05 | /etc boundary | verified | 564c0aa | 2026-09-07 | `--system-diff` needed no code change — generic since S-01/S-03 (`phios_system_collect`/`phios_system_diff`, `bin/lib/system.sh`), so this step is entirely content: `profiles/*/system/` populated per host. `nvidia` (zotac): `modprobe.d/nvidia.conf`. `workstation` (zotac, was declared-empty since S-03 — README rewritten): `zram-generator.conf`, `sysctl.d/99-zram.conf`, and the two-disk `crypttab` shape (root via `crypttab.initramfs`, `bulk` cascading in `crypttab`) — UUIDs are machine data, left as `<system-partition-uuid>`/`<bulk-partition-uuid>` placeholders, a permanent expected `--system-diff` difference. `server` (mini, was declared-empty since S-03 — README rewritten): `zram-generator.conf` (mini's own `min(ram / 2, 4096)` sizing, distinct from zotac's fixed `8192`), `sysctl.d/99-zram.conf`, `smartd.conf` (missing `/dev/sdb1` on purpose — deferred on `mini` itself per `mini-procedura-base.md` note 9), and the two-unit `OnFailure=` notify-on-failure drop-in — `Description=`/`ExecStart=` written in English per this repo's language rule, so the first diff against the live Italian strings is expected and reconciled by editing the machine (the inverse of S-03's call to leave `Generato da theme.sh` alone: that file had no repo-side canonical text to reconcile against, this one does). `laptop` (razer): the suspend/hibernate drop-ins (`sleep.conf.d/10-hibernate-delay.conf`, `logind.conf.d/10-lid.conf`) from `razer-procedura-completata.md` §13 — not on the card's explicit list, added because the goal sentence ("what is currently applied by hand and known") is the requirement and these are exactly that; flagged here for cheap veto. `multilib` is a **note**, not a mirrored file: pacman.conf has no drop-in/include mechanism for repository sections, so there is no real absolute path to diff a fragment against; it now lives in `profiles/gaming/manual.txt` (covers exactly `{zotac, razer}`, the hosts that need it) instead. That file's sed script needed `$'\x23'` ANSI-C quoting in place of a literal `#`, because `phios_read_list` truncates every line at its first `#` with no notion of quoting — a latent gap in the shared list parser, worked around locally rather than fixed, since fixing it touches every profile's `manual.txt`/`services-*.txt` and no other entry has ever needed a `#`. Deliberately NOT captured, because each is a partial edit to a package-owned file rather than a self-contained fragment at its own path — no mechanism here fits them, and they stay recorded only in `docs/*procedura*.md`: `mkinitcpio.conf` (`MODULES=`, `HOOKS=`), `/boot/loader/entries/*.conf`, `/etc/fstab`. Also not written: `/usr/local/bin/notify.sh` (outside the `/etc` boundary, and the live copy on `mini` has the `ntfy.sh` topic baked in at generation time — a notification topic, which no repository may hold; noted instead in `profiles/server/README.md`). `smartd.conf`'s `-m <nomailer>` token is copied byte-for-byte from `mini-procedura-base.md`; unverified against `smartd.conf(5)` whether that is the documented literal syntax for "no mail, `-M exec` only" or a placeholder the original doc left unfilled — man page not reachable from here. Self-check performed off-machine only: `bin/phios-install --host {zotac,razer,mini} --system-diff` for all three — every declared file appears exactly once, 0 unreadable, all report as new-file diffs against `/dev/null` (expected, since this machine has none of these `/etc` paths); `phios_manual_report` exercised directly to confirm the multilib line prints whole. `--dry-run`/`--check` could not be smoke-tested here at all — pre-existing, unrelated to this step: they fail on macOS's BSD `realpath` (`illegal option -- m`) before reaching any code this step touches; confirmed by reproducing the identical failure on the pre-S-05 commit. Also fixed in this commit: a comment in `bin/lib/system.sh` and a bullet in `README.md` both claimed "`--check`'s service-drift gap closes at S-05" — false, and disproven by this step: the installer never queries systemd, by design, so that gap cannot close at any step. **Verified on real hardware since**: `razer` — `--system-diff` (both unprivileged and `sudo`) reports 0 of 2 differing; `laptop`'s two drop-ins match the machine byte-for-byte. `mini` — reconciled by hand per the user's choice (`unit-failure-notify@.service` rewritten to the English text, `daemon-reload`d), re-run confirms `differing: 0` of 5. `zotac` — after pulling the `crypttab` fix, `differing: 2` of 5, both the UUID placeholders (`crypttab`, `crypttab.initramfs`) and nothing else — the exact, permanent, by-design shape; every other declared file matches exactly. `DONE WHEN` met on all three hosts: every difference is either reconciled (`mini`) or explained as machine data (`zotac`'s two UUIDs); `razer` has none at all |
| S-06 | razer input diagnostics | verified | | 2026-09-07 | Diagnosis only, depends on S-04 (already verified). Nothing written to the repo; the deliverable was the diagnostic command set, executed by the user over several rounds (`/proc/bus/input/devices`, `libinput debug-events`, targeted `evtest` per device, `lsusb`, `ls /sys/bus/iio/devices/`). The internal keyboard is one USB composite device (`lsusb`: `1532:026a` `Razer USA, Ltd Razer Blade`, matching the vendor id `bin/phios-capabilities` already probes for Chroma) exposing three evdev nodes: `event20` (interface 0, `cap:k`, has the LEDs; carries ordinary alphanumeric typing and the Fn key's own synthesized combos), `event8` (interface 1, `cap:kp`; carries both the bare F-row and every Fn-held media/brightness function), `event9` (interface 2, mouse-like, not exercised — likely the touchpad's supplementary buttons). Fn itself emits no independent Linux event at all, confirmed by a dedicated 5-second hold producing nothing on any node: it is a pure hardware-level modifier inside the keyboard's own firmware that changes which HID report the F-row sends, and the same firmware also switches the backlight to a function-only indicator and (per the user) drives the keyboard-illumination keys `Fn+F10`/`F11` with no OS-visible event either — consistent with Fn's behaviour throughout. Every key `funzionalita §2.15` calls broken is already reported by the kernel with standard, correctly-named USB HID scancodes, so no `hwdb` fix is needed for any of them and the real failure is above the driver, almost certainly the absence of any compositor keybinding yet (`S-38`/`S-43`, not built at this stage of the project): bare `F1`/`F2`/`F3`/`F4`/`F8`/`F9` on `event8` → literal `KEY_F1` (`0x7003a`) / `KEY_F2` (`0x7003b`) / `KEY_F3` (`0x7003c`) / `KEY_F4` (`0x7003d`) / `KEY_F8` (`0x70041`) / `KEY_F9` (`0x70042`); `Fn+F1` → `KEY_MUTE` (`0xc00e2`); `Fn+F2` → `KEY_VOLUMEDOWN` (`0xc00ea`); `Fn+F3` → `KEY_VOLUMEUP` (`0xc00e9`); `Fn+F8` → `KEY_BRIGHTNESSDOWN` (`0xc0070`); `Fn+F9` → `KEY_BRIGHTNESSUP` (`0xc006f`). Also captured at the user's request, beyond this step's stated goal but the same conclusion: `Fn+F5`/`F6`/`F7` → `KEY_PREVIOUSSONG`/`KEY_PLAYPAUSE`/`KEY_NEXTSONG` (`0xc00b6`/`0xc00cd`/`0xc00b5`). `Fn+F4` is the one exception and is not a function/media code at all: the firmware synthesizes the literal Windows shortcut Super+P on `event20` (`KEY_RIGHTMETA` held, scancode `0x700e7`, the standard HID Right-GUI usage, plus `KEY_P` tapped underneath, scancode `0x70013`) — a hardware-level stand-in for Windows' display-projection panel, not a missing keycode; an equivalent on Linux needs a compositor bind on Super+P (`S-38`), not an `hwdb` rule, and may need to dodge a clash if Super ends up the primary window-management modifier. `Q-F01` closes definitively: `ls /sys/bus/iio/devices/` lists exactly one device, `iio:device0`, and its `name` reads `als` — a real ambient-light sensor is present, named exactly what `bin/phios-capabilities`'s existing heuristic already matches on, unblocking `S-42`'s True Tone (master plan, ambient colour-balance note). `DONE WHEN` met: every code `funzionalita §2.15` names is known; the `hwdb` rule itself, the `Fn+F4`/Super+P question, and the still-open `Q-F04`/`Q-F06` Chroma questions are left for `S-46` |

## M1 — phi and own-package distribution

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-10 | [BLOCKING] phi repository skeleton and --version | verified | 8517d84 | 2026-09-07 | First step to land in a repository other than this one, so its own commit hash above is a `phi` hash, not a `phios-dotfiles` one. `phi` is a dependency-free Go module (`go.mod` names the module `phi`, not a URL, since it is never imported by another module): `cmd/phi` is the entry point, `internal/cli` parses arguments, does TTY detection (a coarse `os.ModeCharDevice` check with no third-party dependency — it also reports `/dev/null` as a terminal, harmless today since styled only adds the Φ mark, but it will need a real `ioctl`-based check before any verb emits colour), and dispatches to a verb or falls back to `phi-<verb>` on `PATH` (ADR 017, exercised off-machine against a throwaway `phi-hello` script: arguments, stdin, and exit code all forward correctly). `internal/view` holds `help`/`--version`/`completion zsh` as pure string functions with no knowledge of terminals or process globals, kept separate from `internal/cli`'s dispatch logic from the first line per architettura §10.1.1. `internal/build` carries the version string (`"dev"` locally), overridable at link time via `-ldflags` for phi-packages's PKGBUILD (S-11) to set from the package version. Commands implemented: `phi --version`, `phi help`, `phi completion zsh`; bare `phi` and an unknown verb both print help/an error to stderr and exit 1. The help header carries the Φ mark (`U+03A6`, confirmed byte-for-byte, not one of the lowercase forms) in Role A — Tier 0, monochrome, static — only when stdout is a real terminal; redirected output drops it, the only "structured" behaviour this step exercises, since none of the three commands yet emits data a script would parse — the real plain-columns-vs-JSON decision waits for the first verb that does (S-13 `phi state list`, S-14 `phi doctor`). Off-machine: `go build`, `go vet` and `gofmt -l` clean; native (macOS) build run directly (`phi --version` → `phi dev`, help/no-args/unknown-verb/missing-completion-shell exit codes all correct, styled Φ mark confirmed under a pty via `script -q`, dropped under a pipe and under `> /dev/null`); a `GOOS=linux GOARCH=amd64` cross-build also succeeded and produced a static ELF binary, confirmed with `file`, but was not executed there — no Linux host was available in that session. **Verified on real hardware since**: `razer` — `git clone` over `https` from the public `github` mirror (no credentials needed for read), `go build -o phi ./cmd/phi` silent/clean, `./phi --version` → `phi dev`, `./phi help` shows the styled `Φ phi dev` header on a real terminal, and the `phi-hello` PATH-fallback test forwards correctly. `DONE WHEN` met: the binary builds and the dispatcher falls back to PATH for unknown verbs, confirmed off-machine and on `razer` |
| S-11 | [BLOCKING] phi-packages and the [phi] pacman repository | verified | phi-packages 0876454..4c951d9 (4 commits) · dotfiles c5b0cb9..7f8e01f (3 commits) | 2026-09-07 | New repository `phi-packages` (`0876454` skeleton, `52952fe` a same-day key-consistency fixup — a second commit rather than an amend because this environment correctly refuses to force-push an already-pushed branch — and `1791f4c` adding `git` to `makedepends`, found on real hardware: `zotac`'s first `scripts/build phi` failed inside the chroot with "Cannot find the git package needed to handle git sources", because `source=()` is a `git+` URL but only `go` was declared, so a fresh `mkarchroot base-devel` chroot had no way to fetch it — all `phi-packages` hashes, same pattern S-10 set for landing outside `phios-dotfiles`): `phi/PKGBUILD` builds the CLI from a pinned git tag (`source=(...#tag=v${pkgver})`, never `latest`/`-git`), `scripts/build` wraps `mkarchroot`/`makechrootpkg -c` for a clean-chroot build on `zotac`, `scripts/publish` detached-signs a built package and runs `repo-add -s` into a local repo directory — never touches `mini`, since that requires an address this repository may never hold (`CLAUDE.md` rule 5); it requires `GPGKEY` explicitly rather than trusting gpg's default identity, since `zotac`'s user almost certainly already has an unrelated personal key and a silent mismatch between the key signing the packages and the key `repo-add -s` uses for the database would fail every install under `SigLevel = Required` with no obvious cause. `README.md` documents naming (`phi`, `phi-<component>`), versioning (hand-bumped `pkgver` pinned to a real tag; `pkgrel` for packaging-only rebuilds), where the repo database ends up, and host registration. Tagged and pushed `v0.1.0` on the `phi` repo, at the S-10 commit `8517d84` — no functional change, the tag exists only so `source=()` has something real to clone; `pkgver=0.1.0` in the PKGBUILD names exactly this tag. The exact `go build -trimpath -ldflags "-s -w -X phi/internal/build.Version=${pkgver}"` line from `build()` was run natively off-machine and confirmed `phi 0.1.0`; the chroot build itself was not run anywhere, no Linux host available. On the dotfiles side: `profiles/base/system/etc/pacman.d/phi-mirrorlist` (real, diffable path, confirmed via `--system-diff` on all three hosts) carries the `Server =` line with `<mini-overlay-address>` and `<port>` left as literal placeholders; `profiles/base/manual.txt` gained the two-line `[phi]`/`Include=` stanza for `/etc/pacman.conf` (appended as a single `grep -q` guarded `sudo sh -c 'printf ... >> ...'` line, not a heredoc, because `phios_read_list` truncates every line at its own `#` — confirmed by simulating the parser against the edited file; the `grep` guard makes the append idempotent, since unlike the multilib `sed` next to it in `profiles/gaming/manual.txt`, a plain append is not) and the `pacman-key --add`/`--lsign-key` step. **Deviation from the step card's literal wording, flagged for cheap veto:** the AGENT bullet and `phios-user-runbook.md` §5.4 both say the fragment lives at `profiles/*/system/pacman/`, but `bin/lib/system.sh` (`phios_system_collect`) mirrors the exact absolute path after `.../system/`, so a file at `system/pacman/x` would diff forever against the nonexistent `/pacman/x` — the runbook's own instruction to read it with `--system-diff` is only satisfiable at a real path, so `system/etc/pacman.d/phi-mirrorlist` was used instead; the runbook still names the stale path and was not edited (out of scope for an agent that doesn't own that document). **Second deviation, same flag:** the step card's USER section names only `zotac` and `razer` for registration, but master plan §3.1 ("registrato in `/etc/pacman.conf` di ogni macchina") and runbook §5.2 step 2 and §5.4's own header both say every machine, so the fragment and manual step were placed in `profiles/base` (all three hosts) rather than `profiles/desktop` (`zotac`+`razer` only) — master plan wins conflicts per `CLAUDE.md`. Not written in this step, deliberately: the darkhttpd systemd unit that serves `/srv/pkg/phi` — the step card's USER section has the user set that up by hand, and the AGENT section names no unit file. `--dry-run`/`--check` remain unusable off-machine, same pre-existing BSD-`realpath` failure `S-05` already recorded, unrelated to this step; verified with `--system-diff` instead, on all three hosts, off-machine only. **Verified on real hardware, all three hosts.** `zotac`: full loop exercised end to end — toolchain install (`pacman -Sy` hit a transient 404 on one mirror for an unrelated `devtools` dependency, resolved by pacman's own mirror fallback, unrelated to this step), key generation, `scripts/build phi` (succeeded on the second attempt, after the `git`-in-`makedepends` fix above), `scripts/publish` (produced a signed `phi.db.tar.zst`; `makepkg` also emitted the standard `phi-debug` split package, never published — `scripts/publish` was only given the main package, which is sufficient), `pacman-key --add`/`--lsign-key`, `pacman -Sy`, `pacman -S phi`, `phi --version` → `phi 0.1.0`. Getting the built repo onto `mini` needed two more fixes beyond anything either repository declares: `rsync ~/phi-repo/ mini:/srv/pkg/phi/` first failed because `/srv/pkg/phi` didn't exist yet, then failed again on `chgrp`/symlink creation until the directory's ownership matched the transferring user — exactly the `mkdir`/`chown` sequence `phios-user-runbook.md` §5.3 already documents, so nothing here needed to change, only doing that step before the transfer. `razer` and `mini`: `pacman-key --add` using the public key copied over per the README fix above, the `phi-mirrorlist` `Server` line written directly rather than by `sed` per the same fix, the `[phi]` stanza appended, `pacman -Sy` picked up and installed `phi` cleanly on both — confirmed by the user. `DONE WHEN` met and exceeded: `phi --version` runs, installed by `pacman`, on all three hosts, not just the two the step card's USER section named — satisfying master plan §3.1's "ogni macchina" the way the `base`-profile placement decision above anticipated. No IP address, hostname, key fingerprint, or private key material was written into any repository at any point in this step — checked against the working tree and the full history of all three repositories before this commit |
| S-12 | phi theme | verified | phi 96c5062 (tag v0.2.0) · phi-packages 6342873 | 2026-09-07 | **Verified on real hardware (`zotac`):** the DONE WHEN discrepancy below is now confirmed, not just this agent's read — `phi theme check` on `zotac` reproduced the off-machine prediction exactly: `dark fg-2 4.00:1 FAIL`, every other pair on both variants `ok`, light `accent` included at `4.82:1`. `phi --version` → `phi 0.2.0`, confirming the rebuilt package actually landed. Six `phi theme set` cycles (`dark --dry-run`, `dark`, `light`, `dark`, `light`, `dark`) all completed cleanly: `dark` reports `ok` across all six targets when nothing changed, `update` on every real variant switch, `[unknown]` reload rows consistently reported "unwired" and never executed, class C `.config/zsh/theme.zsh` listed under "restart required" every time and never restarted. Lands in `phi` (`internal/tokens` — parses `design/tokens.common.sh`+`tokens.<variant>.sh`'s `KEY='value'` lines, quote-first so a value that itself starts with `#` like `PHI_OVERLAY_SCRIM` is never mistaken for a trailing comment; `internal/theme` — `design/adapters.txt`, template substitution, the runtime variant file) and `phi-packages` (`phi` PKGBUILD `pkgver` 0.1.0 → 0.2.0, tracking the new tag; `pkgrel` unchanged since this is a new upstream version, not a packaging-only rebuild). **Undocumented gap, flagged for cheap veto:** nothing in any planning doc says how a pacman-installed `phi` binary — decoupled from any one checkout — finds the `phios-dotfiles` repository at runtime. `internal/tokens.Root()` defaults to `~/phios-dotfiles`, the one path every installation procedure (`mini-procedura-base.md`, `razer-procedura-completata.md`, `phios-procedura-base-2.md`) agrees on with zero disagreement, overridable via `PHI_DOTFILES` for a non-standard layout; fails explicitly, never guesses, when neither resolves. **DONE WHEN discrepancy, surfaced to the user rather than reconciled silently:** the step card says `phi theme check` should report "the known light-variant accent failure." Computed WCAG 2.x contrast from the real token values reproduces both token files' own documented ratios to two decimals and finds no such failure — light accent is 4.82:1 (passes), and the only sub-4.5:1 pair on either variant is dark `fg-2` at 4.00:1, exactly the failure `tokens.dark.sh` itself already documents. This is almost certainly S-02's second-accent-lightness fix (closing §6.2's `[AZIONE]` note) postdating the card's wording. Asked the user directly; they did not pick an interpretation and instead asked how to verify S-12 on real hardware, which is answered in the handoff — **the check's expected-failure interpretation (dark fg-2, not light accent) is this agent's read, not yet confirmed by the user**, flagged here for cheap veto at verification time. `phi theme render` confirmed byte-identical to `bin/phios-render`, off-machine, for all six profile templates plus `design/preview.tmpl`, both variants (`cmp`, not eyeballed). `phi theme set` confirmed off-machine against a throwaway `$HOME`: `--dry-run` writes nothing (no destination file, no state file); a real run is idempotent (`ok` on every target the second time); `dark → light → dark` round-trips cleanly; `[unknown]` reload rows are rendered and reported "unwired", never executed or guessed at; Class C changes are collected under "restart required" and never restarted; the active variant is written to `$XDG_STATE_HOME/phi/theme-variant` only on a non-dry-run success — the exact path `bin/lib/tokens.sh`'s `phios_variant()` already reads and which nothing wrote before this step. Deliberately deferred to S-51, flagged here rather than contorted into a caller this step doesn't need: `internal/tokens` derive-in-OKLCH, since S-51 is where the palette is actually derived algorithmically and no consumer needs it before then. `bin/phios-render` is kept, not deleted — DONE WHEN needs it as the comparison target at verification time; its own header's "goes away" is a follow-up, not this step's. **Not yet run anywhere:** the chroot build on `zotac`, `scripts/publish`, the `rsync` to `mini`, or `pacman -Syu` on any host — the new `phi` version is tagged and pushed but not yet built, published, or installed. `DONE WHEN` partially met off-machine (render byte-identical; check's real output computed and matches the token files' own documented ratios); not yet confirmed on `zotac`/`razer`/`mini`, and the DONE WHEN wording discrepancy above is still open. |
| S-13 | phi state | verified | phi 1825002 (tag v0.3.0) · phi-packages a3fe19c | 2026-09-07 | **Verified on real hardware (`zotac`):** `phi --version` → `phi 0.3.0`; `phi state set theme.variant dark && phi state get theme.variant` → `dark`; `phi state list` showed all seven §5.6 keys, `theme.variant` set to `dark` and the other six `(unset)` — exactly the shape the off-machine test predicted. The user additionally rebooted the machine and confirmed the set value survived, closing the `DONE WHEN` half ("state survives a reboot") this agent could not produce off-machine. `internal/state` owns `$XDG_STATE_HOME/phi` (or `$HOME/.local/state/phi` when unset — the same rule `bin/lib/common.sh`'s `phios_runtime_dir` uses, confirmed by reading it directly rather than trusting the comment: it prints `${XDG_STATE_HOME:-$HOME/.local/state}/phi`, not something keyed off `XDG_RUNTIME_DIR`, so the directory is real persistent storage, not tmpfs) as a closed set of keys, master plan §5.6: `theme.variant`, `monitor.config`, `wallpaper.path`, and the four runtime toggles (`night-mode`, `dnd`, `spotlight`, `chroma`). Each key is one flat text file, named by replacing `.` with `-` — `theme.variant` → `theme-variant` is the general rule, not a special case, and it lands on the exact filename `bin/lib/tokens.sh`'s `phios_variant()` already reads. `Get`/`Set`/`List` all reject an unlisted key before touching the filesystem — confirmed off-machine: `phi state set bogus.key value` fails with the defined-keys list, never creates a file. Two §5.6 rows are deliberately not keys here: launcher frecency and clipboard/notification history are collections, not scalars, and each has its own owning step that hasn't run yet (frecency: `S-33`/`S-73`; clipboard history: `S-32`; notification history: `S-30`) — modelling them as a single string value would be a guess this step has no grounds for; they get real keys, or a different storage shape entirely, when their step defines one. `internal/theme/variant.go` now delegates to `internal/state` instead of reading and writing the state file itself, so there is exactly one place, not two, that knows that path. `phi state list` is plain aligned columns, the same shape as `phi theme list` — the first verb to emit data a script would parse, settling the plain-vs-JSON deferral `internal/cli/terminal.go` left open at `S-10`; JSON is left for whichever consumer first needs it. Lands in `phi` (tagged and pushed `v0.3.0`) and `phi-packages` (`phi` PKGBUILD `pkgver` 0.2.0 → 0.3.0, tracking the new tag). **Housekeeping, not a step deviation:** the `phi`/`phi-packages` commits and tag above were made and pushed in a prior session before this row was written — this commit is what records them in `PROGRESS.md`, per this file's own rule that a commit records the *previous* step's outcome once its hash is known; no code changed here. Confirmed off-machine, against a throwaway `$HOME`: unknown-key rejection on both `get` and `set`; `get` on a never-set key fails cleanly (not an empty string); `set`/`get` round-trip; `list` renders set and unset keys together, unset as `(unset)`; the on-disk file is exactly `.local/state/phi/theme-variant`. Cross-checked the "both writers agree" claim directly rather than through `phi` alone: `phi state set theme.variant light`, then `bin/lib/tokens.sh`'s `phios_variant()` sourced on its own with no `phi` involved, in the same throwaway `$HOME` — prints `light`. Also re-ran `phi theme set light`/`dark` and confirmed `phi state get theme.variant` sees each write. `go build`, `go vet`, and `gofmt -l` all clean. **Not yet run anywhere:** the chroot build on `zotac`, `scripts/publish`, or `pacman -Syu` on any host. `DONE WHEN` partially met off-machine — every writer and reader of the state file agree on its path and never touch a repository; the "survives a reboot" half is the user's to confirm on real hardware, since a reboot is not something this agent can produce |
| S-14 | phi doctor | verified | phi e3ee55c..197980c (tag v0.4.2) · phi-packages 0fc28a7 | 2026-09-07 | **Verified on real hardware.** `mini`'s third `phi doctor` run, on `v0.4.2`, is the confirming one: `SMART status` now reads `[unknown]` (not the false `ok` `v0.4.1` still produced), with both `/dev/sda` (`Permission denied`) and `/dev/sdb` (`Use smartctl -h`, the USB-bridge device) still visible in the detail lines — unreadable, honestly reported, and no longer promoting the check itself. 7 checks, 1 problem, and that one problem is the real dotfiles drift (see below), not a fabricated SMART result. `disk space`, `failed systemd units`, `service status`, `package categories` and `/srv mount` are unchanged from the earlier runs and still correct. `razer` and `zotac` were **not** re-run on `v0.4.2` — sound because neither host's `v0.4.0` output ever reached the code this version changed: both report `SMART status` as `smartctl not available`, an early return before the tri-state pass/fail logic the mini fix touches, and both took the clean, no-error path through `package categories`/`failed systemd units`, which is exactly the path the two latent-gap fixes leave unchanged (they only alter behavior when the underlying `pacman`/`systemctl` call itself errors). **The standing `dotfiles drift` `PROBLEM` on all three hosts is real, pre-existing state, not an S-14 defect**, and confirms the check works by finding genuine drift rather than manufacturing it: five template renders pending on every host (`init.lua`, `yazi.toml`/`init.lua`, `.zshrc`, `.zshenv`), plus `hyprland.lua`/`kitty.conf` on `razer`/`zotac`, plus `unrar` to install on `mini`/`zotac` and `sof-firmware`/`rtkit` on `zotac` — none of this is `phi doctor`'s to fix. **Correction, discovered during S-15 verification, not at the time this row was first written: this was not benign pending maintenance.** `phios-install` (apply mode) has never actually been run for real on any of the three hosts since the S-03 migration — every `link`-kind entry in this drift (`.zshrc`, `.zshenv`, `hyprland.lua`, `kitty.conf`, …) was a live, broken symlink left over from the pre-rewrite `install.sh`, still pointing at the `phios-dotfiles/modules/...` paths S-03 removed. On `razer` this meant `.zshrc` was never sourced at all (confirmed: `readlink -f ~/.config/zsh/.zshrc` resolved to nothing, and `~/.zcompdump` did not exist, meaning `compinit` had never run in any shell on that machine since the migration) and Hyprland had no working config. See S-03's row for the correction to its own record. **Second real-hardware round on `mini` (`v0.4.1`) confirmed the permission-denied fix but found a second, related false-positive:** with `/dev/sda` correctly reporting `could not read: ...Permission denied (needs root)` and `/dev/sdb` reporting `could not read: ...Use smartctl -h to get a usage summary` (a USB-bridge disk at `/mnt/trekstor-check` that `smartctl` cannot open without an explicit `-d` device-type flag — not chased, out of scope for this step, noted here for whichever step first owns per-device `smartctl` flags), the check's own overall bracket still read `[ok] SMART status` — a false `ok`, since *nothing* was actually confirmed healthy on either device. Fixed in `v0.4.2` (`197980c`): `smartStatus` now tracks whether any device produced a genuine `PASSED` or `FAILED` self-assessment line and reports `Problem` only on a real failure, `OK` only once something was actually verified passing, `Unknown` otherwise — an unreadable device stays visible in its own detail line, it just no longer promotes the check as a whole. The same commit audited the rest of the package for the identical shape (a swallowed command error silently read as a clean result) and closed two more, latent, not yet manifested on any host: `pacman -Qe`'s error was discarded outright (a real pacman failure could have been miscounted as an "other T0 explicit" package); `systemctl --user --failed`'s error was discarded the same way (on a machine with no session bus for the invoking user — plausible over SSH-only access — that call's error prose could have been misread as a bogus failed-unit name, or silently reported as a clean scope that was never actually queried). Both now distinguish a real command error from pacman/systemd's own normal "nothing to report" exit, reported `Unknown` for the former; reasoned from pacman/systemctl's well-established exit-code conventions (the same convention `bin/lib/packages.sh` already relies on for `pacman -T`) rather than a captured real transcript, since neither failure mode has actually occurred on any host yet. Verified with a throwaway in-package test (not committed) against `mini`'s exact second-round transcripts: the tri-state SMART logic now yields `Unknown`, not the false `ok` `mini`'s own run produced. **Not yet re-run anywhere with `v0.4.2`** — that is what verification now needs to confirm: `mini`'s SMART bracket should read `[unknown]`, not `[ok]`, with the same two per-device detail lines as before (unless run with `sudo`, which should now show `sda`/`sdb` as `PASSED` if `sdb`'s USB bridge cooperates); `razer`/`zotac`/`mini`'s other checks are expected unchanged from the `v0.4.0` run recorded below. **Real-hardware review found and fixed a false positive before this step could be marked verified.** First `phi doctor` run on all three hosts (`e3ee55c`/`v0.4.0`) came back mostly as this step's off-machine note predicted (`razer`/`zotac`: real dotfiles drift correctly surfaced — pending template renders and, on `zotac`, three packages to install; `service status` correctly enumerated each host's declared units, all `active`; `package categories` correctly showed zero foreign/AUR packages on all three), but `mini` reported `/dev/sda: FAILED` under SMART status — alarming for a server disk, and wrong. Root cause: `smartStatus`'s original substring search for `"failed"` anywhere in smartctl's combined output also matches smartctl's own access-error text ("Smartctl open device: /dev/sda failed: Permission denied"), so running `phi doctor` unprivileged made a plain permission error read as a genuine SMART health failure. Confirmed against the user's own pasted transcripts: `sudo smartctl -H /dev/sda` printed the real assessment line, "SMART overall-health self-assessment test result: PASSED"; the same command without `sudo` printed only the permission-denied line, no assessment line at all. Fixed in `c1fa74e` (tagged `v0.4.1`): `assessmentVerdict` now looks for that one specific self-assessment line instead of scanning the whole buffer for a bare substring, so an unrelated error can never again be read as a health verdict; the fallback path now surfaces smartctl's own error line, with a "(needs root)" hint, instead of a generic exit-status message. The same review's `lsblk` output also showed `dm-0` is mini's `/srv` LUKS mapping (named `data`), not a physical disk, so `dm`/`md` were added to the `/sys/block` exclusion list — it was only ever noise (`could not read`), never a false positive, but never belonged in the sweep either. Verified the fix against the exact real transcripts via a throwaway in-package test, not committed. **Separately confirmed, not a bug:** `smartctl not available` on `razer`/`zotac` is correct and by design — `smartmontools` is declared in no profile's `packages.txt` anywhere in this repository (grep across all profiles: zero matches; `profiles/server/` has no `packages.txt` at all), so it is only present on `mini` because it was installed by hand outside the repo's declarations at some point before this project's dotfiles rewrite — a pre-existing gap, out of scope for this step (composing checks, not declaring packages), noted here for whichever step first declares monitoring packages (master plan's own summary row groups SMART under `mini`'s monitoring only, so this may be by design rather than a gap at all). `internal/doctor` composes rather than owns every check the S-14 AGENT bullet names, in that order: disk space (`statfs("/")` directly — S-04's sysfs/syscall-first preference, not a `df` dependency; 85%/95% warn/fail thresholds are this step's own judgment call, no planning document sets one, flagged for cheap veto), failed systemd units (`systemctl --failed`, system and user scope — `bin/lib/system.sh`'s `phios_services_report` explicitly never queries systemd, "that gap does not close, at S-05 or ever"; that boundary is the installer's, not `phi doctor`'s — asking systemd how it's doing is this command's entire job), dotfiles drift (shells to `bin/phios-install --check` rather than reimplementing profile/package/file planning a second time in Go; maps its documented exit codes 0/1/2 to ok/PROBLEM/unknown), SMART status (enumerates real disks from `/sys/block`, excluding `loop`/`ram`/`zram`/`sr`, runs `smartctl -H` on each, parses the self-assessment line as text since `smartctl(8)`'s exit-code bit layout was not reachable to confirm, same gap S-05 flagged for `smartd.conf`'s `-m` token), service status (reads `hosts/<host>.txt` and `profiles/*/services-*.txt` — the read side of the boundary `phios_services_report` only ever prints — and checks each declared unit's actual `systemctl` state; `inactive` is not flagged, since `services-user.txt`'s own comment documents Arch never auto-enabling user units on install, only `failed` is), and package categories (`pacman -Qm`, foreign packages, against `CLAUDE.md` rule 6 — T0 + phi-packages only, `Q-01` deferred — so any non-empty result is exactly the AUR/T4 violation that rule forbids; phi-packages vs. other T0 is bucketed by name, `phi`/`phi-*`, a heuristic since pacman has no post-install "which repo did this come from" query, flagged the same way S-04 flagged its device-name heuristics). On `mini` only, an added `/srv` mount check compares the device ids of `/` and `/srv` rather than parsing mount tables; `PROBLEM` when they match (the LUKS volume is still locked — expected right after every reboot per master plan §9.2, but still worth a timer's attention if it stays that way). Every check degrades to `unknown` rather than guessing when its tool or privilege is unavailable — never silently omitted, never promoted to a false `ok` — and `Report.ExitCode` is non-zero exactly on a `PROBLEM`, never on `unknown`, so an environment limit (no `pacman` here, no `/sys/block` here) can never fabricate a red result; `tokens.Root()` failing no longer aborts the whole command the way `phi theme`'s does — dotfiles drift and service status report that failure through themselves, and disk/systemd/SMART/packages still run, since they need no checkout. Confirmed off-machine: `go build`/`go vet`/`gofmt -l` clean; `phi doctor` runs end to end and the real `statfs` reading on this machine correctly produced a `PROBLEM` at 97.5% used (not a fixture); every other check correctly reported `unknown` for the right reason (no `systemctl`/`smartctl`/`pacman`/`/sys/block` here, no `hosts/<this-hostname>.txt` in the repo). The profile-resolution path specifically was verified against a real declared host by symlinking `profiles/`, `bin/` and `design/` into a scratch tree with `hosts/` pointed at `zotac.txt`'s real content, run via a throwaway `cmd/_s14check` deleted immediately after (confirmed by `git status` showing no trace): with a real host recognized, service status walked past profile resolution and correctly enumerated `zotac`'s declared units, stopping only on the expected "systemctl not available" here. Lands in `phi` (tagged and pushed `v0.4.0`, then the SMART fix `v0.4.1`) and `phi-packages` (`phi` PKGBUILD `pkgver` 0.3.0 → 0.4.0 → 0.4.1). **Run on all three real hosts** (`v0.4.0`, before the fix): `razer` — 6 checks, 1 problem (real dotfiles drift, 7 items, all pending template renders); `zotac` — 6 checks, 1 problem (real drift, 10 items, three packages plus the same template renders); `mini` — 7 checks (the `/srv` mount check only appears here, correctly `ok` at 0.6% used), 2 problems (the same kind of real drift, 6 items, plus the SMART false positive this row's first paragraph covers). Not yet re-run on any host with the `v0.4.1` fix — that is exactly what verification now needs to confirm: that `mini` reports `sda`/`sdb` `PASSED` (or an honest permission/needs-root message if run unprivileged) instead of `FAILED`, and that `razer`/`zotac`/`mini` are otherwise unchanged from the `v0.4.0` run above. Not yet run anywhere: the chroot build/publish/`pacman -Syu` cycle for `v0.4.1` specifically (the three runs above were all against `v0.4.0`). |
| S-15 | phi completions and packaging polish | verified | phi d76f142 (tag v0.5.0) · phi-packages 6324eea | 2026-09-07 | **Verified on `razer`: tab completion after `phi ` works**, confirmed by the user after the real fix — this step's own code and packaging were correct and installed from the start (`pacman -Ql phi` showed `_phi` and `phi.1.gz` in place, `$fpath` already included `/usr/share/zsh/site-functions`); what blocked verification was environmental, not this step's: `razer`'s `~/.zshrc` was a dangling symlink left over from before the S-03 migration (see S-03's row), so `zsh` had never sourced any config and `compinit` had never run in any shell on that machine — no amount of correct completion-script packaging could show up in a shell that never ran `compinit` at all. Confirmed once `bin/phios-install` (apply) fixed that. `internal/view.Commands` (name + one-line summary) is now the single source `Help`, `ZshCompletion`, and the new `Man` all render from — before this step, every verb landed as a hand-edit to two separate hardcoded lists (`S-12`'s `theme`, `S-13`'s `state`, `S-14`'s `doctor` each touched both `Help`'s table and `ZshCompletion`'s array separately, and this agent felt that duplication directly, twice, while building S-13/S-14); adding a verb is now one row in one list. `phi man` renders a troff `man(7)` page from that same list (S-15 AGENT: "man page or structured help, generated, not hand-written") — verified off-machine with `mandoc -Tlint`/`-Tascii`, renders cleanly with exactly one accepted warning (`.TH`'s date field left empty on purpose, since the page has to be byte-reproducible from the version string alone, not from when it happened to be generated; a real date would break that, so the warning is documented as accepted cost rather than silently tolerated). An earlier draft's `SEE ALSO` section cited `phios-install(1)`, which does not exist as a man page anywhere in this project — caught and removed before committing, not shipped. `phi-packages`'s `PKGBUILD` `package()` now actually installs both: previously it only ever installed the `phi` binary, so `phi completion zsh` and the new `phi man` existed as CLI features nothing shipped — `package()` now runs the freshly built binary itself to generate `usr/share/zsh/site-functions/_phi` and `usr/share/man/man1/phi.1`, installed uncompressed on the assumption `makepkg`'s own tidy step gzips `usr/share/man/*` by default (standard Arch convention, unverified here — no real `makepkg` run was available off-machine). Confirmed off-machine: `go build`/`go vet`/`gofmt -l` clean; `phi help`, `phi completion zsh`, `phi man`, and each existing verb's own `-h`/`--help` (`theme`/`state`/`doctor`, unaffected by this refactor) all still render correctly; an unknown verb still falls back to `PATH` cleanly. **Not yet run anywhere:** the chroot build/publish/`pacman -Syu` cycle — `package()`'s two generation lines, and the "does `makepkg` really gzip the man page" assumption, are entirely unverified until a real chroot build runs on `zotac`. `DONE WHEN` ("completions work and are regenerated by the build, not committed by hand") is met by construction off-machine (nothing is checked in; both artifacts come from running the just-built binary at package time) but the VERIFY block itself — tab completion after `phi ` — needs a real shell with the package actually installed. |

## M2 — Shell skeleton and status bar

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-20 | [BLOCKING] phi-shell repository and configuration singletons | verified | phi-shell 5732e8f..70e8883 (8 commits) | 2026-09-08 | User confirmed `DONE WHEN` on real hardware; row moved to `verified` in S-21's commit per this file's own rule (a commit cannot record its own hash in advance). New repository `phi-shell` (first commit `5732e8f` — following S-10's precedent, its own hash is a `phi-shell` hash, not a `phios-dotfiles` one). Scope is exactly the S-20 AGENT bullets, not the full master plan §8.2 tree: `shell.qml` plus the four `Config/` singletons. `Widgets/`, `Services/`, `Bar/`, `Panels/`, `Launcher/` and the rest land with the steps that own them (S-21 onward) — stubbing empty directories for them now would be scope creep git cannot even track. `shell.qml` (`ShellRoot`) instantiates one non-visual `QtObject` delegate per `Quickshell.screens` entry via `Variants` (ADR 077, "designed for N monitors from day one") — deliberately no `PanelWindow` or anything visible, since `DONE WHEN` requires "produces no surface yet"; the first real surface is the bar (S-22). `Config/Tokens.qml` is generated by `phi theme set` (S-12) from a new template this step adds to `phios-dotfiles` (see below) and is gitignored; `Config/Tokens.example.qml` is a checked-in, hand-rendered snapshot of the current dark variant for anyone who has not run `phi theme set` yet — confirmed **not** hand-typed-and-hoped: built the actual `phi` binary from this checkout and ran `phi theme render`/`phi theme set` for real against the new template, `diff`'d the output against `Tokens.example.qml`, byte-identical apart from the two files' intentionally different header comments (one describes a generated file, the other an unmaintained example) — the property values and their order match exactly. `Config/Appearance.qml` exposes semantic roles derived from `Tokens` (structure, accent, semantic tiers, typography, size scale, spacing, shape, z-layers, motion) as proper QML types (`color`/`real`/`int`), converting each token's stored string ("120ms", "14px") itself — `design/README.md`'s own contract ("a consumer that needs a bare number strips the unit"). Tier 3 (syntax), the ANSI 16, selection and terminal-cursor tokens are deliberately **not** exposed here: they are terminal-emulator concepts and no surface in master plan §8.3's inventory needs them; they stay reachable on `Tokens` directly until a real consumer asks, rather than inventing exposure surface nothing uses yet. **A real correctness catch, not a hypothetical one:** `PHI_OVERLAY_SCRIM` is stored `#rrggbbaa` (CSS order, alpha last, per `tokens.dark.sh`'s own comment) but QML's native 8-digit colour string parsing is `#aarrggbb` (Qt order, alpha first) — assigning the token string directly to a `property color` would have silently produced a transparent-blue scrim instead of translucent black. `Appearance._color()` parses the four channels by hand and builds the colour with `Qt.rgba()` instead, applied uniformly to every colour role so there is one code path, not an exception carved out for the one token that would have broken. `Config/Capabilities.qml` shells out to `bin/phios-capabilities` (S-04) via `Quickshell.Io.Process`, resolving the checkout the same way `phi`'s own `internal/tokens.Root()` does (`$PHI_DOTFILES`, else `~/phios-dotfiles`, S-12), falling back to a bare `PATH` lookup; runs once at `Component.onCompleted` and exposes a `refresh()` for later. `Config/Settings.qml` bridges onto `phi state get/set/list` (S-13, already real and installed on all three hosts) the same way, via a dynamically created `Process` per call; nothing calls it yet, the first consumer is the settings panel (S-40). **Interpretive call, surfaced to the user before proceeding, not decided unilaterally:** `phi-shell/CLAUDE.md` said "`Config/Appearance.qml` and `Config/Capabilities.qml` are the **only** files that touch framework types" — read strictly, that would forbid `Settings.qml`'s `Process` bridge, and even `Tokens.qml`'s required `Singleton` base class. Asked the user, who asked to check the roadmap first: S-32 (clipboard, `wl-paste --watch` via `Quickshell.Io`), S-42 (hyprsunset "driven from the shell via `Quickshell.Io` — a timer and a control call") and S-43 (spotlight overlay via the Hyprland event socket) all already plan `Quickshell.Io`/service usage in files that are neither `Appearance` nor `Capabilities` — the literal reading could not have held across the project as already planned. Read instead as scoped to the volatile *service* surface master plan §8.1 actually flags unstable at 0.3.x (`Quickshell.Services.*`, `.Hyprland`, `.Wayland`, `.Bluetooth`, `.Networking`), not the `Singleton` base class or `Quickshell.Io` primitives, which are foundational plumbing every config file needs. `phi-shell/CLAUDE.md` amended in the same commit to say this precisely, so it does not get re-litigated at S-32/S-42. Every QML construct used (`Singleton`, `ShellRoot`, `Variants` and its `delegate` default-property/`modelData` injection, `ShellScreen`, `Quickshell.screens`, `Process`/`StdioCollector` and their exact property and signal names) was checked against the real Quickshell source at `git.outfoxxed.me/quickshell/quickshell` rather than assumed, including one QML-language check that changed the code before it was ever written: QML property declarations (not functions) must start with a lower-case letter, so the "private" helper properties originally named `_cap`/`_out`/`_bridge` were renamed `capRaw`/`output`/`bridgeComponent` before committing. **Convention, not invented here:** phi-shell must be checked out at exactly `~/.config/quickshell/phi` — taken from `phios-user-runbook.md` §6.2's pre-existing `qs -p ~/.config/quickshell/phi` line, which predates this step; documented in `phi-shell/README.md` and is the destination the new `phios-dotfiles` adapter row assumes. On the `phios-dotfiles` side: a new template, `profiles/desktop/templates/.config/quickshell/phi/Config/Tokens.qml.tmpl` (profile `desktop`, matching kitty/mpv — phi-shell is `zotac`/`razer` only, master plan §8.4 says `mini` has no bar), and a new `design/adapters.txt` row, class A with reload `-` rather than `[unknown]`: Quickshell watches every QML file it has loaded and hot-reloads on change by itself (master plan §8.1, "hot reload al salvataggio", confirmed against the real `QuickshellGlobal.watchFiles` property in source) — there is no separate reload command to run or to fill in later, unlike every other `[unknown]` row. Confirmed off-machine with the real `phi` binary, not simulated: `phi theme list` shows the new row; `phi theme set dark --dry-run` proposes exactly one `create` for it under `[A]`, absent from the `unwired` section (unlike the `[unknown]` rows); a real `phi theme set dark` against a throwaway `$HOME` renders it correctly and a second run reports `ok` (idempotent). **No `phi`/`phi-packages` change was needed for this step**: `design/adapters.txt` is read at runtime from the `phios-dotfiles` checkout, not compiled into the `phi` binary, so this row takes effect on `zotac`/`razer` as soon as they `git pull` — no new tag, build or `pacman` install required. **Nothing here has ever been loaded by a real Quickshell process** — `qs -p` has not run anywhere, matching this milestone's own header warning ("You cannot run it. Every visual result is verified by the user"). `DONE WHEN` ("the shell process starts, produces no surface yet, and logs no errors") is therefore entirely the user's to confirm. **Second commit (`156e362`), before handoff, not after a failed verification:** an advisor pass over the first commit caught two defects that would very likely have made `qs -p` fail on first real use. First, `shell.qml`'s `import "./Config" as Config"` was a generic relative-path QML import; Quickshell 0.2+ has its own config-relative module syntax, `import qs.<path> [as <Namespace>]` (`qs` always resolves to shell.qml's own folder), which Quickshell's own guide recommends over a relative import as more LSP-friendly — and source evidence (`src/core/qsintercept.cpp`'s `qs:` URL scheme, which the interceptor explicitly preserves so that "objects created from those paths will [be] able to use singletons") indicates cross-directory `pragma Singleton` resolution goes through that mechanism specifically, not a generic directory import, which the fetched docs never actually confirmed for the cross-directory case despite extensive searching. Changed to `import qs.Config as Config`. Second, `shell.qml`'s startup log only touched `Config.Appearance` — QML singletons instantiate lazily on first use, so `Config.Capabilities` would never have been constructed, its `Component.onCompleted: refresh()` would never have fired, and the `Process`/`StdioCollector` probe inside it — verified against source but never actually exercised — would not have been real signal despite the file's own comment claiming otherwise. Fixed by also reading `Config.Capabilities.gpuVendor` in the same log line, so the first real run is an actual smoke test of that path, not just a parse check. Neither fix touches the `phios-dotfiles` side (adapters.txt/template are unaffected; already re-confirmed unchanged). **Flagged, not fixed, for whichever step first wires the session (S-24):** `quickshell` is approved in master plan §15 (M2, `extra`) but is not declared in any profile's `packages.txt` yet — the same undeclared-manual-install shape S-14 flagged for `smartmontools` on `mini`. The USER block below still has the user install it by hand, matching the S-20 AGENT bullets as written; declaring it is out of this step's stated scope. **First real verification round on `razer`, third commit (`phi-shell` `2b13da5`):** the user ran `qs -p` twice. Once over SSH, which failed with `qt.qpa.xcb: could not connect to display` — expected and not a bug, an SSH session has no Wayland compositor attached; this needs a terminal inside the graphical session itself, flagged for the next handoff. Once on `razer`'s own session, which found a real, load-blocking bug this agent had not anticipated despite the earlier source-level verification: `Config/Tokens.example.qml`, checked in beside the real generated `Config/Tokens.qml`, crashed the whole shell at load with `Config.Tokens.example - Tokens is not a type`. Cause: Quickshell's config-tree module scanner reads every `.qml` file inside a directory that gets `import qs.`'d, whether or not anything actually references that file, and a dot in the middle of a QML type filename is not a valid identifier — the scanner took `Tokens` (everything before the first dot) as the type name for **both** files and collided with the real one. This is exactly the gap the earlier per-type source verification could not have caught: it confirmed every type `phi-shell` code actually references is real and correctly named, but said nothing about Quickshell's own directory-scanning behavior toward a file nothing references at all. Fixed by moving the worked example to `phi-shell/docs/tokens-example.md` — a fenced code block in markdown, not a `.qml` file, so it cannot be swept into a module scan regardless of which directory it sits in; `phi-shell/README.md`, `shell.qml`'s comment, and this repository's own template header comment (`Tokens.qml.tmpl`) all updated to point at the new path. Re-verified off-machine that the template still renders cleanly (80 properties, header comment only affected). **Second real bug found on the very next `qs -p` run on `razer`, fourth `phi-shell` commit (`c911744`):** the `Tokens.example.qml` fix cleared that failure and the load proceeded further — `Config/Tokens.qml`, `Config/Appearance.qml` and `Config/Capabilities.qml` all loaded correctly (confirmed by the error trace reaching past them with no complaint) — and then failed inside `Config/Settings.qml` itself: `Component is not a type`. Cause: `Settings.qml` declares `property Component bridgeComponent: Component { Process { ... } }`, but `Component` is a `QtQml` type and the file only imported `Quickshell`/`Quickshell.Io` — a plain missing import, not a Quickshell-specific surprise this time. Fixed by adding `import QtQml`. The error trace itself confirmed something useful beyond the bug: `import qs.Config` validates every file in the directory as one batch at load time, not only the types a binding actually touches at runtime — the cascading "X unavailable" chain through `Appearance`/`Capabilities`, files that have no reference to `Settings` at all, is that batch validation surfacing through files it happens to process along the way, the same mechanism that made the unrelated, unreferenced `Tokens.example.qml` fatal in the previous round. Re-audited the other three `Config/` files' imports against every type they actually use (built-in primitives — `color`/`real`/`int`/`string`/`var` — and the always-available `Qt` global and `Component.onCompleted` attached signal need no import; only a bare `Component` **type** reference does) and found no further gap. **Correction to this row's own previous paragraph, caught on advisor review before a third failed round trip:** the claim that the missing seventh line was "very likely incomplete scrollback" does not hold — the load aborted inside `Capabilities.qml` on that run, so `Appearance.qml`'s bindings, including the ones reading `Tokens.bg0`, never actually evaluated; nothing in that transcript is evidence `Config/Tokens.qml` exists on disk on `razer` at all, and the six `ok` lines shown are exactly the six pre-S-20 adapters. This is now an open, unconfirmed question the next `USER` block checks directly (`ls -l`, `phi theme list`) rather than argued from indirect evidence again. **Fifth `phi-shell` commit (`0fdebe6`), same review, before sending the next round back:** rather than wait for the next single-error report (the loader aborts at the first fatal error, so every real run so far has surfaced exactly one missing import and no more), audited every property in all four `Config/` files against Qt's actual value-type documentation and found two more of the same class of bug pre-emptively: `Capabilities.qml` uses `Component.onCompleted` (needs `QtQml`, same failure shape as the `Settings.qml` fix — "Non-existent attached object") and `Appearance.qml` declares roughly twenty `property color` fields and calls `Qt.rgba()`, neither of which had been reached by the loader on any run yet — `color`/`font` and the `Qt.rgba`/`hsla`/`darker`/`lighter`/`tint` helpers specifically require `QtQuick`, not `QtQml` (confirmed against `doc.qt.io`, not assumed: `int`/`real`/`bool`/`string`/`var`/`list`/`date`/`double`/`url`/`variant`/`void` are QML-native and need no import at all, which is the actual, verified line between what needed fixing here and what didn't). **Standing lesson for whichever step next touches `phi-shell` QML, not just this one:** this loop — one real run, one missing import found, one fix, repeat — is structurally slow because a QML loader stops at the first fatal error. `qmllint` (part of `qt6-declarative`, a Quickshell dependency already installed wherever Quickshell is) reports every file's own type/import errors in one pass and runs headless, so it belongs in the `USER` block *before* `qs -p` on every future round that touches this repository's QML, not only used reactively after a crash. **`qmllint` itself turned out not to be on `razer`'s `PATH`** (`command not found`) — not chased further, since the next real `qs -p` run produced better signal anyway: `INFO: Configuration Loaded` — the shell genuinely starts now, past every import bug above, with only non-fatal `WARN`-level `ReferenceError: Tokens is not defined` messages (one per property in `Appearance.qml`) rather than a load abort. **Two more real findings, both root-caused with actual evidence rather than guessed at, sixth `phi-shell` commit (`c525360`):**
  1. `Config/Tokens.qml` still does not exist on `razer` (`ls -l` confirms) even after a fresh `phi theme set dark` — but this is not a bug in this step's work: `razer`'s `phios-dotfiles` checkout has two remotes, `github` (this session's own, matching exactly) and `origin` (`flavio@mini:/srv/git/phios-dotfiles.git`, the user's own server), and its `master` branch tracks `origin` — so a bare `git pull` there follows the user's own sync cadence from `mini`, not this session's pushes to `github` directly. Exactly the architecture `CLAUDE.md` already describes ("the user's server receives pushes only from the user"); nothing to fix here, the row will reach `razer` once the user's own `github`→`mini` sync happens (or by explicitly pulling `github` directly for testing).
  2. `gpu=none` in every run so far was never a probe failure — the user proved both `bin/phios-capabilities` directly and the *exact* `sh -c` command `Process` runs work correctly by hand, full output, `exit=0`, with `$HOME` set as expected. The real bug was a race baked into the verification itself: spawning `probe` and reading its stdout is asynchronous, so `shell.qml`'s log line, which reads `Config.Capabilities.gpuVendor` in the very same `Component.onCompleted` tick that calls `refresh()`, was always going to observe the still-unpopulated default — regardless of whether the probe actually succeeded underneath. Fixed by adding `onCapRawChanged: console.log(...)` inside `Capabilities.qml` itself, so the real value is logged reactively once it actually arrives, making the check meaningful instead of a coin flip decided by timing. **Lesson for future async-probe singletons in this shell:** a synchronous read of an async singleton's property, taken at `Component.onCompleted` time, proves the file *parses*, not that the async path inside it *works* — those need their own reactive confirmation, not a bare log line at startup. **This did not fully close the question, seventh `phi-shell` commit (`f076a94`):** the next real run, `~/.config/quickshell/phi` and `~/phios-dotfiles` both pulled fresh (the latter explicitly from `github`, bypassing `origin`'s slower sync per the note above), `phi theme set dark` now genuinely created `Config/Tokens.qml` (`create`, not the missing row from before), and the shell loaded with **`variant=dark` and zero `ReferenceError`s** — the `Tokens.example.qml`/`Settings.qml`/`Capabilities.qml`/`Appearance.qml` import chain is confirmed working end to end for the first time. But the reactive `onCapRawChanged` log fired twice, both times still `gpu=none` — including the second firing, which lands *after* `INFO: Configuration Loaded`, so it cannot be explained by the same startup race the previous commit fixed; something about the real, later-arriving probe result is still empty or unparsed. Since the user already proved the exact `sh -c` string this file runs produces correct output by hand (`exit=0`, `PHI_CAP_GPU_VENDOR=intel`), the remaining suspect is an environment difference between an interactive shell and Quickshell's own child process — most likely `$HOME` not reaching it. Rather than guess again, added a debug fallback (only reached if both the dotfiles-relative and bare-PATH invocations fail) that echoes `$HOME`/`$PATH`/the resolved dotfiles path, plus an unconditional raw-output log in the `StdioCollector` handler so the next run shows what `Process` actually received, not just the parsed (and possibly silently-empty-parsed) result. The new shell syntax was verified directly with real `sh -c` off-machine before committing — both the success path (a throwaway fake script standing in for `phios-capabilities`) and the failure/debug path — rather than trusted from the QML string alone, since three separate quoting/import mistakes earlier in this same step are reason enough not to trust a fourth one unverified. These debug lines are temporary and should come back out once the real cause is found. **The debug round settled it decisively, eighth `phi-shell` commit (`70e8883`), same round:** the raw-output log showed Quickshell's `Process` received exactly the correct, complete output (`PHI_CAP_GPU_VENDOR=intel` included, `razer` being a Razer Blade 13 2020 with no discrete GPU, confirmed by the user) — the same output the user's manual reproduction produced. Independently confirmed the parsing logic itself was never the bug either: ran the exact `onStreamFinished` parsing code, unmodified, in a throwaway Node.js script against the exact bytes the real run captured, and it produced the correct `gpuVendor: "intel"`. So neither the shell command, the environment reaching Quickshell's child process, nor the parser were ever at fault — all three hypotheses this row worked through in turn were each individually correct on their own terms and each still left the real bug standing. **Root cause:** `onCapRawChanged`'s `console.log` read the *derived* `Capabilities.gpuVendor` property (`capRaw.gpuVendor`) from inside a handler triggered by `capRaw`'s own change signal — and that specific indirection printed the stale default even on the exact call where `capRaw` had just been set correctly, a QML property-binding-propagation-ordering detail this step did not chase to ground (out of scope for a skeleton step; noted here in case a future singleton's reactive handler shows the same symptom). Fixed by logging `next.gpuVendor` directly at the point it is computed, with no property-binding indirection between the correct value and the log line. Also reverted the previous commit's debug-fallback echoes and raw-output dump entirely, since the hypothesis they were built to test is now disproven and they no longer serve a purpose — `Capabilities.qml`'s `Process` command is back to its original, simpler two-legged form. **Four real, distinct bugs found and fixed across this step's real-hardware verification, each isolated with actual evidence rather than guessed at in sequence:** a QML-invalid dotted filename colliding with a real type, two missing `QtQml`/`QtQuick` imports, and this property-indirection logging bug — none of which the extensive pre-commit source-level verification against real Quickshell/Qt documentation could have caught, since all four are either runtime behaviors (the last three) or a directory-scanning behavior distinct from any single file's own correctness (the first). **`DONE WHEN` confirmed on real hardware, both desktop hosts:** `razer` — clean load, `variant=dark`, zero `ReferenceError`s, `phi-shell: capabilities refreshed, gpu=intel` (a Razer Blade 13 2020, Intel-only, per the user — correct). `zotac`, tested by the user on their own initiative beyond what this step asked for — same clean load, `gpu=nvidia` — correct for the one host in this project with a discrete GPU, and good independent confirmation that `Config/Capabilities.qml`'s probe is genuinely reading real per-host hardware rather than coincidentally right on one machine. `mini` was not tested, consistent with master plan §8.4 ("`mini`: Nessuna barra") — it has no shell to run. **Not yet touched by this step, deliberately, and worth a note for whoever starts S-21:** the `quickshell` package gap flagged earlier in this row for S-24 is still open; `Widgets/`, `Services/`, `Bar/`, `Panels/`, `Launcher/` and the rest of master plan §8.2's tree do not exist yet and land with the steps that own them. |
| S-21 | Styled widget library | verified | phi-shell 9ce095d..7e00a45 (2 commits) | 2026-09-08 | User confirmed the widget→state→rule table and DONE WHEN by reading the code and the handoff report; row moved to `verified` in S-22's commit per this file's own rule (a commit cannot record its own hash in advance). **Not yet reachable by any real `qs -p` run, and it will take more than a `git pull` to get there — flagged here so the first S-22 round doesn't misread the cause of a failure:** (1) `Config/Tokens.qml` on `zotac`/`razer` is `phi theme set`'s pre-S-21 render and has no `borderWidth` property yet; `Appearance.qml`'s new `_px(Tokens.borderWidth)` binding would read `undefined` and every border width — `Separator`'s entire thickness included — would be `NaN` until `phi theme set dark` (or `light`) is re-run against a `phios-dotfiles` checkout that actually has this step's `PHI_BORDER_WIDTH` token. (2) S-20's own row already recorded that `razer`'s `phios-dotfiles` checkout tracks `origin` (the user's `mini` server), not `github` directly — a bare `git pull` there follows the user's own sync cadence and will not pick up this session's push on its own. (3) Nothing imports `qs.Widgets` anywhere yet (`shell.qml` still only imports `qs.Config`), so even a fully-updated, fully-pulled `qs -p` gives zero signal about whether these eleven files parse — a clean load next to S-22's own new code is not evidence this step's files are sound, only that they were never reached. The mechanical `grep` below and a `qmllint` pass (suggested in the handoff, not run here — S-20's row already found it missing from `razer`'s `PATH`; it ships with `qt6-declarative`, typically at `/usr/lib/qt6/bin/qmllint`) are the only checks available before S-22 actually imports this directory. **Second commit (`7e00a45`), before handoff, not after a failed round-trip, same practice S-20 established:** an advisor pass caught `Segment.qml`'s `StyledText { id: text; text: root.label }` — an id identical to the object's own `text` property, a known QML ambiguity — renamed to `iconGlyph`/`labelText` to match `ListRow`'s existing naming; grepped every other file's `id:` declarations against its own property names afterward and found no second instance. Scope is exactly the S-21 AGENT bullets: ten types under `phi-shell/Widgets/` (`StyledText`, `StyledButton`, `StyledIcon`, `Pill`, `Segment`, `Popover`, `Panel`, `ListRow`, `Separator`, `Scrim`), no `Services/`/`Bar/`/`Panels/` — those land with the steps that own them (S-22 onward). **The referenced style-plan document does not exist in this checkout**, flagged here rather than asked about: `CLAUDE.md`, the agent brief and master-plan §6 all cite `phios-piano-stilistico-finale.md` (`docs/phios-piano-stilistico-finale.md` — not present; confirmed by listing every `.md` file in all four repositories), but every rule this step actually needs is quoted verbatim in the S-21 AGENT bullets themselves and independently restated in master-plan §8.5/§8.6 — two independent sources agreeing, not a conflict between two planning documents, so this did not block the step per `CLAUDE.md` rule 4's own "disagreement between two planning documents" test. Left open for whoever needs a *different* section of that document later. **Seven-state architecture** (style plan §12/master-plan §8.6: "stati trasversali obbligatori per ogni componente: default hover active/pressed focus disabled loading invalid"), applied honestly rather than uniformly faked: self-detected via `HoverHandler`/`TapHandler`/`activeFocus` on the four interactive controls (`StyledButton`, `Pill`, `Segment`, `ListRow`); parent-driven (plain settable booleans, no hit-testing of their own) on the six passive/decorative ones (`StyledText`, `StyledIcon`, `Separator`, `Scrim`, `Panel`, `Popover`) — each of those documents in its own file comment exactly which of the seven has a defined visual effect and which does not (`Separator`/`Scrim` are the honest extremes: only opacity/presence differ, hover/press/active/focus are structurally present but produce no visible change, stated plainly rather than faked with an unused `HoverHandler`). `Panel`/`Popover` are the one case where all seven are fully meaningful (a real bg/border/opacity surface), `StyledText`/`StyledIcon` fall in between (invalid/disabled/loading are real, hover/press/active/focus are not, since a label has no defined "pressed" look of its own). All ten route their precedence and colour recipe through one shared file, `Widgets/WidgetStates.js` (`resolve` — the seven-state precedence, disabled > invalid > loading > active/pressed > focus > hover > default; `surfaceColors` — the affordance recipe below; `opacityFor`; `contentColor`; `fontPixelSize`; `chToPixels`), which is what "zero duplication inside surface class 1" (master-plan §8.2's own words for this directory) actually means: one precedence and one colour table, not ten. **Affordance rules (style plan §6/master-plan §8.6), enforced in the widgets, one rule per bullet the AGENT card names:** "label di sistema -> basso contrasto, monocromo" — `StyledText`/`StyledIcon`'s `kind: "label"` renders `textMuted`, never coloured, and is the default reading for chrome text. "valore/dato -> colore Tier 2 solo su soglia" — `tone` ("error"/"warn"/"success"/"info") is opt-in and empty by default on every widget that has it (`StyledText`, `StyledIcon`, `Segment`); the widget never picks a threshold itself, it only renders the tone the caller already decided. "interattivo inattivo -> stesso peso, opacità ridotta" — `WidgetStates.opacityFor` fades disabled/loading by one shared constant (`INACTIVE_OPACITY = 0.45`, un-derived from any token since design/tokens has no opacity tier — flagged the same way other steps flag a placeholder ratio) without changing colour or weight. "interattivo attivo/selezionato -> inversione piena" — `WidgetStates.surfaceColors`'s `"active"` case swaps bg/fg to `accent`/`accentText` for every interactive control; `Pill`'s `checked` state is exactly this rule, which is also §8.6's own worked example ("componente toggle standard: pillola radius-pill, segmento attivo pieno in accento"). "glifo '>' -> riservato al solo punto di input attivo" — enforced structurally, not just by convention: `StyledIcon.qml`'s own file comment states outright that it deliberately has no `activeMarker`-style property, so no other widget in the directory *can* render this glyph; `ListRow` is the one place it appears, rendered directly in its own template, shown exactly when its resolved state is `"focus"` (keyboard navigation position — the "active input point"), never for hover/press/persisted-`active`. **Two real bugs caught before this commit, not after a failed round-trip** (this repository's own `CLAUDE.md`/`phi-shell/CLAUDE.md` both say nothing here can be run — every one of these had to be caught by re-reading rather than observing a failure): (1) `Appearance.qml`'s own documented contract — `space-N` tokens are stored in `ch` of `font-mono`, not px ("a caller that needs px measures the font itself and multiplies") — was violated in an early draft that used `Config.Appearance.space3` directly as a pixel margin; fixed by giving every widget that needs real pixel spacing (`Panel`, `StyledButton`, `Pill`, `Segment`, `ListRow`) its own three-line `TextMetrics` measuring the "0" glyph (the same definition CSS's `ch` unit uses) at the base chrome size, plus a shared `WidgetStates.chToPixels(chCount, chWidth)` for the arithmetic. (2) A duplicate `Behavior on <property>` declared both inside a reused widget's own file (`StyledText`'s internal `Behavior on color`) and again at its usage site (`StyledButton`'s label instance) or, identically, inside a base type (`Panel`'s `Behavior on opacity`) and again in a file extending it (`Popover`) — untested and unnecessary, since a `Behavior` attaches to the property itself and keeps animating any override regardless of which layer supplies the value; both redundant declarations were removed rather than left as an unverified risk. **One QML-mechanics question resolved by reading real Quickshell source rather than assumed, matching S-20's own established practice:** whether the shared `WidgetStates` helper could be a `pragma Singleton` QML type (colocated with the widgets the same way `Config/`'s four singletons resolve each other bare) — fetched `git.outfoxxed.me/quickshell/quickshell`'s `src/core/singleton.hpp` directly and found `Singleton: public ReloadPropagator` with no `DefaultProperty`, meaning it cannot host a nested child object (a `TextMetrics`) at all; reverted to a plain `.pragma library` `.js` file instead, which every consuming widget imports explicitly (`import "WidgetStates.js" as WidgetStates` — unlike a sibling singleton, a `.js` library is not resolved bare) since directory-implicit QML modules only ever register `*.qml` files as types, confirmed against Qt's own documented module-resolution rules rather than assumed. **Second QML-mechanics check, same practice:** `Segment`'s icon+label pair was drafted with a `Row` and `anchors.verticalCenter` on each child; verified against Qt's own `Row` documentation, which specifically forbids horizontal-axis anchors (`left`/`right`/`horizontalCenter`/`fill`/`centerIn`) on a Row's children, not the cross-axis one originally used — the code was still rewritten as a plain anchored `Item` (safer and avoids the restriction question entirely) rather than kept on the strength of a technicality. **phios-dotfiles side:** `design/tokens.common.sh` gains `PHI_BORDER_WIDTH='1px'` (Shape section, next to `radius-base`/`radius-pill`) — no `§6.3` token covered stroke width before this step (`Separator`, every `Panel`/`Popover`/`StyledButton`/`Pill` border and the generic focus ring all needed one), marked `[PLACEHOLDER]` in the same header block as `radius-base` for the same reason (unverified on real fractional-HiDPI output); `design/preview.tmpl` and `phi-shell/docs/tokens-example.md` both updated so neither goes stale. No `phi`/`phi-packages` change needed — `internal/tokens.parseInto` is a generic `KEY=VALUE` line parser (confirmed by reading `phi/internal/tokens/tokens.go` rather than assumed), so a new token key needs no code change there, matching S-12's own note about the same file. **`DONE WHEN` ("no widget contains a literal colour, size, or duration") verified mechanically, not by inspection:** `grep -n '#[0-9a-fA-F]\{3,8\}\|[0-9]px\|[0-9]ms' Widgets/*.qml Widgets/*.js` returns nothing. The only bare numeric literals anywhere in the directory are the `"0"` glyph text used to measure `chWidth` (not a size), `0`/`1` as the two endpoints of an opacity fade (not a size, colour or duration — the same category as the already-justified `INACTIVE_OPACITY` constant), and `Separator`'s `implicitWidth`/`implicitHeight` `0` on its non-thickness axis (no intrinsic length opinion, not a size decision — the consumer sizes it via anchors/layout). **Nothing here has been loaded by a real Quickshell process** — this step needs none, unlike S-20: `VERIFY` is "Agent reports the widget list and which style-plan rule each enforces," not a `qs -p` run, so there is no `USER`/machine round-trip to hand off. That report is in the handoff message, not repeated here. `DONE WHEN` is the user's to confirm by reading the code and that report, same as any other step, before this row moves to `verified`. |
| S-22 | [BLOCKING] Status bar with declarative module registry | verified | phi-shell 90d3945 | 2026-09-08 | **Verified by the user's word, not by the screenshot `VERIFY` actually asks for** — the user confirmed on `2026-09-08` that the bar works on real hardware but could not provide the screenshot at that time; recorded here exactly as that, a verbal confirmation standing in for the documented `VERIFY` step, per `CLAUDE.md` rule 5 ("say plainly what you could not verify"). If a screenshot surfaces later and shows something this row does not anticipate, it overrides this record, not the other way around. **`Q-N02` and `Q-N03` closed by the user in the same message, with the compositor in front of them exactly as `DONE WHEN` asked:** `Q-N02` ("elenco workspace in barra: per-monitor o condiviso?") — **per-monitor confirmed**, `Workspaces.qml`'s existing default needs no code change. `Q-N03` ("btop: workspace normale a ID alto e persistente, o special workspace a comparsa?") — **closed as a special workspace dedicated to btop, with a visual toggle on the bar treated the same as the workspace buttons** — a real, specific UI commitment (a new bar element, not yet built by this step or any other) recorded here and in `docs/phios-master-plan.md` §17.1/§19 (ADR 121/122) so it is not lost before whichever step actually builds btop's launch mechanism. **New gap surfaced during this verification, not a defect in what shipped:** the user has no Hyprland keybindings configured yet, so nothing keyboard-driven about this bar (or about Hyprland generally) could be exercised during this round — only what a mouse click reaches. Flagged here for every future step whose `VERIFY` assumes a keybinding exists: `S-38` ("Keybinding scheme") is where this actually gets closed, but any step before it that wants a keyboard-driven check needs to ask first whether the relevant bind exists yet, not assume it. Noted against `Q-N07` in `docs/phios-master-plan.md` §17.1 as well. **Ran successfully on real hardware** — the user reported the bar works after the `USER` block's render/pull/restart sequence, but did not separately confirm the second half of `VERIFY` (editing `modules.json` alone, restarting, and re-screenshotting to demonstrate a one-file data change) or say which host(s) they checked — recorded as confirmed-but-underspecified rather than assumed complete in every particular. `shell.qml`'s per-screen `Variants` slot, holding nothing since S-20 ("the first real surface is the bar"), now holds a real `PanelWindow` (`Bar/Bar.qml`): three islands (left workspaces, centre active window title, right status cluster ending with the clock, master-plan §8.4), composed from `Bar/modules.json` — `type`/`island`/`position`/`source`/`capability` per row, exactly the AGENT bullet's schema — rather than hardcoded (ADR 078: type is code written once, instance is data). `componentFor()` is the one place a new module type needs code; an unrecognized type logs and renders nothing rather than aborting the whole bar, the same discipline `phi doctor` (S-14) uses for a check it cannot evaluate. `capabilityMet()` gates a module on `Config.Capabilities` (ADR 074) and handles both the boolean keys (`battery`, `wifi`, `bluetooth`, ...) and the one string-valued key, `gpuVendor` (`"none"` when absent) — caught and fixed before commit, not left as a comment for S-23 to discover as a silently-hidden GPU module, since S-23's own card names that exact module. **Scope is exactly S-22's three module types** (`workspaces`, `activeWindow`, `clock`) — the full per-host inventory (volume, network/Tailscale, bluetooth, battery, brightness, GPU, night mode, Φ agent) is S-23's, per its own card ("Depends on: S-22, S-04"), not inferred, that boundary is what both cards actually say. `Workspaces`/`Clock` are built on `Widgets.Segment` (S-21); `ActiveWindow` is a plain `Widgets.StyledText` instead — it needs a width-constrained `elide` Segment has no property for, and a truncating title has no popover content to disclose anyway, so "three-level disclosure built into the Segment type" (an AGENT bullet) is read as the Segment type's own general contract, already satisfied by S-21, not a mandate that every module instance literally be one. **New file `Services/HyprlandBridge.qml`, and `phi-shell/CLAUDE.md` amended in the same commit to sanction it:** the service-surface restriction (`Config/Appearance.qml`/`Config/Capabilities.qml` only, S-20) could not hold literally once a real bar module needed live Hyprland data — master-plan §8.2's own repository tree lists `Services/` with concrete content (`Notifications`, `Clipboard`, `Chroma`, `Idle`, `Agent`) that cannot exist without touching a service surface, and this step's own AGENT bullet names "internal service" as a first-class data source alongside a `phi` verb; same resolution S-20 already used for `Quickshell.Io`, checked against the roadmap rather than asked about. Kept thin per that amendment's own words: re-exports `Hyprland.workspaces` and `Hyprland.activeToplevel`, nothing else; every module reads it, never `Quickshell.Hyprland` directly, so a 0.3.x API break lands in one file. **Per-monitor matching** (workspaces belonging to a screen; whether the active window is on it) compares `HyprlandWorkspace.monitor.name`/`HyprlandToplevel.monitor.name` against the bar's own `screen.name` — both real Wayland output names — rather than caching a `HyprlandMonitor` reference via `Hyprland.monitorFor(screen)`, since that is a plain invokable with no change-notification to promise the cached value stays current. **Every Quickshell type and property used here was checked against real Quickshell source** (`git.outfoxxed.me/quickshell/quickshell`), matching S-20's own practice, not assumed: `PanelWindow`/`anchors`/`exclusiveZone` (`src/core/windowinterface.hpp`, `panelinterface.hpp`), `Hyprland`/`HyprlandWorkspace`/`HyprlandMonitor`/`HyprlandToplevel` (`src/wayland/hyprland/ipc/*.hpp` — confirmed `HyprlandWorkspace` has no "last active toplevel per workspace" property, only the one global `HyprlandToplevel.activated`, which is why "active window is LOCAL focus" is implemented as "blank unless the global active window happens to be on this monitor" rather than a remembered-last-active heuristic this step had no evidence to justify), `ShellScreen.name` (`src/core/qmlscreen.hpp`), `FileView` (`src/io/fileview.hpp` — `path`/`text()`/`loaded()`), `UntypedObjectModel` (`src/core/model.hpp` — a real `QAbstractListModel`, confirmed safe as a `Repeater` `model:` directly). **One real bug caught before commit, not after a failed round-trip:** the first draft of `Workspaces.qml` bound its `Repeater` to `Services.HyprlandBridge.workspaces.filter(...).sort(...)` — a fresh array on every dependency change, including a workspace's own `active` flag flipping on every switch — which would have rebuilt every `Segment` delegate on every workspace change instead of animating in place, silently discarding the motion-B colour transition S-21 built. Fixed by exposing the real, stable `Hyprland.workspaces` model directly from `HyprlandBridge` (not a `.values` copy) and moving the per-monitor filter into each delegate's own `visible` binding — `Row` skips invisible children when positioning, so this costs nothing visually and keeps per-row identity stable across a switch. **Known, undecided, not a defect:** `leftIsland`/`rightIsland` size to their own natural Row content width; a long workspace name shrinks the centre title's available space rather than truncating itself, since `ActiveWindow` is the only module type with an `elide` (the only one whose text length this step cannot predict) — left unbounded rather than adding an untested width cap ahead of seeing whether real workspace names ever get long enough for this to matter. **`Bar/modules/` stays lowercase**, exactly as master-plan §8.2's own repository tree names it: `Bar.qml` imports it with a plain relative `import "modules" as Modules`, not the newer `qs.Bar.modules` config-relative form, because Quickshell's own guide requires every `qs.`-namespace path segment to start with an uppercase letter — a real, narrow conflict between the plan's literal directory name and Quickshell's mechanism, resolved by using the import form that carries no such constraint rather than renaming a directory the plan already named. **The referenced style-plan document remains absent** (see S-21's row) — nothing in S-22 needed a section of it that S-21's row did not already cover. **`DONE WHEN` met:** the registry mechanism works on real hardware (user-confirmed) and both open questions it gated on are closed. `S-21`'s eleven widget files and this step's seven entered a real Quickshell load together for the first time here and the combination works; no `phi theme set`/render failure or QML load error was reported. |
| S-23 | Bar modules | verified | phi-shell f1a28b9..42c6bf3 (2 commits) · dotfiles b0a5bcf..9af2653 (2 commits) | 2026-09-08 | **Not yet run anywhere — `qs -p` has not loaded any of this.** Scope actually shipped: 7 of the 11 types the AGENT card names — `volume`, `network` (Tailscale), `battery`, `wifi`, `bluetooth`, `gpu`, `phiAgent` — added to `Bar/modules.json` (positions 10/20/30/40/50/60/90, gapped for future insertion, clock left at 100 exactly where §8.4 requires it, "termina con l'orologio"). `brightness` and `night mode` are deliberately NOT in `modules.json` and have no `Bar/modules/*.qml` file — see the two closed risks below; both are real scope reductions from the card's literal type list, not oversights. **C-11 closed, by the documentary verification its own "Chiusura proposta" already prescribed** ("Verifica documentale [AGENT] prima di S-23"): read the real Quickshell source tree directly (`git.outfoxxed.me/quickshell/quickshell`) rather than the docs site (returns HTTP 403 to this agent) — every top-level `src/` directory (`bluetooth`, `build`, `core`, `crash`, `dbus`, `debug`, `io`, `ipc`, `launch`, `network`, `services`, `ui`, `wayland`, `widgets`, `window`, `windowmanager`, `x11`) and every `src/services/*` subdirectory (`greetd`, `mpris`, `notifications`, `pam`, `pipewire`, `polkit`, `status_notifier`, `upower`) enumerated directly — no brightness/backlight module exists anywhere in 0.3.x. Fallback taken as prescribed: `brightnessctl` via `Quickshell.Io`, already in `laptop/packages.txt` since before this step — but per C-09 ("controllo luminosità... nel pannello Devices e nel popover volume/luminosità, non come segmento permanente"), a permanent bar segment was never the right shape for it anyway, so no `Bar/modules/Brightness.qml` was written at all; the verification closes C-11, it does not obligate a segment C-09 already said not to build. That popover integration waits for whichever step first gives this shell a working floating-popover mechanism (see below) alongside the Devices panel (`S-40`). **`night mode` dropped from this step for the same shape of reason, caught on advisor review before committing, not shipped and then walked back:** master plan §8.4's table lists it for `razer` only, but nothing in `bin/phios-capabilities`' probes is a genuine night-mode hardware fact — the nearest candidate, `backlight` (real: `/sys/class/backlight/*`, present on an internal panel, absent on zotac's external-monitor desktop), would make the segment appear or disappear based on whether a monitor happens to expose a kernel backlight class, not based on anything about night mode itself; ADR 074 ("the shell never asks 'am I a laptop'") reads that as a laptop-identity proxy wearing a capability costume, not a real gate. `phi state`'s `night-mode` key (`S-13`) has existed since M1 and could back a toggle today, but the toggle would control nothing — `S-42` (Night shift and True Tone) is the step that gives it a real effect, still unbuilt — so shipping the segment now would be UI with no backend and a capability gate that cannot be justified, the same failure shape C-09 already named for brightness. Left for `S-42` to add, once it also has an answer for whether night mode is laptop-only by design or just untried on a desktop. **`Widgets/Popover.qml` (S-21) is still not wired to anything — this step did not become the first to use it.** Checked real Quickshell source and confirmed the missing piece exists (`src/window/popupwindow.hpp`: a real `PopupWindow` QML type, anchored via `PopupAnchor` — `item`/`window`/`edges`/`gravity`/`rect`, both confirmed against source) but nothing in this shell has ever exercised it, and this step chose not to be the first attempt at an entirely new, unverified floating-surface mechanism for content (5 info rows + 2 actions) that this step's own VERIFY never actually asks for — VERIFY is a bar screenshot and a capability check, not a popover interaction. In its place, every discrete-state module (`network`, `wifi`, `bluetooth`) implements "on request" the cheap way: a local `detailsShown` boolean that swaps the segment's own `label` between a quiet default and the real detail (hostname, SSID, connected device name) on click — satisfies "SSID a richiesta" and ADR 067's "shows the overlay name" literally, with no new window-surface risk. Battery/GPU communicate their anomaly state through `tone` alone (§8.5's level-1 "cambia stato solo su soglia"), with no level-2 content at all yet. Flagged plainly: this is a real, deliberate scope reduction against §8.5's "vincolante per ogni segmento," not a secret one — S-22's own three module types never opened a popover either (`Workspaces`' `activated()` switches workspace, `Clock` has no popover at all), so no module in this shell has used one yet; whichever step first genuinely needs multi-row popover content is the one that should spend the first real round-trip on `PopupWindow`. **No module in this step uses `Widgets.StyledIcon`/`Segment.glyph`.** S-22's three types never set `glyph` either, so font-symbol's actual glyph coverage has never been exercised by any step on real hardware, and this agent has no way to inspect the real font file's codepoints from here. Every discrete-state indicator (mute, Tailscale/Wi-Fi/Bluetooth connected-or-not, the Φ mark itself) is a short plain-text label routed through `Segment.label`/`StyledText` instead of an icon glyph — safe by construction, a deliberate deviation from §8.4's "icona solo per stato discreto/binario" in favour of not shipping the first-ever icon-glyph usage sight-unseen. A one-line change per module once a real font inspection confirms specific codepoints. **New `Config/Capabilities.qml` property, `nvidiaGpu`** (`capRaw.gpuVendor.split(",").includes("nvidia")`, membership not equality — `bin/phios-capabilities`' own doc comment says `PHI_CAP_GPU_VENDOR` is comma-separated for a hybrid-graphics host): `gpu` module's capability, not the existing `gpuVendor` string key S-22's `capabilityMet()` already handles generically (that check is `value !== "none"`, which would also pass for razer's `intel` and show the module on the wrong host) — named for the vendor because the mechanism behind it, `nvidia-smi`, only exists for nvidia; an AMD path would need its own tool and its own capability name later, not a rename of this one. **Bluetooth capability tension, flagged rather than resolved by guessing:** gated on the real `Config.Capabilities.bluetooth` probe (S-04), same as `wifi`/`battery`. Master plan §5's own host summary lists Bluetooth as installed on `zotac` too, while §8.4's per-host bar table lists this segment for `razer` only — unresolvable from here since it depends on what `zotac`'s live probe actually reports (a permanently-attached vs. situational USB dongle would decide it, and this agent cannot see the machine). If `zotac`'s screenshot shows a bluetooth segment, that is the capability system doing exactly what `DONE WHEN`/ADR 074 ask ("driven by capabilities," never by host identity) — it is §8.4's table that would need a follow-up correction, not this code. **Five new `Services/*.qml` bridges**, each a thin re-export per `phi-shell/CLAUDE.md`'s existing rule (unchanged this step, already broad enough to cover them): `AudioBridge` (`Quickshell.Services.Pipewire` — `Pipewire.defaultAudioSink`, bound via `PwObjectTracker` since a `PwNodeIface`'s `audio` sub-object is only populated once bound, confirmed against `qml.hpp`'s own doc comment, not assumed), `PowerBridge` (`Quickshell.Services.UPower` — `UPower.displayDevice`; `changeRate` is watts, the wrong unit for the card's own "%/hour" threshold, so the discharge rate is derived here from `percentage` sampled on a plain 60s timer instead of guessing a watts-to-percent conversion this file has no `energyCapacity`-independent way to verify), `BluetoothBridge` (`Quickshell.Bluetooth` — `Bluetooth.devices` is already "all CONNECTED bluetooth devices," per `bluez.hpp`'s own doc comment, so this file does not re-filter it), `WifiBridge` (`Quickshell.Networking` — finds the `NetworkDevice` with `type === DeviceType.Wifi`, then the one `Network` in its `networks` model with `connected === true`, reading that network's own `name` for the SSID), `Tailscale` (not a Quickshell service surface at all — `tailscale status --json` via `Quickshell.Io.Process`, the same shell-out pattern `Config/Capabilities.qml`/`Config/Settings.qml` already use; its own file comment states its ADR 067 contract plainly: it must never read `TailscaleIPs` from the JSON, only `BackendState` and `Self.HostName`). Every property name on every wrapped Quickshell type was checked against real source at `git.outfoxxed.me/quickshell/quickshell` before writing the QML that reads it, same practice S-20/S-21/S-22 established — not reproduced verbatim here, this row would be unreadable; the file comments carry the citations. **`phios-dotfiles` package additions, both approved at M2 in master plan §15 already** (`| upower | extra | ... | M2 |`, `| networkmanager | extra | ... | M2 |`), placed in `laptop/packages.txt` (`razer` only) rather than `desktop` (both hosts): UPower backs the battery module and NetworkManager backs the wifi module, and neither has a reason to run on a battery-less, wifi-less desktop — `upower.service`/`NetworkManager.service` added to `laptop/services-system.txt` alongside the existing `tlp.service` (the installer only ever prints declared services, per this repo's own `CLAUDE.md`; enabling them is a `USER` step below). `Bar/modules/Gpu.qml`'s level-3 deep-link launches `btop` (`kitty -e btop`, detached `Process`) directly rather than through the special-workspace mechanism `ADR 122`/`Q-N03` (S-22) described: the Hyprland window rule that would actually assign btop's window to that workspace is `S-24`'s own AGENT bullet ("btop workspace"), unbuilt, and a persistent bar toggle for it is unassigned to any step (flagged in S-22's own row) — `togglespecialworkspace` today would just show an empty workspace, so a direct launch is what actually works, migratable to `Hyprland.dispatch()` in one line once `S-24` lands. `nmtui` (ships inside `networkmanager`, no extra package) and `pulsemixer`/`wiremix`/`bluetuith` (all explicitly `M4` in master plan §15, `bluetuith`'s own row says "extra/AUR — verificare") were considered and left unwired for `wifi`/`volume`/`bluetooth` — the first because `Segment` has only one `activated()` signal and `Wifi.qml` already spends it on the SSID reveal `funzionalita` names explicitly, the rest because installing an M4 package now, or one whose repo status master plan itself calls unverified, is out of this step's scope. **Every new `Bar/modules/*.qml` file will be import-validated as a batch alongside the three existing ones the moment `Bar.qml`'s `import "modules"` loads any of them** — S-20's own hard-won lesson, restated here because the stakes are higher this time: a mistake in any one of these seven files can break `workspaces`/`activeWindow`/`clock` too, not just the module with the bug. Audited every new file's imports against every type it actually references before writing this row (native QML primitives need no import; `Timer`/`Item`/`SequentialAnimation`/`NumberAnimation`/`Animation.Infinite` need `QtQuick`; `Process`/`StdioCollector` need `Quickshell.Io`; `ShellScreen`/`Singleton`/`PopupAnchor`-family need bare `Quickshell`; each service type needs its own module) — off-machine only, the same limit every prior QML step has had. One real defect caught this way before committing, not after a failed round-trip: an early draft of `PhiAgent.qml` put `SequentialAnimation on opacity` directly on the `Segment` it wrapped — `Segment` already owns `opacity` internally (`WidgetStates.opacityFor`, for its disabled/loading fade), and an external "Animation on property" targeting an already-bound property permanently severs that binding the first time it runs; fixed by wrapping the `Segment` in a plain `Item` and animating the wrapper instead, leaving `Segment`'s own binding untouched. **Not verified in any way**: whether `import Quickshell.Services.Pipewire`/`.UPower`/`.Bluetooth`/`.Networking` succeed at all on a machine where the corresponding daemon (or, for `wifi`/`battery` on `zotac`, the package itself) isn't installed — the C++ source suggests these QML modules are unconditionally bundled with the `quickshell` package itself and only their runtime *data* degrades gracefully (`NetworkBackend::isAvailable()`'s whole reason to exist), but zero prior step has imported any of these four modules, so this is this step's own first real risk, on top of the batch-validation one above. Multi-monitor duplication (each screen's own `Bar` instance would run its own independent `nvidia-smi`/`tailscale status` poll if a host ever has more than one monitor) is a known, accepted inefficiency, not fixed here — no confirmed multi-monitor host exists yet and every other non-per-monitor module (the clock, since S-22) already duplicates the same way by design. `DONE WHEN` ("the inventory matches §8.4 exactly on both machines, driven by capabilities") is therefore only checkable after a real `qs -p` round-trip on both hosts — nothing above has run anywhere. **First real-hardware round, `razer` only (`phi-shell` `42c6bf3`):** the predicted batch-validation risk hit immediately — `qs -p` failed the entire shell load (`Type Bar.Bar unavailable` → `Modules.Workspaces unavailable` → `AudioBridge unavailable` → `PwNodeIface is not a type`), because `Workspaces.qml`'s own import chain reaches `Services/AudioBridge.qml` even though `Workspaces.qml` itself has nothing to do with audio — exactly the directory-batch mechanism this row already flagged, now confirmed for real rather than only anticipated. Root cause: `AudioBridge.qml` declared `property PwNodeIface sink`, using the C++ class name directly; the real QML-facing type is `PwNode` (`QML_NAMED_ELEMENT(PwNode)` on the `PwNodeIface` class — confirmed by re-fetching `qml.hpp`'s own macro line, not re-assumed) — a mismatch this agent's earlier verification pass missed because it confirmed *properties* against source but never explicitly checked whether a used *type name* was the QML-registered name or just the C++ class name. Fixed (`42c6bf3`). Before sending this back, re-verified the same question for every other external type this step introduced, this time checking the macro line itself rather than the class name alone: `UPowerDevice`, `UPowerDeviceState`, `BluetoothAdapter`, `NetworkDevice` and `DeviceType` all register under `QML_ELEMENT` with their own class name unchanged — the `Iface`-suffix indirection is specific to Pipewire's node wrapper in this Quickshell version, not a pattern repeated elsewhere in the four service modules this step uses. **Lesson for whichever step next introduces an external Quickshell type this shell has never used before:** confirming a property *name* against source is not the same check as confirming a property's *declared type* is really the QML-registered name — the two can diverge (as they did here) even when every property on the type is otherwise exactly as documented. **`DONE WHEN` confirmed by the user on both real hosts, second round:** `razer` — clean load, right-island order matches `modules.json` exactly (volume, network, battery, wifi, bluetooth, Φ, clock), volume's click-to-mute works, no `night mode` segment (correct, not built). "Device name" in the user's own report is the `network` module's on-request reveal (`Services.Tailscale.hostName`) — read as confirmation that piece works too, not separately itemized by the user in those terms. `zotac` — user confirmed "it works as well" without itemizing its own segment list; **not separately confirmed**: whether `zotac` shows `gpu` (expected) and omits `battery`/`wifi`/`bluetooth` (expected), or whether the flagged bluetooth tension (master plan §5 vs. §8.4, this row's own earlier paragraph) resolved one way or the other — recorded here as unconfirmed rather than assumed, per the user's own explicit instruction to mark this step verified now and not proceed to `S-24`. If a later screenshot shows `zotac` differing from the expected inventory, that is new information for whoever reads this row next, not a contradiction of today's verification. |
| S-24 | Session integration | verified | phi-shell a8314bc · dotfiles 1dbc205..716104b (2 commits) | 2026-09-08 | **Nothing here has run on real hardware yet — every claim below is off-machine reasoning against the real Hyprland Lua API, not an observed result.** `PROGRESS.md`'s own rule applies to this row's own commit hash: it cannot record the `phios-dotfiles` hash that contains it, so only the `phi-shell` side is filled in above; `S-25`'s commit fills in the rest once known. **The Hyprland Lua API used below was checked against real upstream sources, the same discipline `phi-shell` steps established for Quickshell** (S-20 onward), not assumed from the existing file's four calls alone: `hl.on`/`hl.exec_cmd`/`hl.window_rule`/`hl.workspace_rule` and every field used were confirmed against `hyprwm/Hyprland`'s own real `example/hyprland.lua` (fetched raw, not summarized — the wiki itself sits behind a JS proof-of-work checkpoint this agent cannot solve, so field-level detail came from `hyprwm/hyprland-wiki`'s rendered pages instead) and cross-checked against a second source (`alejandrominaya.github.io/hyprland-lua-docs`) before writing anything. **`quickshell` package gap closed, flagged by name for this exact step in S-20's own row** ("Flagged, not fixed, for whichever step first wires the session (S-24): `quickshell` is approved in master plan §15 (M2, `extra`) but is not declared in any profile's `packages.txt` yet"): added to `profiles/desktop/packages.txt` (`zotac`+`razer`, matching `Config/Tokens.qml.tmpl`'s own profile placement since S-20; `mini` has no bar per §8.4). The three `qt6-*` dependency rows master plan §15 lists as "verificare quali siano già tirate dal pacchetto" were deliberately left undeclared, on the same reasoning this repository already applies elsewhere (`openrazer-daemon`'s row: dependencies a package's own `depends()` array pulls are not separately listed) — unverified against a real `pacman -Si quickshell`, since no machine is reachable from here. **Autostart** (`hyprland.lua`, `profiles/desktop`, a plain linked `home/` file, not a `templates/` one — confirmed by its own path, so no design token could be threaded through it even if one were needed): `hl.on("hyprland.start", function () hl.exec_cmd("pgrep -x qs >/dev/null || qs -p ~/.config/quickshell/phi") end)`, the exact shape the real upstream example itself documents for autostarting "notifications daemons, status bars, etc." (its own words, matching phi-shell's role exactly) — not invented syntax. Whether `hyprland.start` can refire on a plain `hyprctl reload` (as opposed to a fresh login) could not be confirmed from any source reachable here; the `pgrep` guard is written as cheap, always-safe insurance either way rather than a claim about confirmed behavior, and the real answer is explicitly left for `S-25` ("review the Hyprland Lua configuration against the version actually installed"). **Window rules — three-way split, not a single "window rules" blob:** (1) *Dynamic workspaces* (master plan §2.3, closed): implemented by omission — Hyprland's own default (create on use, destroy when empty) already is what the decision asks for, and the only way to violate it would be adding a static monitor-pinned or `default = true` rule, which this commit does not do; a comment records the reasoning so `S-25`'s compatibility review does not mistake the absence of a rule for the absence of a decision. (2) *btop workspace* (ADR 122 / `Q-N03`, closed at S-22, unbuilt since): `hl.workspace_rule({ workspace = "special:btop", persistent = true })` plus `hl.window_rule({ name = "btop-workspace", match = { class = "^phios-btop$" }, workspace = "special:btop" })`. The matched class is not Steam-style guesswork: it is a Wayland app id this agent itself assigns, in the same commit's sibling change to `phi-shell` (`Bar/modules/Gpu.qml`'s `btopLauncher.command` gains `--class phios-btop`, a real, standard kitty CLI flag confirmed against `man kitty` search results before use — a deliberate choice over matching btop's own window *title*, which a TUI can rewrite at runtime via its own OSC escape sequences and which this agent had no way to confirm one way or the other without a live run; matching a self-assigned class needed no such confirmation at all). This closes the mechanism S-23's own row named opinion this step would own verbatim ("the Hyprland window rule that would actually assign btop's window to that workspace is S-24's own AGENT bullet... unbuilt") — the direct `kitty -e btop` launch S-23 shipped as a stopgap still works exactly as before, now additionally landing on the right workspace once one exists; the still-unbuilt persistent bar toggle (flagged in S-22's own row, not owned by any step) means visiting that workspace still needs a manual `togglespecialworkspace btop`, not a click. (3) *Steam* (master plan §2.3: dedicated workspace, secondary windows floating) — **deliberately NOT written**, per the AGENT card's own explicit instruction, not this agent's judgment call: "Steam window rules need real class and title values. Ask the user for hyprctl clients output rather than guessing." This agent has no path to either machine to run that command (`CLAUDE.md` rule 4) and the card forecloses guessing outright — unlike every other placeholder value this project has shipped and flagged for cheap veto (`<mini-overlay-address>`, `[PLACEHOLDER]` tokens, provisional thresholds), a wrong Steam class is not a flaggable guess but the exact thing the card says not to do. A commented scaffold is left in `hyprland.lua` showing the three rules' shape with `<angle-bracket>` placeholders, mechanical to fill in once real data arrives — this is also why `VERIFY`'s existing "hyprctl clients for Steam windows" line is not decorative: it is where that data was always going to come from, one bullet the card and this row agree on without having to invent a second round-trip. **Hot-reload path documented** (AGENT: "so the user can iterate without restarting"), in `phi-shell/README.md` next to the pre-existing `qs -p` convention rather than in this repository, since the reload mechanism itself (Quickshell's own file-watcher, `S-20`) lives entirely in that repository's domain: QML edits need no restart at all (unchanged, pre-existing behavior, now just written down); a process-level restart, when genuinely needed, is `pkill -x qs && qs -p ~/.config/quickshell/phi`, with no Hyprland session reload involved either way. **Removed nothing**, per the AGENT card's own instruction — waybar was never installed, confirmed again by the unchanged `profiles/desktop/packages.txt` diff (only an addition). **`DONE WHEN` genuinely unverifiable from here in either half:** "starts automatically" needs a real login on `zotac`/`razer`, and "survives a shell reload" needs a real `hyprctl reload` (or Hyprland's own config-reload path) on real hardware — neither is something this agent can produce or has produced. The `USER`/`VERIFY` blocks below ask for exactly the two things this row is missing: a session restart on both machines, and `hyprctl clients` output while Steam is open (main window plus one secondary window, e.g. the friends list) so the deferred Steam rules can be written for real in a follow-up commit under this same step, before `DONE WHEN` can honestly be called met. **First real-hardware round, `razer` only, found and fixed a real bug before this row could be marked verified — not shipped and walked back later.** `DONE WHEN`'s own two literal criteria are met: `pgrep -x qs` showed `qs` running after the session restart (bar starts automatically, once Hyprland itself is up — see the session-startup gap below), and the user confirmed the session was unchanged after running `hyprctl reload` (survives a reload). But the `hyprctl clients` capture taken during the same round surfaced a real defect in the btop rule, not a clean pass: **every** window present — the user's own terminal running the capture command, and Steam itself — showed `workspace: -98 (special:btop)`, not just btop's own window. Root cause: the original rule assigned btop's window with a bare `workspace = "special:btop"`, no `silent` suffix, which (confirmed against real Hyprland source, not re-guessed) switches the monitor's active workspace to the target the moment the window opens, the same as a normal workspace assignment. With no workspace-switching keybinding built yet (`S-38`), there was no way back out once that happened, so everything opened afterward — the user's own diagnostic terminal, then Steam — landed on `special:btop` by simple inheritance of "whatever the active workspace happens to be." This also explains the user's second observation, that "the special:btop button does nothing when pressed": the GPU module's click only ever launches a fresh `kitty --class phios-btop -e btop` (S-23's stopgap, unchanged by this step, no toggle wired to any bar element per S-22's own flagged gap) — with the session already sitting inside `special:btop` from the earlier launch, a second click had nothing visibly different to switch to. **Fixed** (same commit as this row): `workspace = "special:btop silent"` — the window still lands on the special workspace, but nothing else follows it there, and the workspace stays hidden until something actually calls `togglespecialworkspace btop` (still unwired, left alone exactly as the user asked: "if that's planned, you should not change it"). **Not yet re-verified on real hardware** — this fix is reasoned from the same real Hyprland source the original (buggy) rule was, but has not itself been through a round-trip yet; the next `USER`/`VERIFY` block asks for exactly that, cheaply (reload, click the GPU module once, confirm nothing else moves workspace). **Steam's real class and title arrived in this same round**, closing the guessing question outright: `hyprctl clients` on `razer` showed Steam's actual main window as `class: steam, title: Steam, initialClass: steam` (xwayland: 1) — used verbatim, not adjusted. `hl.window_rule({ name = "steam-workspace", match = { class = "^steam$" }, workspace = "name:steam" })` added: a plain named workspace, not special and not `silent`, since — unlike btop — Steam is something you deliberately switch to, not a background peek. **"Secondary windows floating" is still not written**, and for the same reason the original card gave for not guessing Steam's class: the one real window captured is the main library window, and distinguishing a secondary one (Friends List, a chat window, a game's own popup) needs either that window's own real title, or a confirmed way to negate a match in this Hyprland version — real sources actively disagree on whether the regex engine here even supports a negative-lookahead title match (`ECMA-262`-style) or requires a separate `negative:` prefix (`RE2`-style), and guessing between two contradictory real sources is the same mistake as guessing a class name blind. Left as a commented scaffold; needs one more real `hyprctl clients` capture with a secondary Steam window open. **New, out-of-scope gap surfaced by the user, not fixed here, exactly per their own instruction** ("i think this is fine by the plans, no need to add things if those are covered in the plans later"): Hyprland itself did not start automatically on `razer` after the reboot — the user had to run `start-hyprland` by hand before `phi-shell` could autostart into it. This is a layer below "the shell starts with the session" (S-24's own goal, which is about `phi-shell` once Hyprland is already running): how Hyprland itself launches at login (`gestione sessione`, listed as "Da definire" in `phios-architettura.md` §8, line 638) has no owning step yet in any planning document read so far. Flagged here for whichever step or new card eventually closes it — not addressed, per the user's own explicit request. **Closed as `verified` on the user's own explicit instruction** ("if nothing is left to be done, you can mark it as verified"), with every remaining gap recorded here rather than silently dropped, none of them requiring a code change — they are coverage gaps in what could be tested today, not defects: (1) **Steam's dedicated-workspace rule is unconfirmed** — the user reported Steam still showing as "just a tiled window," not on a workspace of its own, and asked whether that was intended. Answer, for the record: yes and no. Tiled (never floating) is exactly right for the *main* window — master plan §2.3 only asks for secondary windows to float, and this rule was deliberately written without `float`. Landing on its own **workspace**, though, is a real Hyprland window-rule limitation, not a bug in the rule itself: a `workspace` assignment only fires when a window is first created, never retroactively, and the Steam window the user was looking at almost certainly predates this commit — it was already open from the previous round's `hyprctl clients` capture. Confirming the workspace assignment for real needs a full Steam quit and relaunch, not yet done; flagged here as a cheap, non-blocking check for whenever it's convenient, not requested as a follow-up round. (2) **The btop `silent` fix is unconfirmed on any host.** `zotac` — where `Bar/modules/Gpu.qml`'s GPU module actually appears, since it is gated on the nvidia-only `nvidiaGpu` capability (S-23) — was not reachable this round ("I cannot test the gpu on zotac currently"). `razer` has no GPU module in its bar at all (Intel-only, no `nvidia-smi`), so the original round's btop test there could only have exercised the window rule directly (by launching `kitty --class phios-btop -e btop` some other way, not a bar click) — asking the user to "click the GPU bar module" on `razer` in the previous handoff was this agent's own mistake, since no such element exists on that host's bar; recorded here so the next agent doesn't repeat it. The mechanism itself (`silent` suppressing the forced workspace switch) is reasoned from real Hyprland source, same as the original rule was, just not yet round-tripped anywhere. (3) **btop's special workspace has no way to be reached today, by design-so-far, not a defect**: no bar toggle exists (flagged since S-22, unassigned to any step) and no keybinding exists either (`S-38`, not built) — the user's own words, "without keybinds i can't reach it," confirm this is the expected, already-documented state, not a new gap. (4) **Hyprland's own session start is still manual** (`start-hyprland`, reported in the previous round) — reiterated here at the user's request to keep it on record; still no owning step in any planning document read so far, still deliberately not addressed. |
| S-25 | Compatibility check against Hyprland | awaiting-verification | | 2026-09-08 | **Off-machine review only — the AGENT half of this step is done, the USER half (real `hyprctl version`/`pacman -Qi hyprland`/log check on `zotac` and `razer`) has not started.** Reviewed every Hyprland Lua symbol `profiles/desktop/home/.config/hypr/hyprland.lua` calls (`hl.monitor`, `hl.on`, `hl.exec_cmd`, `hl.bind`, `hl.dsp.exec_cmd`, `hl.dsp.window.close`, `hl.dsp.focus`, `hl.workspace_rule`, `hl.window_rule`) against real upstream sources, fetched directly rather than summarized, the same discipline S-24 set: `hyprwm/Hyprland`'s own `example/hyprland.lua` at both the `v0.56.2` tag (the version master plan §18 names for `extra`) and the `main` branch tip (2026-09-08), plus the rendered `hyprwm/hyprland-wiki` pages for autostart, events, dispatchers, and window/workspace rules — the wiki's live site sits behind a JS proof-of-work checkpoint this agent cannot solve, same gap S-24 hit, worked around the same way. Also scanned every Hyprland release's notes from `v0.55.0` through `v0.56.2` for the string `lua` looking for a breaking rename — found only additive/internal entries, nothing that touches a symbol this file uses. **Result: no deprecation found, nothing to fix.** The full symbol-by-symbol table and sourcing detail now live in `profiles/desktop/README.md` (new file, this step's real documentation deliverable — the AGENT card's "document which API surfaces the shell depends on and where they are used"), not repeated here. Same file also resolves the one open question S-24 explicitly deferred to this step: whether `hyprland.start` can refire on a plain `hyprctl reload`. It cannot — `hyprwm/hyprland-wiki`'s own events reference documents it as "Emitted once on start" — so `hyprland.lua`'s comment above the autostart hook is corrected in this commit to say so, replacing the "unconfirmed... that check is S-25's job" language it carried since S-24. The `pgrep` guard stays regardless, now correctly described as insurance against a refire that was never actually possible rather than one that might have been. **Scope boundary, stated explicitly rather than left implicit:** `phi-shell`'s own `Quickshell.Hyprland` usage (`Services/HyprlandBridge.qml`, reading workspaces/active-window over Hyprland's IPC socket) is a different dependency on a different piece of Hyprland, already tracked under master plan §18's separate quickshell-instability risk row — deliberately not re-audited under risk C-06, which master plan §18 names specifically as "API Lua di Hyprland", not the IPC socket. **What could not be done from here, and is exactly what `DONE WHEN` still needs:** confirming any of the above against the version actually installed on `zotac`/`razer` rather than upstream's own reference — this agent has no path to either machine (`CLAUDE.md` rule 4). The real Hyprland session log path needed for that check was itself looked up rather than guessed, from `hyprwm/hyprland-wiki`'s crashes-and-bugs page: `` $XDG_RUNTIME_DIR/hypr/$(ls -t $XDG_RUNTIME_DIR/hypr/ \| head -n 1)/hyprland.log ``. That same page documents the log as populated only when `debug.disable_logs` is `false` (this file never sets it, so its value is whatever Hyprland defaults to, unconfirmed from here) — an empty `grep` for a deprecation string is ambiguous between "none found" and "no log", so `VERIFY` asks for proof the log is non-empty before trusting a clean grep. **Second gap, found only by re-reading S-03's own correction rather than assumed clean:** this review reads the file at `profiles/desktop/home/.config/hypr/hyprland.lua`, but S-03's row records that `phios-install` apply was only ever confirmed for real on `razer` — `zotac` "has not had the equivalent apply run yet" as of that row, unconfirmed since. If `zotac`'s `~/.config/hypr/hyprland.lua` is not a live symlink into this repo, this whole review is about a file Hyprland on `zotac` may not even be parsing, so `VERIFY` also asks for `readlink -f ~/.config/hypr/hyprland.lua` on both hosts before the version/log check is treated as meaningful. Master plan §18's risk row itself needed no edit — nothing found here rises to a recorded risk; `DONE WHEN`'s "or recorded as a known risk in the plan" branch does not apply this round. |

## M3 — Session surfaces

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-30 | Notification daemon and toasts | todo | | | |
| S-31 | [BLOCKING] Sidebar with declarative tab registry | todo | | | |
| S-32 | Clipboard history | todo | | | |
| S-33 | [BLOCKING] Launcher | todo | | | |
| S-34 | Lock screen | todo | | | |
| S-35 | Window overview | todo | | | |
| S-36 | Screenshot, OCR, QR, recording | todo | | | |
| S-37 | Alt+Tab overlay, tooltips, context menu, cheat sheet | todo | | | |
| S-38 | Keybinding scheme | todo | | | |
| S-39 | Daily-use consolidation | todo | | | |

## M4 — Settings panel and system features

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-40 | [BLOCKING] Settings panel, nine sections | todo | | | |
| S-41 | Live light/dark theme, GTK/Qt, CSD decision | todo | | | |
| S-42 | Night shift and True Tone | todo | | | |
| S-43 | Cursor spotlight, idle inhibit, OSD, timer, colour picker, fullscreen auto-hide bar | todo | | | |
| S-44 | Wallpaper | todo | | | |
| S-45 | Package management surface and theme regeneration hook | todo | | | |
| S-46 | razer hardware: hwdb fix and Chroma | todo | | | |

## M5 — Identity and advanced styling

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-50 | Final palette, derived in OKLCH | todo | | | |
| S-51 | Typography and the patched-font correction | todo | | | |
| S-52 | Motion implementation | todo | | | |
| S-53 | Plymouth boot theme and Φ identity assets | todo | | | |
| S-54 | Cursor theme, magnifier, remaining chrome | todo | | | |

## M6 — Services and tools

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-60 | Synchronised cloud | todo | | | |
| S-61 | Photo library | todo | | | |
| S-62 | Jellyfin and the video libraries | todo | | | |
| S-63 | Indexers and download client | todo | | | |
| S-64 | Media and music pipelines | todo | | | |
| S-65 | ClamAV | todo | | | |
| S-66 | LanguageTool | todo | | | |
| S-67 | Office, study tools, secrets | todo | | | |
| S-68 | Clipboard continuity between zotac and razer | todo | | | |

## M7 — AI agent

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-70 | [BLOCKING] Containment and its proof | todo | | | |
| S-71 | Credential brokering | todo | | | |
| S-72 | A2 network whitelist | todo | | | |
| S-73 | Agent data model and phi MCP server | todo | | | |
| S-74 | Inline command line and remote surface | todo | | | |
| S-75 | Shell panel: connect the placeholder | todo | | | |
| S-76 | Transition from the current uncontained usage | todo | | | |

## M8 — Custom applications

| Step | Title | Status | Commit | Date | Note |
|---|---|---|---|---|---|
| S-80 | phi-notes | todo | | | |
| S-81 | Language decision checkpoint | todo | | | |
| S-82 | phi-music | todo | | | |
| S-83 | phi-media | todo | | | |
