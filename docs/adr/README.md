# docs/adr/

Architecture decisions local to `phios-dotfiles`, if any are ever needed.

The project's original decision records (ADR 001–100) are archived in the
workspace under `docs/archive/` (`phios-architettura.md`,
`phios-agente.md`); a closed ADR is never reopened. This directory exists
for decisions that concern only the shape of this repository.

Empty so far.
