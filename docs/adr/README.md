# docs/adr/

Architecture decisions local to `phios-dotfiles`, if any are ever needed.

The project's own decision records live in `docs/phios-architettura.md`
(ADR 001–083) and `docs/phios-agente.md` (ADR 084–100), and a closed ADR is
never reopened. This directory exists for decisions that concern only the
shape of this repository and would be noise in those documents.

Empty so far.
